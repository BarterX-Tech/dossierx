id: widget.markdown-cases.loose-list-blank-line-fence-nested
facet: contract
module: widget
status: draft
layout: card
body: |
  - Top level item:

    - Nested item:

      ```sh
      go test ./...
      ```

  - Second top level item.
governed_by:
  type: none
  reason: markdown renderer test fixture - the loose-list rule at depth; blank lines before a nested item and before a fence indented under that nested item, with the fence still landing inside the nested item
