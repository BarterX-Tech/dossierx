id: widget.markdown-cases.loose-list-blank-line-fence-unordered
facet: contract
module: widget
status: draft
layout: card
body: |
  - Install it:

    ```sh
    go install ./...
    ```

  - Then run it.
rests_on:
  none: true
  reason: markdown renderer test fixture - the loose-list rule for an unordered list; a blank line before an indented fence must not split the list
