id: widget.markdown-cases.loose-list-blank-line-fence-unordered
facet: contract
module: widget
status: draft
layout: card
body: |
  - Install it:

    ```sh
    go install ./...
    ```

  - Then run it.
