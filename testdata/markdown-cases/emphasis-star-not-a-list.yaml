id: widget.markdown-cases.emphasis-star-not-a-list
facet: contract
module: widget
status: draft
layout: card
body: |
  *italic* rest of the line.

  *not a list*item, run together.

  - *italic* inside a list item
  - **bold** inside a list item
rests_on:
  none: true
  reason: markdown renderer test fixture - the L3 matrix rows where a star could mean either thing
