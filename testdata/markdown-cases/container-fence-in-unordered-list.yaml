id: widget.markdown-cases.container-fence-in-unordered-list
facet: contract
module: widget
status: draft
layout: card
body: |
  - First item
  - Second item with code:

  ```
  code block
  inside list
  ```

  - Third item
governed_by:
  type: none
  reason: markdown renderer test fixture - tests fence inside unordered list (currently splits list)
