# Shared viewer-regression corpus. Its claims cover the reader structures that
# Curtainly does not instantiate in one module: tables, steps, a mockup, an
# open review thread, tracks, sources, and a locked build order.
schema_version: 1
title: Viewer Regression Corpus
eyebrow: shared reader structures on one page
facets:
  - contract
  - internals
modules:
  - widget
  - panel
tracks:
  - id: checkout
    title: Checkout
    summary: The cross-cutting track this corpus uses to put a .track-head on the page.
claims_dir: claims
mockup_modules:
  - panel
