id: panel.contract.api
summary: Fixture claim used by the engine test corpus.
facet: contract
module: panel
status: locked
layout: card
section: The panel's surface
body: |
  The panel exposes one call, `render()`, and nothing else. This claim is
  locked so the corpus has a locked claim beside its drafts.
rests_on:
  - panel.contract.behavior
