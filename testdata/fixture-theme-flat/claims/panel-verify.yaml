id: panel.internals.verification
summary: Fixture claim used by the engine test corpus.
facet: internals
module: panel
status: locked
build_role: verification
layout: card
section: How the panel is proven
body: |
  Every claim above is proven by the browser suite, not by reading the
  source. This claim carries the verification build_role so the module's
  build-order artifact covers every phase.
rests_on:
  - panel.contract.api
