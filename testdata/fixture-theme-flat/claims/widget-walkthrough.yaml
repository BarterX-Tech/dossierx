id: widget.contract.walkthrough
summary: Fixture claim used by the engine test corpus.
facet: contract
module: widget
status: draft
layout: steps
section: How a widget is reviewed
steps:
  - |
    Read the diagram, which is the `.sbody .md-img` the `image-bg` token
    paints:

    ![Review flow](assets/flow.svg)
  - |
    Check the step's own table, which is the `.sbody .md-table th` the
    `table-head-bg` token paints:

    | stage | who |
    | --- | --- |
    | draft | agent |
    | locked | human |
  - Approve it, or leave a comment thread on it.
tracks:
  - id: checkout
rests_on:
  - widget.internals.fields
