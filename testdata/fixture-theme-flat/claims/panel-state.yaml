id: panel.internals.state
summary: Fixture claim used by the engine test corpus.
facet: internals
module: panel
status: locked
layout: list
section: What the panel holds
body: |
  - the claim currently open
  - the scroll position of the reading pane
  - nothing else
rests_on:
  - panel.internals.mockup
audit_notes:
  - 'stub: ProposeDiff has no real LLM backend yet; dependency "panel.decision.mockup" changed but no proposal was generated'
