id: widget.contract.reviewed
facet: contract
module: widget
status: locked
layout: card
section: A claim under standing review
body: |
  This claim is locked and carries an open comment thread, which is what puts
  it in `review_pending` and renders the `.claim-review-pending` marker the
  parity probe reads.
governed_by:
  type: none
  reason: fixture claim, not backed by any real doctrine
tracks:
  - id: checkout
rests_on:
  - panel.contract.api
review_pending: true
comments:
  - id: c-7e728a
    status: open
    author: human
    created: "2026-09-02T12:24:55Z"
    body: 'Does this still hold after the panel took over the reading pane? Leaving this open on purpose: an open thread on a locked claim is what puts it in review_pending.'
    edited: false
