id: telemetry.verification.offline-suite
facet: verification
module: telemetry
status: draft
layout: card
build_role: verification
body: |
  The suite runs with the network removed and asserts every command still completes.
governed_by:
  type: none
  reason: a demo corpus claim, not backed by any real doctrine
rests_on:
  - telemetry.contract.no-egress
