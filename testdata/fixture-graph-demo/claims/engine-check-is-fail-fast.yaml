id: engine.contract.check-is-fail-fast
summary: Fixture claim used by the engine test corpus.
facet: contract
module: engine
status: locked
layout: card
body: |
  The check entry point stops at the first failing stage and names the stage it stopped at.
rests_on:
  - engine.internals.lint-runs-before-catalog
