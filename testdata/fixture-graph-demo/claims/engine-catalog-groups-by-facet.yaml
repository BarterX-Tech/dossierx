id: engine.internals.catalog-groups-by-facet
summary: Fixture claim used by the engine test corpus.
facet: internals
module: engine
status: locked
layout: card
body: |
  Grouping is built by an explicit sort rather than by ranging over a map, so two builds over the same input are byte-identical.
rests_on:
  - engine.contract.catalog-shape
