id: engine.internals.determinism-suite
facet: internals
module: engine
status: locked
layout: card
build_role: verification
body: |
  Build the same corpus a hundred times from shuffled input and assert the encoded bytes are identical every time.
rests_on:
  - engine.contract.build-is-pure
