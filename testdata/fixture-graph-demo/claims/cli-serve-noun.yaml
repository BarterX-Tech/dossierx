id: cli.contract.serve-noun
summary: Fixture claim used by the engine test corpus.
facet: contract
module: cli
status: draft
layout: card
body: |
  Serving binds the loopback interface only and never lints, so a live session can carry shapes a reviewed corpus cannot.
rests_on:
  - cli.internals.json-envelope
