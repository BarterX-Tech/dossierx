id: engine.internals.edge-kinds
summary: Fixture claim used by the engine test corpus.
facet: internals
module: engine
status: locked
layout: card
body: |
  There is one edge kind and no more: a claim rests on another. A claim that rests on nothing states that absence, with a reason, instead of leaving the edge out.
rests_on:
  - engine.contract.claim-shape
