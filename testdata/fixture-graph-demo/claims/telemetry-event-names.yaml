id: telemetry.internals.event-names
facet: internals
module: telemetry
status: draft
layout: card
build_role: schema
body: |
  Names are drawn from a fixed list checked at build time, so a typo is a compile failure rather than a silently lost count.
rests_on:
  - telemetry.internals.counter-shape
