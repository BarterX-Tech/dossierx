id: cli.internals.json-envelope
summary: Fixture claim used by the engine test corpus.
facet: internals
module: cli
status: locked
layout: card
build_role: schema
body: |
  Every run emits exactly one envelope carrying a success flag, a data object and, on failure, a stable machine-readable code.
rests_on:
  - engine.contract.claim-shape
review_pending: true
