id: engine.verification.nil-safety-suite
facet: verification
module: engine
status: draft
layout: card
build_role: verification
body: |
  Every entry point is exercised with absent inputs and with empty identifiers, and must return a valid result rather than panic.
governed_by:
  type: none
  reason: a demo corpus claim, not backed by any real doctrine
rests_on:
  - engine.contract.build-is-pure
