id: lock.behavior.unlock-keeps-the-record
facet: behavior
module: lock
status: locked
layout: card
build_role: behavior
body: |
  Releasing an approval stamps the record rather than deleting it, precisely so the evidence outlives the release.
rests_on:
  - lock.schema.ledger-record
