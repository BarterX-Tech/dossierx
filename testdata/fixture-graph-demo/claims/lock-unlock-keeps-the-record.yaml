id: lock.internals.unlock-keeps-the-record
summary: Fixture claim used by the engine test corpus.
facet: internals
module: lock
status: locked
layout: card
body: |
  Releasing an approval stamps the record rather than deleting it, precisely so the evidence outlives the release.
rests_on:
  - lock.internals.ledger-record
