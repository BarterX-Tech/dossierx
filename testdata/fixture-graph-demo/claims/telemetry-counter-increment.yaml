id: telemetry.internals.counter-increment
summary: Fixture claim used by the engine test corpus.
facet: internals
module: telemetry
status: draft
layout: card
body: |
  Incrementing is lock-free and never allocates, so measuring a hot path does not change what it measures.
rests_on:
  - telemetry.internals.counter-shape
