id: cli.internals.exit-codes
facet: internals
module: cli
status: locked
layout: card
build_role: behavior
body: |
  The exit status distinguishes a clean run, a review failure and an operational failure, so a hook can branch without parsing prose.
rests_on:
  - cli.internals.json-envelope
