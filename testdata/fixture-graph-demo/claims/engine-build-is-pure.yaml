id: engine.contract.build-is-pure
summary: Fixture claim used by the engine test corpus.
facet: contract
module: engine
status: locked
layout: card
emphasis: true
body: |
  Building the render payload performs no input or output, reads no clock, and returns no error. Rendering cannot fail because of it.
rests_on:
  - engine.contract.catalog-shape
