id: engine.internals.degree-counting
summary: Fixture claim used by the engine test corpus.
facet: internals
module: engine
status: draft
layout: card
body: |
  A node's project-wide degree counts all three edge kinds. It is a fact about the corpus; the reader's current scope produces a different, smaller number that is computed separately.
rests_on:
  - engine.internals.edge-kinds
