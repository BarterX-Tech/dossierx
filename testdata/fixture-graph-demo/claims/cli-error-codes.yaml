id: cli.schema.error-codes
facet: schema
module: cli
status: draft
layout: card
build_role: schema
body: |
  Each code maps to one documented recovery. A code without a recovery is a message with extra steps.
rests_on:
  - cli.schema.json-envelope
