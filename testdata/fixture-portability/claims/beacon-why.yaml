id: beacon.internals.why-simple
summary: Fixture claim used by the engine test corpus.
facet: internals
module: beacon
status: draft
layout: card
body: |
  Beacons stay deliberately simple so this fixture project can exercise the
  rationale facet without any doctrine hub configured at all.
rests_on:
  - beacon.internals.overview
