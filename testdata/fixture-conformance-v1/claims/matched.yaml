id: widget.contract.matched
summary: Fixture claim used by the engine test corpus.
facet: contract
module: widget
status: draft
layout: card
body: Every observed value matches its authored expectation.
embodiment:
  mode: compare
  checks:
    - id: public-values
      adapter: neutral/v1
      target: widget://matched-values
      expectation:
        shape: set
        value: [waiting, ready]
    - id: schema-version
      adapter: neutral/v1
      target: widget://matched-version
      expectation:
        shape: scalar
        value: "3"
rests_on:
  none: true
  reason: neutral fixture
