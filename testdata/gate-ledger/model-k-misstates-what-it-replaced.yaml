# gate-cost-model.yaml fixture — an edit that invents the history it moved from
#
# It claims to have replaced 2.0. The anchor is the seed, at 1.2. An edit that
# can state its own predecessor can launder any move into a small one.
schema: dossierx.gate-cost-model/1
reserve:
  value: 20000
  unit: output_tokens
backstop_factor:
  value: 1.5
classes:
  - name: falsification-dense
    seed:
      value: 1.2
      numerator_unit: output_tokens
      denominator_unit: bytes
      derived_from: "A fixture value."
    k:
      - value: 1.3
        numerator_unit: output_tokens
        denominator_unit: bytes
        previous: 2.0
        rationale: "Trimmed slightly."
  - name: newest-entry-only
    seed:
      value: 0.6
      numerator_unit: output_tokens
      denominator_unit: bytes
      derived_from: "A fixture value."
surfaces:
  - name: readme
    class: falsification-dense
    document_basis: manifest.tracked_files
  - name: changelog
    class: newest-entry-only
    document_basis: manifest.tracked_files
  - name: roadmap
    class: newest-entry-only
    document_basis: manifest.tracked_files
