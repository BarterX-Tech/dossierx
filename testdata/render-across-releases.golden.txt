# render-across-releases.golden.txt — how this project's rendered output has
# moved since the previous release.
#
# Generated by tests/render_across_releases_test.go. Regenerate with:
#
#     go test ./tests -run TestRenderedOutputAcrossReleases -regenerate-goldens
#
# WHAT IT IS FOR. v0.4.0 changed table layout and v0.4.1 changed the edges footer
# and the chips, with every claim locked and byte-identical, the lock store
# untouched and `dossierx check` reporting nothing. A consumer's own merge gate
# cannot detect that for them; only a comparison against the release tag can.
#
# SILENT RENDER CHANGES is the section that matters: an artifact whose inputs did
# not move by a byte and whose rendered output did. Every entry there needs a
# CHANGELOG line, and the diff printed under it is what that line is written from.
#
# EXPLAINED BY AN INPUT CHANGE carries THE SAME DIFF, and the inputs that moved.
# The two sections differ in what they oblige you to do, never in what they show
# you. A fixture project's inputs are every tracked non-artifact file under it —
# nothing here knows which claim fed which line of a ten-thousand-line viewer —
# so one edited byte anywhere in a fixture moves that whole fixture's viewer into
# this section. If the section could suppress the diff, that one byte would also
# hide a renamed CSS class in the same viewer, which is precisely the v0.4.1
# change. So: read this section's diffs, and satisfy yourself that every hunk in
# them is accounted for by the named inputs. A hunk that is not is a silent
# render change wearing an explanation.
#
# THE BASELINE LINE IS PART OF THE SNAPSHOT. It records which release this was
# compared against, so the comparison is auditable rather than implied. The
# baseline defaults to the newest tag reachable from HEAD; DOSSIERX_PREV_RELEASE_TAG
# overrides it, and an override that names a different release must be
# regenerated, because a report whose header says one release and whose diffs
# describe another would be worse than no report. Regenerating after a tag is a
# release step, in the same breath as regenerating surface.json.
#
# An empty report is the expected steady state immediately after a release.

baseline   v0.5.1 (8c3a9c1c1b0a4ef8f752d3d73c3f072432584c1e)
compared   147 rendered artifacts present at both revisions
added      1
removed    0
silent     3 rendered differently from byte-identical inputs
explained  0 rendered differently after their own inputs changed

SILENT RENDER CHANGES
---------------------

testdata/fixture-basic/viewer/index.html  (+8 -10)
    size            9951 lines / 348496 bytes -> 9949 lines / 348445 bytes
@@ -237,16 +237,14 @@
    =========================================================================
 
    From here down, every colour is a var(--token) read from the base
-   stylesheet's fixed allowlist, with exactly two exceptions, both of which
-   the base stylesheet itself already makes for the same reasons: the backdrop
-   dim and the panel drop shadow are rgba() blacks, because "how dark is the
-   thing behind this overlay" and "how deep is this shadow" are not colours a
-   project themes — they are the same value at every accent.
+   stylesheet's fixed allowlist, with no exceptions: this section holds no rgba()
+   literal. The pane is opaque (var(--paper)), has no box-shadow, and has no
+   backdrop element at all — see "no scrim, no inset, no drop shadow" below.
 
    Z-INDEX. The existing stack is hand-assigned with explicit reasoning: nav
    overlay 20/30, nav toggle 40, status strip 45, comments overlay 50,
    comments rail 60, toast 70. The graph pane is a full-viewport overlay and
-   takes the band ABOVE all of them — backdrop 80, surface 81 — and the base
+   takes the band ABOVE all of them — pane root 80, surface 81 — and the base
    stylesheet's z-index ledger comment records that band, because that comment
    is the single place this stack is reasoned about.
 
@@ -1283,8 +1281,8 @@
    deliberately excluded everywhere below — RenderInline never emits any
    of this, so a bare selector here would be dead weight at best and,
    for table/hr/blockquote, would collide with existing chrome (a
-   layout: table claim's own table element, style.css:173's bare table/td/th
-   rules) at worst. */
+   layout: table claim's own table element, this file's own bare table, td/th and th
+   rules further down) at worst. */
 
 .claim-body hr,
 .claim-list-items hr,
@@ -2358,8 +2356,8 @@
      50  comments overlay
      60  comments rail
      70  toast
-     80  graph pane backdrop  \  claims graph, added in v0.5.0
-     81  graph pane surface   /
+     80  graph pane root      \  claims graph, added in v0.5.0 (opaque, not a
+     81  graph pane surface   /  backdrop — the pane dims nothing behind it)
 
    The graph pane takes the band ABOVE all of them because it is the only
    full-viewport overlay here: it is not a rail beside the document or a

testdata/fixture-graph-demo/viewer/index.html  (+8 -10)
    size            10695 lines / 475538 bytes -> 10693 lines / 475487 bytes
@@ -237,16 +237,14 @@
    =========================================================================
 
    From here down, every colour is a var(--token) read from the base
-   stylesheet's fixed allowlist, with exactly two exceptions, both of which
-   the base stylesheet itself already makes for the same reasons: the backdrop
-   dim and the panel drop shadow are rgba() blacks, because "how dark is the
-   thing behind this overlay" and "how deep is this shadow" are not colours a
-   project themes — they are the same value at every accent.
+   stylesheet's fixed allowlist, with no exceptions: this section holds no rgba()
+   literal. The pane is opaque (var(--paper)), has no box-shadow, and has no
+   backdrop element at all — see "no scrim, no inset, no drop shadow" below.
 
    Z-INDEX. The existing stack is hand-assigned with explicit reasoning: nav
    overlay 20/30, nav toggle 40, status strip 45, comments overlay 50,
    comments rail 60, toast 70. The graph pane is a full-viewport overlay and
-   takes the band ABOVE all of them — backdrop 80, surface 81 — and the base
+   takes the band ABOVE all of them — pane root 80, surface 81 — and the base
    stylesheet's z-index ledger comment records that band, because that comment
    is the single place this stack is reasoned about.
 
@@ -1283,8 +1281,8 @@
    deliberately excluded everywhere below — RenderInline never emits any
    of this, so a bare selector here would be dead weight at best and,
    for table/hr/blockquote, would collide with existing chrome (a
-   layout: table claim's own table element, style.css:173's bare table/td/th
-   rules) at worst. */
+   layout: table claim's own table element, this file's own bare table, td/th and th
+   rules further down) at worst. */
 
 .claim-body hr,
 .claim-list-items hr,
@@ -2358,8 +2356,8 @@
      50  comments overlay
      60  comments rail
      70  toast
-     80  graph pane backdrop  \  claims graph, added in v0.5.0
-     81  graph pane surface   /
+     80  graph pane root      \  claims graph, added in v0.5.0 (opaque, not a
+     81  graph pane surface   /  backdrop — the pane dims nothing behind it)
 
    The graph pane takes the band ABOVE all of them because it is the only
    full-viewport overlay here: it is not a rail beside the document or a

testdata/fixture-portability/viewer/index.html  (+8 -10)
    size            9942 lines / 350175 bytes -> 9940 lines / 350124 bytes
@@ -237,16 +237,14 @@
    =========================================================================
 
    From here down, every colour is a var(--token) read from the base
-   stylesheet's fixed allowlist, with exactly two exceptions, both of which
-   the base stylesheet itself already makes for the same reasons: the backdrop
-   dim and the panel drop shadow are rgba() blacks, because "how dark is the
-   thing behind this overlay" and "how deep is this shadow" are not colours a
-   project themes — they are the same value at every accent.
+   stylesheet's fixed allowlist, with no exceptions: this section holds no rgba()
+   literal. The pane is opaque (var(--paper)), has no box-shadow, and has no
+   backdrop element at all — see "no scrim, no inset, no drop shadow" below.
 
    Z-INDEX. The existing stack is hand-assigned with explicit reasoning: nav
    overlay 20/30, nav toggle 40, status strip 45, comments overlay 50,
    comments rail 60, toast 70. The graph pane is a full-viewport overlay and
-   takes the band ABOVE all of them — backdrop 80, surface 81 — and the base
+   takes the band ABOVE all of them — pane root 80, surface 81 — and the base
    stylesheet's z-index ledger comment records that band, because that comment
    is the single place this stack is reasoned about.
 
@@ -1283,8 +1281,8 @@
    deliberately excluded everywhere below — RenderInline never emits any
    of this, so a bare selector here would be dead weight at best and,
    for table/hr/blockquote, would collide with existing chrome (a
-   layout: table claim's own table element, style.css:173's bare table/td/th
-   rules) at worst. */
+   layout: table claim's own table element, this file's own bare table, td/th and th
+   rules further down) at worst. */
 
 .claim-body hr,
 .claim-list-items hr,
@@ -2358,8 +2356,8 @@
      50  comments overlay
      60  comments rail
      70  toast
-     80  graph pane backdrop  \  claims graph, added in v0.5.0
-     81  graph pane surface   /
+     80  graph pane root      \  claims graph, added in v0.5.0 (opaque, not a
+     81  graph pane surface   /  backdrop — the pane dims nothing behind it)
 
    The graph pane takes the band ABOVE all of them because it is the only
    full-viewport overlay here: it is not a rail beside the document or a

ADDED SINCE THE BASELINE
------------------------
testdata/markdown-cases/link-mailto.golden.html
