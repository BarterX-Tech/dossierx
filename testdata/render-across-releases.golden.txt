# render-across-releases.golden.txt — how this project's rendered output has
# moved since the previous release.
#
# Generated by tests/render_across_releases_test.go. Regenerate with:
#
#     go test ./tests -run TestRenderedOutputAcrossReleases -regenerate-goldens
#
# WHAT IT IS FOR. v0.4.0 changed table layout and v0.4.1 changed the edges footer
# and the chips, with every claim locked and byte-identical, the lock store
# untouched and `dossierx check` reporting nothing. A consumer's own merge gate
# cannot detect that for them; only a comparison against the release tag can.
#
# SILENT RENDER CHANGES is the section that matters: an artifact whose inputs did
# not move by a byte and whose rendered output did. Every entry there needs a
# CHANGELOG line, and the diff printed under it is what that line is written from.
#
# EXPLAINED BY AN INPUT CHANGE carries THE SAME DIFF, and the inputs that moved.
# The two sections differ in what they oblige you to do, never in what they show
# you. A fixture project's inputs are every tracked non-artifact file under it —
# nothing here knows which claim fed which line of a ten-thousand-line viewer —
# so one edited byte anywhere in a fixture moves that whole fixture's viewer into
# this section. If the section could suppress the diff, that one byte would also
# hide a renamed CSS class in the same viewer, which is precisely the v0.4.1
# change. So: read this section's diffs, and satisfy yourself that every hunk in
# them is accounted for by the named inputs. A hunk that is not is a silent
# render change wearing an explanation.
#
# THE BASELINE LINE IS PART OF THE SNAPSHOT. It records which release this was
# compared against, so the comparison is auditable rather than implied. The
# baseline defaults to the newest tag reachable from HEAD; DOSSIERX_PREV_RELEASE_TAG
# overrides it, and an override that names a different release must be
# regenerated, because a report whose header says one release and whose diffs
# describe another would be worse than no report. Regenerating after a tag is a
# release step, in the same breath as regenerating surface.json.
#
# An empty report is the expected steady state immediately after a release.

baseline   v0.7.4 (a0830a4c87e58e56f87d1b2452d7def5319bf105)
compared   148 rendered artifacts present at both revisions
added      4
removed    0
silent     0 rendered differently from byte-identical inputs
explained  3 rendered differently after their own inputs changed

EXPLAINED BY AN INPUT CHANGE
----------------------------

testdata/fixture-basic/build/viewer/index.html  (+1019 -481)
    inputs changed  testdata/fixture-basic/build/.gitignore
                    testdata/fixture-basic/build/ledger/comment-digest.json
    size            12712 lines / 438567 bytes -> 13250 lines / 461071 bytes
@@ -135,22 +135,35 @@
    in the same metric the ramp is solved in rather than in degrees of hue: the
    ramp solver treats --dxg-governed, --dxg-cycle, --dxg-halo and
    --dxg-facet-other as members of the already-chosen set with a hard floor of
-   CIEDE2000 20, so no slot it picks can land near any of them. Measured over
-   the shipped values, the closest any of the twenty slots comes to a reserved
-   colour is 14.4 in dark and 11.1 in light — and the light 11.1 is frozen
-   slot 5 (#67717E) against --dxg-facet-other (#7D8C85), two greys this file
-   inherited rather than chose. Against --dxg-governed itself, every slot in
-   both themes is at least 20 away. A governance edge cannot be mistaken for a
-   facet's colour at any facet count in either theme.
+   CIEDE2000 20, so no slot it picks can land near any of them. Measured
+   against the values DECLARED IN THIS FILE, the closest any of the twenty
+   slots comes to a reserved colour is 14.4 in dark and 11.1 in light — and
+   the light 11.1 is frozen slot 5 (#67717E) against this file's
+   --dxg-facet-other (#7D8C85), two greys this file inherited rather than
+   chose. Read that figure as a bound on this file alone: shell.html emits
+   style.css after graph.css, and style.css re-declares --dxg-facet-other
+   (blue, not grey) for the shipped viewer, so the only project that sees the
+   grey — and therefore that 11.1 — is one whose viewer.template_overrides
+   replaces style.css. Against --dxg-governed itself, which style.css does not
+   re-declare, every slot in both themes is at least 20 away. A governance
+   edge cannot be mistaken for a facet's colour at any facet count in either
+   theme.

    --dxg-cycle and --dxg-halo are STATE colours, not identity colours. They
    are drawn as a ring and a halo — never as a plain node fill — except under
    the one overlay that is specifically about that state, where every
    non-matching node is dimmed and there is nothing to confuse them with. */

-/* DARK IS THE BASE, light is the override — the same convention the base
-   stylesheet uses, so the two files move together rather than fighting over
-   which one a reader's OS appearance reaches first. Both sets are complete:
+/* DARK IS THE BASE, light is the override. This file keeps that convention
+   DELIBERATELY, and it is now the opposite of style.css, which is light-first
+   (one unconditional :root plus a `screen and (prefers-color-scheme: dark)`
+   query). The two files differ on purpose: this ramp was solved dark-first and
+   renumbering it to light-first would mean re-solving both sets, which changes
+   every existing project's facet colours for no reader-visible gain. What the
+   two files DO share is the print outcome — the light query below carries
+   `, print`, so paper gets the light ramp whatever the OS appearance is, the
+   same result style.css reaches by scoping its dark query to `screen`.
+   Both sets are complete:
    a categorical ramp has to move in BOTH directions, because the dark values
    are lifted and desaturated to stay legible on --paper's near-black and the
    light values are darkened to stay legible on its near-white. Neither set
@@ -184,7 +197,11 @@
   --dxg-facet-19: #B6B994;
   --dxg-facet-20: #A9D6C0;

-  /* No facet, an unlisted facet, or a collapsed module group. */
+  /* No facet, an unlisted facet, or a collapsed module group.
+     KEPT AS A FALLBACK, NOT AS THE SHIPPED VALUE: style.css declares
+     --dxg-facet-other too and is emitted after this file, so this grey only
+     reaches a project whose viewer.template_overrides replaces style.css.
+     Do not delete it — for such a project it is the only declaration. */
   --dxg-facet-other: #6C7F75;

   /* State colours. Ring, halo and the two structural overlays only. */
@@ -195,7 +212,10 @@
   --dxg-governed: #F06A9C;
 }

-@media (prefers-color-scheme: light) {
+/* `, print`: paper is light, whatever the OS says. A printed graph pane with
+   the dark ramp on white would be the same defect style.css's `screen and`
+   dark query prevents for the base palette. */
+@media (prefers-color-scheme: light), print {
   :root {
     --dxg-facet-1: #4257C4;
     --dxg-facet-2: #12897F;
@@ -223,6 +243,8 @@
     --dxg-facet-19: #6F4D37;
     --dxg-facet-20: #8782A8;

+    /* Light fallback, same standing as the dark one above: style.css's own
+       light value wins for the shipped sheet. */
     --dxg-facet-other: #7D8C85;

     --dxg-cycle: #D1201A;
@@ -1136,42 +1158,129 @@
    a project's own template_overrides CSS; a project can restyle any of
    this by overriding component partials via viewer.template_overrides.

-   Token names below (--accent, --accent-bg, --ink, --muted, --faint, --paper,
-   --card-bg, --border, --link, --warn, --warn-bg, --font-sans, --font-mono,
-   --radius) are the engine's fixed viewer.theme allowlist (see
-   internal/config/config.go). Defaults here mirror the reference docs
-   palette this engine's authoring project uses, so an unconfigured project
-   still renders with a sane look;
-   render.go injects a second <style> block after this one (and after the
-   dark-mode media query below) so any project-supplied theme token wins in
-   both OS color modes without disturbing tokens the project left unset. */
+   COLOUR MECHANISM, in cascade order. shell.html emits three <style> blocks:
+   graph.css, then this file, then the generated theme block.

+   1. One unconditional `:root` holds the palette, and it is LIGHT-FIRST:
+      every value in it is both the light-OS value and the printed value.
+      There is exactly one such block in this file.
+   2. `@media screen and (prefers-color-scheme: dark)` re-points the handful
+      of tokens that invert. The `screen and` prefix is load-bearing: it is
+      what keeps a dark-mode OS from dragging dark ink onto paper.
+   3. The single `@media print` block is the LAST block in this file. It has
+      to be last, because it overrides screen layout rules at equal
+      specificity (`.content-area { padding: 0 }` beats the padded screen
+      rule only on source order), and it pins `color-scheme: light`.
+   4. internal/render appends the project's theme as the third <style>: an
+      optional @font-face run, a shared `:root`, then
+      `@media (prefers-color-scheme: light), print` and
+      `@media screen and (prefers-color-scheme: dark)`. The project's light
+      block deliberately also matches `print` and its dark block deliberately
+      cannot, so a token a project sets only under `dark:` prints the engine's
+      light value rather than its dark one.
+
+   The names a project may set are the 28-entry allowlist in
+   internal/config/config.go (ThemeTokenAllowlist). Each token's :root default
+   is the value a light OS gets, and the screen-dark block carries the dark
+   values for the three that differ. A consumer spells the token as
+   `var(--token, <fallback>)`; the fallback is the LIGHT value, because this
+   sheet is light-first and because the fallback only ever fires for a
+   viewer.template_overrides project that forked this file and dropped the
+   :root — which should degrade to light, not to dark.
+
+   A token has to be read by the declaration that WINS, not merely by a
+   declaration. Two rules in this file are re-declared later at equal
+   specificity (`code` and `.claim-body pre, .claim-tree-body`), so their
+   token reads live in the later copy; internal/render/theme_tokens_test.go
+   asserts, per token, that the last declaration of the consumer's
+   selector+property pair is the one carrying the var(). It also fails if an
+   allowlisted token has no consumer in this file or in graph.css, and pins
+   the structural invariants listed above.
+
+   graph.css keeps the OPPOSITE convention for its own --dxg-* ramp (dark
+   base, light override) and says so in its own header; the two files differ
+   deliberately. */
+
+/* THE palette. One unconditional :root, light-first: the values below are what
+   a light-OS screen and a printed page both get. Nothing else in this file
+   declares any of these properties, so this block's position is free; it sits
+   here, at the top, because that is where a reader looks for it.
+
+   The dark query is scoped to `screen` on purpose. Without the prefix, a
+   reader whose OS is in dark mode and who hits Print gets near-black --paper
+   and pale --ink; print media does not carry an appearance, so nothing else
+   would put the light palette back. `screen and` is the whole fix, and the
+   @media print block at the end of the file pins `color-scheme: light` beside
+   it so form controls and scrollbars agree. This is unverified for Firefox and
+   Safari; where a browser already forces light for print it is a no-op, so it
+   is safe either way. */
+
 :root {
   color-scheme: light dark;
-  --accent: #3fb950;
-  --accent-bg: rgba(63, 185, 80, .14);
-  --ink: #e6ece8;
-  --muted: #9aa6a0;
-  --faint: #6f7a74;
-  --paper: #0f1311;
-  --card-bg: #1b211d;
-  --border: #2a322c;
-  --link: #4c9bf5;
-  --warn: #e0623f;
-  --warn-bg: rgba(224, 98, 63, .15);
-  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
-  --font-mono: ui-monospace, SFMono-Regular, Menlo, monospace;
-  --radius: 12px;
+  --accent: #287052;
+  --accent-bg: rgba(40, 112, 82, .12);
+  --ink: #091426;
+  --muted: #536179;
+  --faint: #7d899a;
+  --paper: #f6f8fc;
+  --card-bg: #ffffff;
+  --border: #d8deea;
+  --link: #205b78;
+  --warn: #a2433d;
+  --warn-bg: rgba(162, 67, 61, .10);
+  --dxg-facet-other: #0d55b5;
+  --font-sans: "Avenir Next", -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", sans-serif;
+  --font-mono: ui-monospace, "SFMono-Regular", "IBM Plex Mono", Menlo, monospace;
+  --radius: 6px;
+
+  /* Added for the custom-theme work. Each value below is what a LIGHT OS and
+     a printed page get; the screen-dark query re-points the three that differ
+     per mode (--shadow, --shadow-strong, --scrim). The other eleven are the
+     same in both modes on purpose: a neutral overlay tint tracks whatever it
+     sits on, and the mockup card is deliberately mode-invariant.
+     Every value is the expression the live consumer used before it read a
+     token, so an unset token renders exactly as it did before. Note "live":
+     for --code-inline-bg and --code-bg that is the System Record layer's
+     color-mix, not the rgba() in the dead declaration those rules have always
+     beaten. Order matches ThemeTokenAllowlist. */
+  --code-inline-bg: color-mix(in srgb, var(--paper) 72%, var(--card-bg));
+  --code-bg: color-mix(in srgb, var(--paper) 82%, var(--card-bg));
+  --table-head-bg: rgba(127, 127, 127, .10);
+  --image-bg: rgba(127, 127, 127, .06);
+  --hover-bg: rgba(125, 137, 154, .08);
+  --border-strong: #aab5c7;
+  --shadow: rgba(0, 0, 0, .08);
+  --shadow-strong: rgba(0, 0, 0, .14);
+  --shadow-cast: rgba(9, 20, 38, .12);
+  --scrim: rgba(0, 0, 0, .22);
+  --selection-bg: rgba(40, 112, 82, .20);
+  --status-draft: #976600;
+  --status-draft-bg: rgba(151, 102, 0, .12);
+  --mockup-bg: #fff;
 }

-@media (prefers-color-scheme: light) {
+@media screen and (prefers-color-scheme: dark) {
   :root {
-    --paper: #f3f6f4;
-    --card-bg: #fafcfb;
-    --border: #e2e8e4;
-    --ink: #1d2622;
-    --muted: #5d6862;
-    --faint: #8a948e;
+    --ink: #e8eef8;
+    --muted: #a9b5c8;
+    --faint: #75839a;
+    --paper: #0a1220;
+    --card-bg: #0f1b2e;
+    --border: #263754;
+    --link: #8ab7ff;
+    --accent: #70c99c;
+    --accent-bg: rgba(112, 201, 156, .12);
+    --warn: #ff8b94;
+    --warn-bg: rgba(255, 139, 148, .10);
+    --dxg-facet-other: #77a9e8;
+
+    /* The three theme tokens that genuinely differ per mode: a shadow needs
+       more alpha to read on a dark surface, and the modal dim needs more to
+       separate the sheet from what is behind it. The other eleven new tokens
+       are mode-invariant and are declared once, above. */
+    --shadow: rgba(0, 0, 0, .28);
+    --shadow-strong: rgba(0, 0, 0, .34);
+    --scrim: rgba(0, 0, 0, .42);
   }
 }

@@ -1217,14 +1326,11 @@
   background: var(--card-bg);
 }

-/* status-draft/status-locked map to the pill family below: draft -> amber
-   (engine's own draft concept, not part of the fixed color allowlist),
-   locked -> accent, review_pending -> warn. */
-.status-draft {
-  color: #d6a128;
-  background: rgba(214, 161, 40, .16);
-}
-
+/* status-draft/status-locked map to the pill family below: locked -> accent,
+   review_pending -> warn. draft -> amber is the engine's own concept and not
+   part of the fixed color allowlist; its one live rule sits with the pills in
+   the System Record layer further down (`.pill.pv, .status-draft`), which is
+   why there is no .status-draft rule here. */
 .status-locked {
   color: var(--accent);
   background: var(--accent-bg);
@@ -1241,11 +1347,14 @@
    every component routes body-shaped text through the shared "markdown"
    template func). Ported verbatim from the reference docs stylesheet's global
    `code{...}` rule so a regex, config key, or field name wrapped in single
-   backticks reads as a distinct code "pill", not plain prose. */
+   backticks reads as a distinct code "pill", not plain prose.
+
+   NO background here. The System Record layer's own `code` rule further down
+   re-declares it at equal specificity and later in the file, so anything set
+   here is dead; --code-inline-bg is read there, where it can win. */
 code {
   font-family: var(--font-mono);
   font-size: 12px;
-  background: rgba(127, 127, 127, .12);
   padding: 1px 5px;
   border-radius: 5px;
 }
@@ -1253,11 +1362,15 @@
 /* Fenced ```code``` spans inside a claim's Body, converted to real
    <pre><code> elements by markdown.Render. white-space is reset off the
    pre-wrap inherited from .claim-body since <pre> already preserves
-   whitespace on its own. Background is the same neutral rgba(127,127,127)
-   overlay tint the reference docs stylesheet's `pre{...}` rule uses (not
-   --card-bg, which is the surrounding card's own background and would
-   make the block invisible against it — the exact "white background, not
-   grey" regression this replaces). */
+   whitespace on its own.
+
+   NO background here, for the same reason as the `code` rule above: the
+   System Record layer re-declares this exact selector list later in the file
+   at equal specificity, so a background set here never reaches a reader.
+   --code-bg is read there. What that rule must NOT be is --card-bg, which is
+   the surrounding card's own background and would make the block invisible
+   against it — the "white background, not grey" regression these rules
+   replaced; it is a tint of --paper over --card-bg instead. */
 .claim-body pre,
 .claim-tree-body {
   font-size: 11.5px;
@@ -1266,7 +1379,6 @@
   overflow-x: auto;
   margin: 0.5rem 0;
   padding: 0.6rem 0.75rem;
-  background: rgba(127, 127, 127, .10);
   border: 1px solid var(--border);
   border-radius: var(--radius);
 }
@@ -1517,7 +1629,7 @@
 .claim-list-items .md-table th,
 .sbody .md-table th,
 .comment-body .md-table th {
-  background: rgba(127, 127, 127, .10);
+  background: var(--table-head-bg, rgba(127, 127, 127, .10));
   font-family: var(--font-sans);
   font-size: inherit;
   font-weight: 600;
@@ -1581,7 +1693,7 @@
   margin: 0.6rem 0;
   border: 1px solid var(--border);
   border-radius: 6px;
-  background: rgba(127, 127, 127, .06);
+  background: var(--image-bg, rgba(127, 127, 127, .06));
 }

 /* An image is the last thing in a list item often enough to be worth the
@@ -1893,8 +2005,8 @@
 }

 /* claim-ref is the shared anchor EVERY claim-to-claim edge renders through
-   (components.go's writeClaimRef): governed_by, mirrors, rests_on,
-   depended-on-by, and build_order.html's rests_on. Its two inner spans carry
+   (components.go's writeClaimRef): governed_by, mirrors, rests_on and
+   depended-on-by, all inside the shared claim footer. Its two inner spans carry
    the readability tiers — a dimmed "Widget · Contract ›" prefix, kept only
    when the target is outside the reading claim's own module/facet, and the
    claim's own label. No new color tokens: the prefix borrows --faint (the
@@ -1905,9 +2017,8 @@
    Specificity note: these are scoped under .claim-edges deliberately. The
    bare .claim-ref-prefix class (0,1,0) would lose to the existing
    ".claim-edges a" rule (0,1,1) and inherit --link; ".claim-edges
-   .claim-ref-prefix" (0,2,0) wins. build_order.html's independently rendered
-   rests_on sits inside a .claim-edges container too, so one scope covers
-   both paths. */
+   .claim-ref-prefix" (0,2,0) wins. The shared claim footer is the only
+   place a claim-ref renders, and this sheet is inlined into every viewer. */
 .claim-edges .claim-ref-prefix {
   color: var(--faint);
   font-weight: 400;
@@ -2095,20 +2206,6 @@
   text-decoration: underline;
 }

-/* Paper has no button to press, so the clamp must not reach it: print shows
-   every note whole and hides the control, the same bargain @media print
-   already strikes with the collapsed footer. */
-@media print {
-  .claim-source-note.is-clamped .claim-source-note-body {
-    display: block;
-    overflow: visible;
-  }
-
-  .claim-source-note-toggle {
-    display: none;
-  }
-}
-
 /* Deep-link auto-open for a SOURCE row, the companion to the .claim:target
    rules above. A citation marker's href names the row, not the claim, so the
    claim is not :target and those rules do not fire — yet the row the reader
@@ -2139,9 +2236,10 @@
   border-radius: 4px;
 }

+/* Colour and background are NOT set here: `.pill.pw, .claim-review-pending`
+   in the System Record layer sets both at equal specificity and later in the
+   file, so anything declared here would be dead. */
 .claim-review-pending {
-  color: var(--warn);
-  background: var(--warn-bg);
   padding: 0.1rem 0.4rem;
   border-radius: 4px;
   font-weight: 600;
@@ -2188,11 +2286,10 @@
    which is why a `.claim-comments { list-style: none; }` rule lived here —
    it is deleted in v0.4.1 along with that <li>, since the slot is a <span>
    and never carried a marker. Every color below is one of the inverting
-   theme tokens declared in :root above (and re-pointed in the
-   prefers-color-scheme: light block), so the whole surface tracks the OS
-   theme with no separate dark rule — the one genuinely per-mode value, the
-   panel's drop shadow, gets its light value in the media query at the end of
-   this block. claim-card--commented is a distinct accent marker,
+   theme tokens (declared in this file's single :root and re-pointed in the
+   screen-dark query beside it), so the whole surface tracks the OS theme with
+   no separate dark rule — including the one genuinely per-mode value, the
+   panel's drop shadow, which is --shadow. claim-card--commented is a distinct accent marker,
    deliberately NOT the --warn boundary family. */
 .comment-chip {
   display: inline-flex;
@@ -2282,7 +2379,7 @@
   border: 1px solid var(--border);
   border-radius: var(--radius, 12px);
   background: var(--paper);
-  box-shadow: 0 1px 2px rgba(0, 0, 0, .28);
+  box-shadow: 0 1px 2px var(--shadow, rgba(0, 0, 0, .08));
 }

 .comments-panel[hidden] {
@@ -2366,14 +2463,6 @@
   }
 }

-/* The panel shadow is the one genuinely per-mode value here: softer on a
-   light background than the deeper shadow the dark base above uses. */
-@media (prefers-color-scheme: light) {
-  .comments-panel {
-    box-shadow: 0 1px 2px rgba(0, 0, 0, .08);
-  }
-}
-
 /* ---- interactive comment UI (Phase 5, serve + file://) -----------------
    The 💬 chip opens a shared overlay panel: a non-modal right rail on desktop
    (role=complementary), a modal bottom sheet on mobile. Its backdrop
@@ -2381,10 +2470,13 @@
    half, so they are scoped to the <= 860px bottom-sheet breakpoint (in the media
    query below) — above it the desktop rail shows neither. They stay deliberately
    distinct from the sidebar's #navOverlay / body.nav-open — opening one overlay
-   closes the other. Colours are the inverting theme tokens from :root; the few
-   genuinely per-mode values (shadows, backdrop dim) get their light values in the
-   media query at the end of this block, matching the dark-base / light-override
-   convention the rest of this stylesheet uses. */
+   closes the other. Colours are the inverting theme tokens from this file's
+   single :root, including the per-mode ones: the backdrop dim is --scrim and
+   the toast shadow is --shadow-strong, both re-pointed in the screen-dark
+   query, so this block needs no light media query of its own. The rail's own
+   shadow is deliberately NOT per-mode: the `.comments-rail` rule in the
+   System Record layer further down sets --shadow-cast for both modes and wins
+   on source order. */

 /* body.comments-open drives the scroll-lock only at the mobile breakpoint (see
    the media query below); it stays a plain open-state marker on desktop. Never
@@ -2398,7 +2490,7 @@
   width: 100%;
   height: 100%;
   border: 0;
-  background: rgba(0, 0, 0, .42);
+  background: var(--scrim, rgba(0, 0, 0, .22));
   cursor: pointer;
 }

@@ -2416,7 +2508,6 @@
   flex-direction: column;
   background: var(--paper);
   border-left: 1px solid var(--border);
-  box-shadow: -2px 0 18px rgba(0, 0, 0, .38);
 }

 .comments-rail[hidden] {
@@ -2603,7 +2694,7 @@
   border: 1px solid var(--warn);
   border-radius: 10px;
   font: 13px var(--font-sans);
-  box-shadow: 0 4px 18px rgba(0, 0, 0, .34);
+  box-shadow: 0 4px 18px var(--shadow-strong, rgba(0, 0, 0, .14));
 }

 .comments-toast[hidden] {
@@ -2653,20 +2744,17 @@
    body.comments-open: three classes each setting overflow: hidden compose
    without a release-order hazard, so no coordination is needed between them. */

-/* Docked clear of the 210px desktop sidebar (its "Generated …" footer sits in
-   exactly this corner); the mobile override below reclaims the full-width left
-   edge, where the sidebar is an off-canvas drawer and owns no space. */
+/* Project health belongs to the reading flow, between the active module's
+   record header and its facet controls. It is deliberately quiet when closed:
+   enough contrast to be noticed, but no floating dock or shadow competing with
+   the document and its two navigation rails. */
 .status-strip {
-  position: fixed;
-  z-index: 45;
-  left: 226px;
-  bottom: 16px;
-  max-width: min(520px, calc(100vw - 242px));
+  width: 100%;
+  margin: -8px 0 22px;
   background: var(--card-bg);
   border: 1px solid var(--border);
-  border-radius: var(--radius);
-  font: 13px var(--font-sans);
-  box-shadow: 0 4px 18px rgba(0, 0, 0, .34);
+  border-radius: 7px;
+  font-family: var(--font-sans);
   overflow: hidden;
 }

@@ -2675,40 +2763,66 @@
 }

 .status-strip--lint {
-  border-color: var(--warn);
+  border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
+  background: color-mix(in srgb, var(--warn-bg) 34%, var(--card-bg));
 }

 .status-strip--integrity {
-  border-color: var(--warn);
-  border-left-width: 4px;
-  background: var(--warn-bg);
+  border-color: color-mix(in srgb, var(--warn) 48%, var(--border));
+  background: color-mix(in srgb, var(--warn-bg) 58%, var(--card-bg));
+  box-shadow: inset 0 2px 0 var(--warn);
 }

 .status-strip-head {
   display: flex;
   align-items: center;
-  gap: 10px;
+  gap: 12px;
   width: 100%;
   border: 0;
   background: none;
-  color: var(--warn);
-  padding: 10px 14px;
-  font: 600 13px var(--font-sans);
+  color: var(--ink);
+  padding: 13px 14px;
   text-align: left;
   cursor: pointer;
 }

 .status-strip-summary {
+  display: grid;
+  gap: 3px;
   flex: 1;
 }

-.status-strip-caret {
+.status-strip-title {
+  font-size: 13px;
+  font-weight: 680;
+  line-height: 1.35;
+}
+
+.status-strip-note {
   color: var(--muted);
+  font-size: 11.5px;
+  line-height: 1.45;
+}
+
+.status-strip-action {
+  color: var(--accent);
+  font: 600 10px var(--font-mono);
+  letter-spacing: .04em;
+  text-transform: uppercase;
+  white-space: nowrap;
+}
+
+.status-strip-caret {
+  width: 7px;
+  height: 7px;
+  border-right: 1.5px solid var(--muted);
+  border-bottom: 1.5px solid var(--muted);
+  transform: rotate(45deg) translateY(-2px);
   transition: transform .15s ease;
 }

 .status-strip--open .status-strip-caret {
-  transform: rotate(180deg);
+  transform: rotate(225deg) translate(-2px, -1px);
 }

 /* The detail list is capped and scrolls internally: a project with forty lint
@@ -2717,7 +2831,7 @@
   max-height: 40vh;
   overflow-y: auto;
   border-top: 1px solid var(--border);
-  padding: 10px 14px 12px;
+  padding: 12px 14px 14px;
 }

 .status-strip-body[hidden] {
@@ -2730,8 +2844,8 @@

 .status-group-head {
   margin: 0 0 6px;
-  font: 600 11px var(--font-sans);
-  letter-spacing: .04em;
+  font: 600 9px var(--font-mono);
+  letter-spacing: .08em;
   text-transform: uppercase;
   color: var(--muted);
 }
@@ -2758,11 +2872,15 @@

 .status-finding-rule,
 .status-finding-claim {
-  font: 11px var(--font-mono);
+  font: 10px var(--font-mono);
 }

 .status-finding-rule {
   color: var(--warn);
+  font-size: 9px;
+  font-weight: 600;
+  letter-spacing: .05em;
+  text-transform: uppercase;
 }

 .status-finding-claim {
@@ -2771,15 +2889,10 @@

 .status-finding-msg {
   flex-basis: 100%;
-  font-size: 12px;
+  font-size: 12.5px;
+  line-height: 1.5;
 }

-@media (prefers-color-scheme: light) {
-  .status-strip {
-    box-shadow: 0 4px 18px rgba(0, 0, 0, .14);
-  }
-}
-
 /* Coarse pointers (touch) need >=44px hit targets on every comment control. */
 @media (pointer: coarse) {
   .comments-rail-close,
@@ -2813,33 +2926,28 @@
     border-left: 0;
     border-top: 1px solid var(--border);
     border-radius: 14px 14px 0 0;
-    box-shadow: 0 -2px 18px rgba(0, 0, 0, .38);
   }
-  /* The sidebar is an off-canvas drawer here, so the left edge is free again. */
   .status-strip {
-    left: 16px;
-    max-width: calc(100vw - 32px);
+    margin-top: -4px;
   }
-}
-
-/* Per-mode values: softer shadows and a lighter backdrop dim on a light OS. */
-@media (prefers-color-scheme: light) {
-  .comments-rail {
-    box-shadow: -2px 0 18px rgba(0, 0, 0, .14);
+  .status-strip-head {
+    display: grid;
+    grid-template-columns: minmax(0, 1fr) 10px;
+    align-items: flex-start;
   }
-
-  .comments-overlay {
-    background: rgba(0, 0, 0, .22);
+  .status-strip-summary {
+    grid-column: 1;
+    grid-row: 1;
   }
-
-  .comments-toast {
-    box-shadow: 0 4px 18px rgba(0, 0, 0, .14);
+  .status-strip-action {
+    grid-column: 1;
+    grid-row: 2;
+    justify-self: start;
   }
-}
-
-@media (prefers-color-scheme: light) and (max-width: 860px) {
-  .comments-rail {
-    box-shadow: 0 -2px 18px rgba(0, 0, 0, .14);
+  .status-strip-caret {
+    grid-column: 2;
+    grid-row: 1;
+    margin-top: 4px;
   }
 }

@@ -2890,10 +2998,9 @@
   color: var(--accent);
 }

-.pill.pv {
-  background: rgba(214, 161, 40, .16);
-  color: #d6a128;
-}
+/* .pill.pv is NOT declared here: `.pill.pv, .status-draft` in the System
+   Record layer sets its background and colour at equal specificity and later
+   in the file, so anything declared here would be dead. */

 .pill.pw {
   background: var(--warn-bg);
@@ -2977,8 +3084,9 @@
    internal/lint/raw_html_scope.go's img allowlist entry). Styling ported
    from docs/tabs/llm-internals.html's inline img style for its health-
    state-machine diagram: a plain white card so an SVG/PNG diagram (often
-   drawn on a transparent or light background) reads consistently in both
-   light and dark viewer themes, same as it did in docs/. */
+   drawn on a transparent or light background) reads consistently. The card
+   is deliberately mode-INVARIANT — it does not follow the OS appearance,
+   because the artwork it frames usually cannot — same as it did in docs/. */
 .mockup-diagram {
   display: block;
   max-width: 100%;
@@ -2986,7 +3094,7 @@
   margin-top: 8px;
   border: 1px solid var(--border);
   border-radius: 6px;
-  background: #fff;
+  background: var(--mockup-bg, #fff);
   padding: 8px;
 }

@@ -3325,7 +3433,6 @@
 .content-area {
   flex: 1;
   min-width: 0;
-  max-width: 880px;
   margin: 0 auto;
 }

@@ -3507,7 +3614,6 @@
     padding: 6px 10px;
     font: 600 12px var(--font-sans);
     cursor: pointer;
-    box-shadow: 0 3px 12px rgba(0, 0, 0, .18);
   }

   .nav-overlay {
@@ -3518,6 +3624,11 @@
     width: 100%;
     height: 100%;
     border: 0;
+    /* Deliberately NOT --scrim. The sidebar backdrop is mode-invariant by
+       design: the sheet it dims is the same weight in both modes, and a
+       project that lightens --scrim for its comment sheet should not thereby
+       lighten this one. It is not a theme token and has no consumer in the
+       allowlist. */
     background: rgba(0, 0, 0, .42);
     opacity: 0;
     pointer-events: none;
@@ -3570,96 +3681,8 @@
   }
 }

-/* ---- print -------------------------------------------------------------
-   The first and only @media print block in this stylesheet. It exists for
-   one reason (Q4): paper has no disclosure widget, so a printed viewer that
-   honoured the collapsed <details> would silently drop every claim's
-   governed_by/rests_on/implemented-in footer — the exact facts a reviewer
-   prints the page to read.
-   Revealing the contents through CSS is deliberate rather than setting `open`,
-   which CSS cannot do: `open` is a content attribute, not a property a rule
-   can write.
-   Same mechanism, and the same two-rule pairing, as the .claim:target rule
-   above: a closed <details> hides its contents through the UA's
-   ::details-content pseudo-element, so that is what is targeted, and the
-   child-selector form is expected to be a no-op wherever the pseudo-element is
-   implemented. It is kept only as a fallback for engines that predate
-   ::details-content (Chrome/Edge 131+, Safari 18.4+, Firefox 139+) and hide
-   the contents some other way; such an engine drops the pseudo-element rule
-   and still prints as it did before v0.4.1. Both rules declare the same value
-   and carry equal specificity (0,1,2), so the pseudo-element rule is written
-   LAST — source order is the only thing ranking them.
-   This block is independent of the .claim:target rule, of the two
-   server-written `open` signals, and of the expand-all toolbar — whichever of
-   those is or is not in effect on screen, print shows all four states
-   identically.
-   SCOPE — stated plainly so the next reader knows this is a decision, not an
-   oversight: only the facet currently ON SCREEN prints. A collapsed footer
-   belonging to a claim in a facet group the reader is not looking at stays
-   unrendered, because its ancestor section.claim-group carries the `hidden`
-   attribute (computed display: none) and no rule here un-hides it. The rules
-   below reveal a disclosure inside a subtree that is being laid out; they
-   cannot resurrect a subtree that display: none removed from the box tree in
-   the first place. That is deliberate: printing a tabbed viewer has always
-   shown one facet, what v0.4.1 changes is that the shown facet's footers now
-   print with it, and un-hiding every facet for print is a larger design
-   change — page order, per-facet headings, the duplicated overview copies —
-   than a patch release should make.
-   Only the disclosure is touched. Nothing here restyles the card, the type
-   scale or the color tokens; a full print stylesheet is out of scope for
-   v0.4.1. */
-@media print {
-  details.claim-links > *:not(summary) {
-    content-visibility: visible;
-  }
-
-  details.claim-links::details-content {
-    content-visibility: visible;
-  }
-
-  .claim-links-summary {
-    cursor: auto;
-  }
-}
-
 /* System Record visual language for the generated viewer. */

-:root {
-  --accent: #287052;
-  --accent-bg: rgba(40, 112, 82, .12);
-  --ink: #091426;
-  --muted: #536179;
-  --faint: #7d899a;
-  --paper: #f6f8fc;
-  --card-bg: #ffffff;
-  --border: #d8deea;
-  --link: #205b78;
-  --warn: #a2433d;
-  --warn-bg: rgba(162, 67, 61, .10);
-  --dxg-facet-other: #0d55b5;
-  --font-sans: "Avenir Next", -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", sans-serif;
-  --font-mono: ui-monospace, "SFMono-Regular", "IBM Plex Mono", Menlo, monospace;
-  --radius: 6px;
-  color-scheme: light;
-}
-
-@media (prefers-color-scheme: dark) {
-  :root {
-    --ink: #e8eef8;
-    --muted: #a9b5c8;
-    --faint: #75839a;
-    --paper: #0a1220;
-    --card-bg: #0f1b2e;
-    --border: #263754;
-    --link: #8ab7ff;
-    --accent: #70c99c;
-    --accent-bg: rgba(112, 201, 156, .12);
-    --warn: #ff8b94;
-    --warn-bg: rgba(255, 139, 148, .10);
-    --dxg-facet-other: #77a9e8;
-  }
-}
-
 html { scroll-behavior: smooth; }

 body.shell-body {
@@ -3673,7 +3696,7 @@
   text-rendering: optimizeLegibility;
 }

-::selection { background: rgba(40, 112, 82, .20); }
+::selection { background: var(--selection-bg, rgba(40, 112, 82, .20)); }

 :where(button, select, summary, a):focus-visible {
   outline: 2px solid var(--accent);
@@ -3726,9 +3749,79 @@
 .system-resizing { cursor: col-resize; user-select: none; }

 .sidebar-header {
+  position: relative;
   padding: 20px 18px 18px;
 }

+.system-panel-toggle {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  width: 32px;
+  height: 32px;
+  padding: 0;
+  border: 1px solid var(--border);
+  border-radius: 6px;
+  background: var(--card-bg);
+  color: var(--muted);
+  cursor: pointer;
+  transition: border-color 150ms ease-out, color 150ms ease-out, background-color 150ms ease-out;
+}
+
+.system-panel-toggle:hover {
+  border-color: color-mix(in srgb, var(--accent) 62%, var(--border));
+  background: var(--accent-bg);
+  color: var(--ink);
+}
+
+.system-panel-toggle__chevron {
+  width: 7px;
+  height: 7px;
+  border-right: 1.5px solid currentColor;
+  border-bottom: 1.5px solid currentColor;
+  transform: rotate(135deg);
+  transition: transform 180ms ease-out;
+}
+
+.system-panel-toggle--sidebar {
+  position: absolute;
+  top: 16px;
+  right: 9px;
+}
+
+.sidebar-header .top { padding-right: 28px; }
+
+@media (min-width: 861px) {
+  .sidebar,
+  .content-area { transition: width 180ms ease-out, padding 180ms ease-out; }
+
+  body.system-sidebar-collapsed .sidebar {
+    width: 44px;
+    min-width: 44px;
+    max-width: 44px;
+    overflow: hidden;
+  }
+
+  body.system-sidebar-collapsed .sidebar-header {
+    padding: 10px 6px;
+    border-bottom: 0;
+  }
+
+  body.system-sidebar-collapsed .sidebar-header .top,
+  body.system-sidebar-collapsed #nav,
+  body.system-sidebar-collapsed #expandAllToggle,
+  body.system-sidebar-collapsed .sidebar-footer,
+  body.system-sidebar-collapsed .system-sidebar-resizer { display: none; }
+
+  body.system-sidebar-collapsed .system-panel-toggle--sidebar {
+    position: static;
+  }
+
+  body.system-sidebar-collapsed .system-panel-toggle--sidebar .system-panel-toggle__chevron {
+    transform: rotate(-45deg);
+  }
+}
+
 .top {
   align-items: flex-start;
   gap: 12px;
@@ -3807,7 +3900,7 @@
 .system-nav-group__toggle::marker { content: ""; }
 .system-nav-group__toggle::-webkit-details-marker { display: none; }

-.system-nav-group__toggle:hover { background: rgba(125, 137, 154, .08); }
+.system-nav-group__toggle:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); }

 .system-nav-group__count {
   min-width: 25px;
@@ -3858,7 +3951,7 @@

 #dxgOpen:hover,
 .sec-tab:hover {
-  background: rgba(125, 137, 154, .08);
+  background: var(--hover-bg, rgba(125, 137, 154, .08));
   color: var(--ink);
 }

@@ -3905,8 +3998,26 @@
   flex: 1;
   margin: 0;
   padding: 0 282px 80px 42px;
+  transition: padding 180ms ease-out;
 }

+@media (min-width: 1181px) {
+  body.system-toc-collapsed .content-area { padding-right: 86px; }
+}
+
+/* The Build order tab reclaims the 282px right rail .content-area reserves
+   for the facet TOC, which renderToc hides on this tab, so the widest
+   diagram on the page is not laid out beside a blank gutter. Scoped to
+   screen because at (0,3,1) an unscoped version would beat the print
+   block's `.content-area { padding: 0 }` (0,1,0) on paper — the first rule
+   to break the invariant the print header states — and to the same
+   min-width as the TOC-collapsed rule above because the same specificity
+   equally outranks the tablet and phone .content-area rules, where the rail
+   it reclaims does not exist. */
+@media screen and (min-width: 1181px) {
+  body:has(.build-order-section:not([hidden])) .content-area { padding-right: 42px; }
+}
+
 .system-record-head {
   display: grid;
   grid-template-columns: minmax(0, 1fr) auto;
@@ -3914,7 +4025,7 @@
   align-items: start;
   margin: 0 0 24px;
   padding: 38px 0 24px;
-  border-bottom: 1px solid #aab5c7;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
 }

 .system-record-head h2 {
@@ -3973,16 +4084,204 @@
   font-weight: 700;
 }

-.build-order-module[hidden] { display: none; }
+/* ---- Build order tab (.bo-*) --------------------------------------------
+   The viewer's build order is a top-level tab of its own (#dossierx-build-order, a
+   .module-section) holding one .bo-module group per locked module, each six
+   .bo-phase blocks in the fixed sequence with one mermaid flowchart per
+   non-empty phase (internal/buildorder/mermaid.go; build-order.html).

-.build-order-module.system-orientation-only > :not(.system-build-title):not(.system-orientation-node) {
-  display: none;
+   .bo-modules is the module strip: NOT .sub-nav, which has never needed
+   horizontal overflow above 860px and has none — 25 locked modules at a 23px
+   gap run off the desktop column with no scrollbar. It scrolls sideways at
+   every width, sticky like the .sub-nav it stands in for (same z-index).
+
+   COLOURS: every colour declaration below is !important, and that is not
+   decoration. mermaid injects its own stylesheet into each SVG, compiled
+   under the SVG's id — "#mermaid-1 .node rect" at (1,1,1) — which outranks
+   any unqualified page rule, so without !important every node paints in
+   mermaid's base-theme lavender: light boxes on --paper: #0a1220 under the
+   dark block, the exact failure these rules exist to prevent. The diagram
+   text carries NO colour literal (PaletteCSS emits only stroke-dasharray
+   classDefs), so the light :root, the screen-dark block and a project's
+   viewer.theme all apply here by construction. The ghost rule names path as
+   well as rect: a stadium is drawn by rough.js as <path> elements inside a
+   <g class="basic label-container outer-path">, never a <rect>. The rules
+   assume mermaid's default look (classic); under look: 'handDrawn' even a
+   rectangle is a <path>, which is why build-order-ui.js never sets look.
+   The edge rule targets .flowchart-link, mermaid 11's class on every edge
+   path; the arrowhead markers live in <defs> and are painted from the
+   lineColor themeVariable build-order-ui.js reads off --muted at
+   initialise time. ---------------------------------------------------- */
+.bo-modules {
+  display: flex;
+  flex-wrap: nowrap;
+  gap: 23px;
+  overflow-x: auto;
+  scrollbar-width: thin;
+  position: sticky;
+  z-index: 8;
+  top: 0;
+  margin: 0 0 24px;
+  padding: 0;
+  border-bottom: 1px solid var(--border);
+  background: var(--paper);
 }

-.build-order-module.system-orientation-only > .build-order-phase.system-orientation-node {
-  display: none;
+.bo-modules .subtab {
+  flex: 0 0 auto;
+  white-space: nowrap;
 }

+.bo-phase {
+  padding: 22px 0 26px;
+  border-top: 1px solid var(--border);
+}
+
+.bo-phase__head {
+  display: grid;
+  gap: 6px;
+  margin: 0 0 14px;
+  padding: 0 0 14px;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
+}
+
+.bo-phase__num {
+  color: var(--accent);
+  font: 700 11px/1.4 var(--font-mono);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+}
+
+.bo-phase__name {
+  margin: 0;
+  color: var(--ink);
+  font-size: 22px;
+  font-weight: 600;
+  letter-spacing: -.02em;
+}
+
+.bo-phase__def {
+  max-width: 72ch;
+  margin: 0;
+  color: var(--ink);
+  font-size: 14px;
+}
+
+.bo-phase__meta,
+.bo-empty {
+  margin: 0;
+  color: var(--muted);
+  font-size: 13px;
+  font-variant-numeric: tabular-nums;
+}
+
+/* The wide-diagram rule: the SVG keeps its natural width (mermaid runs with
+   useMaxWidth: false and the max-width rule is belt and braces), a block
+   wider than the column scrolls inside .bo-diagram, and .content-area never
+   gains a horizontal scrollbar. Nothing scales a diagram down. */
+.bo-diagram {
+  overflow-x: auto;
+  overflow-y: hidden;
+  padding: 14px 10px;
+  border: 1px solid var(--border);
+  border-radius: 8px;
+  background: var(--card-bg);
+}
+
+.bo-diagram svg {
+  display: block;
+  max-width: none !important;
+  height: auto;
+}
+
+/* Nothing else in this sheet styles a bare pre (only .claim-body pre), so
+   without these the block shows its flowchart source as a wall of monospace
+   between parse and the first render pass. mermaid stamps data-processed
+   BEFORE it reads the text, so the hiding stops the moment a render starts
+   and never hides a failed block (whose reader sees .bo-error and mermaid's
+   own message). After the render the <pre> survives around the SVG and its
+   white-space reaches the html labels' foreignObject content. */
+.bo-diagram pre.mermaid {
+  margin: 0;
+  white-space: normal;
+  font-family: var(--font-sans);
+}
+
+.bo-diagram pre.mermaid:not([data-processed]) {
+  visibility: hidden;
+  min-height: 2rem;
+}
+
+.bo-diagram .node.locked_con rect {
+  fill: var(--accent-bg) !important;
+  stroke: var(--accent) !important;
+}
+
+.bo-diagram .node.draft_con rect {
+  fill: var(--card-bg) !important;
+  stroke: var(--accent) !important;
+}
+
+.bo-diagram .node.locked_int rect {
+  fill: color-mix(in srgb, var(--link) 12%, var(--card-bg)) !important;
+  stroke: var(--link) !important;
+}
+
+.bo-diagram .node.draft_int rect {
+  fill: var(--card-bg) !important;
+  stroke: var(--link) !important;
+}
+
+.bo-diagram .node.ghost rect,
+.bo-diagram .node.ghost path {
+  fill: var(--card-bg) !important;
+  stroke: var(--border) !important;
+}
+
+.bo-diagram .node .nodeLabel {
+  color: var(--ink) !important;
+}
+
+.bo-diagram .node.ghost .nodeLabel {
+  color: var(--muted) !important;
+}
+
+.bo-diagram .flowchart-link {
+  stroke: var(--muted) !important;
+}
+
+.bo-diagram g.node { cursor: pointer; }
+
+.bo-diagram g.node.bo-missing {
+  opacity: .55;
+  cursor: not-allowed;
+}
+
+.bo-cross,
+.bo-excluded {
+  margin: 12px 0 0;
+  padding: 0 0 0 18px;
+  color: var(--muted);
+  font-size: 13px;
+}
+
+.bo-cross__module {
+  color: var(--ink);
+  font-weight: 600;
+}
+
+.bo-excluded-deps {
+  margin: 8px 0 0;
+  color: var(--muted);
+  font-size: 13px;
+}
+
+.bo-error {
+  margin: 8px 0 0;
+  color: var(--warn);
+  font-size: 13px;
+}
+
 .facet-toc {
   position: fixed;
   z-index: 9;
@@ -3993,6 +4292,7 @@
   border-left: 1px solid var(--border);
   background: var(--paper);
   color: var(--muted);
+  transition: width 180ms ease-out;
 }

 .facet-toc[hidden] { display: none; }
@@ -4022,6 +4322,36 @@
   color: var(--ink);
 }

+.facet-toc__head .system-panel-toggle {
+  flex: 0 0 auto;
+  width: 28px;
+  height: 28px;
+  margin: 0 0 -4px;
+}
+
+.system-panel-toggle--toc .system-panel-toggle__chevron { transform: rotate(-45deg); }
+
+body.system-toc-collapsed .facet-toc {
+  width: 44px;
+  border-left: 0;
+  background: transparent;
+}
+
+body.system-toc-collapsed .facet-toc__head {
+  justify-content: center;
+  padding: 4px;
+  border-bottom: 0;
+}
+
+body.system-toc-collapsed .facet-toc__identity,
+body.system-toc-collapsed .facet-toc__total,
+body.system-toc-collapsed .facet-toc__list,
+body.system-toc-collapsed .facet-toc__select { display: none; }
+
+body.system-toc-collapsed .system-panel-toggle--toc .system-panel-toggle__chevron {
+  transform: rotate(135deg);
+}
+
 .facet-toc__head small {
   display: block;
   margin-bottom: 3px;
@@ -4072,10 +4402,69 @@

 .facet-toc__item > span { color: var(--faint); font: 9px/1.45 var(--font-mono); }
 .facet-toc__item > strong { font-size: 11px; font-weight: 520; line-height: 1.38; }
-.facet-toc__item:hover { background: rgba(125, 137, 154, .08); color: var(--ink); }
+.facet-toc__item:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); color: var(--ink); }
 .facet-toc__item.on { background: var(--accent-bg); box-shadow: inset 2px 0 0 var(--accent); color: var(--ink); }
 .facet-toc__item.on > span { color: var(--accent); }

+.facet-claim-controls {
+  display: flex;
+  justify-content: flex-end;
+  margin: 0 0 12px;
+}
+
+.facet-claims-toggle {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  min-height: 34px;
+  padding: 6px 10px;
+  border: 1px solid var(--border);
+  border-radius: 6px;
+  background: var(--card-bg);
+  color: var(--muted);
+  font: 650 10px/1.2 var(--font-mono);
+  cursor: pointer;
+}
+
+.facet-claims-toggle:hover {
+  border-color: color-mix(in srgb, var(--accent) 62%, var(--border));
+  color: var(--ink);
+}
+
+.facet-claims-toggle:disabled { cursor: default; opacity: .5; }
+
+.facet-claims-toggle__icon {
+  position: relative;
+  width: 11px;
+  height: 11px;
+}
+
+.facet-claims-toggle__icon::before,
+.facet-claims-toggle__icon::after {
+  content: "";
+  position: absolute;
+  left: 2px;
+  width: 6px;
+  height: 6px;
+  border-right: 1.5px solid currentColor;
+  border-bottom: 1.5px solid currentColor;
+  transform: rotate(45deg);
+  transition: transform 150ms ease-out, top 150ms ease-out;
+}
+
+.facet-claims-toggle__icon::before { top: -1px; }
+.facet-claims-toggle__icon::after { top: 3px; }
+
+.facet-claims-toggle[aria-pressed="true"] .facet-claims-toggle__icon::before {
+  top: 2px;
+  transform: rotate(225deg);
+}
+
+.facet-claims-toggle[aria-pressed="true"] .facet-claims-toggle__icon::after {
+  top: 6px;
+  transform: rotate(225deg);
+}
+
 .claim { scroll-margin-top: 72px; }

 .section-heading {
@@ -4166,7 +4555,7 @@
   transform: translateY(-50%);
 }

-.claim-collapse-toggle:hover { background: rgba(125, 137, 154, .08); }
+.claim-collapse-toggle:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); }

 .claim-collapse-toggle > .label {
   display: flex;
@@ -4219,8 +4608,8 @@

 .pill.pv,
 .status-draft {
-  background: rgba(151, 102, 0, .12);
-  color: #976600;
+  background: var(--status-draft-bg, rgba(151, 102, 0, .12));
+  color: var(--status-draft, #976600);
 }

 .pill.pw,
@@ -4244,18 +4633,22 @@
 .claim-body strong,
 .sbody strong { font-weight: 700; }

+/* The LIVE `code` background: this rule beats the inline-code pill rule near
+   the top of the file at equal specificity, on source order. --code-inline-bg
+   is read here so the token actually reaches a reader. */
 code {
   border: 1px solid var(--border);
-  background: color-mix(in srgb, var(--paper) 72%, var(--card-bg));
+  background: var(--code-inline-bg, color-mix(in srgb, var(--paper) 72%, var(--card-bg)));
   color: var(--ink);
   font-size: .86em;
 }

+/* The LIVE fenced-block background, same story as `code` above. */
 .claim-body pre,
 .claim-tree-body {
   padding: 13px 14px;
   border-radius: 7px;
-  background: color-mix(in srgb, var(--paper) 82%, var(--card-bg));
+  background: var(--code-bg, color-mix(in srgb, var(--paper) 82%, var(--card-bg)));
   font-size: 12px;
 }

@@ -4280,6 +4673,11 @@

 .claim-links-summary::marker { content: ""; }
 .claim-links-summary::-webkit-details-marker { display: none; }
+/* NOT --hover-bg, and deliberately so. This row is much larger than the other
+   hover targets and takes a fainter wash: .07, not .08. Reading the token here
+   would compute .08, because :root always declares --hover-bg and the fallback
+   would never fire — the row would silently change. It is not themable through
+   --hover-bg; a project that wants it themable needs its own token. */
 .claim-links-summary:hover { background: rgba(125, 137, 154, .07); color: var(--ink); }

 .claim-footer__identity { display: flex; gap: 10px; align-items: center; min-width: 0; }
@@ -4385,7 +4783,7 @@
 .track-head {
   margin: 0 0 24px;
   padding: 38px 0 23px;
-  border-bottom: 1px solid #aab5c7;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
 }

 .track-title {
@@ -4411,69 +4809,10 @@

 .track-cite > .pill { margin-left: auto; }

-.build-order-module {
-  margin-top: 34px;
-  padding-top: 22px;
-  border-top: 1px solid var(--border);
-}
-
-.build-order-module > .system-build-title {
-  margin: 0 0 24px;
-  padding: 0 0 15px;
-  border-top: 0;
-  border-bottom: 1px solid #aab5c7;
-  color: var(--ink);
-  font-size: 20px;
-  letter-spacing: -.02em;
-  text-transform: none;
-}
-
-.build-order-module > .build-order-phase {
-  margin: 30px 0 10px;
-  padding: 0;
-  border: 0;
-  color: var(--muted);
-  font: 700 10px/1.4 var(--font-mono);
-  letter-spacing: .09em;
-}
-
-.build-order-module .card { padding: 15px 17px; }
-.build-order-module .k { margin-bottom: 11px; font-size: 14px; }
-.build-order-module .claim-edges { display: grid; gap: 8px; }
-
-.build-order-module .claim-file,
-.build-order-module .claim-rests-on {
-  display: grid;
-  grid-template-columns: 72px minmax(0, 1fr);
-  gap: 10px;
-  align-items: start;
-  color: var(--muted);
-  font-size: 11px;
-}
-
-.build-order-field__label {
-  color: var(--faint);
-  font: 700 9px/1.55 var(--font-mono);
-  letter-spacing: .07em;
-  text-transform: uppercase;
-}
-
-.build-order-module code {
-  border: 0;
-  background: transparent;
-  overflow-wrap: anywhere;
-}
-
-.build-order-module .claim-edge-id-list {
-  margin: 0;
-  padding: 0;
-  list-style: none;
-}
-
 .comments-rail {
   border-left-color: var(--border);
   background: var(--card-bg);
-  box-shadow: -14px 0 34px rgba(9, 20, 38, .12);
+  box-shadow: -14px 0 34px var(--shadow-cast, rgba(9, 20, 38, .12));
 }

 .comments-rail-head {
@@ -4483,7 +4822,6 @@

 .status-strip {
   border-top-color: var(--border);
-  background: var(--card-bg);
   color: var(--muted);
 }

@@ -4803,13 +5141,23 @@
   }

   .facet-toc__disclosure,
-  .facet-toc__head,
   .facet-toc__list { display: none; }
+  .facet-toc__head {
+    position: absolute;
+    z-index: 1;
+    top: 0;
+    right: 0;
+    justify-content: flex-end;
+    width: 40px;
+    padding: 6px;
+    border: 0;
+  }
+  .facet-toc__head > :not(.system-panel-toggle) { display: none; }
   .facet-toc__select {
     display: block;
     width: 100%;
     min-height: 40px;
-    padding: 7px 30px 7px 10px;
+    padding: 7px 46px 7px 10px;
     border: 0;
     border-radius: 6px;
     background: var(--card-bg);
@@ -4832,11 +5180,13 @@
     border-radius: 6px;
     background: var(--card-bg);
     color: var(--ink);
-    box-shadow: 0 7px 18px rgba(9, 20, 38, .12);
+    box-shadow: 0 7px 18px var(--shadow-cast, rgba(9, 20, 38, .12));
   }

   .sidebar { width: min(310px, calc(100vw - 42px)); min-width: 0; max-width: none; }
   .system-sidebar-resizer { display: none; }
+  .system-panel-toggle--sidebar { display: none; }
+  .sidebar-header .top { padding-right: 0; }

   .facet-toc {
     position: fixed;
@@ -4848,17 +5198,27 @@
     border: 1px solid var(--border);
     border-radius: 7px;
     background: var(--card-bg);
-    box-shadow: 0 7px 18px rgba(9, 20, 38, .12);
+    box-shadow: 0 7px 18px var(--shadow-cast, rgba(9, 20, 38, .12));
   }

   .facet-toc__disclosure,
-  .facet-toc__head,
   .facet-toc__list { display: none; }
+  .facet-toc__head {
+    position: absolute;
+    z-index: 1;
+    top: 0;
+    right: 0;
+    justify-content: flex-end;
+    width: 40px;
+    padding: 5px;
+    border: 0;
+  }
+  .facet-toc__head > :not(.system-panel-toggle) { display: none; }
   .facet-toc__select {
     display: block;
     width: 100%;
     min-height: 38px;
-    padding: 7px 30px 7px 10px;
+    padding: 7px 46px 7px 10px;
     border: 0;
     border-radius: 7px;
     background: var(--card-bg);
@@ -4931,16 +5291,101 @@
   .claim-footer__counts { justify-content: flex-start; }
   .claim-footer__counts > span:not(.claim-footer__chevron) { padding: 4px 6px; }
   .claim-footer__chevron { margin-left: auto; }
-
-  .build-order-module .claim-file,
-  .build-order-module .claim-rests-on { grid-template-columns: 1fr; gap: 3px; }
 }

+/* ---- print — THE @media print block ------------------------------------
+   There is exactly one @media print block in this stylesheet and this is it;
+   internal/render/theme_tokens_test.go fails if a second one appears.
+
+   IT MUST STAY LAST IN THE FILE. Several of the rules below beat a screen
+   rule of identical specificity purely on source order — `.content-area
+   { padding: 0 }` against the padded `.content-area` rule in the System
+   Record layer, `.system-record-head { padding-top: 0 }` against that layer's
+   `.system-record-head`. Move this block earlier and the printed page silently
+   regains the sidebar gutter.
+
+   color-scheme is pinned to light here to match the palette: the :root at the
+   top of the file is light-first and its dark query is `screen`-scoped, so
+   print already gets light custom properties; this line makes the UA-painted
+   parts (form controls, scrollbars, canvas) agree. Unverified for Firefox and
+   Safari; where a browser already forces light for print it is a no-op, so it
+   is safe either way.
+
+   Consolidated from three separate @media print blocks. Their reasons follow,
+   unchanged, one per group.
+
+   NOTE (Q4): paper has no button to press, so the source-note clamp must not
+   reach it — print shows every note whole and hides the control.
+
+   DISCLOSURE (Q4). Paper has no disclosure widget, so a printed viewer that
+   honoured the collapsed <details> would silently drop every claim's
+   governed_by/rests_on/implemented-in footer — the exact facts a reviewer
+   prints the page to read.
+   Revealing the contents through CSS is deliberate rather than setting `open`,
+   which CSS cannot do: `open` is a content attribute, not a property a rule
+   can write.
+   Same mechanism, and the same two-rule pairing, as the .claim:target rule
+   above: a closed <details> hides its contents through the UA's
+   ::details-content pseudo-element, so that is what is targeted, and the
+   child-selector form is expected to be a no-op wherever the pseudo-element is
+   implemented. It is kept only as a fallback for engines that predate
+   ::details-content (Chrome/Edge 131+, Safari 18.4+, Firefox 139+) and hide
+   the contents some other way; such an engine drops the pseudo-element rule
+   and still prints as it did before v0.4.1. Both rules declare the same value
+   and carry equal specificity (0,1,2), so the pseudo-element rule is written
+   LAST — source order is the only thing ranking them.
+   This block is independent of the .claim:target rule, of the two
+   server-written `open` signals, and of the expand-all toolbar — whichever of
+   those is or is not in effect on screen, print shows all four states
+   identically.
+   SCOPE — stated plainly so the next reader knows this is a decision, not an
+   oversight: only the facet currently ON SCREEN prints. A collapsed footer
+   belonging to a claim in a facet group the reader is not looking at stays
+   unrendered, because its ancestor section.claim-group carries the `hidden`
+   attribute (computed display: none) and no rule here un-hides it. The rules
+   below reveal a disclosure inside a subtree that is being laid out; they
+   cannot resurrect a subtree that display: none removed from the box tree in
+   the first place. That is deliberate: printing a tabbed viewer has always
+   shown one facet, what v0.4.1 changes is that the shown facet's footers now
+   print with it, and un-hiding every facet for print is a larger design
+   change — page order, per-facet headings, the duplicated overview copies —
+   than a patch release should make.
+   Only the disclosure is touched. Nothing here restyles the card, the type
+   scale or the color tokens; a full print stylesheet is out of scope for
+   v0.4.1.
+*/
 @media print {
+  :root { color-scheme: light; }
+
+  /* source-note clamp */
+  .claim-source-note.is-clamped .claim-source-note-body {
+    display: block;
+    overflow: visible;
+  }
+
+  .claim-source-note-toggle {
+    display: none;
+  }
+
+  /* collapsed claim-links footer */
+  details.claim-links > *:not(summary) {
+    content-visibility: visible;
+  }
+
+  details.claim-links::details-content {
+    content-visibility: visible;
+  }
+
+  .claim-links-summary {
+    cursor: auto;
+  }
+
+  /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
   .nav-toggle,
   .facet-toc,
+  .facet-claim-controls,
   .comments-rail,
   .status-strip { display: none !important; }
   .content-area { padding: 0; }
@@ -4949,6 +5394,27 @@
   .claim-collapse-toggle { pointer-events: none; }
   .claim-collapse-chevron { display: none; }
   .claim-collapse-content[hidden] { display: block !important; }
+
+  /* Build order tab. What this buys is honest and small: a phase block is
+     not split across a page break. Browsers do not paginate horizontally, so
+     a diagram wider than the paper is CLIPPED at the page edge; nothing here
+     makes it spill onto further pages. The marker rules exist because
+     lineColor and background are baked into the SVG at initialise time from
+     the SCREEN palette while this block pins color-scheme: light and the
+     dark block above is screen-scoped: a dark-mode reader printing would get
+     light node fills beside pale-grey arrowheads on a near-black backdrop.
+     The module strip is the ONLY place the printed module's name appears
+     (build-order.html's section has no heading of its own), so the strip
+     stays on the page: only the buttons for the OTHER modules are dropped,
+     the selected one prints as the page's title line, unstuck from the
+     viewport it no longer has. */
+  .bo-modules { position: static; }
+  .bo-modules .subtab:not(.on) { display: none; }
+  .bo-diagram { overflow: visible; }
+  .bo-phase { break-inside: avoid; }
+  .bo-diagram svg { background: transparent !important; }
+  .bo-diagram .marker,
+  .bo-diagram marker path { fill: var(--muted) !important; stroke: var(--muted) !important; }
 }
 </style>
   <style></style>
@@ -4963,6 +5429,9 @@
           <div class="logo"><span></span></div>
           <div><h1>dossierx viewer</h1></div>
         </div>
+        <button id="sidebarCollapseToggle" class="system-panel-toggle system-panel-toggle--sidebar" type="button" aria-controls="sidebar" aria-expanded="true" aria-label="Hide navigation" title="Hide navigation">
+          <span class="system-panel-toggle__chevron" aria-hidden="true"></span>
+        </button>
       </div>
       <nav id="nav">
         
@@ -5037,7 +5506,6 @@
                 
               </section>
             
-            
           </section>
         
       
@@ -5058,8 +5526,12 @@
   
   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
     <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
-      <span id="statusStripSummary" class="status-strip-summary"></span>
-      <span class="status-strip-caret" aria-hidden="true">▾</span>
+      <span id="statusStripSummary" class="status-strip-summary">
+        <strong id="statusStripTitle" class="status-strip-title"></strong>
+        <span id="statusStripNote" class="status-strip-note"></span>
+      </span>
+      <span id="statusStripAction" class="status-strip-action">Show issues</span>
+      <span class="status-strip-caret" aria-hidden="true"></span>
     </button>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>
@@ -5084,7 +5556,7 @@
       
       
       var moduleSections, moduleTabs;
-      var facetToModule, moduleDefaultFacet, firstModuleID, claimToFacet, buildOrderToModule, sourceToFacet;
+      var facetToModule, moduleDefaultFacet, firstModuleID, claimToFacet, sourceToFacet;

       
       
@@ -5149,22 +5621,6 @@
         
         
         
-        
-        
-        buildOrderToModule = {};
-        moduleSections.forEach(function (sec) {
-          sec.querySelectorAll('.build-order-module').forEach(function (bo) {
-            if (bo.id) { buildOrderToModule[bo.id] = sec.id; }
-            bo.querySelectorAll('.claim[id]').forEach(function (c) {
-              buildOrderToModule[c.id] = sec.id;
-            });
-          });
-        });
-
-        
-        
-        
-        
         syncEmptyChips();

         
@@ -5183,6 +5639,8 @@
       
       
       
+      
+      
       function resolve(id) {
         if (Object.prototype.hasOwnProperty.call(claimToFacet, id)) {
           var facetID = claimToFacet[id];
@@ -5199,10 +5657,6 @@
           var srcFacet = sourceToFacet[id];
           return { module: facetToModule[srcFacet], facet: srcFacet, claim: id };
         }
-        if (Object.prototype.hasOwnProperty.call(buildOrderToModule, id)) {
-          var boModule = buildOrderToModule[id];
-          return { module: boModule, facet: moduleDefaultFacet[boModule], claim: id };
-        }
         if (Object.prototype.hasOwnProperty.call(facetToModule, id)) {
           return { module: facetToModule[id], facet: id };
         }
@@ -5235,6 +5689,12 @@
           });
         }

+        if (lastStatusData) {
+          renderStatusStrip(lastStatusData);
+        } else {
+          positionStatusStrip();
+        }
+
         
         
         
@@ -6072,7 +6532,11 @@
       var stripEl = document.getElementById('statusStrip');
       var stripToggle = document.getElementById('statusStripToggle');
       var stripSummary = document.getElementById('statusStripSummary');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripNote = document.getElementById('statusStripNote');
+      var stripAction = document.getElementById('statusStripAction');
       var stripBody = document.getElementById('statusStripBody');
+      var lastStatusData = null;
       
       
       
@@ -6086,6 +6550,59 @@
         return n + ' ' + word + (n === 1 ? '' : 's');
       }

+      function humanRule(name) {
+        return String(name || '').split('-').filter(Boolean).map(function (part) {
+          return part.charAt(0).toUpperCase() + part.slice(1);
+        }).join(' ');
+      }
+
+      function activeFacetClaimIDs() {
+        var ids = Object.create(null);
+        
+        
+        var section = document.querySelector('.module-section:not([hidden]):not(.build-order-section)');
+        var group = section && section.querySelector(':scope > .claim-group:not([hidden])');
+        if (!group) { return ids; }
+        group.querySelectorAll('.claim').forEach(function (claim) {
+          var id = claim.dataset.claimId || claim.id;
+          if (id) { ids[id] = true; }
+        });
+        return ids;
+      }
+
+      function findingsForActiveFacet(findings, claimIDs) {
+        return findings.filter(function (finding) {
+
+
+
+          return !finding.claim_id || claimIDs[finding.claim_id] === true;
+        });
+      }
+
+
+
+
+
+
+      function positionStatusStrip() {
+        if (!stripEl) { return; }
+        
+        
+        var section = document.querySelector('.module-section:not([hidden]):not(.build-order-section)');
+        if (!section) {
+          var content = document.querySelector('.content-area');
+          if (content && stripEl.parentNode !== content) { content.insertBefore(stripEl, content.firstChild); }
+          return;
+        }
+        var header = section.querySelector(':scope > .system-record-head, :scope > .track-head');
+        if (header) {
+          if (header.nextElementSibling !== stripEl) { header.insertAdjacentElement('afterend', stripEl); }
+        } else if (section.firstElementChild !== stripEl) {
+          section.insertBefore(stripEl, section.firstChild);
+        }
+      }
+      window.dossierxPositionStatusStrip = positionStatusStrip;
+
       
       
       
@@ -6114,9 +6631,11 @@
       
       
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary) { return; }
-        var ledger = (data && data.ledger_findings) || [];
-        var lintErrors = (data && data.lint_errors) || [];
+        if (!stripEl || !stripBody || !stripSummary || !stripTitle || !stripNote) { return; }
+        lastStatusData = data || {};
+        var claimIDs = activeFacetClaimIDs();
+        var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
+        var lintErrors = findingsForActiveFacet(lastStatusData.lint_errors || [], claimIDs);

         if (!ledger.length && !lintErrors.length) {
           stripEl.hidden = true;
@@ -6128,31 +6647,36 @@
         stripBody.textContent = '';
         if (ledger.length) {
           stripBody.appendChild(findingGroup(
-            'Lock ledger — these claims were changed without an approval record',
-            ledger.map(function (f) { return findingRow(f.rule, f.claim_id, f.message); })
+            'Approval record issues',
+            ledger.map(function (f) { return findingRow(humanRule(f.rule), f.claim_id, f.message); })
           ));
         }
         if (lintErrors.length) {
           stripBody.appendChild(findingGroup(
-            'Lint errors',
-            lintErrors.map(function (f) { return findingRow(f.lint, f.claim_id, f.message); })
+            'Issues to resolve',
+            lintErrors.map(function (f) { return findingRow(humanRule(f.lint), f.claim_id, f.message); })
           ));
         }

         
         
-        var summary = ledger.length
-          ? 'Integrity: ' + countLabel(ledger.length, 'lock-ledger finding')
-          : 'Lint: ' + countLabel(lintErrors.length, 'error');
-        if (ledger.length && lintErrors.length) {
-          summary += ' · ' + countLabel(lintErrors.length, 'lint error');
+        if (ledger.length) {
+          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
+          stripNote.textContent = lintErrors.length
+            ? 'There ' + (lintErrors.length === 1 ? 'is ' : 'are ') + countLabel(lintErrors.length, 'issue') + ' in this facet also blocking the next successful DossierX check.'
+            : 'Review these locked claims before relying on their approved state.';
+        } else {
+          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
+          stripNote.textContent = lintErrors.length === 1
+            ? 'This issue blocks the next successful DossierX check.'
+            : 'These issues block the next successful DossierX check.';
         }
-        stripSummary.textContent = summary; 

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        if (ledger.length && !stripUserToggled) { stripExpanded = true; }
+        if (!stripUserToggled) { stripExpanded = ledger.length > 0; }
         setStripExpanded(stripExpanded);
+        positionStatusStrip();
         stripEl.hidden = false;
       }

@@ -6160,6 +6684,7 @@
         stripExpanded = !!expanded;
         if (stripBody) { stripBody.hidden = !stripExpanded; }
         if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
+        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
         if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
       }

@@ -6529,6 +7054,23 @@
         oldNav.outerHTML = frag.nav;           

         
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        if (document.querySelector('.build-order-section') && typeof window.mermaid === 'undefined') {
+          window.location.reload();
+          return;
+        }
+
+        
         initViewer();

         
@@ -6715,10 +7257,44 @@
     });
   }

-  function syncNavigation() {
+  function bindSidebarCollapse() {
+    var toggle = document.getElementById('sidebarCollapseToggle');
+    if (!toggle || toggle.dataset.bound === 'true') { return; }
+    toggle.dataset.bound = 'true';
+    toggle.addEventListener('click', function () {
+      var collapsed = !document.body.classList.contains('system-sidebar-collapsed');
+      document.body.classList.toggle('system-sidebar-collapsed', collapsed);
+      toggle.setAttribute('aria-expanded', String(!collapsed));
+      toggle.setAttribute('aria-label', collapsed ? 'Show navigation' : 'Hide navigation');
+      toggle.title = collapsed ? 'Show navigation' : 'Hide navigation';
+    });
+  }
+
+  function bindNavigationGroupPreferences() {
+    document.querySelectorAll('.system-nav-group').forEach(function (group) {
+      if (group.dataset.readerPreferenceBound === 'true') { return; }
+      group.dataset.readerPreferenceBound = 'true';
+      var summary = group.querySelector(':scope > summary');
+      if (summary) {
+        summary.addEventListener('click', function (event) {
+          event.preventDefault();
+          var willOpen = !group.open;
+          group.dataset.readerClosed = String(!willOpen);
+          group.open = willOpen;
+        });
+      }
+      group.addEventListener('toggle', function () {
+        group.dataset.readerClosed = group.open ? 'false' : 'true';
+      });
+    });
+  }
+
+  function syncNavigation(force) {
     var active = document.querySelector('.system-nav-group .sec-tab.on');
     var group = active && active.closest('.system-nav-group');
-    if (group) { group.open = true; }
+    if (!group || (!force && group.dataset.readerClosed === 'true')) { return; }
+    group.dataset.readerClosed = 'false';
+    group.open = true;
   }

   function enhanceFooters() {
@@ -6766,18 +7342,16 @@
       ['.claim-links > .claim-edges > .claim-migrated', 'Migrated From', /^\s*migrated_from:\s*/i],
       ['.claim-links > .claim-edges > .claim-implemented-in', 'Implemented In', /^\s*implemented\s+in:\s*/i],
       ['.claim-links > .claim-edges > .claim-review-pending', 'Review Pending', /^\s*review_pending\s*/i],
-      ['.claim-links > .claim-edges > .claim-sources', 'Sources', /^\s*sources:\s*/i],
-      ['.build-order-module .claim-file', 'File', /^\s*file:\s*/i],
-      ['.build-order-module .claim-rests-on', 'Rests On', /^\s*rests_on:\s*/i]
+      ['.claim-links > .claim-edges > .claim-sources', 'Sources', /^\s*sources:\s*/i]
     ];
     rows.forEach(function (entry) {
       document.querySelectorAll(entry[0]).forEach(function (row) {
-        if (row.querySelector(':scope > .claim-relation-label, :scope > .build-order-field__label')) { return; }
+        if (row.querySelector(':scope > .claim-relation-label')) { return; }
         var textNode = Array.prototype.slice.call(row.childNodes).find(function (node) { return node.nodeType === 3 && node.textContent.trim(); });
         if (!textNode) { return; }
         textNode.textContent = textNode.textContent.replace(entry[2], ' ');
         var label = document.createElement('span');
-        label.className = entry[0].indexOf('.build-order-module') === 0 ? 'build-order-field__label' : 'claim-relation-label';
+        label.className = 'claim-relation-label';
         label.textContent = entry[1];
         row.insertBefore(label, textNode);
       });
@@ -6795,6 +7369,7 @@
     toggle.setAttribute('aria-expanded', String(expanded));
     var title = toggle.dataset.claimTitle || 'claim';
     toggle.setAttribute('aria-label', (expanded ? 'Collapse ' : 'Expand ') + title);
+    updateFacetClaimControl();
   }

   function enhanceClaimDisclosures() {
@@ -6852,7 +7427,7 @@

   function cleanTitle(claim) {
     var title = claim.querySelector(':scope > .k');
-    if (!title) { return (claim.id || '').replace(/^build-order-/, '').replace(/[.-]/g, ' '); }
+    if (!title) { return (claim.id || '').replace(/[.-]/g, ' '); }
     var copy = title.cloneNode(true);
     copy.querySelectorAll('.pill, .claim-comments-slot').forEach(function (node) { node.remove(); });
     return copy.textContent.replace(/\s+/g, ' ').trim();
@@ -6869,14 +7444,13 @@
     });
   }

+  // activeFacet returns null while the Build order tab is active (it is not a
+  // module section: no header, TOC, status strip or claim controls are built
+  // over the diagrams), so renderToc hides the TOC there.
   function activeFacet() {
-    var modules = Array.prototype.slice.call(document.querySelectorAll('.module-section:not(.track-section)'));
+    var modules = Array.prototype.slice.call(document.querySelectorAll('.module-section:not(.track-section):not(.build-order-section)'));
     var module = modules.find(function (section) { return !section.hidden; });
     if (!module) { return null; }
-    var build = module.querySelector(':scope > .build-order-module:not([hidden])');
-    if (build) {
-      return { module: module, view: build, label: build.classList.contains('system-orientation-only') ? 'Orientation' : 'Build Order' };
-    }
     var groups = Array.prototype.slice.call(module.querySelectorAll(':scope > .claim-group'));
     var group = groups.find(function (section) { return !section.hidden; });
     if (!group) { return null; }
@@ -6884,6 +7458,45 @@
     return { module: module, view: group, label: tab ? tab.textContent.replace(/🔒/g, '').trim() : 'Claims' };
   }

+  function updateFacetClaimControl(active, claims) {
+    active = active || activeFacet();
+    if (!active) { return; }
+    claims = claims || visibleClaims(active.view);
+    var control = active.view.querySelector(':scope > .facet-claim-controls');
+    if (!control) { return; }
+    var toggle = control.querySelector('.facet-claims-toggle');
+    if (!toggle) { return; }
+    var allCollapsed = claims.length > 0 && claims.every(function (claim) {
+      var disclosure = claim.querySelector(':scope > .k > .claim-collapse-toggle');
+      return disclosure && disclosure.getAttribute('aria-expanded') === 'false';
+    });
+    toggle.disabled = claims.length === 0;
+    toggle.setAttribute('aria-pressed', String(allCollapsed));
+    toggle.querySelector('.facet-claims-toggle__label').textContent = allCollapsed ? 'Expand all claims' : 'Collapse all claims';
+  }
+
+  function renderFacetClaimControl(active, claims) {
+    var control = active.view.querySelector(':scope > .facet-claim-controls');
+    if (!control) {
+      control = document.createElement('div');
+      control.className = 'facet-claim-controls';
+      control.innerHTML = '<button class="facet-claims-toggle" type="button" aria-pressed="false"><span class="facet-claims-toggle__icon" aria-hidden="true"></span><span class="facet-claims-toggle__label">Collapse all claims</span></button>';
+      active.view.insertBefore(control, active.view.firstChild);
+      control.querySelector('.facet-claims-toggle').addEventListener('click', function () {
+        var current = activeFacet();
+        if (!current) { return; }
+        var currentClaims = visibleClaims(current.view);
+        var shouldExpand = currentClaims.length > 0 && currentClaims.every(function (claim) {
+          var disclosure = claim.querySelector(':scope > .k > .claim-collapse-toggle');
+          return disclosure && disclosure.getAttribute('aria-expanded') === 'false';
+        });
+        currentClaims.forEach(function (claim) { setClaimExpanded(claim, shouldExpand); });
+        updateFacetClaimControl(current, currentClaims);
+      });
+    }
+    updateFacetClaimControl(active, claims);
+  }
+
   function updateTocActive() {
     var toc = document.getElementById('systemFacetToc');
     if (!toc || toc.hidden) { return; }
@@ -6906,11 +7519,18 @@
       toc.id = 'systemFacetToc';
       toc.className = 'facet-toc';
       toc.setAttribute('aria-label', 'Claims in this facet');
-      toc.innerHTML = '<div class="facet-toc__head"><span><small>On this facet</small><strong class="facet-toc__name">Claims</strong></span><span class="facet-toc__total"></span></div><nav class="facet-toc__list"></nav><select class="facet-toc__select" aria-label="Jump to a claim in this facet"></select>';
+      toc.innerHTML = '<div class="facet-toc__head"><span class="facet-toc__identity"><small>On this facet</small><strong class="facet-toc__name">Claims</strong></span><span class="facet-toc__total"></span><button class="system-panel-toggle system-panel-toggle--toc" type="button" aria-controls="systemFacetToc" aria-expanded="true" aria-label="Hide table of contents" title="Hide table of contents"><span class="system-panel-toggle__chevron" aria-hidden="true"></span></button></div><nav class="facet-toc__list"></nav><select class="facet-toc__select" aria-label="Jump to a claim in this facet"></select>';
       toc.querySelector('.facet-toc__select').addEventListener('change', function (event) {
         var claim = document.getElementById(event.target.value);
         if (claim) { claim.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
       });
+      toc.querySelector('.system-panel-toggle--toc').addEventListener('click', function (event) {
+        var collapsed = !document.body.classList.contains('system-toc-collapsed');
+        document.body.classList.toggle('system-toc-collapsed', collapsed);
+        event.currentTarget.setAttribute('aria-expanded', String(!collapsed));
+        event.currentTarget.setAttribute('aria-label', collapsed ? 'Show table of contents' : 'Hide table of contents');
+        event.currentTarget.title = collapsed ? 'Show table of contents' : 'Hide table of contents';
+      });
       document.body.appendChild(toc);
     }
     var active = activeFacet();
@@ -6919,6 +7539,7 @@
     var claims = visibleClaims(active.view);
     toc.querySelector('.facet-toc__name').textContent = active.label;
     toc.querySelector('.facet-toc__total').textContent = claims.length + (claims.length === 1 ? ' claim' : ' claims');
+    renderFacetClaimControl(active, claims);
     var list = toc.querySelector('.facet-toc__list');
     var select = toc.querySelector('.facet-toc__select');
     list.replaceChildren();
@@ -6945,99 +7566,12 @@
     updateTocActive();
   }

-  function prepareBuildOrder(module) {
-    var build = module.querySelector(':scope > .build-order-module');
-    if (!build || build.dataset.systemPrepared === 'true') { return build; }
-    build.dataset.systemPrepared = 'true';
-    var title = build.querySelector(':scope > h3');
-    if (title) { title.classList.add('system-build-title'); title.dataset.originalText = title.textContent; }
-    var children = Array.prototype.slice.call(build.children);
-    var orientation = children.find(function (node) {
-      return node.classList.contains('build-order-phase') && node.textContent.trim().toLowerCase() === 'orientation';
-    });
-    if (orientation) {
-      var start = children.indexOf(orientation);
-      for (var index = start; index < children.length; index += 1) {
-        var node = children[index];
-        if (index > start && node.classList.contains('build-order-phase')) { break; }
-        node.classList.add('system-orientation-node');
-      }
-      build.dataset.hasOrientation = 'true';
-    }
-    return build;
-  }
-
-  function activateFacet(module, mode, targetID, updateHash) {
-    var build = prepareBuildOrder(module);
-    var isBuild = mode === 'build-order' || mode === 'orientation';
-    module.querySelectorAll(':scope > .claim-group').forEach(function (group) { group.hidden = isBuild || group.id !== targetID; });
-    if (build) {
-      build.hidden = !isBuild;
-      build.classList.toggle('system-orientation-only', mode === 'orientation');
-      var title = build.querySelector(':scope > .system-build-title');
-      if (title) { title.textContent = mode === 'orientation' ? 'Orientation' : title.dataset.originalText; }
-    }
-    module.querySelectorAll(':scope > .sub-nav .subtab').forEach(function (tab) {
-      var tabMode = tab.dataset.systemMode || 'claims';
-      tab.classList.toggle('on', tabMode === mode && (isBuild || tab.dataset.target === '#' + targetID));
-    });
-    if (updateHash) { history.replaceState(null, '', '#' + targetID); }
-    scrollTo(0, 0);
-    renderToc();
-  }
-
-  function enhanceFacetNavigation() {
-    document.querySelectorAll('.module-section:not(.track-section)').forEach(function (module) {
-      var build = prepareBuildOrder(module);
-      if (!build) { return; }
-      var subnav = module.querySelector(':scope > .sub-nav');
-      if (!subnav) {
-        subnav = document.createElement('div');
-        subnav.className = 'sub-nav system-created-sub-nav';
-        var firstGroup = module.querySelector(':scope > .claim-group');
-        var firstTab = document.createElement('button');
-        firstTab.className = 'subtab';
-        firstTab.dataset.target = '#' + firstGroup.id;
-        firstTab.textContent = 'Claims';
-        subnav.appendChild(firstTab);
-        module.insertBefore(subnav, firstGroup);
-      }
-      if (!subnav.querySelector('[data-system-mode="build-order"]')) {
-        [['build-order', 'Build Order', '#' + build.id], ['orientation', 'Orientation', '#orientation-' + module.id]].forEach(function (entry) {
-          if (entry[0] === 'orientation' && build.dataset.hasOrientation !== 'true') { return; }
-          var tab = document.createElement('button');
-          tab.type = 'button';
-          tab.className = 'subtab system-added-subtab';
-          tab.dataset.target = entry[2];
-          tab.dataset.systemMode = entry[0];
-          tab.textContent = entry[1];
-          tab.addEventListener('click', function (event) {
-            event.preventDefault();
-            event.stopImmediatePropagation();
-            activateFacet(module, entry[0], entry[2].slice(1), true);
-          }, true);
-          subnav.appendChild(tab);
-        });
-      }
-      subnav.querySelectorAll('.subtab:not(.system-added-subtab)').forEach(function (tab) {
-        if (tab.dataset.systemBound === 'true') { return; }
-        tab.dataset.systemBound = 'true';
-        tab.addEventListener('click', function () { setTimeout(function () { activateFacet(module, 'claims', tab.dataset.target.slice(1), false); }, 0); });
-      });
-      var active = Array.prototype.slice.call(module.querySelectorAll(':scope > .claim-group')).find(function (group) { return !group.hidden; });
-      if (active && !module.dataset.systemActivated) {
-        module.dataset.systemActivated = 'true';
-        activateFacet(module, 'claims', active.id, false);
-      }
-    });
-  }
-
   function addModuleHeaders() {
     var labels = {};
     document.querySelectorAll('.sec-tab[data-target]').forEach(function (button) {
       labels[button.dataset.target.slice(1)] = button.textContent.replace(/🔒/g, '').trim();
     });
-    document.querySelectorAll('.module-section:not(.track-section)').forEach(function (section) {
+    document.querySelectorAll('.module-section:not(.track-section):not(.build-order-section)').forEach(function (section) {
       if (section.querySelector(':scope > .system-record-head')) { return; }
       var claims = Array.prototype.slice.call(section.querySelectorAll('.claim-group .claim[data-status]'));
       var unique = {};
@@ -7088,12 +7622,16 @@
     running = true;
     enhanceTimestamp();
     bindResizer();
+    bindSidebarCollapse();
+    bindNavigationGroupPreferences();
     enhanceFooters();
     enhanceFieldLabels();
     enhanceClaimDisclosures();
     revealHashTarget();
     addModuleHeaders();
-    enhanceFacetNavigation();
+    if (typeof window.dossierxPositionStatusStrip === 'function') {
+      window.dossierxPositionStatusStrip();
+    }
     syncNavigation();
     renderToc();
     enhanceGraphLabels();
@@ -7119,11 +7657,11 @@
     // in the same event cycle whenever it contained the active tab.
     var navigationChanged = !!event.target.closest('.sec-tab');
     setTimeout(function () {
-      if (navigationChanged) { syncNavigation(); }
+      if (navigationChanged) { syncNavigation(true); }
       renderToc();
     }, 0);
   });
-  window.addEventListener('hashchange', function () { setTimeout(function () { revealHashTarget(); syncNavigation(); renderToc(); }, 0); });
+  window.addEventListener('hashchange', function () { setTimeout(function () { revealHashTarget(); syncNavigation(true); renderToc(); }, 0); });
   window.addEventListener('scroll', updateTocActive, { passive: true });
   window.dossierxEnhanceSystemRecord = enhance;
   enhance();

testdata/fixture-graph-demo/build/viewer/index.html  (+1019 -485)
    inputs changed  testdata/fixture-graph-demo/build/.gitignore
                    testdata/fixture-graph-demo/build/ledger/comment-digest.json
                    testdata/fixture-graph-demo/build/ledger/lock-store.json
    size            13452 lines / 565597 bytes -> 13986 lines / 588049 bytes
@@ -135,22 +135,35 @@
    in the same metric the ramp is solved in rather than in degrees of hue: the
    ramp solver treats --dxg-governed, --dxg-cycle, --dxg-halo and
    --dxg-facet-other as members of the already-chosen set with a hard floor of
-   CIEDE2000 20, so no slot it picks can land near any of them. Measured over
-   the shipped values, the closest any of the twenty slots comes to a reserved
-   colour is 14.4 in dark and 11.1 in light — and the light 11.1 is frozen
-   slot 5 (#67717E) against --dxg-facet-other (#7D8C85), two greys this file
-   inherited rather than chose. Against --dxg-governed itself, every slot in
-   both themes is at least 20 away. A governance edge cannot be mistaken for a
-   facet's colour at any facet count in either theme.
+   CIEDE2000 20, so no slot it picks can land near any of them. Measured
+   against the values DECLARED IN THIS FILE, the closest any of the twenty
+   slots comes to a reserved colour is 14.4 in dark and 11.1 in light — and
+   the light 11.1 is frozen slot 5 (#67717E) against this file's
+   --dxg-facet-other (#7D8C85), two greys this file inherited rather than
+   chose. Read that figure as a bound on this file alone: shell.html emits
+   style.css after graph.css, and style.css re-declares --dxg-facet-other
+   (blue, not grey) for the shipped viewer, so the only project that sees the
+   grey — and therefore that 11.1 — is one whose viewer.template_overrides
+   replaces style.css. Against --dxg-governed itself, which style.css does not
+   re-declare, every slot in both themes is at least 20 away. A governance
+   edge cannot be mistaken for a facet's colour at any facet count in either
+   theme.

    --dxg-cycle and --dxg-halo are STATE colours, not identity colours. They
    are drawn as a ring and a halo — never as a plain node fill — except under
    the one overlay that is specifically about that state, where every
    non-matching node is dimmed and there is nothing to confuse them with. */

-/* DARK IS THE BASE, light is the override — the same convention the base
-   stylesheet uses, so the two files move together rather than fighting over
-   which one a reader's OS appearance reaches first. Both sets are complete:
+/* DARK IS THE BASE, light is the override. This file keeps that convention
+   DELIBERATELY, and it is now the opposite of style.css, which is light-first
+   (one unconditional :root plus a `screen and (prefers-color-scheme: dark)`
+   query). The two files differ on purpose: this ramp was solved dark-first and
+   renumbering it to light-first would mean re-solving both sets, which changes
+   every existing project's facet colours for no reader-visible gain. What the
+   two files DO share is the print outcome — the light query below carries
+   `, print`, so paper gets the light ramp whatever the OS appearance is, the
+   same result style.css reaches by scoping its dark query to `screen`.
+   Both sets are complete:
    a categorical ramp has to move in BOTH directions, because the dark values
    are lifted and desaturated to stay legible on --paper's near-black and the
    light values are darkened to stay legible on its near-white. Neither set
@@ -184,7 +197,11 @@
   --dxg-facet-19: #B6B994;
   --dxg-facet-20: #A9D6C0;

-  /* No facet, an unlisted facet, or a collapsed module group. */
+  /* No facet, an unlisted facet, or a collapsed module group.
+     KEPT AS A FALLBACK, NOT AS THE SHIPPED VALUE: style.css declares
+     --dxg-facet-other too and is emitted after this file, so this grey only
+     reaches a project whose viewer.template_overrides replaces style.css.
+     Do not delete it — for such a project it is the only declaration. */
   --dxg-facet-other: #6C7F75;

   /* State colours. Ring, halo and the two structural overlays only. */
@@ -195,7 +212,10 @@
   --dxg-governed: #F06A9C;
 }

-@media (prefers-color-scheme: light) {
+/* `, print`: paper is light, whatever the OS says. A printed graph pane with
+   the dark ramp on white would be the same defect style.css's `screen and`
+   dark query prevents for the base palette. */
+@media (prefers-color-scheme: light), print {
   :root {
     --dxg-facet-1: #4257C4;
     --dxg-facet-2: #12897F;
@@ -223,6 +243,8 @@
     --dxg-facet-19: #6F4D37;
     --dxg-facet-20: #8782A8;

+    /* Light fallback, same standing as the dark one above: style.css's own
+       light value wins for the shipped sheet. */
     --dxg-facet-other: #7D8C85;

     --dxg-cycle: #D1201A;
@@ -1136,42 +1158,129 @@
    a project's own template_overrides CSS; a project can restyle any of
    this by overriding component partials via viewer.template_overrides.

-   Token names below (--accent, --accent-bg, --ink, --muted, --faint, --paper,
-   --card-bg, --border, --link, --warn, --warn-bg, --font-sans, --font-mono,
-   --radius) are the engine's fixed viewer.theme allowlist (see
-   internal/config/config.go). Defaults here mirror the reference docs
-   palette this engine's authoring project uses, so an unconfigured project
-   still renders with a sane look;
-   render.go injects a second <style> block after this one (and after the
-   dark-mode media query below) so any project-supplied theme token wins in
-   both OS color modes without disturbing tokens the project left unset. */
+   COLOUR MECHANISM, in cascade order. shell.html emits three <style> blocks:
+   graph.css, then this file, then the generated theme block.

+   1. One unconditional `:root` holds the palette, and it is LIGHT-FIRST:
+      every value in it is both the light-OS value and the printed value.
+      There is exactly one such block in this file.
+   2. `@media screen and (prefers-color-scheme: dark)` re-points the handful
+      of tokens that invert. The `screen and` prefix is load-bearing: it is
+      what keeps a dark-mode OS from dragging dark ink onto paper.
+   3. The single `@media print` block is the LAST block in this file. It has
+      to be last, because it overrides screen layout rules at equal
+      specificity (`.content-area { padding: 0 }` beats the padded screen
+      rule only on source order), and it pins `color-scheme: light`.
+   4. internal/render appends the project's theme as the third <style>: an
+      optional @font-face run, a shared `:root`, then
+      `@media (prefers-color-scheme: light), print` and
+      `@media screen and (prefers-color-scheme: dark)`. The project's light
+      block deliberately also matches `print` and its dark block deliberately
+      cannot, so a token a project sets only under `dark:` prints the engine's
+      light value rather than its dark one.
+
+   The names a project may set are the 28-entry allowlist in
+   internal/config/config.go (ThemeTokenAllowlist). Each token's :root default
+   is the value a light OS gets, and the screen-dark block carries the dark
+   values for the three that differ. A consumer spells the token as
+   `var(--token, <fallback>)`; the fallback is the LIGHT value, because this
+   sheet is light-first and because the fallback only ever fires for a
+   viewer.template_overrides project that forked this file and dropped the
+   :root — which should degrade to light, not to dark.
+
+   A token has to be read by the declaration that WINS, not merely by a
+   declaration. Two rules in this file are re-declared later at equal
+   specificity (`code` and `.claim-body pre, .claim-tree-body`), so their
+   token reads live in the later copy; internal/render/theme_tokens_test.go
+   asserts, per token, that the last declaration of the consumer's
+   selector+property pair is the one carrying the var(). It also fails if an
+   allowlisted token has no consumer in this file or in graph.css, and pins
+   the structural invariants listed above.
+
+   graph.css keeps the OPPOSITE convention for its own --dxg-* ramp (dark
+   base, light override) and says so in its own header; the two files differ
+   deliberately. */
+
+/* THE palette. One unconditional :root, light-first: the values below are what
+   a light-OS screen and a printed page both get. Nothing else in this file
+   declares any of these properties, so this block's position is free; it sits
+   here, at the top, because that is where a reader looks for it.
+
+   The dark query is scoped to `screen` on purpose. Without the prefix, a
+   reader whose OS is in dark mode and who hits Print gets near-black --paper
+   and pale --ink; print media does not carry an appearance, so nothing else
+   would put the light palette back. `screen and` is the whole fix, and the
+   @media print block at the end of the file pins `color-scheme: light` beside
+   it so form controls and scrollbars agree. This is unverified for Firefox and
+   Safari; where a browser already forces light for print it is a no-op, so it
+   is safe either way. */
+
 :root {
   color-scheme: light dark;
-  --accent: #3fb950;
-  --accent-bg: rgba(63, 185, 80, .14);
-  --ink: #e6ece8;
-  --muted: #9aa6a0;
-  --faint: #6f7a74;
-  --paper: #0f1311;
-  --card-bg: #1b211d;
-  --border: #2a322c;
-  --link: #4c9bf5;
-  --warn: #e0623f;
-  --warn-bg: rgba(224, 98, 63, .15);
-  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
-  --font-mono: ui-monospace, SFMono-Regular, Menlo, monospace;
-  --radius: 12px;
+  --accent: #287052;
+  --accent-bg: rgba(40, 112, 82, .12);
+  --ink: #091426;
+  --muted: #536179;
+  --faint: #7d899a;
+  --paper: #f6f8fc;
+  --card-bg: #ffffff;
+  --border: #d8deea;
+  --link: #205b78;
+  --warn: #a2433d;
+  --warn-bg: rgba(162, 67, 61, .10);
+  --dxg-facet-other: #0d55b5;
+  --font-sans: "Avenir Next", -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", sans-serif;
+  --font-mono: ui-monospace, "SFMono-Regular", "IBM Plex Mono", Menlo, monospace;
+  --radius: 6px;
+
+  /* Added for the custom-theme work. Each value below is what a LIGHT OS and
+     a printed page get; the screen-dark query re-points the three that differ
+     per mode (--shadow, --shadow-strong, --scrim). The other eleven are the
+     same in both modes on purpose: a neutral overlay tint tracks whatever it
+     sits on, and the mockup card is deliberately mode-invariant.
+     Every value is the expression the live consumer used before it read a
+     token, so an unset token renders exactly as it did before. Note "live":
+     for --code-inline-bg and --code-bg that is the System Record layer's
+     color-mix, not the rgba() in the dead declaration those rules have always
+     beaten. Order matches ThemeTokenAllowlist. */
+  --code-inline-bg: color-mix(in srgb, var(--paper) 72%, var(--card-bg));
+  --code-bg: color-mix(in srgb, var(--paper) 82%, var(--card-bg));
+  --table-head-bg: rgba(127, 127, 127, .10);
+  --image-bg: rgba(127, 127, 127, .06);
+  --hover-bg: rgba(125, 137, 154, .08);
+  --border-strong: #aab5c7;
+  --shadow: rgba(0, 0, 0, .08);
+  --shadow-strong: rgba(0, 0, 0, .14);
+  --shadow-cast: rgba(9, 20, 38, .12);
+  --scrim: rgba(0, 0, 0, .22);
+  --selection-bg: rgba(40, 112, 82, .20);
+  --status-draft: #976600;
+  --status-draft-bg: rgba(151, 102, 0, .12);
+  --mockup-bg: #fff;
 }

-@media (prefers-color-scheme: light) {
+@media screen and (prefers-color-scheme: dark) {
   :root {
-    --paper: #f3f6f4;
-    --card-bg: #fafcfb;
-    --border: #e2e8e4;
-    --ink: #1d2622;
-    --muted: #5d6862;
-    --faint: #8a948e;
+    --ink: #e8eef8;
+    --muted: #a9b5c8;
+    --faint: #75839a;
+    --paper: #0a1220;
+    --card-bg: #0f1b2e;
+    --border: #263754;
+    --link: #8ab7ff;
+    --accent: #70c99c;
+    --accent-bg: rgba(112, 201, 156, .12);
+    --warn: #ff8b94;
+    --warn-bg: rgba(255, 139, 148, .10);
+    --dxg-facet-other: #77a9e8;
+
+    /* The three theme tokens that genuinely differ per mode: a shadow needs
+       more alpha to read on a dark surface, and the modal dim needs more to
+       separate the sheet from what is behind it. The other eleven new tokens
+       are mode-invariant and are declared once, above. */
+    --shadow: rgba(0, 0, 0, .28);
+    --shadow-strong: rgba(0, 0, 0, .34);
+    --scrim: rgba(0, 0, 0, .42);
   }
 }

@@ -1217,14 +1326,11 @@
   background: var(--card-bg);
 }

-/* status-draft/status-locked map to the pill family below: draft -> amber
-   (engine's own draft concept, not part of the fixed color allowlist),
-   locked -> accent, review_pending -> warn. */
-.status-draft {
-  color: #d6a128;
-  background: rgba(214, 161, 40, .16);
-}
-
+/* status-draft/status-locked map to the pill family below: locked -> accent,
+   review_pending -> warn. draft -> amber is the engine's own concept and not
+   part of the fixed color allowlist; its one live rule sits with the pills in
+   the System Record layer further down (`.pill.pv, .status-draft`), which is
+   why there is no .status-draft rule here. */
 .status-locked {
   color: var(--accent);
   background: var(--accent-bg);
@@ -1241,11 +1347,14 @@
    every component routes body-shaped text through the shared "markdown"
    template func). Ported verbatim from the reference docs stylesheet's global
    `code{...}` rule so a regex, config key, or field name wrapped in single
-   backticks reads as a distinct code "pill", not plain prose. */
+   backticks reads as a distinct code "pill", not plain prose.
+
+   NO background here. The System Record layer's own `code` rule further down
+   re-declares it at equal specificity and later in the file, so anything set
+   here is dead; --code-inline-bg is read there, where it can win. */
 code {
   font-family: var(--font-mono);
   font-size: 12px;
-  background: rgba(127, 127, 127, .12);
   padding: 1px 5px;
   border-radius: 5px;
 }
@@ -1253,11 +1362,15 @@
 /* Fenced ```code``` spans inside a claim's Body, converted to real
    <pre><code> elements by markdown.Render. white-space is reset off the
    pre-wrap inherited from .claim-body since <pre> already preserves
-   whitespace on its own. Background is the same neutral rgba(127,127,127)
-   overlay tint the reference docs stylesheet's `pre{...}` rule uses (not
-   --card-bg, which is the surrounding card's own background and would
-   make the block invisible against it — the exact "white background, not
-   grey" regression this replaces). */
+   whitespace on its own.
+
+   NO background here, for the same reason as the `code` rule above: the
+   System Record layer re-declares this exact selector list later in the file
+   at equal specificity, so a background set here never reaches a reader.
+   --code-bg is read there. What that rule must NOT be is --card-bg, which is
+   the surrounding card's own background and would make the block invisible
+   against it — the "white background, not grey" regression these rules
+   replaced; it is a tint of --paper over --card-bg instead. */
 .claim-body pre,
 .claim-tree-body {
   font-size: 11.5px;
@@ -1266,7 +1379,6 @@
   overflow-x: auto;
   margin: 0.5rem 0;
   padding: 0.6rem 0.75rem;
-  background: rgba(127, 127, 127, .10);
   border: 1px solid var(--border);
   border-radius: var(--radius);
 }
@@ -1517,7 +1629,7 @@
 .claim-list-items .md-table th,
 .sbody .md-table th,
 .comment-body .md-table th {
-  background: rgba(127, 127, 127, .10);
+  background: var(--table-head-bg, rgba(127, 127, 127, .10));
   font-family: var(--font-sans);
   font-size: inherit;
   font-weight: 600;
@@ -1581,7 +1693,7 @@
   margin: 0.6rem 0;
   border: 1px solid var(--border);
   border-radius: 6px;
-  background: rgba(127, 127, 127, .06);
+  background: var(--image-bg, rgba(127, 127, 127, .06));
 }

 /* An image is the last thing in a list item often enough to be worth the
@@ -1893,8 +2005,8 @@
 }

 /* claim-ref is the shared anchor EVERY claim-to-claim edge renders through
-   (components.go's writeClaimRef): governed_by, mirrors, rests_on,
-   depended-on-by, and build_order.html's rests_on. Its two inner spans carry
+   (components.go's writeClaimRef): governed_by, mirrors, rests_on and
+   depended-on-by, all inside the shared claim footer. Its two inner spans carry
    the readability tiers — a dimmed "Widget · Contract ›" prefix, kept only
    when the target is outside the reading claim's own module/facet, and the
    claim's own label. No new color tokens: the prefix borrows --faint (the
@@ -1905,9 +2017,8 @@
    Specificity note: these are scoped under .claim-edges deliberately. The
    bare .claim-ref-prefix class (0,1,0) would lose to the existing
    ".claim-edges a" rule (0,1,1) and inherit --link; ".claim-edges
-   .claim-ref-prefix" (0,2,0) wins. build_order.html's independently rendered
-   rests_on sits inside a .claim-edges container too, so one scope covers
-   both paths. */
+   .claim-ref-prefix" (0,2,0) wins. The shared claim footer is the only
+   place a claim-ref renders, and this sheet is inlined into every viewer. */
 .claim-edges .claim-ref-prefix {
   color: var(--faint);
   font-weight: 400;
@@ -2095,20 +2206,6 @@
   text-decoration: underline;
 }

-/* Paper has no button to press, so the clamp must not reach it: print shows
-   every note whole and hides the control, the same bargain @media print
-   already strikes with the collapsed footer. */
-@media print {
-  .claim-source-note.is-clamped .claim-source-note-body {
-    display: block;
-    overflow: visible;
-  }
-
-  .claim-source-note-toggle {
-    display: none;
-  }
-}
-
 /* Deep-link auto-open for a SOURCE row, the companion to the .claim:target
    rules above. A citation marker's href names the row, not the claim, so the
    claim is not :target and those rules do not fire — yet the row the reader
@@ -2139,9 +2236,10 @@
   border-radius: 4px;
 }

+/* Colour and background are NOT set here: `.pill.pw, .claim-review-pending`
+   in the System Record layer sets both at equal specificity and later in the
+   file, so anything declared here would be dead. */
 .claim-review-pending {
-  color: var(--warn);
-  background: var(--warn-bg);
   padding: 0.1rem 0.4rem;
   border-radius: 4px;
   font-weight: 600;
@@ -2188,11 +2286,10 @@
    which is why a `.claim-comments { list-style: none; }` rule lived here —
    it is deleted in v0.4.1 along with that <li>, since the slot is a <span>
    and never carried a marker. Every color below is one of the inverting
-   theme tokens declared in :root above (and re-pointed in the
-   prefers-color-scheme: light block), so the whole surface tracks the OS
-   theme with no separate dark rule — the one genuinely per-mode value, the
-   panel's drop shadow, gets its light value in the media query at the end of
-   this block. claim-card--commented is a distinct accent marker,
+   theme tokens (declared in this file's single :root and re-pointed in the
+   screen-dark query beside it), so the whole surface tracks the OS theme with
+   no separate dark rule — including the one genuinely per-mode value, the
+   panel's drop shadow, which is --shadow. claim-card--commented is a distinct accent marker,
    deliberately NOT the --warn boundary family. */
 .comment-chip {
   display: inline-flex;
@@ -2282,7 +2379,7 @@
   border: 1px solid var(--border);
   border-radius: var(--radius, 12px);
   background: var(--paper);
-  box-shadow: 0 1px 2px rgba(0, 0, 0, .28);
+  box-shadow: 0 1px 2px var(--shadow, rgba(0, 0, 0, .08));
 }

 .comments-panel[hidden] {
@@ -2366,14 +2463,6 @@
   }
 }

-/* The panel shadow is the one genuinely per-mode value here: softer on a
-   light background than the deeper shadow the dark base above uses. */
-@media (prefers-color-scheme: light) {
-  .comments-panel {
-    box-shadow: 0 1px 2px rgba(0, 0, 0, .08);
-  }
-}
-
 /* ---- interactive comment UI (Phase 5, serve + file://) -----------------
    The 💬 chip opens a shared overlay panel: a non-modal right rail on desktop
    (role=complementary), a modal bottom sheet on mobile. Its backdrop
@@ -2381,10 +2470,13 @@
    half, so they are scoped to the <= 860px bottom-sheet breakpoint (in the media
    query below) — above it the desktop rail shows neither. They stay deliberately
    distinct from the sidebar's #navOverlay / body.nav-open — opening one overlay
-   closes the other. Colours are the inverting theme tokens from :root; the few
-   genuinely per-mode values (shadows, backdrop dim) get their light values in the
-   media query at the end of this block, matching the dark-base / light-override
-   convention the rest of this stylesheet uses. */
+   closes the other. Colours are the inverting theme tokens from this file's
+   single :root, including the per-mode ones: the backdrop dim is --scrim and
+   the toast shadow is --shadow-strong, both re-pointed in the screen-dark
+   query, so this block needs no light media query of its own. The rail's own
+   shadow is deliberately NOT per-mode: the `.comments-rail` rule in the
+   System Record layer further down sets --shadow-cast for both modes and wins
+   on source order. */

 /* body.comments-open drives the scroll-lock only at the mobile breakpoint (see
    the media query below); it stays a plain open-state marker on desktop. Never
@@ -2398,7 +2490,7 @@
   width: 100%;
   height: 100%;
   border: 0;
-  background: rgba(0, 0, 0, .42);
+  background: var(--scrim, rgba(0, 0, 0, .22));
   cursor: pointer;
 }

@@ -2416,7 +2508,6 @@
   flex-direction: column;
   background: var(--paper);
   border-left: 1px solid var(--border);
-  box-shadow: -2px 0 18px rgba(0, 0, 0, .38);
 }

 .comments-rail[hidden] {
@@ -2603,7 +2694,7 @@
   border: 1px solid var(--warn);
   border-radius: 10px;
   font: 13px var(--font-sans);
-  box-shadow: 0 4px 18px rgba(0, 0, 0, .34);
+  box-shadow: 0 4px 18px var(--shadow-strong, rgba(0, 0, 0, .14));
 }

 .comments-toast[hidden] {
@@ -2653,20 +2744,17 @@
    body.comments-open: three classes each setting overflow: hidden compose
    without a release-order hazard, so no coordination is needed between them. */

-/* Docked clear of the 210px desktop sidebar (its "Generated …" footer sits in
-   exactly this corner); the mobile override below reclaims the full-width left
-   edge, where the sidebar is an off-canvas drawer and owns no space. */
+/* Project health belongs to the reading flow, between the active module's
+   record header and its facet controls. It is deliberately quiet when closed:
+   enough contrast to be noticed, but no floating dock or shadow competing with
+   the document and its two navigation rails. */
 .status-strip {
-  position: fixed;
-  z-index: 45;
-  left: 226px;
-  bottom: 16px;
-  max-width: min(520px, calc(100vw - 242px));
+  width: 100%;
+  margin: -8px 0 22px;
   background: var(--card-bg);
   border: 1px solid var(--border);
-  border-radius: var(--radius);
-  font: 13px var(--font-sans);
-  box-shadow: 0 4px 18px rgba(0, 0, 0, .34);
+  border-radius: 7px;
+  font-family: var(--font-sans);
   overflow: hidden;
 }

@@ -2675,40 +2763,66 @@
 }

 .status-strip--lint {
-  border-color: var(--warn);
+  border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
+  background: color-mix(in srgb, var(--warn-bg) 34%, var(--card-bg));
 }

 .status-strip--integrity {
-  border-color: var(--warn);
-  border-left-width: 4px;
-  background: var(--warn-bg);
+  border-color: color-mix(in srgb, var(--warn) 48%, var(--border));
+  background: color-mix(in srgb, var(--warn-bg) 58%, var(--card-bg));
+  box-shadow: inset 0 2px 0 var(--warn);
 }

 .status-strip-head {
   display: flex;
   align-items: center;
-  gap: 10px;
+  gap: 12px;
   width: 100%;
   border: 0;
   background: none;
-  color: var(--warn);
-  padding: 10px 14px;
-  font: 600 13px var(--font-sans);
+  color: var(--ink);
+  padding: 13px 14px;
   text-align: left;
   cursor: pointer;
 }

 .status-strip-summary {
+  display: grid;
+  gap: 3px;
   flex: 1;
 }

-.status-strip-caret {
+.status-strip-title {
+  font-size: 13px;
+  font-weight: 680;
+  line-height: 1.35;
+}
+
+.status-strip-note {
   color: var(--muted);
+  font-size: 11.5px;
+  line-height: 1.45;
+}
+
+.status-strip-action {
+  color: var(--accent);
+  font: 600 10px var(--font-mono);
+  letter-spacing: .04em;
+  text-transform: uppercase;
+  white-space: nowrap;
+}
+
+.status-strip-caret {
+  width: 7px;
+  height: 7px;
+  border-right: 1.5px solid var(--muted);
+  border-bottom: 1.5px solid var(--muted);
+  transform: rotate(45deg) translateY(-2px);
   transition: transform .15s ease;
 }

 .status-strip--open .status-strip-caret {
-  transform: rotate(180deg);
+  transform: rotate(225deg) translate(-2px, -1px);
 }

 /* The detail list is capped and scrolls internally: a project with forty lint
@@ -2717,7 +2831,7 @@
   max-height: 40vh;
   overflow-y: auto;
   border-top: 1px solid var(--border);
-  padding: 10px 14px 12px;
+  padding: 12px 14px 14px;
 }

 .status-strip-body[hidden] {
@@ -2730,8 +2844,8 @@

 .status-group-head {
   margin: 0 0 6px;
-  font: 600 11px var(--font-sans);
-  letter-spacing: .04em;
+  font: 600 9px var(--font-mono);
+  letter-spacing: .08em;
   text-transform: uppercase;
   color: var(--muted);
 }
@@ -2758,11 +2872,15 @@

 .status-finding-rule,
 .status-finding-claim {
-  font: 11px var(--font-mono);
+  font: 10px var(--font-mono);
 }

 .status-finding-rule {
   color: var(--warn);
+  font-size: 9px;
+  font-weight: 600;
+  letter-spacing: .05em;
+  text-transform: uppercase;
 }

 .status-finding-claim {
@@ -2771,15 +2889,10 @@

 .status-finding-msg {
   flex-basis: 100%;
-  font-size: 12px;
+  font-size: 12.5px;
+  line-height: 1.5;
 }

-@media (prefers-color-scheme: light) {
-  .status-strip {
-    box-shadow: 0 4px 18px rgba(0, 0, 0, .14);
-  }
-}
-
 /* Coarse pointers (touch) need >=44px hit targets on every comment control. */
 @media (pointer: coarse) {
   .comments-rail-close,
@@ -2813,33 +2926,28 @@
     border-left: 0;
     border-top: 1px solid var(--border);
     border-radius: 14px 14px 0 0;
-    box-shadow: 0 -2px 18px rgba(0, 0, 0, .38);
   }
-  /* The sidebar is an off-canvas drawer here, so the left edge is free again. */
   .status-strip {
-    left: 16px;
-    max-width: calc(100vw - 32px);
+    margin-top: -4px;
   }
-}
-
-/* Per-mode values: softer shadows and a lighter backdrop dim on a light OS. */
-@media (prefers-color-scheme: light) {
-  .comments-rail {
-    box-shadow: -2px 0 18px rgba(0, 0, 0, .14);
+  .status-strip-head {
+    display: grid;
+    grid-template-columns: minmax(0, 1fr) 10px;
+    align-items: flex-start;
   }
-
-  .comments-overlay {
-    background: rgba(0, 0, 0, .22);
+  .status-strip-summary {
+    grid-column: 1;
+    grid-row: 1;
   }
-
-  .comments-toast {
-    box-shadow: 0 4px 18px rgba(0, 0, 0, .14);
+  .status-strip-action {
+    grid-column: 1;
+    grid-row: 2;
+    justify-self: start;
   }
-}
-
-@media (prefers-color-scheme: light) and (max-width: 860px) {
-  .comments-rail {
-    box-shadow: 0 -2px 18px rgba(0, 0, 0, .14);
+  .status-strip-caret {
+    grid-column: 2;
+    grid-row: 1;
+    margin-top: 4px;
   }
 }

@@ -2890,10 +2998,9 @@
   color: var(--accent);
 }

-.pill.pv {
-  background: rgba(214, 161, 40, .16);
-  color: #d6a128;
-}
+/* .pill.pv is NOT declared here: `.pill.pv, .status-draft` in the System
+   Record layer sets its background and colour at equal specificity and later
+   in the file, so anything declared here would be dead. */

 .pill.pw {
   background: var(--warn-bg);
@@ -2977,8 +3084,9 @@
    internal/lint/raw_html_scope.go's img allowlist entry). Styling ported
    from docs/tabs/llm-internals.html's inline img style for its health-
    state-machine diagram: a plain white card so an SVG/PNG diagram (often
-   drawn on a transparent or light background) reads consistently in both
-   light and dark viewer themes, same as it did in docs/. */
+   drawn on a transparent or light background) reads consistently. The card
+   is deliberately mode-INVARIANT — it does not follow the OS appearance,
+   because the artwork it frames usually cannot — same as it did in docs/. */
 .mockup-diagram {
   display: block;
   max-width: 100%;
@@ -2986,7 +3094,7 @@
   margin-top: 8px;
   border: 1px solid var(--border);
   border-radius: 6px;
-  background: #fff;
+  background: var(--mockup-bg, #fff);
   padding: 8px;
 }

@@ -3325,7 +3433,6 @@
 .content-area {
   flex: 1;
   min-width: 0;
-  max-width: 880px;
   margin: 0 auto;
 }

@@ -3507,7 +3614,6 @@
     padding: 6px 10px;
     font: 600 12px var(--font-sans);
     cursor: pointer;
-    box-shadow: 0 3px 12px rgba(0, 0, 0, .18);
   }

   .nav-overlay {
@@ -3518,6 +3624,11 @@
     width: 100%;
     height: 100%;
     border: 0;
+    /* Deliberately NOT --scrim. The sidebar backdrop is mode-invariant by
+       design: the sheet it dims is the same weight in both modes, and a
+       project that lightens --scrim for its comment sheet should not thereby
+       lighten this one. It is not a theme token and has no consumer in the
+       allowlist. */
     background: rgba(0, 0, 0, .42);
     opacity: 0;
     pointer-events: none;
@@ -3570,96 +3681,8 @@
   }
 }

-/* ---- print -------------------------------------------------------------
-   The first and only @media print block in this stylesheet. It exists for
-   one reason (Q4): paper has no disclosure widget, so a printed viewer that
-   honoured the collapsed <details> would silently drop every claim's
-   governed_by/rests_on/implemented-in footer — the exact facts a reviewer
-   prints the page to read.
-   Revealing the contents through CSS is deliberate rather than setting `open`,
-   which CSS cannot do: `open` is a content attribute, not a property a rule
-   can write.
-   Same mechanism, and the same two-rule pairing, as the .claim:target rule
-   above: a closed <details> hides its contents through the UA's
-   ::details-content pseudo-element, so that is what is targeted, and the
-   child-selector form is expected to be a no-op wherever the pseudo-element is
-   implemented. It is kept only as a fallback for engines that predate
-   ::details-content (Chrome/Edge 131+, Safari 18.4+, Firefox 139+) and hide
-   the contents some other way; such an engine drops the pseudo-element rule
-   and still prints as it did before v0.4.1. Both rules declare the same value
-   and carry equal specificity (0,1,2), so the pseudo-element rule is written
-   LAST — source order is the only thing ranking them.
-   This block is independent of the .claim:target rule, of the two
-   server-written `open` signals, and of the expand-all toolbar — whichever of
-   those is or is not in effect on screen, print shows all four states
-   identically.
-   SCOPE — stated plainly so the next reader knows this is a decision, not an
-   oversight: only the facet currently ON SCREEN prints. A collapsed footer
-   belonging to a claim in a facet group the reader is not looking at stays
-   unrendered, because its ancestor section.claim-group carries the `hidden`
-   attribute (computed display: none) and no rule here un-hides it. The rules
-   below reveal a disclosure inside a subtree that is being laid out; they
-   cannot resurrect a subtree that display: none removed from the box tree in
-   the first place. That is deliberate: printing a tabbed viewer has always
-   shown one facet, what v0.4.1 changes is that the shown facet's footers now
-   print with it, and un-hiding every facet for print is a larger design
-   change — page order, per-facet headings, the duplicated overview copies —
-   than a patch release should make.
-   Only the disclosure is touched. Nothing here restyles the card, the type
-   scale or the color tokens; a full print stylesheet is out of scope for
-   v0.4.1. */
-@media print {
-  details.claim-links > *:not(summary) {
-    content-visibility: visible;
-  }
-
-  details.claim-links::details-content {
-    content-visibility: visible;
-  }
-
-  .claim-links-summary {
-    cursor: auto;
-  }
-}
-
 /* System Record visual language for the generated viewer. */

-:root {
-  --accent: #287052;
-  --accent-bg: rgba(40, 112, 82, .12);
-  --ink: #091426;
-  --muted: #536179;
-  --faint: #7d899a;
-  --paper: #f6f8fc;
-  --card-bg: #ffffff;
-  --border: #d8deea;
-  --link: #205b78;
-  --warn: #a2433d;
-  --warn-bg: rgba(162, 67, 61, .10);
-  --dxg-facet-other: #0d55b5;
-  --font-sans: "Avenir Next", -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", sans-serif;
-  --font-mono: ui-monospace, "SFMono-Regular", "IBM Plex Mono", Menlo, monospace;
-  --radius: 6px;
-  color-scheme: light;
-}
-
-@media (prefers-color-scheme: dark) {
-  :root {
-    --ink: #e8eef8;
-    --muted: #a9b5c8;
-    --faint: #75839a;
-    --paper: #0a1220;
-    --card-bg: #0f1b2e;
-    --border: #263754;
-    --link: #8ab7ff;
-    --accent: #70c99c;
-    --accent-bg: rgba(112, 201, 156, .12);
-    --warn: #ff8b94;
-    --warn-bg: rgba(255, 139, 148, .10);
-    --dxg-facet-other: #77a9e8;
-  }
-}
-
 html { scroll-behavior: smooth; }

 body.shell-body {
@@ -3673,7 +3696,7 @@
   text-rendering: optimizeLegibility;
 }

-::selection { background: rgba(40, 112, 82, .20); }
+::selection { background: var(--selection-bg, rgba(40, 112, 82, .20)); }

 :where(button, select, summary, a):focus-visible {
   outline: 2px solid var(--accent);
@@ -3726,9 +3749,79 @@
 .system-resizing { cursor: col-resize; user-select: none; }

 .sidebar-header {
+  position: relative;
   padding: 20px 18px 18px;
 }

+.system-panel-toggle {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  width: 32px;
+  height: 32px;
+  padding: 0;
+  border: 1px solid var(--border);
+  border-radius: 6px;
+  background: var(--card-bg);
+  color: var(--muted);
+  cursor: pointer;
+  transition: border-color 150ms ease-out, color 150ms ease-out, background-color 150ms ease-out;
+}
+
+.system-panel-toggle:hover {
+  border-color: color-mix(in srgb, var(--accent) 62%, var(--border));
+  background: var(--accent-bg);
+  color: var(--ink);
+}
+
+.system-panel-toggle__chevron {
+  width: 7px;
+  height: 7px;
+  border-right: 1.5px solid currentColor;
+  border-bottom: 1.5px solid currentColor;
+  transform: rotate(135deg);
+  transition: transform 180ms ease-out;
+}
+
+.system-panel-toggle--sidebar {
+  position: absolute;
+  top: 16px;
+  right: 9px;
+}
+
+.sidebar-header .top { padding-right: 28px; }
+
+@media (min-width: 861px) {
+  .sidebar,
+  .content-area { transition: width 180ms ease-out, padding 180ms ease-out; }
+
+  body.system-sidebar-collapsed .sidebar {
+    width: 44px;
+    min-width: 44px;
+    max-width: 44px;
+    overflow: hidden;
+  }
+
+  body.system-sidebar-collapsed .sidebar-header {
+    padding: 10px 6px;
+    border-bottom: 0;
+  }
+
+  body.system-sidebar-collapsed .sidebar-header .top,
+  body.system-sidebar-collapsed #nav,
+  body.system-sidebar-collapsed #expandAllToggle,
+  body.system-sidebar-collapsed .sidebar-footer,
+  body.system-sidebar-collapsed .system-sidebar-resizer { display: none; }
+
+  body.system-sidebar-collapsed .system-panel-toggle--sidebar {
+    position: static;
+  }
+
+  body.system-sidebar-collapsed .system-panel-toggle--sidebar .system-panel-toggle__chevron {
+    transform: rotate(-45deg);
+  }
+}
+
 .top {
   align-items: flex-start;
   gap: 12px;
@@ -3807,7 +3900,7 @@
 .system-nav-group__toggle::marker { content: ""; }
 .system-nav-group__toggle::-webkit-details-marker { display: none; }

-.system-nav-group__toggle:hover { background: rgba(125, 137, 154, .08); }
+.system-nav-group__toggle:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); }

 .system-nav-group__count {
   min-width: 25px;
@@ -3858,7 +3951,7 @@

 #dxgOpen:hover,
 .sec-tab:hover {
-  background: rgba(125, 137, 154, .08);
+  background: var(--hover-bg, rgba(125, 137, 154, .08));
   color: var(--ink);
 }

@@ -3905,8 +3998,26 @@
   flex: 1;
   margin: 0;
   padding: 0 282px 80px 42px;
+  transition: padding 180ms ease-out;
 }

+@media (min-width: 1181px) {
+  body.system-toc-collapsed .content-area { padding-right: 86px; }
+}
+
+/* The Build order tab reclaims the 282px right rail .content-area reserves
+   for the facet TOC, which renderToc hides on this tab, so the widest
+   diagram on the page is not laid out beside a blank gutter. Scoped to
+   screen because at (0,3,1) an unscoped version would beat the print
+   block's `.content-area { padding: 0 }` (0,1,0) on paper — the first rule
+   to break the invariant the print header states — and to the same
+   min-width as the TOC-collapsed rule above because the same specificity
+   equally outranks the tablet and phone .content-area rules, where the rail
+   it reclaims does not exist. */
+@media screen and (min-width: 1181px) {
+  body:has(.build-order-section:not([hidden])) .content-area { padding-right: 42px; }
+}
+
 .system-record-head {
   display: grid;
   grid-template-columns: minmax(0, 1fr) auto;
@@ -3914,7 +4025,7 @@
   align-items: start;
   margin: 0 0 24px;
   padding: 38px 0 24px;
-  border-bottom: 1px solid #aab5c7;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
 }

 .system-record-head h2 {
@@ -3973,16 +4084,204 @@
   font-weight: 700;
 }

-.build-order-module[hidden] { display: none; }
+/* ---- Build order tab (.bo-*) --------------------------------------------
+   The viewer's build order is a top-level tab of its own (#dossierx-build-order, a
+   .module-section) holding one .bo-module group per locked module, each six
+   .bo-phase blocks in the fixed sequence with one mermaid flowchart per
+   non-empty phase (internal/buildorder/mermaid.go; build-order.html).

-.build-order-module.system-orientation-only > :not(.system-build-title):not(.system-orientation-node) {
-  display: none;
+   .bo-modules is the module strip: NOT .sub-nav, which has never needed
+   horizontal overflow above 860px and has none — 25 locked modules at a 23px
+   gap run off the desktop column with no scrollbar. It scrolls sideways at
+   every width, sticky like the .sub-nav it stands in for (same z-index).
+
+   COLOURS: every colour declaration below is !important, and that is not
+   decoration. mermaid injects its own stylesheet into each SVG, compiled
+   under the SVG's id — "#mermaid-1 .node rect" at (1,1,1) — which outranks
+   any unqualified page rule, so without !important every node paints in
+   mermaid's base-theme lavender: light boxes on --paper: #0a1220 under the
+   dark block, the exact failure these rules exist to prevent. The diagram
+   text carries NO colour literal (PaletteCSS emits only stroke-dasharray
+   classDefs), so the light :root, the screen-dark block and a project's
+   viewer.theme all apply here by construction. The ghost rule names path as
+   well as rect: a stadium is drawn by rough.js as <path> elements inside a
+   <g class="basic label-container outer-path">, never a <rect>. The rules
+   assume mermaid's default look (classic); under look: 'handDrawn' even a
+   rectangle is a <path>, which is why build-order-ui.js never sets look.
+   The edge rule targets .flowchart-link, mermaid 11's class on every edge
+   path; the arrowhead markers live in <defs> and are painted from the
+   lineColor themeVariable build-order-ui.js reads off --muted at
+   initialise time. ---------------------------------------------------- */
+.bo-modules {
+  display: flex;
+  flex-wrap: nowrap;
+  gap: 23px;
+  overflow-x: auto;
+  scrollbar-width: thin;
+  position: sticky;
+  z-index: 8;
+  top: 0;
+  margin: 0 0 24px;
+  padding: 0;
+  border-bottom: 1px solid var(--border);
+  background: var(--paper);
 }

-.build-order-module.system-orientation-only > .build-order-phase.system-orientation-node {
-  display: none;
+.bo-modules .subtab {
+  flex: 0 0 auto;
+  white-space: nowrap;
 }

+.bo-phase {
+  padding: 22px 0 26px;
+  border-top: 1px solid var(--border);
+}
+
+.bo-phase__head {
+  display: grid;
+  gap: 6px;
+  margin: 0 0 14px;
+  padding: 0 0 14px;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
+}
+
+.bo-phase__num {
+  color: var(--accent);
+  font: 700 11px/1.4 var(--font-mono);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+}
+
+.bo-phase__name {
+  margin: 0;
+  color: var(--ink);
+  font-size: 22px;
+  font-weight: 600;
+  letter-spacing: -.02em;
+}
+
+.bo-phase__def {
+  max-width: 72ch;
+  margin: 0;
+  color: var(--ink);
+  font-size: 14px;
+}
+
+.bo-phase__meta,
+.bo-empty {
+  margin: 0;
+  color: var(--muted);
+  font-size: 13px;
+  font-variant-numeric: tabular-nums;
+}
+
+/* The wide-diagram rule: the SVG keeps its natural width (mermaid runs with
+   useMaxWidth: false and the max-width rule is belt and braces), a block
+   wider than the column scrolls inside .bo-diagram, and .content-area never
+   gains a horizontal scrollbar. Nothing scales a diagram down. */
+.bo-diagram {
+  overflow-x: auto;
+  overflow-y: hidden;
+  padding: 14px 10px;
+  border: 1px solid var(--border);
+  border-radius: 8px;
+  background: var(--card-bg);
+}
+
+.bo-diagram svg {
+  display: block;
+  max-width: none !important;
+  height: auto;
+}
+
+/* Nothing else in this sheet styles a bare pre (only .claim-body pre), so
+   without these the block shows its flowchart source as a wall of monospace
+   between parse and the first render pass. mermaid stamps data-processed
+   BEFORE it reads the text, so the hiding stops the moment a render starts
+   and never hides a failed block (whose reader sees .bo-error and mermaid's
+   own message). After the render the <pre> survives around the SVG and its
+   white-space reaches the html labels' foreignObject content. */
+.bo-diagram pre.mermaid {
+  margin: 0;
+  white-space: normal;
+  font-family: var(--font-sans);
+}
+
+.bo-diagram pre.mermaid:not([data-processed]) {
+  visibility: hidden;
+  min-height: 2rem;
+}
+
+.bo-diagram .node.locked_con rect {
+  fill: var(--accent-bg) !important;
+  stroke: var(--accent) !important;
+}
+
+.bo-diagram .node.draft_con rect {
+  fill: var(--card-bg) !important;
+  stroke: var(--accent) !important;
+}
+
+.bo-diagram .node.locked_int rect {
+  fill: color-mix(in srgb, var(--link) 12%, var(--card-bg)) !important;
+  stroke: var(--link) !important;
+}
+
+.bo-diagram .node.draft_int rect {
+  fill: var(--card-bg) !important;
+  stroke: var(--link) !important;
+}
+
+.bo-diagram .node.ghost rect,
+.bo-diagram .node.ghost path {
+  fill: var(--card-bg) !important;
+  stroke: var(--border) !important;
+}
+
+.bo-diagram .node .nodeLabel {
+  color: var(--ink) !important;
+}
+
+.bo-diagram .node.ghost .nodeLabel {
+  color: var(--muted) !important;
+}
+
+.bo-diagram .flowchart-link {
+  stroke: var(--muted) !important;
+}
+
+.bo-diagram g.node { cursor: pointer; }
+
+.bo-diagram g.node.bo-missing {
+  opacity: .55;
+  cursor: not-allowed;
+}
+
+.bo-cross,
+.bo-excluded {
+  margin: 12px 0 0;
+  padding: 0 0 0 18px;
+  color: var(--muted);
+  font-size: 13px;
+}
+
+.bo-cross__module {
+  color: var(--ink);
+  font-weight: 600;
+}
+
+.bo-excluded-deps {
+  margin: 8px 0 0;
+  color: var(--muted);
+  font-size: 13px;
+}
+
+.bo-error {
+  margin: 8px 0 0;
+  color: var(--warn);
+  font-size: 13px;
+}
+
 .facet-toc {
   position: fixed;
   z-index: 9;
@@ -3993,6 +4292,7 @@
   border-left: 1px solid var(--border);
   background: var(--paper);
   color: var(--muted);
+  transition: width 180ms ease-out;
 }

 .facet-toc[hidden] { display: none; }
@@ -4022,6 +4322,36 @@
   color: var(--ink);
 }

+.facet-toc__head .system-panel-toggle {
+  flex: 0 0 auto;
+  width: 28px;
+  height: 28px;
+  margin: 0 0 -4px;
+}
+
+.system-panel-toggle--toc .system-panel-toggle__chevron { transform: rotate(-45deg); }
+
+body.system-toc-collapsed .facet-toc {
+  width: 44px;
+  border-left: 0;
+  background: transparent;
+}
+
+body.system-toc-collapsed .facet-toc__head {
+  justify-content: center;
+  padding: 4px;
+  border-bottom: 0;
+}
+
+body.system-toc-collapsed .facet-toc__identity,
+body.system-toc-collapsed .facet-toc__total,
+body.system-toc-collapsed .facet-toc__list,
+body.system-toc-collapsed .facet-toc__select { display: none; }
+
+body.system-toc-collapsed .system-panel-toggle--toc .system-panel-toggle__chevron {
+  transform: rotate(135deg);
+}
+
 .facet-toc__head small {
   display: block;
   margin-bottom: 3px;
@@ -4072,10 +4402,69 @@

 .facet-toc__item > span { color: var(--faint); font: 9px/1.45 var(--font-mono); }
 .facet-toc__item > strong { font-size: 11px; font-weight: 520; line-height: 1.38; }
-.facet-toc__item:hover { background: rgba(125, 137, 154, .08); color: var(--ink); }
+.facet-toc__item:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); color: var(--ink); }
 .facet-toc__item.on { background: var(--accent-bg); box-shadow: inset 2px 0 0 var(--accent); color: var(--ink); }
 .facet-toc__item.on > span { color: var(--accent); }

+.facet-claim-controls {
+  display: flex;
+  justify-content: flex-end;
+  margin: 0 0 12px;
+}
+
+.facet-claims-toggle {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  min-height: 34px;
+  padding: 6px 10px;
+  border: 1px solid var(--border);
+  border-radius: 6px;
+  background: var(--card-bg);
+  color: var(--muted);
+  font: 650 10px/1.2 var(--font-mono);
+  cursor: pointer;
+}
+
+.facet-claims-toggle:hover {
+  border-color: color-mix(in srgb, var(--accent) 62%, var(--border));
+  color: var(--ink);
+}
+
+.facet-claims-toggle:disabled { cursor: default; opacity: .5; }
+
+.facet-claims-toggle__icon {
+  position: relative;
+  width: 11px;
+  height: 11px;
+}
+
+.facet-claims-toggle__icon::before,
+.facet-claims-toggle__icon::after {
+  content: "";
+  position: absolute;
+  left: 2px;
+  width: 6px;
+  height: 6px;
+  border-right: 1.5px solid currentColor;
+  border-bottom: 1.5px solid currentColor;
+  transform: rotate(45deg);
+  transition: transform 150ms ease-out, top 150ms ease-out;
+}
+
+.facet-claims-toggle__icon::before { top: -1px; }
+.facet-claims-toggle__icon::after { top: 3px; }
+
+.facet-claims-toggle[aria-pressed="true"] .facet-claims-toggle__icon::before {
+  top: 2px;
+  transform: rotate(225deg);
+}
+
+.facet-claims-toggle[aria-pressed="true"] .facet-claims-toggle__icon::after {
+  top: 6px;
+  transform: rotate(225deg);
+}
+
 .claim { scroll-margin-top: 72px; }

 .section-heading {
@@ -4166,7 +4555,7 @@
   transform: translateY(-50%);
 }

-.claim-collapse-toggle:hover { background: rgba(125, 137, 154, .08); }
+.claim-collapse-toggle:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); }

 .claim-collapse-toggle > .label {
   display: flex;
@@ -4219,8 +4608,8 @@

 .pill.pv,
 .status-draft {
-  background: rgba(151, 102, 0, .12);
-  color: #976600;
+  background: var(--status-draft-bg, rgba(151, 102, 0, .12));
+  color: var(--status-draft, #976600);
 }

 .pill.pw,
@@ -4244,18 +4633,22 @@
 .claim-body strong,
 .sbody strong { font-weight: 700; }

+/* The LIVE `code` background: this rule beats the inline-code pill rule near
+   the top of the file at equal specificity, on source order. --code-inline-bg
+   is read here so the token actually reaches a reader. */
 code {
   border: 1px solid var(--border);
-  background: color-mix(in srgb, var(--paper) 72%, var(--card-bg));
+  background: var(--code-inline-bg, color-mix(in srgb, var(--paper) 72%, var(--card-bg)));
   color: var(--ink);
   font-size: .86em;
 }

+/* The LIVE fenced-block background, same story as `code` above. */
 .claim-body pre,
 .claim-tree-body {
   padding: 13px 14px;
   border-radius: 7px;
-  background: color-mix(in srgb, var(--paper) 82%, var(--card-bg));
+  background: var(--code-bg, color-mix(in srgb, var(--paper) 82%, var(--card-bg)));
   font-size: 12px;
 }

@@ -4280,6 +4673,11 @@

 .claim-links-summary::marker { content: ""; }
 .claim-links-summary::-webkit-details-marker { display: none; }
+/* NOT --hover-bg, and deliberately so. This row is much larger than the other
+   hover targets and takes a fainter wash: .07, not .08. Reading the token here
+   would compute .08, because :root always declares --hover-bg and the fallback
+   would never fire — the row would silently change. It is not themable through
+   --hover-bg; a project that wants it themable needs its own token. */
 .claim-links-summary:hover { background: rgba(125, 137, 154, .07); color: var(--ink); }

 .claim-footer__identity { display: flex; gap: 10px; align-items: center; min-width: 0; }
@@ -4385,7 +4783,7 @@
 .track-head {
   margin: 0 0 24px;
   padding: 38px 0 23px;
-  border-bottom: 1px solid #aab5c7;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
 }

 .track-title {
@@ -4411,69 +4809,10 @@

 .track-cite > .pill { margin-left: auto; }

-.build-order-module {
-  margin-top: 34px;
-  padding-top: 22px;
-  border-top: 1px solid var(--border);
-}
-
-.build-order-module > .system-build-title {
-  margin: 0 0 24px;
-  padding: 0 0 15px;
-  border-top: 0;
-  border-bottom: 1px solid #aab5c7;
-  color: var(--ink);
-  font-size: 20px;
-  letter-spacing: -.02em;
-  text-transform: none;
-}
-
-.build-order-module > .build-order-phase {
-  margin: 30px 0 10px;
-  padding: 0;
-  border: 0;
-  color: var(--muted);
-  font: 700 10px/1.4 var(--font-mono);
-  letter-spacing: .09em;
-}
-
-.build-order-module .card { padding: 15px 17px; }
-.build-order-module .k { margin-bottom: 11px; font-size: 14px; }
-.build-order-module .claim-edges { display: grid; gap: 8px; }
-
-.build-order-module .claim-file,
-.build-order-module .claim-rests-on {
-  display: grid;
-  grid-template-columns: 72px minmax(0, 1fr);
-  gap: 10px;
-  align-items: start;
-  color: var(--muted);
-  font-size: 11px;
-}
-
-.build-order-field__label {
-  color: var(--faint);
-  font: 700 9px/1.55 var(--font-mono);
-  letter-spacing: .07em;
-  text-transform: uppercase;
-}
-
-.build-order-module code {
-  border: 0;
-  background: transparent;
-  overflow-wrap: anywhere;
-}
-
-.build-order-module .claim-edge-id-list {
-  margin: 0;
-  padding: 0;
-  list-style: none;
-}
-
 .comments-rail {
   border-left-color: var(--border);
   background: var(--card-bg);
-  box-shadow: -14px 0 34px rgba(9, 20, 38, .12);
+  box-shadow: -14px 0 34px var(--shadow-cast, rgba(9, 20, 38, .12));
 }

 .comments-rail-head {
@@ -4483,7 +4822,6 @@

 .status-strip {
   border-top-color: var(--border);
-  background: var(--card-bg);
   color: var(--muted);
 }

@@ -4803,13 +5141,23 @@
   }

   .facet-toc__disclosure,
-  .facet-toc__head,
   .facet-toc__list { display: none; }
+  .facet-toc__head {
+    position: absolute;
+    z-index: 1;
+    top: 0;
+    right: 0;
+    justify-content: flex-end;
+    width: 40px;
+    padding: 6px;
+    border: 0;
+  }
+  .facet-toc__head > :not(.system-panel-toggle) { display: none; }
   .facet-toc__select {
     display: block;
     width: 100%;
     min-height: 40px;
-    padding: 7px 30px 7px 10px;
+    padding: 7px 46px 7px 10px;
     border: 0;
     border-radius: 6px;
     background: var(--card-bg);
@@ -4832,11 +5180,13 @@
     border-radius: 6px;
     background: var(--card-bg);
     color: var(--ink);
-    box-shadow: 0 7px 18px rgba(9, 20, 38, .12);
+    box-shadow: 0 7px 18px var(--shadow-cast, rgba(9, 20, 38, .12));
   }

   .sidebar { width: min(310px, calc(100vw - 42px)); min-width: 0; max-width: none; }
   .system-sidebar-resizer { display: none; }
+  .system-panel-toggle--sidebar { display: none; }
+  .sidebar-header .top { padding-right: 0; }

   .facet-toc {
     position: fixed;
@@ -4848,17 +5198,27 @@
     border: 1px solid var(--border);
     border-radius: 7px;
     background: var(--card-bg);
-    box-shadow: 0 7px 18px rgba(9, 20, 38, .12);
+    box-shadow: 0 7px 18px var(--shadow-cast, rgba(9, 20, 38, .12));
   }

   .facet-toc__disclosure,
-  .facet-toc__head,
   .facet-toc__list { display: none; }
+  .facet-toc__head {
+    position: absolute;
+    z-index: 1;
+    top: 0;
+    right: 0;
+    justify-content: flex-end;
+    width: 40px;
+    padding: 5px;
+    border: 0;
+  }
+  .facet-toc__head > :not(.system-panel-toggle) { display: none; }
   .facet-toc__select {
     display: block;
     width: 100%;
     min-height: 38px;
-    padding: 7px 30px 7px 10px;
+    padding: 7px 46px 7px 10px;
     border: 0;
     border-radius: 7px;
     background: var(--card-bg);
@@ -4931,16 +5291,101 @@
   .claim-footer__counts { justify-content: flex-start; }
   .claim-footer__counts > span:not(.claim-footer__chevron) { padding: 4px 6px; }
   .claim-footer__chevron { margin-left: auto; }
-
-  .build-order-module .claim-file,
-  .build-order-module .claim-rests-on { grid-template-columns: 1fr; gap: 3px; }
 }

+/* ---- print — THE @media print block ------------------------------------
+   There is exactly one @media print block in this stylesheet and this is it;
+   internal/render/theme_tokens_test.go fails if a second one appears.
+
+   IT MUST STAY LAST IN THE FILE. Several of the rules below beat a screen
+   rule of identical specificity purely on source order — `.content-area
+   { padding: 0 }` against the padded `.content-area` rule in the System
+   Record layer, `.system-record-head { padding-top: 0 }` against that layer's
+   `.system-record-head`. Move this block earlier and the printed page silently
+   regains the sidebar gutter.
+
+   color-scheme is pinned to light here to match the palette: the :root at the
+   top of the file is light-first and its dark query is `screen`-scoped, so
+   print already gets light custom properties; this line makes the UA-painted
+   parts (form controls, scrollbars, canvas) agree. Unverified for Firefox and
+   Safari; where a browser already forces light for print it is a no-op, so it
+   is safe either way.
+
+   Consolidated from three separate @media print blocks. Their reasons follow,
+   unchanged, one per group.
+
+   NOTE (Q4): paper has no button to press, so the source-note clamp must not
+   reach it — print shows every note whole and hides the control.
+
+   DISCLOSURE (Q4). Paper has no disclosure widget, so a printed viewer that
+   honoured the collapsed <details> would silently drop every claim's
+   governed_by/rests_on/implemented-in footer — the exact facts a reviewer
+   prints the page to read.
+   Revealing the contents through CSS is deliberate rather than setting `open`,
+   which CSS cannot do: `open` is a content attribute, not a property a rule
+   can write.
+   Same mechanism, and the same two-rule pairing, as the .claim:target rule
+   above: a closed <details> hides its contents through the UA's
+   ::details-content pseudo-element, so that is what is targeted, and the
+   child-selector form is expected to be a no-op wherever the pseudo-element is
+   implemented. It is kept only as a fallback for engines that predate
+   ::details-content (Chrome/Edge 131+, Safari 18.4+, Firefox 139+) and hide
+   the contents some other way; such an engine drops the pseudo-element rule
+   and still prints as it did before v0.4.1. Both rules declare the same value
+   and carry equal specificity (0,1,2), so the pseudo-element rule is written
+   LAST — source order is the only thing ranking them.
+   This block is independent of the .claim:target rule, of the two
+   server-written `open` signals, and of the expand-all toolbar — whichever of
+   those is or is not in effect on screen, print shows all four states
+   identically.
+   SCOPE — stated plainly so the next reader knows this is a decision, not an
+   oversight: only the facet currently ON SCREEN prints. A collapsed footer
+   belonging to a claim in a facet group the reader is not looking at stays
+   unrendered, because its ancestor section.claim-group carries the `hidden`
+   attribute (computed display: none) and no rule here un-hides it. The rules
+   below reveal a disclosure inside a subtree that is being laid out; they
+   cannot resurrect a subtree that display: none removed from the box tree in
+   the first place. That is deliberate: printing a tabbed viewer has always
+   shown one facet, what v0.4.1 changes is that the shown facet's footers now
+   print with it, and un-hiding every facet for print is a larger design
+   change — page order, per-facet headings, the duplicated overview copies —
+   than a patch release should make.
+   Only the disclosure is touched. Nothing here restyles the card, the type
+   scale or the color tokens; a full print stylesheet is out of scope for
+   v0.4.1.
+*/
 @media print {
+  :root { color-scheme: light; }
+
+  /* source-note clamp */
+  .claim-source-note.is-clamped .claim-source-note-body {
+    display: block;
+    overflow: visible;
+  }
+
+  .claim-source-note-toggle {
+    display: none;
+  }
+
+  /* collapsed claim-links footer */
+  details.claim-links > *:not(summary) {
+    content-visibility: visible;
+  }
+
+  details.claim-links::details-content {
+    content-visibility: visible;
+  }
+
+  .claim-links-summary {
+    cursor: auto;
+  }
+
+  /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
   .nav-toggle,
   .facet-toc,
+  .facet-claim-controls,
   .comments-rail,
   .status-strip { display: none !important; }
   .content-area { padding: 0; }
@@ -4949,6 +5394,27 @@
   .claim-collapse-toggle { pointer-events: none; }
   .claim-collapse-chevron { display: none; }
   .claim-collapse-content[hidden] { display: block !important; }
+
+  /* Build order tab. What this buys is honest and small: a phase block is
+     not split across a page break. Browsers do not paginate horizontally, so
+     a diagram wider than the paper is CLIPPED at the page edge; nothing here
+     makes it spill onto further pages. The marker rules exist because
+     lineColor and background are baked into the SVG at initialise time from
+     the SCREEN palette while this block pins color-scheme: light and the
+     dark block above is screen-scoped: a dark-mode reader printing would get
+     light node fills beside pale-grey arrowheads on a near-black backdrop.
+     The module strip is the ONLY place the printed module's name appears
+     (build-order.html's section has no heading of its own), so the strip
+     stays on the page: only the buttons for the OTHER modules are dropped,
+     the selected one prints as the page's title line, unstuck from the
+     viewport it no longer has. */
+  .bo-modules { position: static; }
+  .bo-modules .subtab:not(.on) { display: none; }
+  .bo-diagram { overflow: visible; }
+  .bo-phase { break-inside: avoid; }
+  .bo-diagram svg { background: transparent !important; }
+  .bo-diagram .marker,
+  .bo-diagram marker path { fill: var(--muted) !important; stroke: var(--muted) !important; }
 }
 </style>
   <style></style>
@@ -4963,6 +5429,9 @@
           <div class="logo"><span></span></div>
           <div><h1>Claims Graph Demo</h1><div class="eyebrow">the corpus the graph pane was designed against</div></div>
         </div>
+        <button id="sidebarCollapseToggle" class="system-panel-toggle system-panel-toggle--sidebar" type="button" aria-controls="sidebar" aria-expanded="true" aria-label="Hide navigation" title="Hide navigation">
+          <span class="system-panel-toggle__chevron" aria-hidden="true"></span>
+        </button>
       </div>
       <nav id="nav">
         
@@ -5178,7 +5647,6 @@
                 
               </section>
             
-            
           </section>
         
           <section class="module-section" id="viewer" hidden>
@@ -5349,7 +5817,6 @@
                 
               </section>
             
-            
           </section>
         
           <section class="module-section" id="cli" hidden>
@@ -5491,7 +5958,6 @@
                 
               </section>
             
-            
           </section>
         
           <section class="module-section" id="lock" hidden>
@@ -5638,7 +6104,6 @@
                 
               </section>
             
-            
           </section>
         
           <section class="module-section" id="telemetry" hidden>
@@ -5777,7 +6242,6 @@
                 
               </section>
             
-            
           </section>
         
       
@@ -5798,8 +6262,12 @@
   
   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
     <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
-      <span id="statusStripSummary" class="status-strip-summary"></span>
-      <span class="status-strip-caret" aria-hidden="true">▾</span>
+      <span id="statusStripSummary" class="status-strip-summary">
+        <strong id="statusStripTitle" class="status-strip-title"></strong>
+        <span id="statusStripNote" class="status-strip-note"></span>
+      </span>
+      <span id="statusStripAction" class="status-strip-action">Show issues</span>
+      <span class="status-strip-caret" aria-hidden="true"></span>
     </button>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>
@@ -5824,7 +6292,7 @@
       
       
       var moduleSections, moduleTabs;
-      var facetToModule, moduleDefaultFacet, firstModuleID, claimToFacet, buildOrderToModule, sourceToFacet;
+      var facetToModule, moduleDefaultFacet, firstModuleID, claimToFacet, sourceToFacet;

       
       
@@ -5889,22 +6357,6 @@
         
         
         
-        
-        
-        buildOrderToModule = {};
-        moduleSections.forEach(function (sec) {
-          sec.querySelectorAll('.build-order-module').forEach(function (bo) {
-            if (bo.id) { buildOrderToModule[bo.id] = sec.id; }
-            bo.querySelectorAll('.claim[id]').forEach(function (c) {
-              buildOrderToModule[c.id] = sec.id;
-            });
-          });
-        });
-
-        
-        
-        
-        
         syncEmptyChips();

         
@@ -5923,6 +6375,8 @@
       
       
       
+      
+      
       function resolve(id) {
         if (Object.prototype.hasOwnProperty.call(claimToFacet, id)) {
           var facetID = claimToFacet[id];
@@ -5939,10 +6393,6 @@
           var srcFacet = sourceToFacet[id];
           return { module: facetToModule[srcFacet], facet: srcFacet, claim: id };
         }
-        if (Object.prototype.hasOwnProperty.call(buildOrderToModule, id)) {
-          var boModule = buildOrderToModule[id];
-          return { module: boModule, facet: moduleDefaultFacet[boModule], claim: id };
-        }
         if (Object.prototype.hasOwnProperty.call(facetToModule, id)) {
           return { module: facetToModule[id], facet: id };
         }
@@ -5975,6 +6425,12 @@
           });
         }

+        if (lastStatusData) {
+          renderStatusStrip(lastStatusData);
+        } else {
+          positionStatusStrip();
+        }
+
         
         
         
@@ -6812,7 +7268,11 @@
       var stripEl = document.getElementById('statusStrip');
       var stripToggle = document.getElementById('statusStripToggle');
       var stripSummary = document.getElementById('statusStripSummary');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripNote = document.getElementById('statusStripNote');
+      var stripAction = document.getElementById('statusStripAction');
       var stripBody = document.getElementById('statusStripBody');
+      var lastStatusData = null;
       
       
       
@@ -6826,6 +7286,59 @@
         return n + ' ' + word + (n === 1 ? '' : 's');
       }

+      function humanRule(name) {
+        return String(name || '').split('-').filter(Boolean).map(function (part) {
+          return part.charAt(0).toUpperCase() + part.slice(1);
+        }).join(' ');
+      }
+
+      function activeFacetClaimIDs() {
+        var ids = Object.create(null);
+        
+        
+        var section = document.querySelector('.module-section:not([hidden]):not(.build-order-section)');
+        var group = section && section.querySelector(':scope > .claim-group:not([hidden])');
+        if (!group) { return ids; }
+        group.querySelectorAll('.claim').forEach(function (claim) {
+          var id = claim.dataset.claimId || claim.id;
+          if (id) { ids[id] = true; }
+        });
+        return ids;
+      }
+
+      function findingsForActiveFacet(findings, claimIDs) {
+        return findings.filter(function (finding) {
+
+
+
+          return !finding.claim_id || claimIDs[finding.claim_id] === true;
+        });
+      }
+
+
+
+
+
+
+      function positionStatusStrip() {
+        if (!stripEl) { return; }
+        
+        
+        var section = document.querySelector('.module-section:not([hidden]):not(.build-order-section)');
+        if (!section) {
+          var content = document.querySelector('.content-area');
+          if (content && stripEl.parentNode !== content) { content.insertBefore(stripEl, content.firstChild); }
+          return;
+        }
+        var header = section.querySelector(':scope > .system-record-head, :scope > .track-head');
+        if (header) {
+          if (header.nextElementSibling !== stripEl) { header.insertAdjacentElement('afterend', stripEl); }
+        } else if (section.firstElementChild !== stripEl) {
+          section.insertBefore(stripEl, section.firstChild);
+        }
+      }
+      window.dossierxPositionStatusStrip = positionStatusStrip;
+
       
       
       
@@ -6854,9 +7367,11 @@
       
       
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary) { return; }
-        var ledger = (data && data.ledger_findings) || [];
-        var lintErrors = (data && data.lint_errors) || [];
+        if (!stripEl || !stripBody || !stripSummary || !stripTitle || !stripNote) { return; }
+        lastStatusData = data || {};
+        var claimIDs = activeFacetClaimIDs();
+        var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
+        var lintErrors = findingsForActiveFacet(lastStatusData.lint_errors || [], claimIDs);

         if (!ledger.length && !lintErrors.length) {
           stripEl.hidden = true;
@@ -6868,31 +7383,36 @@
         stripBody.textContent = '';
         if (ledger.length) {
           stripBody.appendChild(findingGroup(
-            'Lock ledger — these claims were changed without an approval record',
-            ledger.map(function (f) { return findingRow(f.rule, f.claim_id, f.message); })
+            'Approval record issues',
+            ledger.map(function (f) { return findingRow(humanRule(f.rule), f.claim_id, f.message); })
           ));
         }
         if (lintErrors.length) {
           stripBody.appendChild(findingGroup(
-            'Lint errors',
-            lintErrors.map(function (f) { return findingRow(f.lint, f.claim_id, f.message); })
+            'Issues to resolve',
+            lintErrors.map(function (f) { return findingRow(humanRule(f.lint), f.claim_id, f.message); })
           ));
         }

         
         
-        var summary = ledger.length
-          ? 'Integrity: ' + countLabel(ledger.length, 'lock-ledger finding')
-          : 'Lint: ' + countLabel(lintErrors.length, 'error');
-        if (ledger.length && lintErrors.length) {
-          summary += ' · ' + countLabel(lintErrors.length, 'lint error');
+        if (ledger.length) {
+          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
+          stripNote.textContent = lintErrors.length
+            ? 'There ' + (lintErrors.length === 1 ? 'is ' : 'are ') + countLabel(lintErrors.length, 'issue') + ' in this facet also blocking the next successful DossierX check.'
+            : 'Review these locked claims before relying on their approved state.';
+        } else {
+          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
+          stripNote.textContent = lintErrors.length === 1
+            ? 'This issue blocks the next successful DossierX check.'
+            : 'These issues block the next successful DossierX check.';
         }
-        stripSummary.textContent = summary; 

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        if (ledger.length && !stripUserToggled) { stripExpanded = true; }
+        if (!stripUserToggled) { stripExpanded = ledger.length > 0; }
         setStripExpanded(stripExpanded);
+        positionStatusStrip();
         stripEl.hidden = false;
       }

@@ -6900,6 +7420,7 @@
         stripExpanded = !!expanded;
         if (stripBody) { stripBody.hidden = !stripExpanded; }
         if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
+        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
         if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
       }

@@ -7269,6 +7790,23 @@
         oldNav.outerHTML = frag.nav;           

         
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        if (document.querySelector('.build-order-section') && typeof window.mermaid === 'undefined') {
+          window.location.reload();
+          return;
+        }
+
+        
         initViewer();

         
@@ -7455,10 +7993,44 @@
     });
   }

-  function syncNavigation() {
+  function bindSidebarCollapse() {
+    var toggle = document.getElementById('sidebarCollapseToggle');
+    if (!toggle || toggle.dataset.bound === 'true') { return; }
+    toggle.dataset.bound = 'true';
+    toggle.addEventListener('click', function () {
+      var collapsed = !document.body.classList.contains('system-sidebar-collapsed');
+      document.body.classList.toggle('system-sidebar-collapsed', collapsed);
+      toggle.setAttribute('aria-expanded', String(!collapsed));
+      toggle.setAttribute('aria-label', collapsed ? 'Show navigation' : 'Hide navigation');
+      toggle.title = collapsed ? 'Show navigation' : 'Hide navigation';
+    });
+  }
+
+  function bindNavigationGroupPreferences() {
+    document.querySelectorAll('.system-nav-group').forEach(function (group) {
+      if (group.dataset.readerPreferenceBound === 'true') { return; }
+      group.dataset.readerPreferenceBound = 'true';
+      var summary = group.querySelector(':scope > summary');
+      if (summary) {
+        summary.addEventListener('click', function (event) {
+          event.preventDefault();
+          var willOpen = !group.open;
+          group.dataset.readerClosed = String(!willOpen);
+          group.open = willOpen;
+        });
+      }
+      group.addEventListener('toggle', function () {
+        group.dataset.readerClosed = group.open ? 'false' : 'true';
+      });
+    });
+  }
+
+  function syncNavigation(force) {
     var active = document.querySelector('.system-nav-group .sec-tab.on');
     var group = active && active.closest('.system-nav-group');
-    if (group) { group.open = true; }
+    if (!group || (!force && group.dataset.readerClosed === 'true')) { return; }
+    group.dataset.readerClosed = 'false';
+    group.open = true;
   }

   function enhanceFooters() {
@@ -7506,18 +8078,16 @@
       ['.claim-links > .claim-edges > .claim-migrated', 'Migrated From', /^\s*migrated_from:\s*/i],
       ['.claim-links > .claim-edges > .claim-implemented-in', 'Implemented In', /^\s*implemented\s+in:\s*/i],
       ['.claim-links > .claim-edges > .claim-review-pending', 'Review Pending', /^\s*review_pending\s*/i],
-      ['.claim-links > .claim-edges > .claim-sources', 'Sources', /^\s*sources:\s*/i],
-      ['.build-order-module .claim-file', 'File', /^\s*file:\s*/i],
-      ['.build-order-module .claim-rests-on', 'Rests On', /^\s*rests_on:\s*/i]
+      ['.claim-links > .claim-edges > .claim-sources', 'Sources', /^\s*sources:\s*/i]
     ];
     rows.forEach(function (entry) {
       document.querySelectorAll(entry[0]).forEach(function (row) {
-        if (row.querySelector(':scope > .claim-relation-label, :scope > .build-order-field__label')) { return; }
+        if (row.querySelector(':scope > .claim-relation-label')) { return; }
         var textNode = Array.prototype.slice.call(row.childNodes).find(function (node) { return node.nodeType === 3 && node.textContent.trim(); });
         if (!textNode) { return; }
         textNode.textContent = textNode.textContent.replace(entry[2], ' ');
         var label = document.createElement('span');
-        label.className = entry[0].indexOf('.build-order-module') === 0 ? 'build-order-field__label' : 'claim-relation-label';
+        label.className = 'claim-relation-label';
         label.textContent = entry[1];
         row.insertBefore(label, textNode);
       });
@@ -7535,6 +8105,7 @@
     toggle.setAttribute('aria-expanded', String(expanded));
     var title = toggle.dataset.claimTitle || 'claim';
     toggle.setAttribute('aria-label', (expanded ? 'Collapse ' : 'Expand ') + title);
+    updateFacetClaimControl();
   }

   function enhanceClaimDisclosures() {
@@ -7592,7 +8163,7 @@

   function cleanTitle(claim) {
     var title = claim.querySelector(':scope > .k');
-    if (!title) { return (claim.id || '').replace(/^build-order-/, '').replace(/[.-]/g, ' '); }
+    if (!title) { return (claim.id || '').replace(/[.-]/g, ' '); }
     var copy = title.cloneNode(true);
     copy.querySelectorAll('.pill, .claim-comments-slot').forEach(function (node) { node.remove(); });
     return copy.textContent.replace(/\s+/g, ' ').trim();
@@ -7609,14 +8180,13 @@
     });
   }

+  // activeFacet returns null while the Build order tab is active (it is not a
+  // module section: no header, TOC, status strip or claim controls are built
+  // over the diagrams), so renderToc hides the TOC there.
   function activeFacet() {
-    var modules = Array.prototype.slice.call(document.querySelectorAll('.module-section:not(.track-section)'));
+    var modules = Array.prototype.slice.call(document.querySelectorAll('.module-section:not(.track-section):not(.build-order-section)'));
     var module = modules.find(function (section) { return !section.hidden; });
     if (!module) { return null; }
-    var build = module.querySelector(':scope > .build-order-module:not([hidden])');
-    if (build) {
-      return { module: module, view: build, label: build.classList.contains('system-orientation-only') ? 'Orientation' : 'Build Order' };
-    }
     var groups = Array.prototype.slice.call(module.querySelectorAll(':scope > .claim-group'));
     var group = groups.find(function (section) { return !section.hidden; });
     if (!group) { return null; }
@@ -7624,6 +8194,45 @@
     return { module: module, view: group, label: tab ? tab.textContent.replace(/🔒/g, '').trim() : 'Claims' };
   }

+  function updateFacetClaimControl(active, claims) {
+    active = active || activeFacet();
+    if (!active) { return; }
+    claims = claims || visibleClaims(active.view);
+    var control = active.view.querySelector(':scope > .facet-claim-controls');
+    if (!control) { return; }
+    var toggle = control.querySelector('.facet-claims-toggle');
+    if (!toggle) { return; }
+    var allCollapsed = claims.length > 0 && claims.every(function (claim) {
+      var disclosure = claim.querySelector(':scope > .k > .claim-collapse-toggle');
+      return disclosure && disclosure.getAttribute('aria-expanded') === 'false';
+    });
+    toggle.disabled = claims.length === 0;
+    toggle.setAttribute('aria-pressed', String(allCollapsed));
+    toggle.querySelector('.facet-claims-toggle__label').textContent = allCollapsed ? 'Expand all claims' : 'Collapse all claims';
+  }
+
+  function renderFacetClaimControl(active, claims) {
+    var control = active.view.querySelector(':scope > .facet-claim-controls');
+    if (!control) {
+      control = document.createElement('div');
+      control.className = 'facet-claim-controls';
+      control.innerHTML = '<button class="facet-claims-toggle" type="button" aria-pressed="false"><span class="facet-claims-toggle__icon" aria-hidden="true"></span><span class="facet-claims-toggle__label">Collapse all claims</span></button>';
+      active.view.insertBefore(control, active.view.firstChild);
+      control.querySelector('.facet-claims-toggle').addEventListener('click', function () {
+        var current = activeFacet();
+        if (!current) { return; }
+        var currentClaims = visibleClaims(current.view);
+        var shouldExpand = currentClaims.length > 0 && currentClaims.every(function (claim) {
+          var disclosure = claim.querySelector(':scope > .k > .claim-collapse-toggle');
+          return disclosure && disclosure.getAttribute('aria-expanded') === 'false';
+        });
+        currentClaims.forEach(function (claim) { setClaimExpanded(claim, shouldExpand); });
+        updateFacetClaimControl(current, currentClaims);
+      });
+    }
+    updateFacetClaimControl(active, claims);
+  }
+
   function updateTocActive() {
     var toc = document.getElementById('systemFacetToc');
     if (!toc || toc.hidden) { return; }
@@ -7646,11 +8255,18 @@
       toc.id = 'systemFacetToc';
       toc.className = 'facet-toc';
       toc.setAttribute('aria-label', 'Claims in this facet');
-      toc.innerHTML = '<div class="facet-toc__head"><span><small>On this facet</small><strong class="facet-toc__name">Claims</strong></span><span class="facet-toc__total"></span></div><nav class="facet-toc__list"></nav><select class="facet-toc__select" aria-label="Jump to a claim in this facet"></select>';
+      toc.innerHTML = '<div class="facet-toc__head"><span class="facet-toc__identity"><small>On this facet</small><strong class="facet-toc__name">Claims</strong></span><span class="facet-toc__total"></span><button class="system-panel-toggle system-panel-toggle--toc" type="button" aria-controls="systemFacetToc" aria-expanded="true" aria-label="Hide table of contents" title="Hide table of contents"><span class="system-panel-toggle__chevron" aria-hidden="true"></span></button></div><nav class="facet-toc__list"></nav><select class="facet-toc__select" aria-label="Jump to a claim in this facet"></select>';
       toc.querySelector('.facet-toc__select').addEventListener('change', function (event) {
         var claim = document.getElementById(event.target.value);
         if (claim) { claim.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
       });
+      toc.querySelector('.system-panel-toggle--toc').addEventListener('click', function (event) {
+        var collapsed = !document.body.classList.contains('system-toc-collapsed');
+        document.body.classList.toggle('system-toc-collapsed', collapsed);
+        event.currentTarget.setAttribute('aria-expanded', String(!collapsed));
+        event.currentTarget.setAttribute('aria-label', collapsed ? 'Show table of contents' : 'Hide table of contents');
+        event.currentTarget.title = collapsed ? 'Show table of contents' : 'Hide table of contents';
+      });
       document.body.appendChild(toc);
     }
     var active = activeFacet();
@@ -7659,6 +8275,7 @@
     var claims = visibleClaims(active.view);
     toc.querySelector('.facet-toc__name').textContent = active.label;
     toc.querySelector('.facet-toc__total').textContent = claims.length + (claims.length === 1 ? ' claim' : ' claims');
+    renderFacetClaimControl(active, claims);
     var list = toc.querySelector('.facet-toc__list');
     var select = toc.querySelector('.facet-toc__select');
     list.replaceChildren();
@@ -7685,99 +8302,12 @@
     updateTocActive();
   }

-  function prepareBuildOrder(module) {
-    var build = module.querySelector(':scope > .build-order-module');
-    if (!build || build.dataset.systemPrepared === 'true') { return build; }
-    build.dataset.systemPrepared = 'true';
-    var title = build.querySelector(':scope > h3');
-    if (title) { title.classList.add('system-build-title'); title.dataset.originalText = title.textContent; }
-    var children = Array.prototype.slice.call(build.children);
-    var orientation = children.find(function (node) {
-      return node.classList.contains('build-order-phase') && node.textContent.trim().toLowerCase() === 'orientation';
-    });
-    if (orientation) {
-      var start = children.indexOf(orientation);
-      for (var index = start; index < children.length; index += 1) {
-        var node = children[index];
-        if (index > start && node.classList.contains('build-order-phase')) { break; }
-        node.classList.add('system-orientation-node');
-      }
-      build.dataset.hasOrientation = 'true';
-    }
-    return build;
-  }
-
-  function activateFacet(module, mode, targetID, updateHash) {
-    var build = prepareBuildOrder(module);
-    var isBuild = mode === 'build-order' || mode === 'orientation';
-    module.querySelectorAll(':scope > .claim-group').forEach(function (group) { group.hidden = isBuild || group.id !== targetID; });
-    if (build) {
-      build.hidden = !isBuild;
-      build.classList.toggle('system-orientation-only', mode === 'orientation');
-      var title = build.querySelector(':scope > .system-build-title');
-      if (title) { title.textContent = mode === 'orientation' ? 'Orientation' : title.dataset.originalText; }
-    }
-    module.querySelectorAll(':scope > .sub-nav .subtab').forEach(function (tab) {
-      var tabMode = tab.dataset.systemMode || 'claims';
-      tab.classList.toggle('on', tabMode === mode && (isBuild || tab.dataset.target === '#' + targetID));
-    });
-    if (updateHash) { history.replaceState(null, '', '#' + targetID); }
-    scrollTo(0, 0);
-    renderToc();
-  }
-
-  function enhanceFacetNavigation() {
-    document.querySelectorAll('.module-section:not(.track-section)').forEach(function (module) {
-      var build = prepareBuildOrder(module);
-      if (!build) { return; }
-      var subnav = module.querySelector(':scope > .sub-nav');
-      if (!subnav) {
-        subnav = document.createElement('div');
-        subnav.className = 'sub-nav system-created-sub-nav';
-        var firstGroup = module.querySelector(':scope > .claim-group');
-        var firstTab = document.createElement('button');
-        firstTab.className = 'subtab';
-        firstTab.dataset.target = '#' + firstGroup.id;
-        firstTab.textContent = 'Claims';
-        subnav.appendChild(firstTab);
-        module.insertBefore(subnav, firstGroup);
-      }
-      if (!subnav.querySelector('[data-system-mode="build-order"]')) {
-        [['build-order', 'Build Order', '#' + build.id], ['orientation', 'Orientation', '#orientation-' + module.id]].forEach(function (entry) {
-          if (entry[0] === 'orientation' && build.dataset.hasOrientation !== 'true') { return; }
-          var tab = document.createElement('button');
-          tab.type = 'button';
-          tab.className = 'subtab system-added-subtab';
-          tab.dataset.target = entry[2];
-          tab.dataset.systemMode = entry[0];
-          tab.textContent = entry[1];
-          tab.addEventListener('click', function (event) {
-            event.preventDefault();
-            event.stopImmediatePropagation();
-            activateFacet(module, entry[0], entry[2].slice(1), true);
-          }, true);
-          subnav.appendChild(tab);
-        });
-      }
-      subnav.querySelectorAll('.subtab:not(.system-added-subtab)').forEach(function (tab) {
-        if (tab.dataset.systemBound === 'true') { return; }
-        tab.dataset.systemBound = 'true';
-        tab.addEventListener('click', function () { setTimeout(function () { activateFacet(module, 'claims', tab.dataset.target.slice(1), false); }, 0); });
-      });
-      var active = Array.prototype.slice.call(module.querySelectorAll(':scope > .claim-group')).find(function (group) { return !group.hidden; });
-      if (active && !module.dataset.systemActivated) {
-        module.dataset.systemActivated = 'true';
-        activateFacet(module, 'claims', active.id, false);
-      }
-    });
-  }
-
   function addModuleHeaders() {
     var labels = {};
     document.querySelectorAll('.sec-tab[data-target]').forEach(function (button) {
       labels[button.dataset.target.slice(1)] = button.textContent.replace(/🔒/g, '').trim();
     });
-    document.querySelectorAll('.module-section:not(.track-section)').forEach(function (section) {
+    document.querySelectorAll('.module-section:not(.track-section):not(.build-order-section)').forEach(function (section) {
       if (section.querySelector(':scope > .system-record-head')) { return; }
       var claims = Array.prototype.slice.call(section.querySelectorAll('.claim-group .claim[data-status]'));
       var unique = {};
@@ -7828,12 +8358,16 @@
     running = true;
     enhanceTimestamp();
     bindResizer();
+    bindSidebarCollapse();
+    bindNavigationGroupPreferences();
     enhanceFooters();
     enhanceFieldLabels();
     enhanceClaimDisclosures();
     revealHashTarget();
     addModuleHeaders();
-    enhanceFacetNavigation();
+    if (typeof window.dossierxPositionStatusStrip === 'function') {
+      window.dossierxPositionStatusStrip();
+    }
     syncNavigation();
     renderToc();
     enhanceGraphLabels();
@@ -7859,11 +8393,11 @@
     // in the same event cycle whenever it contained the active tab.
     var navigationChanged = !!event.target.closest('.sec-tab');
     setTimeout(function () {
-      if (navigationChanged) { syncNavigation(); }
+      if (navigationChanged) { syncNavigation(true); }
       renderToc();
     }, 0);
   });
-  window.addEventListener('hashchange', function () { setTimeout(function () { revealHashTarget(); syncNavigation(); renderToc(); }, 0); });
+  window.addEventListener('hashchange', function () { setTimeout(function () { revealHashTarget(); syncNavigation(true); renderToc(); }, 0); });
   window.addEventListener('scroll', updateTocActive, { passive: true });
   window.dossierxEnhanceSystemRecord = enhance;
   enhance();

testdata/fixture-portability/build/viewer/index.html  (+1019 -481)
    inputs changed  testdata/fixture-portability/build/.gitignore
                    testdata/fixture-portability/build/ledger/comment-digest.json
    size            12703 lines / 440246 bytes -> 13241 lines / 462750 bytes
@@ -135,22 +135,35 @@
    in the same metric the ramp is solved in rather than in degrees of hue: the
    ramp solver treats --dxg-governed, --dxg-cycle, --dxg-halo and
    --dxg-facet-other as members of the already-chosen set with a hard floor of
-   CIEDE2000 20, so no slot it picks can land near any of them. Measured over
-   the shipped values, the closest any of the twenty slots comes to a reserved
-   colour is 14.4 in dark and 11.1 in light — and the light 11.1 is frozen
-   slot 5 (#67717E) against --dxg-facet-other (#7D8C85), two greys this file
-   inherited rather than chose. Against --dxg-governed itself, every slot in
-   both themes is at least 20 away. A governance edge cannot be mistaken for a
-   facet's colour at any facet count in either theme.
+   CIEDE2000 20, so no slot it picks can land near any of them. Measured
+   against the values DECLARED IN THIS FILE, the closest any of the twenty
+   slots comes to a reserved colour is 14.4 in dark and 11.1 in light — and
+   the light 11.1 is frozen slot 5 (#67717E) against this file's
+   --dxg-facet-other (#7D8C85), two greys this file inherited rather than
+   chose. Read that figure as a bound on this file alone: shell.html emits
+   style.css after graph.css, and style.css re-declares --dxg-facet-other
+   (blue, not grey) for the shipped viewer, so the only project that sees the
+   grey — and therefore that 11.1 — is one whose viewer.template_overrides
+   replaces style.css. Against --dxg-governed itself, which style.css does not
+   re-declare, every slot in both themes is at least 20 away. A governance
+   edge cannot be mistaken for a facet's colour at any facet count in either
+   theme.

    --dxg-cycle and --dxg-halo are STATE colours, not identity colours. They
    are drawn as a ring and a halo — never as a plain node fill — except under
    the one overlay that is specifically about that state, where every
    non-matching node is dimmed and there is nothing to confuse them with. */

-/* DARK IS THE BASE, light is the override — the same convention the base
-   stylesheet uses, so the two files move together rather than fighting over
-   which one a reader's OS appearance reaches first. Both sets are complete:
+/* DARK IS THE BASE, light is the override. This file keeps that convention
+   DELIBERATELY, and it is now the opposite of style.css, which is light-first
+   (one unconditional :root plus a `screen and (prefers-color-scheme: dark)`
+   query). The two files differ on purpose: this ramp was solved dark-first and
+   renumbering it to light-first would mean re-solving both sets, which changes
+   every existing project's facet colours for no reader-visible gain. What the
+   two files DO share is the print outcome — the light query below carries
+   `, print`, so paper gets the light ramp whatever the OS appearance is, the
+   same result style.css reaches by scoping its dark query to `screen`.
+   Both sets are complete:
    a categorical ramp has to move in BOTH directions, because the dark values
    are lifted and desaturated to stay legible on --paper's near-black and the
    light values are darkened to stay legible on its near-white. Neither set
@@ -184,7 +197,11 @@
   --dxg-facet-19: #B6B994;
   --dxg-facet-20: #A9D6C0;

-  /* No facet, an unlisted facet, or a collapsed module group. */
+  /* No facet, an unlisted facet, or a collapsed module group.
+     KEPT AS A FALLBACK, NOT AS THE SHIPPED VALUE: style.css declares
+     --dxg-facet-other too and is emitted after this file, so this grey only
+     reaches a project whose viewer.template_overrides replaces style.css.
+     Do not delete it — for such a project it is the only declaration. */
   --dxg-facet-other: #6C7F75;

   /* State colours. Ring, halo and the two structural overlays only. */
@@ -195,7 +212,10 @@
   --dxg-governed: #F06A9C;
 }

-@media (prefers-color-scheme: light) {
+/* `, print`: paper is light, whatever the OS says. A printed graph pane with
+   the dark ramp on white would be the same defect style.css's `screen and`
+   dark query prevents for the base palette. */
+@media (prefers-color-scheme: light), print {
   :root {
     --dxg-facet-1: #4257C4;
     --dxg-facet-2: #12897F;
@@ -223,6 +243,8 @@
     --dxg-facet-19: #6F4D37;
     --dxg-facet-20: #8782A8;

+    /* Light fallback, same standing as the dark one above: style.css's own
+       light value wins for the shipped sheet. */
     --dxg-facet-other: #7D8C85;

     --dxg-cycle: #D1201A;
@@ -1136,42 +1158,129 @@
    a project's own template_overrides CSS; a project can restyle any of
    this by overriding component partials via viewer.template_overrides.

-   Token names below (--accent, --accent-bg, --ink, --muted, --faint, --paper,
-   --card-bg, --border, --link, --warn, --warn-bg, --font-sans, --font-mono,
-   --radius) are the engine's fixed viewer.theme allowlist (see
-   internal/config/config.go). Defaults here mirror the reference docs
-   palette this engine's authoring project uses, so an unconfigured project
-   still renders with a sane look;
-   render.go injects a second <style> block after this one (and after the
-   dark-mode media query below) so any project-supplied theme token wins in
-   both OS color modes without disturbing tokens the project left unset. */
+   COLOUR MECHANISM, in cascade order. shell.html emits three <style> blocks:
+   graph.css, then this file, then the generated theme block.

+   1. One unconditional `:root` holds the palette, and it is LIGHT-FIRST:
+      every value in it is both the light-OS value and the printed value.
+      There is exactly one such block in this file.
+   2. `@media screen and (prefers-color-scheme: dark)` re-points the handful
+      of tokens that invert. The `screen and` prefix is load-bearing: it is
+      what keeps a dark-mode OS from dragging dark ink onto paper.
+   3. The single `@media print` block is the LAST block in this file. It has
+      to be last, because it overrides screen layout rules at equal
+      specificity (`.content-area { padding: 0 }` beats the padded screen
+      rule only on source order), and it pins `color-scheme: light`.
+   4. internal/render appends the project's theme as the third <style>: an
+      optional @font-face run, a shared `:root`, then
+      `@media (prefers-color-scheme: light), print` and
+      `@media screen and (prefers-color-scheme: dark)`. The project's light
+      block deliberately also matches `print` and its dark block deliberately
+      cannot, so a token a project sets only under `dark:` prints the engine's
+      light value rather than its dark one.
+
+   The names a project may set are the 28-entry allowlist in
+   internal/config/config.go (ThemeTokenAllowlist). Each token's :root default
+   is the value a light OS gets, and the screen-dark block carries the dark
+   values for the three that differ. A consumer spells the token as
+   `var(--token, <fallback>)`; the fallback is the LIGHT value, because this
+   sheet is light-first and because the fallback only ever fires for a
+   viewer.template_overrides project that forked this file and dropped the
+   :root — which should degrade to light, not to dark.
+
+   A token has to be read by the declaration that WINS, not merely by a
+   declaration. Two rules in this file are re-declared later at equal
+   specificity (`code` and `.claim-body pre, .claim-tree-body`), so their
+   token reads live in the later copy; internal/render/theme_tokens_test.go
+   asserts, per token, that the last declaration of the consumer's
+   selector+property pair is the one carrying the var(). It also fails if an
+   allowlisted token has no consumer in this file or in graph.css, and pins
+   the structural invariants listed above.
+
+   graph.css keeps the OPPOSITE convention for its own --dxg-* ramp (dark
+   base, light override) and says so in its own header; the two files differ
+   deliberately. */
+
+/* THE palette. One unconditional :root, light-first: the values below are what
+   a light-OS screen and a printed page both get. Nothing else in this file
+   declares any of these properties, so this block's position is free; it sits
+   here, at the top, because that is where a reader looks for it.
+
+   The dark query is scoped to `screen` on purpose. Without the prefix, a
+   reader whose OS is in dark mode and who hits Print gets near-black --paper
+   and pale --ink; print media does not carry an appearance, so nothing else
+   would put the light palette back. `screen and` is the whole fix, and the
+   @media print block at the end of the file pins `color-scheme: light` beside
+   it so form controls and scrollbars agree. This is unverified for Firefox and
+   Safari; where a browser already forces light for print it is a no-op, so it
+   is safe either way. */
+
 :root {
   color-scheme: light dark;
-  --accent: #3fb950;
-  --accent-bg: rgba(63, 185, 80, .14);
-  --ink: #e6ece8;
-  --muted: #9aa6a0;
-  --faint: #6f7a74;
-  --paper: #0f1311;
-  --card-bg: #1b211d;
-  --border: #2a322c;
-  --link: #4c9bf5;
-  --warn: #e0623f;
-  --warn-bg: rgba(224, 98, 63, .15);
-  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
-  --font-mono: ui-monospace, SFMono-Regular, Menlo, monospace;
-  --radius: 12px;
+  --accent: #287052;
+  --accent-bg: rgba(40, 112, 82, .12);
+  --ink: #091426;
+  --muted: #536179;
+  --faint: #7d899a;
+  --paper: #f6f8fc;
+  --card-bg: #ffffff;
+  --border: #d8deea;
+  --link: #205b78;
+  --warn: #a2433d;
+  --warn-bg: rgba(162, 67, 61, .10);
+  --dxg-facet-other: #0d55b5;
+  --font-sans: "Avenir Next", -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", sans-serif;
+  --font-mono: ui-monospace, "SFMono-Regular", "IBM Plex Mono", Menlo, monospace;
+  --radius: 6px;
+
+  /* Added for the custom-theme work. Each value below is what a LIGHT OS and
+     a printed page get; the screen-dark query re-points the three that differ
+     per mode (--shadow, --shadow-strong, --scrim). The other eleven are the
+     same in both modes on purpose: a neutral overlay tint tracks whatever it
+     sits on, and the mockup card is deliberately mode-invariant.
+     Every value is the expression the live consumer used before it read a
+     token, so an unset token renders exactly as it did before. Note "live":
+     for --code-inline-bg and --code-bg that is the System Record layer's
+     color-mix, not the rgba() in the dead declaration those rules have always
+     beaten. Order matches ThemeTokenAllowlist. */
+  --code-inline-bg: color-mix(in srgb, var(--paper) 72%, var(--card-bg));
+  --code-bg: color-mix(in srgb, var(--paper) 82%, var(--card-bg));
+  --table-head-bg: rgba(127, 127, 127, .10);
+  --image-bg: rgba(127, 127, 127, .06);
+  --hover-bg: rgba(125, 137, 154, .08);
+  --border-strong: #aab5c7;
+  --shadow: rgba(0, 0, 0, .08);
+  --shadow-strong: rgba(0, 0, 0, .14);
+  --shadow-cast: rgba(9, 20, 38, .12);
+  --scrim: rgba(0, 0, 0, .22);
+  --selection-bg: rgba(40, 112, 82, .20);
+  --status-draft: #976600;
+  --status-draft-bg: rgba(151, 102, 0, .12);
+  --mockup-bg: #fff;
 }

-@media (prefers-color-scheme: light) {
+@media screen and (prefers-color-scheme: dark) {
   :root {
-    --paper: #f3f6f4;
-    --card-bg: #fafcfb;
-    --border: #e2e8e4;
-    --ink: #1d2622;
-    --muted: #5d6862;
-    --faint: #8a948e;
+    --ink: #e8eef8;
+    --muted: #a9b5c8;
+    --faint: #75839a;
+    --paper: #0a1220;
+    --card-bg: #0f1b2e;
+    --border: #263754;
+    --link: #8ab7ff;
+    --accent: #70c99c;
+    --accent-bg: rgba(112, 201, 156, .12);
+    --warn: #ff8b94;
+    --warn-bg: rgba(255, 139, 148, .10);
+    --dxg-facet-other: #77a9e8;
+
+    /* The three theme tokens that genuinely differ per mode: a shadow needs
+       more alpha to read on a dark surface, and the modal dim needs more to
+       separate the sheet from what is behind it. The other eleven new tokens
+       are mode-invariant and are declared once, above. */
+    --shadow: rgba(0, 0, 0, .28);
+    --shadow-strong: rgba(0, 0, 0, .34);
+    --scrim: rgba(0, 0, 0, .42);
   }
 }

@@ -1217,14 +1326,11 @@
   background: var(--card-bg);
 }

-/* status-draft/status-locked map to the pill family below: draft -> amber
-   (engine's own draft concept, not part of the fixed color allowlist),
-   locked -> accent, review_pending -> warn. */
-.status-draft {
-  color: #d6a128;
-  background: rgba(214, 161, 40, .16);
-}
-
+/* status-draft/status-locked map to the pill family below: locked -> accent,
+   review_pending -> warn. draft -> amber is the engine's own concept and not
+   part of the fixed color allowlist; its one live rule sits with the pills in
+   the System Record layer further down (`.pill.pv, .status-draft`), which is
+   why there is no .status-draft rule here. */
 .status-locked {
   color: var(--accent);
   background: var(--accent-bg);
@@ -1241,11 +1347,14 @@
    every component routes body-shaped text through the shared "markdown"
    template func). Ported verbatim from the reference docs stylesheet's global
    `code{...}` rule so a regex, config key, or field name wrapped in single
-   backticks reads as a distinct code "pill", not plain prose. */
+   backticks reads as a distinct code "pill", not plain prose.
+
+   NO background here. The System Record layer's own `code` rule further down
+   re-declares it at equal specificity and later in the file, so anything set
+   here is dead; --code-inline-bg is read there, where it can win. */
 code {
   font-family: var(--font-mono);
   font-size: 12px;
-  background: rgba(127, 127, 127, .12);
   padding: 1px 5px;
   border-radius: 5px;
 }
@@ -1253,11 +1362,15 @@
 /* Fenced ```code``` spans inside a claim's Body, converted to real
    <pre><code> elements by markdown.Render. white-space is reset off the
    pre-wrap inherited from .claim-body since <pre> already preserves
-   whitespace on its own. Background is the same neutral rgba(127,127,127)
-   overlay tint the reference docs stylesheet's `pre{...}` rule uses (not
-   --card-bg, which is the surrounding card's own background and would
-   make the block invisible against it — the exact "white background, not
-   grey" regression this replaces). */
+   whitespace on its own.
+
+   NO background here, for the same reason as the `code` rule above: the
+   System Record layer re-declares this exact selector list later in the file
+   at equal specificity, so a background set here never reaches a reader.
+   --code-bg is read there. What that rule must NOT be is --card-bg, which is
+   the surrounding card's own background and would make the block invisible
+   against it — the "white background, not grey" regression these rules
+   replaced; it is a tint of --paper over --card-bg instead. */
 .claim-body pre,
 .claim-tree-body {
   font-size: 11.5px;
@@ -1266,7 +1379,6 @@
   overflow-x: auto;
   margin: 0.5rem 0;
   padding: 0.6rem 0.75rem;
-  background: rgba(127, 127, 127, .10);
   border: 1px solid var(--border);
   border-radius: var(--radius);
 }
@@ -1517,7 +1629,7 @@
 .claim-list-items .md-table th,
 .sbody .md-table th,
 .comment-body .md-table th {
-  background: rgba(127, 127, 127, .10);
+  background: var(--table-head-bg, rgba(127, 127, 127, .10));
   font-family: var(--font-sans);
   font-size: inherit;
   font-weight: 600;
@@ -1581,7 +1693,7 @@
   margin: 0.6rem 0;
   border: 1px solid var(--border);
   border-radius: 6px;
-  background: rgba(127, 127, 127, .06);
+  background: var(--image-bg, rgba(127, 127, 127, .06));
 }

 /* An image is the last thing in a list item often enough to be worth the
@@ -1893,8 +2005,8 @@
 }

 /* claim-ref is the shared anchor EVERY claim-to-claim edge renders through
-   (components.go's writeClaimRef): governed_by, mirrors, rests_on,
-   depended-on-by, and build_order.html's rests_on. Its two inner spans carry
+   (components.go's writeClaimRef): governed_by, mirrors, rests_on and
+   depended-on-by, all inside the shared claim footer. Its two inner spans carry
    the readability tiers — a dimmed "Widget · Contract ›" prefix, kept only
    when the target is outside the reading claim's own module/facet, and the
    claim's own label. No new color tokens: the prefix borrows --faint (the
@@ -1905,9 +2017,8 @@
    Specificity note: these are scoped under .claim-edges deliberately. The
    bare .claim-ref-prefix class (0,1,0) would lose to the existing
    ".claim-edges a" rule (0,1,1) and inherit --link; ".claim-edges
-   .claim-ref-prefix" (0,2,0) wins. build_order.html's independently rendered
-   rests_on sits inside a .claim-edges container too, so one scope covers
-   both paths. */
+   .claim-ref-prefix" (0,2,0) wins. The shared claim footer is the only
+   place a claim-ref renders, and this sheet is inlined into every viewer. */
 .claim-edges .claim-ref-prefix {
   color: var(--faint);
   font-weight: 400;
@@ -2095,20 +2206,6 @@
   text-decoration: underline;
 }

-/* Paper has no button to press, so the clamp must not reach it: print shows
-   every note whole and hides the control, the same bargain @media print
-   already strikes with the collapsed footer. */
-@media print {
-  .claim-source-note.is-clamped .claim-source-note-body {
-    display: block;
-    overflow: visible;
-  }
-
-  .claim-source-note-toggle {
-    display: none;
-  }
-}
-
 /* Deep-link auto-open for a SOURCE row, the companion to the .claim:target
    rules above. A citation marker's href names the row, not the claim, so the
    claim is not :target and those rules do not fire — yet the row the reader
@@ -2139,9 +2236,10 @@
   border-radius: 4px;
 }

+/* Colour and background are NOT set here: `.pill.pw, .claim-review-pending`
+   in the System Record layer sets both at equal specificity and later in the
+   file, so anything declared here would be dead. */
 .claim-review-pending {
-  color: var(--warn);
-  background: var(--warn-bg);
   padding: 0.1rem 0.4rem;
   border-radius: 4px;
   font-weight: 600;
@@ -2188,11 +2286,10 @@
    which is why a `.claim-comments { list-style: none; }` rule lived here —
    it is deleted in v0.4.1 along with that <li>, since the slot is a <span>
    and never carried a marker. Every color below is one of the inverting
-   theme tokens declared in :root above (and re-pointed in the
-   prefers-color-scheme: light block), so the whole surface tracks the OS
-   theme with no separate dark rule — the one genuinely per-mode value, the
-   panel's drop shadow, gets its light value in the media query at the end of
-   this block. claim-card--commented is a distinct accent marker,
+   theme tokens (declared in this file's single :root and re-pointed in the
+   screen-dark query beside it), so the whole surface tracks the OS theme with
+   no separate dark rule — including the one genuinely per-mode value, the
+   panel's drop shadow, which is --shadow. claim-card--commented is a distinct accent marker,
    deliberately NOT the --warn boundary family. */
 .comment-chip {
   display: inline-flex;
@@ -2282,7 +2379,7 @@
   border: 1px solid var(--border);
   border-radius: var(--radius, 12px);
   background: var(--paper);
-  box-shadow: 0 1px 2px rgba(0, 0, 0, .28);
+  box-shadow: 0 1px 2px var(--shadow, rgba(0, 0, 0, .08));
 }

 .comments-panel[hidden] {
@@ -2366,14 +2463,6 @@
   }
 }

-/* The panel shadow is the one genuinely per-mode value here: softer on a
-   light background than the deeper shadow the dark base above uses. */
-@media (prefers-color-scheme: light) {
-  .comments-panel {
-    box-shadow: 0 1px 2px rgba(0, 0, 0, .08);
-  }
-}
-
 /* ---- interactive comment UI (Phase 5, serve + file://) -----------------
    The 💬 chip opens a shared overlay panel: a non-modal right rail on desktop
    (role=complementary), a modal bottom sheet on mobile. Its backdrop
@@ -2381,10 +2470,13 @@
    half, so they are scoped to the <= 860px bottom-sheet breakpoint (in the media
    query below) — above it the desktop rail shows neither. They stay deliberately
    distinct from the sidebar's #navOverlay / body.nav-open — opening one overlay
-   closes the other. Colours are the inverting theme tokens from :root; the few
-   genuinely per-mode values (shadows, backdrop dim) get their light values in the
-   media query at the end of this block, matching the dark-base / light-override
-   convention the rest of this stylesheet uses. */
+   closes the other. Colours are the inverting theme tokens from this file's
+   single :root, including the per-mode ones: the backdrop dim is --scrim and
+   the toast shadow is --shadow-strong, both re-pointed in the screen-dark
+   query, so this block needs no light media query of its own. The rail's own
+   shadow is deliberately NOT per-mode: the `.comments-rail` rule in the
+   System Record layer further down sets --shadow-cast for both modes and wins
+   on source order. */

 /* body.comments-open drives the scroll-lock only at the mobile breakpoint (see
    the media query below); it stays a plain open-state marker on desktop. Never
@@ -2398,7 +2490,7 @@
   width: 100%;
   height: 100%;
   border: 0;
-  background: rgba(0, 0, 0, .42);
+  background: var(--scrim, rgba(0, 0, 0, .22));
   cursor: pointer;
 }

@@ -2416,7 +2508,6 @@
   flex-direction: column;
   background: var(--paper);
   border-left: 1px solid var(--border);
-  box-shadow: -2px 0 18px rgba(0, 0, 0, .38);
 }

 .comments-rail[hidden] {
@@ -2603,7 +2694,7 @@
   border: 1px solid var(--warn);
   border-radius: 10px;
   font: 13px var(--font-sans);
-  box-shadow: 0 4px 18px rgba(0, 0, 0, .34);
+  box-shadow: 0 4px 18px var(--shadow-strong, rgba(0, 0, 0, .14));
 }

 .comments-toast[hidden] {
@@ -2653,20 +2744,17 @@
    body.comments-open: three classes each setting overflow: hidden compose
    without a release-order hazard, so no coordination is needed between them. */

-/* Docked clear of the 210px desktop sidebar (its "Generated …" footer sits in
-   exactly this corner); the mobile override below reclaims the full-width left
-   edge, where the sidebar is an off-canvas drawer and owns no space. */
+/* Project health belongs to the reading flow, between the active module's
+   record header and its facet controls. It is deliberately quiet when closed:
+   enough contrast to be noticed, but no floating dock or shadow competing with
+   the document and its two navigation rails. */
 .status-strip {
-  position: fixed;
-  z-index: 45;
-  left: 226px;
-  bottom: 16px;
-  max-width: min(520px, calc(100vw - 242px));
+  width: 100%;
+  margin: -8px 0 22px;
   background: var(--card-bg);
   border: 1px solid var(--border);
-  border-radius: var(--radius);
-  font: 13px var(--font-sans);
-  box-shadow: 0 4px 18px rgba(0, 0, 0, .34);
+  border-radius: 7px;
+  font-family: var(--font-sans);
   overflow: hidden;
 }

@@ -2675,40 +2763,66 @@
 }

 .status-strip--lint {
-  border-color: var(--warn);
+  border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
+  background: color-mix(in srgb, var(--warn-bg) 34%, var(--card-bg));
 }

 .status-strip--integrity {
-  border-color: var(--warn);
-  border-left-width: 4px;
-  background: var(--warn-bg);
+  border-color: color-mix(in srgb, var(--warn) 48%, var(--border));
+  background: color-mix(in srgb, var(--warn-bg) 58%, var(--card-bg));
+  box-shadow: inset 0 2px 0 var(--warn);
 }

 .status-strip-head {
   display: flex;
   align-items: center;
-  gap: 10px;
+  gap: 12px;
   width: 100%;
   border: 0;
   background: none;
-  color: var(--warn);
-  padding: 10px 14px;
-  font: 600 13px var(--font-sans);
+  color: var(--ink);
+  padding: 13px 14px;
   text-align: left;
   cursor: pointer;
 }

 .status-strip-summary {
+  display: grid;
+  gap: 3px;
   flex: 1;
 }

-.status-strip-caret {
+.status-strip-title {
+  font-size: 13px;
+  font-weight: 680;
+  line-height: 1.35;
+}
+
+.status-strip-note {
   color: var(--muted);
+  font-size: 11.5px;
+  line-height: 1.45;
+}
+
+.status-strip-action {
+  color: var(--accent);
+  font: 600 10px var(--font-mono);
+  letter-spacing: .04em;
+  text-transform: uppercase;
+  white-space: nowrap;
+}
+
+.status-strip-caret {
+  width: 7px;
+  height: 7px;
+  border-right: 1.5px solid var(--muted);
+  border-bottom: 1.5px solid var(--muted);
+  transform: rotate(45deg) translateY(-2px);
   transition: transform .15s ease;
 }

 .status-strip--open .status-strip-caret {
-  transform: rotate(180deg);
+  transform: rotate(225deg) translate(-2px, -1px);
 }

 /* The detail list is capped and scrolls internally: a project with forty lint
@@ -2717,7 +2831,7 @@
   max-height: 40vh;
   overflow-y: auto;
   border-top: 1px solid var(--border);
-  padding: 10px 14px 12px;
+  padding: 12px 14px 14px;
 }

 .status-strip-body[hidden] {
@@ -2730,8 +2844,8 @@

 .status-group-head {
   margin: 0 0 6px;
-  font: 600 11px var(--font-sans);
-  letter-spacing: .04em;
+  font: 600 9px var(--font-mono);
+  letter-spacing: .08em;
   text-transform: uppercase;
   color: var(--muted);
 }
@@ -2758,11 +2872,15 @@

 .status-finding-rule,
 .status-finding-claim {
-  font: 11px var(--font-mono);
+  font: 10px var(--font-mono);
 }

 .status-finding-rule {
   color: var(--warn);
+  font-size: 9px;
+  font-weight: 600;
+  letter-spacing: .05em;
+  text-transform: uppercase;
 }

 .status-finding-claim {
@@ -2771,15 +2889,10 @@

 .status-finding-msg {
   flex-basis: 100%;
-  font-size: 12px;
+  font-size: 12.5px;
+  line-height: 1.5;
 }

-@media (prefers-color-scheme: light) {
-  .status-strip {
-    box-shadow: 0 4px 18px rgba(0, 0, 0, .14);
-  }
-}
-
 /* Coarse pointers (touch) need >=44px hit targets on every comment control. */
 @media (pointer: coarse) {
   .comments-rail-close,
@@ -2813,33 +2926,28 @@
     border-left: 0;
     border-top: 1px solid var(--border);
     border-radius: 14px 14px 0 0;
-    box-shadow: 0 -2px 18px rgba(0, 0, 0, .38);
   }
-  /* The sidebar is an off-canvas drawer here, so the left edge is free again. */
   .status-strip {
-    left: 16px;
-    max-width: calc(100vw - 32px);
+    margin-top: -4px;
   }
-}
-
-/* Per-mode values: softer shadows and a lighter backdrop dim on a light OS. */
-@media (prefers-color-scheme: light) {
-  .comments-rail {
-    box-shadow: -2px 0 18px rgba(0, 0, 0, .14);
+  .status-strip-head {
+    display: grid;
+    grid-template-columns: minmax(0, 1fr) 10px;
+    align-items: flex-start;
   }
-
-  .comments-overlay {
-    background: rgba(0, 0, 0, .22);
+  .status-strip-summary {
+    grid-column: 1;
+    grid-row: 1;
   }
-
-  .comments-toast {
-    box-shadow: 0 4px 18px rgba(0, 0, 0, .14);
+  .status-strip-action {
+    grid-column: 1;
+    grid-row: 2;
+    justify-self: start;
   }
-}
-
-@media (prefers-color-scheme: light) and (max-width: 860px) {
-  .comments-rail {
-    box-shadow: 0 -2px 18px rgba(0, 0, 0, .14);
+  .status-strip-caret {
+    grid-column: 2;
+    grid-row: 1;
+    margin-top: 4px;
   }
 }

@@ -2890,10 +2998,9 @@
   color: var(--accent);
 }

-.pill.pv {
-  background: rgba(214, 161, 40, .16);
-  color: #d6a128;
-}
+/* .pill.pv is NOT declared here: `.pill.pv, .status-draft` in the System
+   Record layer sets its background and colour at equal specificity and later
+   in the file, so anything declared here would be dead. */

 .pill.pw {
   background: var(--warn-bg);
@@ -2977,8 +3084,9 @@
    internal/lint/raw_html_scope.go's img allowlist entry). Styling ported
    from docs/tabs/llm-internals.html's inline img style for its health-
    state-machine diagram: a plain white card so an SVG/PNG diagram (often
-   drawn on a transparent or light background) reads consistently in both
-   light and dark viewer themes, same as it did in docs/. */
+   drawn on a transparent or light background) reads consistently. The card
+   is deliberately mode-INVARIANT — it does not follow the OS appearance,
+   because the artwork it frames usually cannot — same as it did in docs/. */
 .mockup-diagram {
   display: block;
   max-width: 100%;
@@ -2986,7 +3094,7 @@
   margin-top: 8px;
   border: 1px solid var(--border);
   border-radius: 6px;
-  background: #fff;
+  background: var(--mockup-bg, #fff);
   padding: 8px;
 }

@@ -3325,7 +3433,6 @@
 .content-area {
   flex: 1;
   min-width: 0;
-  max-width: 880px;
   margin: 0 auto;
 }

@@ -3507,7 +3614,6 @@
     padding: 6px 10px;
     font: 600 12px var(--font-sans);
     cursor: pointer;
-    box-shadow: 0 3px 12px rgba(0, 0, 0, .18);
   }

   .nav-overlay {
@@ -3518,6 +3624,11 @@
     width: 100%;
     height: 100%;
     border: 0;
+    /* Deliberately NOT --scrim. The sidebar backdrop is mode-invariant by
+       design: the sheet it dims is the same weight in both modes, and a
+       project that lightens --scrim for its comment sheet should not thereby
+       lighten this one. It is not a theme token and has no consumer in the
+       allowlist. */
     background: rgba(0, 0, 0, .42);
     opacity: 0;
     pointer-events: none;
@@ -3570,96 +3681,8 @@
   }
 }

-/* ---- print -------------------------------------------------------------
-   The first and only @media print block in this stylesheet. It exists for
-   one reason (Q4): paper has no disclosure widget, so a printed viewer that
-   honoured the collapsed <details> would silently drop every claim's
-   governed_by/rests_on/implemented-in footer — the exact facts a reviewer
-   prints the page to read.
-   Revealing the contents through CSS is deliberate rather than setting `open`,
-   which CSS cannot do: `open` is a content attribute, not a property a rule
-   can write.
-   Same mechanism, and the same two-rule pairing, as the .claim:target rule
-   above: a closed <details> hides its contents through the UA's
-   ::details-content pseudo-element, so that is what is targeted, and the
-   child-selector form is expected to be a no-op wherever the pseudo-element is
-   implemented. It is kept only as a fallback for engines that predate
-   ::details-content (Chrome/Edge 131+, Safari 18.4+, Firefox 139+) and hide
-   the contents some other way; such an engine drops the pseudo-element rule
-   and still prints as it did before v0.4.1. Both rules declare the same value
-   and carry equal specificity (0,1,2), so the pseudo-element rule is written
-   LAST — source order is the only thing ranking them.
-   This block is independent of the .claim:target rule, of the two
-   server-written `open` signals, and of the expand-all toolbar — whichever of
-   those is or is not in effect on screen, print shows all four states
-   identically.
-   SCOPE — stated plainly so the next reader knows this is a decision, not an
-   oversight: only the facet currently ON SCREEN prints. A collapsed footer
-   belonging to a claim in a facet group the reader is not looking at stays
-   unrendered, because its ancestor section.claim-group carries the `hidden`
-   attribute (computed display: none) and no rule here un-hides it. The rules
-   below reveal a disclosure inside a subtree that is being laid out; they
-   cannot resurrect a subtree that display: none removed from the box tree in
-   the first place. That is deliberate: printing a tabbed viewer has always
-   shown one facet, what v0.4.1 changes is that the shown facet's footers now
-   print with it, and un-hiding every facet for print is a larger design
-   change — page order, per-facet headings, the duplicated overview copies —
-   than a patch release should make.
-   Only the disclosure is touched. Nothing here restyles the card, the type
-   scale or the color tokens; a full print stylesheet is out of scope for
-   v0.4.1. */
-@media print {
-  details.claim-links > *:not(summary) {
-    content-visibility: visible;
-  }
-
-  details.claim-links::details-content {
-    content-visibility: visible;
-  }
-
-  .claim-links-summary {
-    cursor: auto;
-  }
-}
-
 /* System Record visual language for the generated viewer. */

-:root {
-  --accent: #287052;
-  --accent-bg: rgba(40, 112, 82, .12);
-  --ink: #091426;
-  --muted: #536179;
-  --faint: #7d899a;
-  --paper: #f6f8fc;
-  --card-bg: #ffffff;
-  --border: #d8deea;
-  --link: #205b78;
-  --warn: #a2433d;
-  --warn-bg: rgba(162, 67, 61, .10);
-  --dxg-facet-other: #0d55b5;
-  --font-sans: "Avenir Next", -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", sans-serif;
-  --font-mono: ui-monospace, "SFMono-Regular", "IBM Plex Mono", Menlo, monospace;
-  --radius: 6px;
-  color-scheme: light;
-}
-
-@media (prefers-color-scheme: dark) {
-  :root {
-    --ink: #e8eef8;
-    --muted: #a9b5c8;
-    --faint: #75839a;
-    --paper: #0a1220;
-    --card-bg: #0f1b2e;
-    --border: #263754;
-    --link: #8ab7ff;
-    --accent: #70c99c;
-    --accent-bg: rgba(112, 201, 156, .12);
-    --warn: #ff8b94;
-    --warn-bg: rgba(255, 139, 148, .10);
-    --dxg-facet-other: #77a9e8;
-  }
-}
-
 html { scroll-behavior: smooth; }

 body.shell-body {
@@ -3673,7 +3696,7 @@
   text-rendering: optimizeLegibility;
 }

-::selection { background: rgba(40, 112, 82, .20); }
+::selection { background: var(--selection-bg, rgba(40, 112, 82, .20)); }

 :where(button, select, summary, a):focus-visible {
   outline: 2px solid var(--accent);
@@ -3726,9 +3749,79 @@
 .system-resizing { cursor: col-resize; user-select: none; }

 .sidebar-header {
+  position: relative;
   padding: 20px 18px 18px;
 }

+.system-panel-toggle {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  width: 32px;
+  height: 32px;
+  padding: 0;
+  border: 1px solid var(--border);
+  border-radius: 6px;
+  background: var(--card-bg);
+  color: var(--muted);
+  cursor: pointer;
+  transition: border-color 150ms ease-out, color 150ms ease-out, background-color 150ms ease-out;
+}
+
+.system-panel-toggle:hover {
+  border-color: color-mix(in srgb, var(--accent) 62%, var(--border));
+  background: var(--accent-bg);
+  color: var(--ink);
+}
+
+.system-panel-toggle__chevron {
+  width: 7px;
+  height: 7px;
+  border-right: 1.5px solid currentColor;
+  border-bottom: 1.5px solid currentColor;
+  transform: rotate(135deg);
+  transition: transform 180ms ease-out;
+}
+
+.system-panel-toggle--sidebar {
+  position: absolute;
+  top: 16px;
+  right: 9px;
+}
+
+.sidebar-header .top { padding-right: 28px; }
+
+@media (min-width: 861px) {
+  .sidebar,
+  .content-area { transition: width 180ms ease-out, padding 180ms ease-out; }
+
+  body.system-sidebar-collapsed .sidebar {
+    width: 44px;
+    min-width: 44px;
+    max-width: 44px;
+    overflow: hidden;
+  }
+
+  body.system-sidebar-collapsed .sidebar-header {
+    padding: 10px 6px;
+    border-bottom: 0;
+  }
+
+  body.system-sidebar-collapsed .sidebar-header .top,
+  body.system-sidebar-collapsed #nav,
+  body.system-sidebar-collapsed #expandAllToggle,
+  body.system-sidebar-collapsed .sidebar-footer,
+  body.system-sidebar-collapsed .system-sidebar-resizer { display: none; }
+
+  body.system-sidebar-collapsed .system-panel-toggle--sidebar {
+    position: static;
+  }
+
+  body.system-sidebar-collapsed .system-panel-toggle--sidebar .system-panel-toggle__chevron {
+    transform: rotate(-45deg);
+  }
+}
+
 .top {
   align-items: flex-start;
   gap: 12px;
@@ -3807,7 +3900,7 @@
 .system-nav-group__toggle::marker { content: ""; }
 .system-nav-group__toggle::-webkit-details-marker { display: none; }

-.system-nav-group__toggle:hover { background: rgba(125, 137, 154, .08); }
+.system-nav-group__toggle:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); }

 .system-nav-group__count {
   min-width: 25px;
@@ -3858,7 +3951,7 @@

 #dxgOpen:hover,
 .sec-tab:hover {
-  background: rgba(125, 137, 154, .08);
+  background: var(--hover-bg, rgba(125, 137, 154, .08));
   color: var(--ink);
 }

@@ -3905,8 +3998,26 @@
   flex: 1;
   margin: 0;
   padding: 0 282px 80px 42px;
+  transition: padding 180ms ease-out;
 }

+@media (min-width: 1181px) {
+  body.system-toc-collapsed .content-area { padding-right: 86px; }
+}
+
+/* The Build order tab reclaims the 282px right rail .content-area reserves
+   for the facet TOC, which renderToc hides on this tab, so the widest
+   diagram on the page is not laid out beside a blank gutter. Scoped to
+   screen because at (0,3,1) an unscoped version would beat the print
+   block's `.content-area { padding: 0 }` (0,1,0) on paper — the first rule
+   to break the invariant the print header states — and to the same
+   min-width as the TOC-collapsed rule above because the same specificity
+   equally outranks the tablet and phone .content-area rules, where the rail
+   it reclaims does not exist. */
+@media screen and (min-width: 1181px) {
+  body:has(.build-order-section:not([hidden])) .content-area { padding-right: 42px; }
+}
+
 .system-record-head {
   display: grid;
   grid-template-columns: minmax(0, 1fr) auto;
@@ -3914,7 +4025,7 @@
   align-items: start;
   margin: 0 0 24px;
   padding: 38px 0 24px;
-  border-bottom: 1px solid #aab5c7;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
 }

 .system-record-head h2 {
@@ -3973,16 +4084,204 @@
   font-weight: 700;
 }

-.build-order-module[hidden] { display: none; }
+/* ---- Build order tab (.bo-*) --------------------------------------------
+   The viewer's build order is a top-level tab of its own (#dossierx-build-order, a
+   .module-section) holding one .bo-module group per locked module, each six
+   .bo-phase blocks in the fixed sequence with one mermaid flowchart per
+   non-empty phase (internal/buildorder/mermaid.go; build-order.html).

-.build-order-module.system-orientation-only > :not(.system-build-title):not(.system-orientation-node) {
-  display: none;
+   .bo-modules is the module strip: NOT .sub-nav, which has never needed
+   horizontal overflow above 860px and has none — 25 locked modules at a 23px
+   gap run off the desktop column with no scrollbar. It scrolls sideways at
+   every width, sticky like the .sub-nav it stands in for (same z-index).
+
+   COLOURS: every colour declaration below is !important, and that is not
+   decoration. mermaid injects its own stylesheet into each SVG, compiled
+   under the SVG's id — "#mermaid-1 .node rect" at (1,1,1) — which outranks
+   any unqualified page rule, so without !important every node paints in
+   mermaid's base-theme lavender: light boxes on --paper: #0a1220 under the
+   dark block, the exact failure these rules exist to prevent. The diagram
+   text carries NO colour literal (PaletteCSS emits only stroke-dasharray
+   classDefs), so the light :root, the screen-dark block and a project's
+   viewer.theme all apply here by construction. The ghost rule names path as
+   well as rect: a stadium is drawn by rough.js as <path> elements inside a
+   <g class="basic label-container outer-path">, never a <rect>. The rules
+   assume mermaid's default look (classic); under look: 'handDrawn' even a
+   rectangle is a <path>, which is why build-order-ui.js never sets look.
+   The edge rule targets .flowchart-link, mermaid 11's class on every edge
+   path; the arrowhead markers live in <defs> and are painted from the
+   lineColor themeVariable build-order-ui.js reads off --muted at
+   initialise time. ---------------------------------------------------- */
+.bo-modules {
+  display: flex;
+  flex-wrap: nowrap;
+  gap: 23px;
+  overflow-x: auto;
+  scrollbar-width: thin;
+  position: sticky;
+  z-index: 8;
+  top: 0;
+  margin: 0 0 24px;
+  padding: 0;
+  border-bottom: 1px solid var(--border);
+  background: var(--paper);
 }

-.build-order-module.system-orientation-only > .build-order-phase.system-orientation-node {
-  display: none;
+.bo-modules .subtab {
+  flex: 0 0 auto;
+  white-space: nowrap;
 }

+.bo-phase {
+  padding: 22px 0 26px;
+  border-top: 1px solid var(--border);
+}
+
+.bo-phase__head {
+  display: grid;
+  gap: 6px;
+  margin: 0 0 14px;
+  padding: 0 0 14px;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
+}
+
+.bo-phase__num {
+  color: var(--accent);
+  font: 700 11px/1.4 var(--font-mono);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+}
+
+.bo-phase__name {
+  margin: 0;
+  color: var(--ink);
+  font-size: 22px;
+  font-weight: 600;
+  letter-spacing: -.02em;
+}
+
+.bo-phase__def {
+  max-width: 72ch;
+  margin: 0;
+  color: var(--ink);
+  font-size: 14px;
+}
+
+.bo-phase__meta,
+.bo-empty {
+  margin: 0;
+  color: var(--muted);
+  font-size: 13px;
+  font-variant-numeric: tabular-nums;
+}
+
+/* The wide-diagram rule: the SVG keeps its natural width (mermaid runs with
+   useMaxWidth: false and the max-width rule is belt and braces), a block
+   wider than the column scrolls inside .bo-diagram, and .content-area never
+   gains a horizontal scrollbar. Nothing scales a diagram down. */
+.bo-diagram {
+  overflow-x: auto;
+  overflow-y: hidden;
+  padding: 14px 10px;
+  border: 1px solid var(--border);
+  border-radius: 8px;
+  background: var(--card-bg);
+}
+
+.bo-diagram svg {
+  display: block;
+  max-width: none !important;
+  height: auto;
+}
+
+/* Nothing else in this sheet styles a bare pre (only .claim-body pre), so
+   without these the block shows its flowchart source as a wall of monospace
+   between parse and the first render pass. mermaid stamps data-processed
+   BEFORE it reads the text, so the hiding stops the moment a render starts
+   and never hides a failed block (whose reader sees .bo-error and mermaid's
+   own message). After the render the <pre> survives around the SVG and its
+   white-space reaches the html labels' foreignObject content. */
+.bo-diagram pre.mermaid {
+  margin: 0;
+  white-space: normal;
+  font-family: var(--font-sans);
+}
+
+.bo-diagram pre.mermaid:not([data-processed]) {
+  visibility: hidden;
+  min-height: 2rem;
+}
+
+.bo-diagram .node.locked_con rect {
+  fill: var(--accent-bg) !important;
+  stroke: var(--accent) !important;
+}
+
+.bo-diagram .node.draft_con rect {
+  fill: var(--card-bg) !important;
+  stroke: var(--accent) !important;
+}
+
+.bo-diagram .node.locked_int rect {
+  fill: color-mix(in srgb, var(--link) 12%, var(--card-bg)) !important;
+  stroke: var(--link) !important;
+}
+
+.bo-diagram .node.draft_int rect {
+  fill: var(--card-bg) !important;
+  stroke: var(--link) !important;
+}
+
+.bo-diagram .node.ghost rect,
+.bo-diagram .node.ghost path {
+  fill: var(--card-bg) !important;
+  stroke: var(--border) !important;
+}
+
+.bo-diagram .node .nodeLabel {
+  color: var(--ink) !important;
+}
+
+.bo-diagram .node.ghost .nodeLabel {
+  color: var(--muted) !important;
+}
+
+.bo-diagram .flowchart-link {
+  stroke: var(--muted) !important;
+}
+
+.bo-diagram g.node { cursor: pointer; }
+
+.bo-diagram g.node.bo-missing {
+  opacity: .55;
+  cursor: not-allowed;
+}
+
+.bo-cross,
+.bo-excluded {
+  margin: 12px 0 0;
+  padding: 0 0 0 18px;
+  color: var(--muted);
+  font-size: 13px;
+}
+
+.bo-cross__module {
+  color: var(--ink);
+  font-weight: 600;
+}
+
+.bo-excluded-deps {
+  margin: 8px 0 0;
+  color: var(--muted);
+  font-size: 13px;
+}
+
+.bo-error {
+  margin: 8px 0 0;
+  color: var(--warn);
+  font-size: 13px;
+}
+
 .facet-toc {
   position: fixed;
   z-index: 9;
@@ -3993,6 +4292,7 @@
   border-left: 1px solid var(--border);
   background: var(--paper);
   color: var(--muted);
+  transition: width 180ms ease-out;
 }

 .facet-toc[hidden] { display: none; }
@@ -4022,6 +4322,36 @@
   color: var(--ink);
 }

+.facet-toc__head .system-panel-toggle {
+  flex: 0 0 auto;
+  width: 28px;
+  height: 28px;
+  margin: 0 0 -4px;
+}
+
+.system-panel-toggle--toc .system-panel-toggle__chevron { transform: rotate(-45deg); }
+
+body.system-toc-collapsed .facet-toc {
+  width: 44px;
+  border-left: 0;
+  background: transparent;
+}
+
+body.system-toc-collapsed .facet-toc__head {
+  justify-content: center;
+  padding: 4px;
+  border-bottom: 0;
+}
+
+body.system-toc-collapsed .facet-toc__identity,
+body.system-toc-collapsed .facet-toc__total,
+body.system-toc-collapsed .facet-toc__list,
+body.system-toc-collapsed .facet-toc__select { display: none; }
+
+body.system-toc-collapsed .system-panel-toggle--toc .system-panel-toggle__chevron {
+  transform: rotate(135deg);
+}
+
 .facet-toc__head small {
   display: block;
   margin-bottom: 3px;
@@ -4072,10 +4402,69 @@

 .facet-toc__item > span { color: var(--faint); font: 9px/1.45 var(--font-mono); }
 .facet-toc__item > strong { font-size: 11px; font-weight: 520; line-height: 1.38; }
-.facet-toc__item:hover { background: rgba(125, 137, 154, .08); color: var(--ink); }
+.facet-toc__item:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); color: var(--ink); }
 .facet-toc__item.on { background: var(--accent-bg); box-shadow: inset 2px 0 0 var(--accent); color: var(--ink); }
 .facet-toc__item.on > span { color: var(--accent); }

+.facet-claim-controls {
+  display: flex;
+  justify-content: flex-end;
+  margin: 0 0 12px;
+}
+
+.facet-claims-toggle {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  min-height: 34px;
+  padding: 6px 10px;
+  border: 1px solid var(--border);
+  border-radius: 6px;
+  background: var(--card-bg);
+  color: var(--muted);
+  font: 650 10px/1.2 var(--font-mono);
+  cursor: pointer;
+}
+
+.facet-claims-toggle:hover {
+  border-color: color-mix(in srgb, var(--accent) 62%, var(--border));
+  color: var(--ink);
+}
+
+.facet-claims-toggle:disabled { cursor: default; opacity: .5; }
+
+.facet-claims-toggle__icon {
+  position: relative;
+  width: 11px;
+  height: 11px;
+}
+
+.facet-claims-toggle__icon::before,
+.facet-claims-toggle__icon::after {
+  content: "";
+  position: absolute;
+  left: 2px;
+  width: 6px;
+  height: 6px;
+  border-right: 1.5px solid currentColor;
+  border-bottom: 1.5px solid currentColor;
+  transform: rotate(45deg);
+  transition: transform 150ms ease-out, top 150ms ease-out;
+}
+
+.facet-claims-toggle__icon::before { top: -1px; }
+.facet-claims-toggle__icon::after { top: 3px; }
+
+.facet-claims-toggle[aria-pressed="true"] .facet-claims-toggle__icon::before {
+  top: 2px;
+  transform: rotate(225deg);
+}
+
+.facet-claims-toggle[aria-pressed="true"] .facet-claims-toggle__icon::after {
+  top: 6px;
+  transform: rotate(225deg);
+}
+
 .claim { scroll-margin-top: 72px; }

 .section-heading {
@@ -4166,7 +4555,7 @@
   transform: translateY(-50%);
 }

-.claim-collapse-toggle:hover { background: rgba(125, 137, 154, .08); }
+.claim-collapse-toggle:hover { background: var(--hover-bg, rgba(125, 137, 154, .08)); }

 .claim-collapse-toggle > .label {
   display: flex;
@@ -4219,8 +4608,8 @@

 .pill.pv,
 .status-draft {
-  background: rgba(151, 102, 0, .12);
-  color: #976600;
+  background: var(--status-draft-bg, rgba(151, 102, 0, .12));
+  color: var(--status-draft, #976600);
 }

 .pill.pw,
@@ -4244,18 +4633,22 @@
 .claim-body strong,
 .sbody strong { font-weight: 700; }

+/* The LIVE `code` background: this rule beats the inline-code pill rule near
+   the top of the file at equal specificity, on source order. --code-inline-bg
+   is read here so the token actually reaches a reader. */
 code {
   border: 1px solid var(--border);
-  background: color-mix(in srgb, var(--paper) 72%, var(--card-bg));
+  background: var(--code-inline-bg, color-mix(in srgb, var(--paper) 72%, var(--card-bg)));
   color: var(--ink);
   font-size: .86em;
 }

+/* The LIVE fenced-block background, same story as `code` above. */
 .claim-body pre,
 .claim-tree-body {
   padding: 13px 14px;
   border-radius: 7px;
-  background: color-mix(in srgb, var(--paper) 82%, var(--card-bg));
+  background: var(--code-bg, color-mix(in srgb, var(--paper) 82%, var(--card-bg)));
   font-size: 12px;
 }

@@ -4280,6 +4673,11 @@

 .claim-links-summary::marker { content: ""; }
 .claim-links-summary::-webkit-details-marker { display: none; }
+/* NOT --hover-bg, and deliberately so. This row is much larger than the other
+   hover targets and takes a fainter wash: .07, not .08. Reading the token here
+   would compute .08, because :root always declares --hover-bg and the fallback
+   would never fire — the row would silently change. It is not themable through
+   --hover-bg; a project that wants it themable needs its own token. */
 .claim-links-summary:hover { background: rgba(125, 137, 154, .07); color: var(--ink); }

 .claim-footer__identity { display: flex; gap: 10px; align-items: center; min-width: 0; }
@@ -4385,7 +4783,7 @@
 .track-head {
   margin: 0 0 24px;
   padding: 38px 0 23px;
-  border-bottom: 1px solid #aab5c7;
+  border-bottom: 1px solid var(--border-strong, #aab5c7);
 }

 .track-title {
@@ -4411,69 +4809,10 @@

 .track-cite > .pill { margin-left: auto; }

-.build-order-module {
-  margin-top: 34px;
-  padding-top: 22px;
-  border-top: 1px solid var(--border);
-}
-
-.build-order-module > .system-build-title {
-  margin: 0 0 24px;
-  padding: 0 0 15px;
-  border-top: 0;
-  border-bottom: 1px solid #aab5c7;
-  color: var(--ink);
-  font-size: 20px;
-  letter-spacing: -.02em;
-  text-transform: none;
-}
-
-.build-order-module > .build-order-phase {
-  margin: 30px 0 10px;
-  padding: 0;
-  border: 0;
-  color: var(--muted);
-  font: 700 10px/1.4 var(--font-mono);
-  letter-spacing: .09em;
-}
-
-.build-order-module .card { padding: 15px 17px; }
-.build-order-module .k { margin-bottom: 11px; font-size: 14px; }
-.build-order-module .claim-edges { display: grid; gap: 8px; }
-
-.build-order-module .claim-file,
-.build-order-module .claim-rests-on {
-  display: grid;
-  grid-template-columns: 72px minmax(0, 1fr);
-  gap: 10px;
-  align-items: start;
-  color: var(--muted);
-  font-size: 11px;
-}
-
-.build-order-field__label {
-  color: var(--faint);
-  font: 700 9px/1.55 var(--font-mono);
-  letter-spacing: .07em;
-  text-transform: uppercase;
-}
-
-.build-order-module code {
-  border: 0;
-  background: transparent;
-  overflow-wrap: anywhere;
-}
-
-.build-order-module .claim-edge-id-list {
-  margin: 0;
-  padding: 0;
-  list-style: none;
-}
-
 .comments-rail {
   border-left-color: var(--border);
   background: var(--card-bg);
-  box-shadow: -14px 0 34px rgba(9, 20, 38, .12);
+  box-shadow: -14px 0 34px var(--shadow-cast, rgba(9, 20, 38, .12));
 }

 .comments-rail-head {
@@ -4483,7 +4822,6 @@

 .status-strip {
   border-top-color: var(--border);
-  background: var(--card-bg);
   color: var(--muted);
 }

@@ -4803,13 +5141,23 @@
   }

   .facet-toc__disclosure,
-  .facet-toc__head,
   .facet-toc__list { display: none; }
+  .facet-toc__head {
+    position: absolute;
+    z-index: 1;
+    top: 0;
+    right: 0;
+    justify-content: flex-end;
+    width: 40px;
+    padding: 6px;
+    border: 0;
+  }
+  .facet-toc__head > :not(.system-panel-toggle) { display: none; }
   .facet-toc__select {
     display: block;
     width: 100%;
     min-height: 40px;
-    padding: 7px 30px 7px 10px;
+    padding: 7px 46px 7px 10px;
     border: 0;
     border-radius: 6px;
     background: var(--card-bg);
@@ -4832,11 +5180,13 @@
     border-radius: 6px;
     background: var(--card-bg);
     color: var(--ink);
-    box-shadow: 0 7px 18px rgba(9, 20, 38, .12);
+    box-shadow: 0 7px 18px var(--shadow-cast, rgba(9, 20, 38, .12));
   }

   .sidebar { width: min(310px, calc(100vw - 42px)); min-width: 0; max-width: none; }
   .system-sidebar-resizer { display: none; }
+  .system-panel-toggle--sidebar { display: none; }
+  .sidebar-header .top { padding-right: 0; }

   .facet-toc {
     position: fixed;
@@ -4848,17 +5198,27 @@
     border: 1px solid var(--border);
     border-radius: 7px;
     background: var(--card-bg);
-    box-shadow: 0 7px 18px rgba(9, 20, 38, .12);
+    box-shadow: 0 7px 18px var(--shadow-cast, rgba(9, 20, 38, .12));
   }

   .facet-toc__disclosure,
-  .facet-toc__head,
   .facet-toc__list { display: none; }
+  .facet-toc__head {
+    position: absolute;
+    z-index: 1;
+    top: 0;
+    right: 0;
+    justify-content: flex-end;
+    width: 40px;
+    padding: 5px;
+    border: 0;
+  }
+  .facet-toc__head > :not(.system-panel-toggle) { display: none; }
   .facet-toc__select {
     display: block;
     width: 100%;
     min-height: 38px;
-    padding: 7px 30px 7px 10px;
+    padding: 7px 46px 7px 10px;
     border: 0;
     border-radius: 7px;
     background: var(--card-bg);
@@ -4931,16 +5291,101 @@
   .claim-footer__counts { justify-content: flex-start; }
   .claim-footer__counts > span:not(.claim-footer__chevron) { padding: 4px 6px; }
   .claim-footer__chevron { margin-left: auto; }
-
-  .build-order-module .claim-file,
-  .build-order-module .claim-rests-on { grid-template-columns: 1fr; gap: 3px; }
 }

+/* ---- print — THE @media print block ------------------------------------
+   There is exactly one @media print block in this stylesheet and this is it;
+   internal/render/theme_tokens_test.go fails if a second one appears.
+
+   IT MUST STAY LAST IN THE FILE. Several of the rules below beat a screen
+   rule of identical specificity purely on source order — `.content-area
+   { padding: 0 }` against the padded `.content-area` rule in the System
+   Record layer, `.system-record-head { padding-top: 0 }` against that layer's
+   `.system-record-head`. Move this block earlier and the printed page silently
+   regains the sidebar gutter.
+
+   color-scheme is pinned to light here to match the palette: the :root at the
+   top of the file is light-first and its dark query is `screen`-scoped, so
+   print already gets light custom properties; this line makes the UA-painted
+   parts (form controls, scrollbars, canvas) agree. Unverified for Firefox and
+   Safari; where a browser already forces light for print it is a no-op, so it
+   is safe either way.
+
+   Consolidated from three separate @media print blocks. Their reasons follow,
+   unchanged, one per group.
+
+   NOTE (Q4): paper has no button to press, so the source-note clamp must not
+   reach it — print shows every note whole and hides the control.
+
+   DISCLOSURE (Q4). Paper has no disclosure widget, so a printed viewer that
+   honoured the collapsed <details> would silently drop every claim's
+   governed_by/rests_on/implemented-in footer — the exact facts a reviewer
+   prints the page to read.
+   Revealing the contents through CSS is deliberate rather than setting `open`,
+   which CSS cannot do: `open` is a content attribute, not a property a rule
+   can write.
+   Same mechanism, and the same two-rule pairing, as the .claim:target rule
+   above: a closed <details> hides its contents through the UA's
+   ::details-content pseudo-element, so that is what is targeted, and the
+   child-selector form is expected to be a no-op wherever the pseudo-element is
+   implemented. It is kept only as a fallback for engines that predate
+   ::details-content (Chrome/Edge 131+, Safari 18.4+, Firefox 139+) and hide
+   the contents some other way; such an engine drops the pseudo-element rule
+   and still prints as it did before v0.4.1. Both rules declare the same value
+   and carry equal specificity (0,1,2), so the pseudo-element rule is written
+   LAST — source order is the only thing ranking them.
+   This block is independent of the .claim:target rule, of the two
+   server-written `open` signals, and of the expand-all toolbar — whichever of
+   those is or is not in effect on screen, print shows all four states
+   identically.
+   SCOPE — stated plainly so the next reader knows this is a decision, not an
+   oversight: only the facet currently ON SCREEN prints. A collapsed footer
+   belonging to a claim in a facet group the reader is not looking at stays
+   unrendered, because its ancestor section.claim-group carries the `hidden`
+   attribute (computed display: none) and no rule here un-hides it. The rules
+   below reveal a disclosure inside a subtree that is being laid out; they
+   cannot resurrect a subtree that display: none removed from the box tree in
+   the first place. That is deliberate: printing a tabbed viewer has always
+   shown one facet, what v0.4.1 changes is that the shown facet's footers now
+   print with it, and un-hiding every facet for print is a larger design
+   change — page order, per-facet headings, the duplicated overview copies —
+   than a patch release should make.
+   Only the disclosure is touched. Nothing here restyles the card, the type
+   scale or the color tokens; a full print stylesheet is out of scope for
+   v0.4.1.
+*/
 @media print {
+  :root { color-scheme: light; }
+
+  /* source-note clamp */
+  .claim-source-note.is-clamped .claim-source-note-body {
+    display: block;
+    overflow: visible;
+  }
+
+  .claim-source-note-toggle {
+    display: none;
+  }
+
+  /* collapsed claim-links footer */
+  details.claim-links > *:not(summary) {
+    content-visibility: visible;
+  }
+
+  details.claim-links::details-content {
+    content-visibility: visible;
+  }
+
+  .claim-links-summary {
+    cursor: auto;
+  }
+
+  /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
   .nav-toggle,
   .facet-toc,
+  .facet-claim-controls,
   .comments-rail,
   .status-strip { display: none !important; }
   .content-area { padding: 0; }
@@ -4949,6 +5394,27 @@
   .claim-collapse-toggle { pointer-events: none; }
   .claim-collapse-chevron { display: none; }
   .claim-collapse-content[hidden] { display: block !important; }
+
+  /* Build order tab. What this buys is honest and small: a phase block is
+     not split across a page break. Browsers do not paginate horizontally, so
+     a diagram wider than the paper is CLIPPED at the page edge; nothing here
+     makes it spill onto further pages. The marker rules exist because
+     lineColor and background are baked into the SVG at initialise time from
+     the SCREEN palette while this block pins color-scheme: light and the
+     dark block above is screen-scoped: a dark-mode reader printing would get
+     light node fills beside pale-grey arrowheads on a near-black backdrop.
+     The module strip is the ONLY place the printed module's name appears
+     (build-order.html's section has no heading of its own), so the strip
+     stays on the page: only the buttons for the OTHER modules are dropped,
+     the selected one prints as the page's title line, unstuck from the
+     viewport it no longer has. */
+  .bo-modules { position: static; }
+  .bo-modules .subtab:not(.on) { display: none; }
+  .bo-diagram { overflow: visible; }
+  .bo-phase { break-inside: avoid; }
+  .bo-diagram svg { background: transparent !important; }
+  .bo-diagram .marker,
+  .bo-diagram marker path { fill: var(--muted) !important; stroke: var(--muted) !important; }
 }
 </style>
   <style></style>
@@ -4963,6 +5429,9 @@
           <div class="logo"><span></span></div>
           <div><h1>dossierx viewer</h1></div>
         </div>
+        <button id="sidebarCollapseToggle" class="system-panel-toggle system-panel-toggle--sidebar" type="button" aria-controls="sidebar" aria-expanded="true" aria-label="Hide navigation" title="Hide navigation">
+          <span class="system-panel-toggle__chevron" aria-hidden="true"></span>
+        </button>
       </div>
       <nav id="nav">
         
@@ -5028,7 +5497,6 @@
                 
               </section>
             
-            
           </section>
         
       
@@ -5049,8 +5517,12 @@
   
   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
     <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
-      <span id="statusStripSummary" class="status-strip-summary"></span>
-      <span class="status-strip-caret" aria-hidden="true">▾</span>
+      <span id="statusStripSummary" class="status-strip-summary">
+        <strong id="statusStripTitle" class="status-strip-title"></strong>
+        <span id="statusStripNote" class="status-strip-note"></span>
+      </span>
+      <span id="statusStripAction" class="status-strip-action">Show issues</span>
+      <span class="status-strip-caret" aria-hidden="true"></span>
     </button>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>
@@ -5075,7 +5547,7 @@
       
       
       var moduleSections, moduleTabs;
-      var facetToModule, moduleDefaultFacet, firstModuleID, claimToFacet, buildOrderToModule, sourceToFacet;
+      var facetToModule, moduleDefaultFacet, firstModuleID, claimToFacet, sourceToFacet;

       
       
@@ -5140,22 +5612,6 @@
         
         
         
-        
-        
-        buildOrderToModule = {};
-        moduleSections.forEach(function (sec) {
-          sec.querySelectorAll('.build-order-module').forEach(function (bo) {
-            if (bo.id) { buildOrderToModule[bo.id] = sec.id; }
-            bo.querySelectorAll('.claim[id]').forEach(function (c) {
-              buildOrderToModule[c.id] = sec.id;
-            });
-          });
-        });
-
-        
-        
-        
-        
         syncEmptyChips();

         
@@ -5174,6 +5630,8 @@
       
       
       
+      
+      
       function resolve(id) {
         if (Object.prototype.hasOwnProperty.call(claimToFacet, id)) {
           var facetID = claimToFacet[id];
@@ -5190,10 +5648,6 @@
           var srcFacet = sourceToFacet[id];
           return { module: facetToModule[srcFacet], facet: srcFacet, claim: id };
         }
-        if (Object.prototype.hasOwnProperty.call(buildOrderToModule, id)) {
-          var boModule = buildOrderToModule[id];
-          return { module: boModule, facet: moduleDefaultFacet[boModule], claim: id };
-        }
         if (Object.prototype.hasOwnProperty.call(facetToModule, id)) {
           return { module: facetToModule[id], facet: id };
         }
@@ -5226,6 +5680,12 @@
           });
         }

+        if (lastStatusData) {
+          renderStatusStrip(lastStatusData);
+        } else {
+          positionStatusStrip();
+        }
+
         
         
         
@@ -6063,7 +6523,11 @@
       var stripEl = document.getElementById('statusStrip');
       var stripToggle = document.getElementById('statusStripToggle');
       var stripSummary = document.getElementById('statusStripSummary');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripNote = document.getElementById('statusStripNote');
+      var stripAction = document.getElementById('statusStripAction');
       var stripBody = document.getElementById('statusStripBody');
+      var lastStatusData = null;
       
       
       
@@ -6077,6 +6541,59 @@
         return n + ' ' + word + (n === 1 ? '' : 's');
       }

+      function humanRule(name) {
+        return String(name || '').split('-').filter(Boolean).map(function (part) {
+          return part.charAt(0).toUpperCase() + part.slice(1);
+        }).join(' ');
+      }
+
+      function activeFacetClaimIDs() {
+        var ids = Object.create(null);
+        
+        
+        var section = document.querySelector('.module-section:not([hidden]):not(.build-order-section)');
+        var group = section && section.querySelector(':scope > .claim-group:not([hidden])');
+        if (!group) { return ids; }
+        group.querySelectorAll('.claim').forEach(function (claim) {
+          var id = claim.dataset.claimId || claim.id;
+          if (id) { ids[id] = true; }
+        });
+        return ids;
+      }
+
+      function findingsForActiveFacet(findings, claimIDs) {
+        return findings.filter(function (finding) {
+
+
+
+          return !finding.claim_id || claimIDs[finding.claim_id] === true;
+        });
+      }
+
+
+
+
+
+
+      function positionStatusStrip() {
+        if (!stripEl) { return; }
+        
+        
+        var section = document.querySelector('.module-section:not([hidden]):not(.build-order-section)');
+        if (!section) {
+          var content = document.querySelector('.content-area');
+          if (content && stripEl.parentNode !== content) { content.insertBefore(stripEl, content.firstChild); }
+          return;
+        }
+        var header = section.querySelector(':scope > .system-record-head, :scope > .track-head');
+        if (header) {
+          if (header.nextElementSibling !== stripEl) { header.insertAdjacentElement('afterend', stripEl); }
+        } else if (section.firstElementChild !== stripEl) {
+          section.insertBefore(stripEl, section.firstChild);
+        }
+      }
+      window.dossierxPositionStatusStrip = positionStatusStrip;
+
       
       
       
@@ -6105,9 +6622,11 @@
       
       
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary) { return; }
-        var ledger = (data && data.ledger_findings) || [];
-        var lintErrors = (data && data.lint_errors) || [];
+        if (!stripEl || !stripBody || !stripSummary || !stripTitle || !stripNote) { return; }
+        lastStatusData = data || {};
+        var claimIDs = activeFacetClaimIDs();
+        var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
+        var lintErrors = findingsForActiveFacet(lastStatusData.lint_errors || [], claimIDs);

         if (!ledger.length && !lintErrors.length) {
           stripEl.hidden = true;
@@ -6119,31 +6638,36 @@
         stripBody.textContent = '';
         if (ledger.length) {
           stripBody.appendChild(findingGroup(
-            'Lock ledger — these claims were changed without an approval record',
-            ledger.map(function (f) { return findingRow(f.rule, f.claim_id, f.message); })
+            'Approval record issues',
+            ledger.map(function (f) { return findingRow(humanRule(f.rule), f.claim_id, f.message); })
           ));
         }
         if (lintErrors.length) {
           stripBody.appendChild(findingGroup(
-            'Lint errors',
-            lintErrors.map(function (f) { return findingRow(f.lint, f.claim_id, f.message); })
+            'Issues to resolve',
+            lintErrors.map(function (f) { return findingRow(humanRule(f.lint), f.claim_id, f.message); })
           ));
         }

         
         
-        var summary = ledger.length
-          ? 'Integrity: ' + countLabel(ledger.length, 'lock-ledger finding')
-          : 'Lint: ' + countLabel(lintErrors.length, 'error');
-        if (ledger.length && lintErrors.length) {
-          summary += ' · ' + countLabel(lintErrors.length, 'lint error');
+        if (ledger.length) {
+          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
+          stripNote.textContent = lintErrors.length
+            ? 'There ' + (lintErrors.length === 1 ? 'is ' : 'are ') + countLabel(lintErrors.length, 'issue') + ' in this facet also blocking the next successful DossierX check.'
+            : 'Review these locked claims before relying on their approved state.';
+        } else {
+          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
+          stripNote.textContent = lintErrors.length === 1
+            ? 'This issue blocks the next successful DossierX check.'
+            : 'These issues block the next successful DossierX check.';
         }
-        stripSummary.textContent = summary; 

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        if (ledger.length && !stripUserToggled) { stripExpanded = true; }
+        if (!stripUserToggled) { stripExpanded = ledger.length > 0; }
         setStripExpanded(stripExpanded);
+        positionStatusStrip();
         stripEl.hidden = false;
       }

@@ -6151,6 +6675,7 @@
         stripExpanded = !!expanded;
         if (stripBody) { stripBody.hidden = !stripExpanded; }
         if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
+        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
         if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
       }

@@ -6520,6 +7045,23 @@
         oldNav.outerHTML = frag.nav;           

         
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        
+        if (document.querySelector('.build-order-section') && typeof window.mermaid === 'undefined') {
+          window.location.reload();
+          return;
+        }
+
+        
         initViewer();

         
@@ -6706,10 +7248,44 @@
     });
   }

-  function syncNavigation() {
+  function bindSidebarCollapse() {
+    var toggle = document.getElementById('sidebarCollapseToggle');
+    if (!toggle || toggle.dataset.bound === 'true') { return; }
+    toggle.dataset.bound = 'true';
+    toggle.addEventListener('click', function () {
+      var collapsed = !document.body.classList.contains('system-sidebar-collapsed');
+      document.body.classList.toggle('system-sidebar-collapsed', collapsed);
+      toggle.setAttribute('aria-expanded', String(!collapsed));
+      toggle.setAttribute('aria-label', collapsed ? 'Show navigation' : 'Hide navigation');
+      toggle.title = collapsed ? 'Show navigation' : 'Hide navigation';
+    });
+  }
+
+  function bindNavigationGroupPreferences() {
+    document.querySelectorAll('.system-nav-group').forEach(function (group) {
+      if (group.dataset.readerPreferenceBound === 'true') { return; }
+      group.dataset.readerPreferenceBound = 'true';
+      var summary = group.querySelector(':scope > summary');
+      if (summary) {
+        summary.addEventListener('click', function (event) {
+          event.preventDefault();
+          var willOpen = !group.open;
+          group.dataset.readerClosed = String(!willOpen);
+          group.open = willOpen;
+        });
+      }
+      group.addEventListener('toggle', function () {
+        group.dataset.readerClosed = group.open ? 'false' : 'true';
+      });
+    });
+  }
+
+  function syncNavigation(force) {
     var active = document.querySelector('.system-nav-group .sec-tab.on');
     var group = active && active.closest('.system-nav-group');
-    if (group) { group.open = true; }
+    if (!group || (!force && group.dataset.readerClosed === 'true')) { return; }
+    group.dataset.readerClosed = 'false';
+    group.open = true;
   }

   function enhanceFooters() {
@@ -6757,18 +7333,16 @@
       ['.claim-links > .claim-edges > .claim-migrated', 'Migrated From', /^\s*migrated_from:\s*/i],
       ['.claim-links > .claim-edges > .claim-implemented-in', 'Implemented In', /^\s*implemented\s+in:\s*/i],
       ['.claim-links > .claim-edges > .claim-review-pending', 'Review Pending', /^\s*review_pending\s*/i],
-      ['.claim-links > .claim-edges > .claim-sources', 'Sources', /^\s*sources:\s*/i],
-      ['.build-order-module .claim-file', 'File', /^\s*file:\s*/i],
-      ['.build-order-module .claim-rests-on', 'Rests On', /^\s*rests_on:\s*/i]
+      ['.claim-links > .claim-edges > .claim-sources', 'Sources', /^\s*sources:\s*/i]
     ];
     rows.forEach(function (entry) {
       document.querySelectorAll(entry[0]).forEach(function (row) {
-        if (row.querySelector(':scope > .claim-relation-label, :scope > .build-order-field__label')) { return; }
+        if (row.querySelector(':scope > .claim-relation-label')) { return; }
         var textNode = Array.prototype.slice.call(row.childNodes).find(function (node) { return node.nodeType === 3 && node.textContent.trim(); });
         if (!textNode) { return; }
         textNode.textContent = textNode.textContent.replace(entry[2], ' ');
         var label = document.createElement('span');
-        label.className = entry[0].indexOf('.build-order-module') === 0 ? 'build-order-field__label' : 'claim-relation-label';
+        label.className = 'claim-relation-label';
         label.textContent = entry[1];
         row.insertBefore(label, textNode);
       });
@@ -6786,6 +7360,7 @@
     toggle.setAttribute('aria-expanded', String(expanded));
     var title = toggle.dataset.claimTitle || 'claim';
     toggle.setAttribute('aria-label', (expanded ? 'Collapse ' : 'Expand ') + title);
+    updateFacetClaimControl();
   }

   function enhanceClaimDisclosures() {
@@ -6843,7 +7418,7 @@

   function cleanTitle(claim) {
     var title = claim.querySelector(':scope > .k');
-    if (!title) { return (claim.id || '').replace(/^build-order-/, '').replace(/[.-]/g, ' '); }
+    if (!title) { return (claim.id || '').replace(/[.-]/g, ' '); }
     var copy = title.cloneNode(true);
     copy.querySelectorAll('.pill, .claim-comments-slot').forEach(function (node) { node.remove(); });
     return copy.textContent.replace(/\s+/g, ' ').trim();
@@ -6860,14 +7435,13 @@
     });
   }

+  // activeFacet returns null while the Build order tab is active (it is not a
+  // module section: no header, TOC, status strip or claim controls are built
+  // over the diagrams), so renderToc hides the TOC there.
   function activeFacet() {
-    var modules = Array.prototype.slice.call(document.querySelectorAll('.module-section:not(.track-section)'));
+    var modules = Array.prototype.slice.call(document.querySelectorAll('.module-section:not(.track-section):not(.build-order-section)'));
     var module = modules.find(function (section) { return !section.hidden; });
     if (!module) { return null; }
-    var build = module.querySelector(':scope > .build-order-module:not([hidden])');
-    if (build) {
-      return { module: module, view: build, label: build.classList.contains('system-orientation-only') ? 'Orientation' : 'Build Order' };
-    }
     var groups = Array.prototype.slice.call(module.querySelectorAll(':scope > .claim-group'));
     var group = groups.find(function (section) { return !section.hidden; });
     if (!group) { return null; }
@@ -6875,6 +7449,45 @@
     return { module: module, view: group, label: tab ? tab.textContent.replace(/🔒/g, '').trim() : 'Claims' };
   }

+  function updateFacetClaimControl(active, claims) {
+    active = active || activeFacet();
+    if (!active) { return; }
+    claims = claims || visibleClaims(active.view);
+    var control = active.view.querySelector(':scope > .facet-claim-controls');
+    if (!control) { return; }
+    var toggle = control.querySelector('.facet-claims-toggle');
+    if (!toggle) { return; }
+    var allCollapsed = claims.length > 0 && claims.every(function (claim) {
+      var disclosure = claim.querySelector(':scope > .k > .claim-collapse-toggle');
+      return disclosure && disclosure.getAttribute('aria-expanded') === 'false';
+    });
+    toggle.disabled = claims.length === 0;
+    toggle.setAttribute('aria-pressed', String(allCollapsed));
+    toggle.querySelector('.facet-claims-toggle__label').textContent = allCollapsed ? 'Expand all claims' : 'Collapse all claims';
+  }
+
+  function renderFacetClaimControl(active, claims) {
+    var control = active.view.querySelector(':scope > .facet-claim-controls');
+    if (!control) {
+      control = document.createElement('div');
+      control.className = 'facet-claim-controls';
+      control.innerHTML = '<button class="facet-claims-toggle" type="button" aria-pressed="false"><span class="facet-claims-toggle__icon" aria-hidden="true"></span><span class="facet-claims-toggle__label">Collapse all claims</span></button>';
+      active.view.insertBefore(control, active.view.firstChild);
+      control.querySelector('.facet-claims-toggle').addEventListener('click', function () {
+        var current = activeFacet();
+        if (!current) { return; }
+        var currentClaims = visibleClaims(current.view);
+        var shouldExpand = currentClaims.length > 0 && currentClaims.every(function (claim) {
+          var disclosure = claim.querySelector(':scope > .k > .claim-collapse-toggle');
+          return disclosure && disclosure.getAttribute('aria-expanded') === 'false';
+        });
+        currentClaims.forEach(function (claim) { setClaimExpanded(claim, shouldExpand); });
+        updateFacetClaimControl(current, currentClaims);
+      });
+    }
+    updateFacetClaimControl(active, claims);
+  }
+
   function updateTocActive() {
     var toc = document.getElementById('systemFacetToc');
     if (!toc || toc.hidden) { return; }
@@ -6897,11 +7510,18 @@
       toc.id = 'systemFacetToc';
       toc.className = 'facet-toc';
       toc.setAttribute('aria-label', 'Claims in this facet');
-      toc.innerHTML = '<div class="facet-toc__head"><span><small>On this facet</small><strong class="facet-toc__name">Claims</strong></span><span class="facet-toc__total"></span></div><nav class="facet-toc__list"></nav><select class="facet-toc__select" aria-label="Jump to a claim in this facet"></select>';
+      toc.innerHTML = '<div class="facet-toc__head"><span class="facet-toc__identity"><small>On this facet</small><strong class="facet-toc__name">Claims</strong></span><span class="facet-toc__total"></span><button class="system-panel-toggle system-panel-toggle--toc" type="button" aria-controls="systemFacetToc" aria-expanded="true" aria-label="Hide table of contents" title="Hide table of contents"><span class="system-panel-toggle__chevron" aria-hidden="true"></span></button></div><nav class="facet-toc__list"></nav><select class="facet-toc__select" aria-label="Jump to a claim in this facet"></select>';
       toc.querySelector('.facet-toc__select').addEventListener('change', function (event) {
         var claim = document.getElementById(event.target.value);
         if (claim) { claim.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
       });
+      toc.querySelector('.system-panel-toggle--toc').addEventListener('click', function (event) {
+        var collapsed = !document.body.classList.contains('system-toc-collapsed');
+        document.body.classList.toggle('system-toc-collapsed', collapsed);
+        event.currentTarget.setAttribute('aria-expanded', String(!collapsed));
+        event.currentTarget.setAttribute('aria-label', collapsed ? 'Show table of contents' : 'Hide table of contents');
+        event.currentTarget.title = collapsed ? 'Show table of contents' : 'Hide table of contents';
+      });
       document.body.appendChild(toc);
     }
     var active = activeFacet();
@@ -6910,6 +7530,7 @@
     var claims = visibleClaims(active.view);
     toc.querySelector('.facet-toc__name').textContent = active.label;
     toc.querySelector('.facet-toc__total').textContent = claims.length + (claims.length === 1 ? ' claim' : ' claims');
+    renderFacetClaimControl(active, claims);
     var list = toc.querySelector('.facet-toc__list');
     var select = toc.querySelector('.facet-toc__select');
     list.replaceChildren();
@@ -6936,99 +7557,12 @@
     updateTocActive();
   }

-  function prepareBuildOrder(module) {
-    var build = module.querySelector(':scope > .build-order-module');
-    if (!build || build.dataset.systemPrepared === 'true') { return build; }
-    build.dataset.systemPrepared = 'true';
-    var title = build.querySelector(':scope > h3');
-    if (title) { title.classList.add('system-build-title'); title.dataset.originalText = title.textContent; }
-    var children = Array.prototype.slice.call(build.children);
-    var orientation = children.find(function (node) {
-      return node.classList.contains('build-order-phase') && node.textContent.trim().toLowerCase() === 'orientation';
-    });
-    if (orientation) {
-      var start = children.indexOf(orientation);
-      for (var index = start; index < children.length; index += 1) {
-        var node = children[index];
-        if (index > start && node.classList.contains('build-order-phase')) { break; }
-        node.classList.add('system-orientation-node');
-      }
-      build.dataset.hasOrientation = 'true';
-    }
-    return build;
-  }
-
-  function activateFacet(module, mode, targetID, updateHash) {
-    var build = prepareBuildOrder(module);
-    var isBuild = mode === 'build-order' || mode === 'orientation';
-    module.querySelectorAll(':scope > .claim-group').forEach(function (group) { group.hidden = isBuild || group.id !== targetID; });
-    if (build) {
-      build.hidden = !isBuild;
-      build.classList.toggle('system-orientation-only', mode === 'orientation');
-      var title = build.querySelector(':scope > .system-build-title');
-      if (title) { title.textContent = mode === 'orientation' ? 'Orientation' : title.dataset.originalText; }
-    }
-    module.querySelectorAll(':scope > .sub-nav .subtab').forEach(function (tab) {
-      var tabMode = tab.dataset.systemMode || 'claims';
-      tab.classList.toggle('on', tabMode === mode && (isBuild || tab.dataset.target === '#' + targetID));
-    });
-    if (updateHash) { history.replaceState(null, '', '#' + targetID); }
-    scrollTo(0, 0);
-    renderToc();
-  }
-
-  function enhanceFacetNavigation() {
-    document.querySelectorAll('.module-section:not(.track-section)').forEach(function (module) {
-      var build = prepareBuildOrder(module);
-      if (!build) { return; }
-      var subnav = module.querySelector(':scope > .sub-nav');
-      if (!subnav) {
-        subnav = document.createElement('div');
-        subnav.className = 'sub-nav system-created-sub-nav';
-        var firstGroup = module.querySelector(':scope > .claim-group');
-        var firstTab = document.createElement('button');
-        firstTab.className = 'subtab';
-        firstTab.dataset.target = '#' + firstGroup.id;
-        firstTab.textContent = 'Claims';
-        subnav.appendChild(firstTab);
-        module.insertBefore(subnav, firstGroup);
-      }
-      if (!subnav.querySelector('[data-system-mode="build-order"]')) {
-        [['build-order', 'Build Order', '#' + build.id], ['orientation', 'Orientation', '#orientation-' + module.id]].forEach(function (entry) {
-          if (entry[0] === 'orientation' && build.dataset.hasOrientation !== 'true') { return; }
-          var tab = document.createElement('button');
-          tab.type = 'button';
-          tab.className = 'subtab system-added-subtab';
-          tab.dataset.target = entry[2];
-          tab.dataset.systemMode = entry[0];
-          tab.textContent = entry[1];
-          tab.addEventListener('click', function (event) {
-            event.preventDefault();
-            event.stopImmediatePropagation();
-            activateFacet(module, entry[0], entry[2].slice(1), true);
-          }, true);
-          subnav.appendChild(tab);
-        });
-      }
-      subnav.querySelectorAll('.subtab:not(.system-added-subtab)').forEach(function (tab) {
-        if (tab.dataset.systemBound === 'true') { return; }
-        tab.dataset.systemBound = 'true';
-        tab.addEventListener('click', function () { setTimeout(function () { activateFacet(module, 'claims', tab.dataset.target.slice(1), false); }, 0); });
-      });
-      var active = Array.prototype.slice.call(module.querySelectorAll(':scope > .claim-group')).find(function (group) { return !group.hidden; });
-      if (active && !module.dataset.systemActivated) {
-        module.dataset.systemActivated = 'true';
-        activateFacet(module, 'claims', active.id, false);
-      }
-    });
-  }
-
   function addModuleHeaders() {
     var labels = {};
     document.querySelectorAll('.sec-tab[data-target]').forEach(function (button) {
       labels[button.dataset.target.slice(1)] = button.textContent.replace(/🔒/g, '').trim();
     });
-    document.querySelectorAll('.module-section:not(.track-section)').forEach(function (section) {
+    document.querySelectorAll('.module-section:not(.track-section):not(.build-order-section)').forEach(function (section) {
       if (section.querySelector(':scope > .system-record-head')) { return; }
       var claims = Array.prototype.slice.call(section.querySelectorAll('.claim-group .claim[data-status]'));
       var unique = {};
@@ -7079,12 +7613,16 @@
     running = true;
     enhanceTimestamp();
     bindResizer();
+    bindSidebarCollapse();
+    bindNavigationGroupPreferences();
     enhanceFooters();
     enhanceFieldLabels();
     enhanceClaimDisclosures();
     revealHashTarget();
     addModuleHeaders();
-    enhanceFacetNavigation();
+    if (typeof window.dossierxPositionStatusStrip === 'function') {
+      window.dossierxPositionStatusStrip();
+    }
     syncNavigation();
     renderToc();
     enhanceGraphLabels();
@@ -7110,11 +7648,11 @@
     // in the same event cycle whenever it contained the active tab.
     var navigationChanged = !!event.target.closest('.sec-tab');
     setTimeout(function () {
-      if (navigationChanged) { syncNavigation(); }
+      if (navigationChanged) { syncNavigation(true); }
       renderToc();
     }, 0);
   });
-  window.addEventListener('hashchange', function () { setTimeout(function () { revealHashTarget(); syncNavigation(); renderToc(); }, 0); });
+  window.addEventListener('hashchange', function () { setTimeout(function () { revealHashTarget(); syncNavigation(true); renderToc(); }, 0); });
   window.addEventListener('scroll', updateTocActive, { passive: true });
   window.dossierxEnhanceSystemRecord = enhance;
   enhance();

ADDED SINCE THE BASELINE
------------------------
testdata/fixture-theme-flat/build/catalog/catalog.json
testdata/fixture-theme-flat/build/viewer/index.html
testdata/fixture-theme-preset/build/catalog/catalog.json
testdata/fixture-theme-preset/build/viewer/index.html
