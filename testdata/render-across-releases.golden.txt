# render-across-releases.golden.txt — how this project's rendered output has
# moved since the previous release.
#
# Generated by tests/render_across_releases_test.go. Regenerate with:
#
#     go test ./tests -run TestRenderedOutputAcrossReleases -regenerate-goldens
#
# WHAT IT IS FOR. v0.4.0 changed table layout and v0.4.1 changed the edges footer
# and the chips, with every claim locked and byte-identical, the lock store
# untouched and `dossierx check` reporting nothing. A consumer's own merge gate
# cannot detect that for them; only a comparison against the release tag can.
#
# SILENT RENDER CHANGES is the section that matters: an artifact whose inputs did
# not move by a byte and whose rendered output did. Every entry there needs a
# CHANGELOG line, and the diff printed under it is what that line is written from.
#
# EXPLAINED BY AN INPUT CHANGE carries THE SAME DIFF, and the inputs that moved.
# The two sections differ in what they oblige you to do, never in what they show
# you. A fixture project's inputs are every tracked non-artifact file under it —
# nothing here knows which claim fed which line of a ten-thousand-line viewer —
# so one edited byte anywhere in a fixture moves that whole fixture's viewer into
# this section. If the section could suppress the diff, that one byte would also
# hide a renamed CSS class in the same viewer, which is precisely the v0.4.1
# change. So: read this section's diffs, and satisfy yourself that every hunk in
# them is accounted for by the named inputs. A hunk that is not is a silent
# render change wearing an explanation.
#
# THE BASELINE LINE IS PART OF THE SNAPSHOT. It records which release this was
# compared against, so the comparison is auditable rather than implied. The
# shared resolver reads the newest stable changelog heading, enumerates canonical
# stable semver tags, and chooses the greatest reachable predecessor below it.
# DOSSIERX_PREV_RELEASE_TAG is validation-only: it must equal that automatic
# predecessor, so a report cannot silently compare against a stale, future, or
# self tag. Regenerating after a tag is a release step, in the same breath as
# regenerating surface.json.
#
# An empty report is the expected steady state immediately after a release.

baseline   v0.7.18 (3f6903abdb717ca6ff47186448b1c3b6a16f0d2c)
compared   153 rendered artifacts present at both revisions
added      0
removed    0
silent     5 rendered differently from byte-identical inputs
explained  0 rendered differently after their own inputs changed

SILENT RENDER CHANGES
---------------------

testdata/fixture-basic/build/viewer/index.html  (+1552 -262)
    size            21986 lines / 1176486 bytes -> 23276 lines / 1234179 bytes
@@ -5365,6 +5365,15 @@
    form; only the bottom hairline separates it from the body beneath. The
    520px inset-card form below is a DIFFERENT, deliberately distinct board
    (R-H.1) and keeps its own margin/border/radius, untouched by this. */
+/* THIS IS THE DESKTOP FORM and is unchanged: a full-bleed band carrying one
+   summary sentence with "Show issues" beside it. At 1440 a row has the space
+   to carry a sentence and an action without either crowding the other.
+
+   The PHONE form is a different component — a card with one row per finding
+   (Paper EH1-0) — and lives under "The banner card" near the end of this
+   file. Both are in the DOM; the 520px block picks one. Everything from here
+   to .status-strip-action-chevron therefore applies to the desktop form only,
+   and the mobile block switches all of it off rather than re-tuning it. */
 .status-strip {
   width: 100%;
   background: var(--card-bg);
@@ -5377,25 +5386,26 @@
   display: none;
 }

+/* The strip itself is card ground. Its TINT lives on each row (below,
+   keyed by data-tone), because a band with two findings on it carries two
+   hues and a surface cannot make two severity claims at once (Paper board
+   15, C2M-0 red over C92-0 amber, hairline-divided). */
 .status-strip--lint {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
 }

 .status-strip--integrity {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
   box-shadow: none;
 }

-/* retry fix 20: 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop
-   banner at 13px/32px, not 13px/14px — 32px is the reading card's own
-   gutter (--card-gutter, G8), reproduced here as a literal because
-   .status-strip mounts as a sibling of the record header, never as a
-   descendant of .card/.claim-tree, so the custom property does not cascade
-   to it. The 520px inset-card form re-pins this back down to 14px in this
-   lane's own appended mobile block, matching the value this rule carried
-   (and was verified against) before this fix. */
+/* 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop banner at
+   13px/32px — 32px is the reading card's own gutter (--card-gutter, G8),
+   reproduced here as a literal because .status-strip mounts as a sibling of
+   the record header, never as a descendant of .card/.claim-tree, so the
+   custom property does not cascade to it. */
 .status-strip-head {
   display: flex;
   align-items: center;
@@ -5409,11 +5419,9 @@
   cursor: pointer;
 }

-/* retry fix 14: 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning
-   triangle ahead of the banner sentence at every width, in both schemes — it
-   was absent everywhere. .status-strip-summary becomes the icon's row; the
-   title/note stack (unchanged) moves into its own -text wrapper so the two
-   IDs the runtime writes to keep working untouched. */
+/* 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning triangle
+   ahead of the banner sentence. .status-strip-summary is the icon's row; the
+   title stack sits in its own -text wrapper. */
 .status-strip-summary {
   display: flex;
   align-items: flex-start;
@@ -5430,20 +5438,70 @@
   flex-shrink: 0;
 }

+/* One row per finding, each its own tint (Paper C2M-0: blocked ground
+   #9E3B360F; CDR-0: edited ground #9A6A1612), divided by the hairline. The
+   icon slot is 15px either way: the triangle for an alarm row, a 7px dot in
+   the draft hue for an edited row, so the sentences of stacked rows share
+   one left edge. */
+.status-strip-head[data-tone="alarm"] { background: var(--status-strip-bg); }
+.status-strip-head[data-tone="draft"] { background: var(--status-draft-bg, rgba(154, 106, 22, .11)); }
+.status-strip-head + .status-strip-head { border-top: 1px solid var(--border); }
+
+.status-strip-dot {
+  display: none;
+  width: 15px;
+  height: 15px;
+  flex-shrink: 0;
+  align-items: center;
+  justify-content: center;
+}
+
+.status-strip-dot::before {
+  content: "";
+  width: 7px;
+  height: 7px;
+  border-radius: 999px;
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-head[data-tone="draft"] .status-strip-icon { display: none; }
+.status-strip-head[data-tone="draft"] .status-strip-dot { display: inline-flex; }
+.status-strip-head[data-tone="draft"] .status-strip-action { color: var(--status-draft, #976600); }
+
 .status-strip-summary-text {
   display: grid;
   gap: 3px;
   min-width: 0;
 }

-/* retry fix 21: 02 §4.10 "Body" / 03 §4.5 "Body text" measure the banner
-   sentence at Inter 14/18 weight 400, not 13px weight 680 — the sentence
-   reads as body prose, not an emphasized heading. */
+/* 02 §4.10 "Body" / 03 §4.5 "Body text": the banner sentence is Inter 14/18
+   weight 400 — it reads as body prose, not as an emphasized heading. */
 .status-strip-title {
   font: 400 14px/18px var(--font-sans);
   color: var(--ink);
 }

+/* 02 §4.10/§4.19, 03 §4.5/§7.5, R-H.1: "Show issues" is the ALARM half of the
+   banner (the body sentence stays --color-ink) and reads in the same red as
+   the icon, never the generic --accent link colour. */
+.status-strip-action {
+  color: var(--warn);
+  font: 500 13px/16px var(--font-sans);
+  letter-spacing: 0;
+  text-transform: none;
+  white-space: nowrap;
+}
+
+.status-strip-action-row {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+}
+
+/* § 4.10 draws no trailing chevron on the desktop banner. */
+.status-strip-action-chevron {
+  display: none;
+}
 /* .status-strip-note (the "Critical 0 · Needs you 3 · ..." tally line) is
    REMOVED per the coordinator's ruling on this retry: no 02/03/04 board
    draws it, and the severity chips already carry every count it repeated.
@@ -5458,30 +5516,12 @@
    500, 13/16 — R-H.1's weight 600 at 13/16 is the PHONE step (added in
    L8's own 520 block below), not the desktop default. --warn is correct at
    both widths and is unchanged. */
-.status-strip-action {
-  color: var(--warn);
-  font: 500 13px/16px var(--font-sans);
-  letter-spacing: 0;
-  text-transform: none;
-  white-space: nowrap;
-}
-
 /* retry fix 15: the action text (#statusStripAction, whose textContent the
    runtime overwrites on every toggle) and its trailing chevron are separate
    elements inside this row wrapper — the row, not the text span, is what
    the 520 block turns into R-H.1's divided action row, so a re-render can
    never wipe the chevron out from under it. The chevron itself is hidden
    above 520 (§ 4.10 draws no trailing chevron on the desktop banner). */
-.status-strip-action-row {
-  display: inline-flex;
-  align-items: center;
-  gap: 6px;
-}
-
-.status-strip-action-chevron {
-  display: none;
-}
-
 /* The detail list is capped and scrolls internally: a project with forty lint
    errors must not grow a dock that swallows the page it is describing. */
 /* retry fix 16: the promoted list is 04 §3/§9 item 4's bottom-open card —
@@ -5744,6 +5784,81 @@
   font-weight: 600;
 }

+/* Paper board 15 (BR5-0 / ETC-0 / EUD-0): a row's third line — the cause
+   kind in mono, a mono summary of what moved, and for a blocker row whose
+   owner was approved and is being rewritten, the way to its diff. */
+.status-finding-detail {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 9px;
+  padding-top: 1px;
+}
+
+.status-finding-kind {
+  padding: 2px 7px;
+  border-radius: 999px;
+  font: 500 10px/12px var(--font-mono);
+  background: var(--warn-bg);
+  color: var(--warn);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-kind,
+.status-finding-kind--edited {
+  background: var(--status-draft-bg);
+  color: var(--status-draft);
+}
+
+/* BR5-0: the chip under a blocker row is the claim chip's word, at the
+   row's scale — Inter 10/14 600, 0.06em, uppercase. */
+.status-finding-kind--edited {
+  font: 600 10px/14px var(--font-sans);
+  letter-spacing: .06em;
+  text-transform: uppercase;
+}
+
+.status-finding-meta {
+  font: 400 11px/14px var(--font-mono);
+  color: var(--muted);
+}
+
+.status-finding-link {
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  font: 600 11px/14px var(--font-sans);
+  color: var(--link);
+}
+
+.status-finding-link:hover { text-decoration: underline; }
+
+/* EUD-0: a Needs-you row's right-hand slot is its way in, in the row's hue,
+   where a blocker row keeps its count. */
+.status-finding-action {
+  flex: 0 0 auto;
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  text-align: right;
+  font: 600 14px/18px var(--font-sans);
+  color: var(--warn);
+}
+
+.status-finding-action:hover { text-decoration: underline; }
+
+/* A row caused by an edit since approval is draft work, not an alarm: its
+   dot and its action take the draft hue (EUD-0). */
+.status-finding--group[data-tone="draft"] .status-finding-dot,
+.status-finding--group[data-severity="needs_you"][data-tone="draft"] .status-finding-dot {
+  background: var(--status-draft);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-action {
+  color: var(--status-draft);
+}
+
 /* file:// has no /api/status and therefore no ledger_findings at all (04 §8
    item 11 / §9 item 5) — stated in the body rather than silently omitted. */
 .status-strip-static-note {
@@ -5870,7 +5985,7 @@
   .comment-reply-trigger,
   .comment-composer-submit,
   .comment-composer-input,
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
   }
@@ -5934,12 +6049,15 @@
     border-left: 0;
     /* R-J.2 / OD14a.7 / M4, verifier RETRY item 4(iii): a dark-only 1px top
        hairline (the light sheet relies on its 16px corners and the scrim
-       alone). `light-dark()` expresses "present only under dark" without a
-       second selector-scoped dark query or a new dark-only token. A
+       alone). --sheet-hairline is the engine-owned token declared for
+       exactly this edge (:root and the two dark blocks): `transparent` in
+       light, var(--border) in dark, on both the OS path and the explicit
+       Dark toggle. It used to be a `light-dark()` literal while the token
+       sat unread; the token is the single place the edge is decided. A
        component-scoped `@media screen and (prefers-color-scheme: dark)`
-       block is unavailable
-       here (theme_tokens_test.go's ExactlyOneScreenScopedDarkQuery). */
-    border-top: 1px solid light-dark(transparent, var(--border));
+       block is unavailable here (theme_tokens_test.go's
+       ExactlyOneScreenScopedDarkQuery). */
+    border-top: 1px solid var(--sheet-hairline);
     border-top-left-radius: 16px;
     border-top-right-radius: 16px;
     border-radius: 16px 16px 0 0;
@@ -6015,25 +6133,10 @@
     max-width: 100%;
     box-sizing: border-box;
   }
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
-  }
-  .status-strip-head {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) 10px;
-    align-items: flex-start;
-  }
-  .status-strip-summary {
-    grid-column: 1;
-    grid-row: 1;
-  }
-  .status-strip-action-row {
-    grid-column: 1;
-    grid-row: 2;
-    justify-self: start;
-  }
-}
+  }}

 /* The claim head line. v0.4.1 gives it two children instead of loose text:
    a <span class="label"> carrying the label plus its status pill, and the
@@ -6576,24 +6679,9 @@
   background: var(--accent-bg);
 }

-/* nav-group-label separates the Tracks run from the modules above it. The
-   sidebar is otherwise a flat list of .sec-tab buttons, and a track tab looks
-   exactly like a module tab because mechanically it IS one — so without a
-   caption a reader would have no way to tell that the axis changed halfway
-   down. It borrows the .eyebrow register (mono, uppercase, --faint) rather
-   than inventing one, because it is the same kind of thing: a quiet caption
-   over the content beneath it, never a control. The top border and margin are
-   what make it read as a division rather than as a first entry in the run. */
-.nav-group-label {
-  margin: 14px 0 4px;
-  padding: 10px 20px 0;
-  border-top: 1px solid var(--border);
-  font-family: var(--font-mono);
-  font-size: 9px;
-  letter-spacing: .14em;
-  text-transform: uppercase;
-  color: var(--faint);
-}
+/* .nav-group-label (the caption over the Tracks run) is gone: the sidebar
+   groups modules and tracks in .system-nav-group <details> (shell.html), and
+   nothing has emitted the class since the system-record redesign. */

 /* Secondary horizontal facet tab strip inside a module's content area,
    ported verbatim (selector + declarations) from the reference docs
@@ -7222,14 +7310,6 @@
   color: var(--link);
 }

-.nav-group-label {
-  margin: 17px 0 5px;
-  padding: 12px 10px 0;
-  color: var(--faint);
-  font-size: 9px;
-  letter-spacing: .12em;
-}
-
 .sidebar-footer {
   padding: 11px 18px 15px;
   color: var(--faint);
@@ -8015,9 +8095,23 @@
   max-width: 760px;
 }

-.claim-body-disclosure > .claim-body { max-width: none; }
+.claim-body-disclosure > .claim-body,
+.claim-body-disclosure > .claim-edit-diff,
+.claim-body-disclosure > .claim-edit-current { max-width: none; }

-.claim-body-disclosure--collapsed > .claim-body {
+/* .claim-edit-diff is clamped alongside .claim-body because it IS the body,
+   shown a passage at a time (see system-record.js's BODY_LIKE). A claim that
+   truncates to four lines in one state and runs to full height in the other
+   is the same claim behaving as two components.
+   The clamp counts LINE BOXES and is not a height: the diff's passages carry
+   their own margins, and a tinted one carries padding, so a collapsed diff is
+   four lines of prose PLUS whatever box the first passage sits in. It is
+   therefore a little taller than a collapsed body and not a fixed multiple of
+   the line height — which is the honest description, and what the test
+   asserts rather than an exact 4x that does not hold. */
+.claim-body-disclosure--collapsed > .claim-body,
+.claim-body-disclosure--collapsed > .claim-edit-diff,
+.claim-body-disclosure--collapsed > .claim-edit-current {
   display: -webkit-box;
   overflow: hidden;
   -webkit-box-orient: vertical;
@@ -9762,31 +9856,10 @@
     border-radius: 10px;
     border-color: color-mix(in srgb, var(--warn) 26%, var(--border));
   }
-  /* retry fix 29 (this lane's third attempt): the desktop banner's own
-     padding moved from a literal 14px to the reading card's 32px gutter
-     (retry fix 20 above), but the 520px form stays the inset-card treatment
-     R-H.1 always specified. 02 §4.19 "Body block" gives the mobile form's
-     own padding as 12px/14px — one px shallower than the pre-retry-3 value
-     this rule carried, which was never checked against §4.19 directly. */
-  .status-strip-head {
-    padding: 12px 14px;
-  }
-  /* retry fix 33 (this lane's third attempt, 02 §4.19 "Icon"): the mobile
-     icon's optical offset is 2px, not the desktop row's 1px. */
-  .status-strip-icon {
-    margin-top: 2px;
-  }
-  /* retry fix 34 (this lane's third attempt, 02 §4.19 "Body text"): the
-     mobile banner sentence is Inter 13/19, a full step down from the
-     desktop 14/18 fix 21 (above) set — §4.19 is a distinct node (`574-0`)
-     from §4.10's `6T-0`, not the same rule at a smaller viewport. */
-  .status-strip-title {
-    font-size: 13px;
-    line-height: 19px;
-  }
-  .status-strip--open {
-    border-radius: 10px 10px 0 0;
-  }
+  /* The banner's own head padding, icon offset and sentence size used to be
+     re-pinned here for the phone. All three described the single-sentence
+     head that Paper EH1-0 replaced with a card, and their rules went with
+     it; the card carries its own metrics at both widths. */
   .status-strip-body {
     border-top-color: color-mix(in srgb, var(--warn) 18%, var(--border));
   }
@@ -9861,32 +9934,6 @@
     height: 30px;
     padding-inline: 12px;
   }
-  /* retry fix 15 (R-H.1, 02 §4.19): the action gets its own divided row —
-     a 1px top hairline at ~18% blocked tint, a 38px row and 14px inline
-     padding, bled to the head's own inline/bottom padding edges so the tap
-     target is the card's full width. `grid-column: 1 / -1` reclaims the
-     10px caret column too, since the divider must span edge to edge. */
-  .status-strip-action-row {
-    grid-column: 1 / -1;
-    justify-self: stretch;
-    display: flex;
-    align-items: center;
-    justify-content: space-between;
-    height: 38px;
-    margin: 8px -14px -13px;
-    padding-inline: 14px;
-    border-top: 1px solid color-mix(in srgb, var(--warn) 18%, transparent);
-  }
-  .status-strip-action {
-    font-weight: 600; /* retry fix 13: the 600 step is the PHONE form only. */
-  }
-  .status-strip-action-chevron {
-    display: inline-block;
-    width: 13px;
-    height: 13px;
-    color: var(--warn);
-    flex-shrink: 0;
-  }
 }

 /* retry fix 17 (LANES.md OD3, and fix 11's technique): .status-severity-chip
@@ -10287,13 +10334,19 @@
   background: var(--paper);
 }

-/* 04 §4.9: the bar is always draft amber, ranking VOLUME, not severity —
-   never colour these bars per row severity. */
+/* 04 §4.9: the module ranking's bar is always draft amber, ranking VOLUME,
+   not severity. The cause ranking under the Needs-you filter (Paper EKL-0,
+   "WHAT IS WAITING") is the one exception: each bar takes its cause's hue,
+   because there the rows ARE kinds of work and the hue is what tells them
+   apart. */
 .issues-rail-bar-fill {
   height: 5px;
   background: var(--status-draft);
 }

+.issues-rail-bar-fill[data-tone="alarm"] { background: var(--warn); }
+.issues-rail-bar-fill[data-tone="draft"] { background: var(--status-draft); }
+
 /* 04 §8 item 8: the rail is a ranking, so it truncates rather than scrolls. */
 .issues-rail-more {
   margin: 0;
@@ -11001,9 +11054,7 @@
   font: 13px/16px var(--font-sans);
 }
 .reading-canvas .claim-footer .comment-chip .dx-icon { width: 14px; height: 14px; }
-@media screen and (min-width: 861px) {
-  .reading-canvas .status-strip-action-chevron { display: none; }
-}
+@media screen and (min-width: 861px) {}
 @media screen and (max-width: 520px) {
   /* `display: flow-root` gives the canvas a block-formatting context without
      the side effects of restoring a border or `overflow: clip`. Without one,
@@ -11048,15 +11099,6 @@
      carries a 20px margin-bottom standing in for both and .reading-canvas has
      no border or padding at this tier, so the banner's own 14px collapses
      into that 20px and the reader gets 20px. State the no-tab-row case. */
-  .reading-canvas .status-strip-head { padding: 12px 14px; }
-  /* 56U-0: the triangle hangs on the FIRST text line (align-items: start,
-     10px gap), not centred against a three-line sentence. */
-  .reading-canvas .status-strip-summary { gap: 10px; align-items: flex-start; }
-  /* 56Y-0: Inter 13/19 — a full step down from the desktop 14/18. */
-  .reading-canvas .status-strip-title { font-size: 13px; line-height: 19px; }
-  /* 56Z-0: the divided action row bleeds to the head's own 14px inline and
-     12px bottom padding edges; Paper draws no extra gap above the rule. */
-  .reading-canvas .status-strip-action-row { margin: 12px -14px -12px; }
   .reading-canvas .claim-footer {
     margin-top: 6px;
     padding-top: 13px;
@@ -11097,6 +11139,534 @@
   .reading-canvas .claim-footer-chip--blocked .claim-footer-chip-count { color: var(--warn); }
 }

+/* ---- The banner card, and unapproved edits -------------------------------
+
+   ONE CARD, A ROW PER FINDING (Paper node EH1-0).
+
+   The banner used to be a single sentence with a "Show issues" action beside
+   it. That shape holds exactly one statement, and a facet routinely has two
+   kinds of thing waiting: claims blocked by unapproved dependencies, and
+   claims rewritten since they were approved. The second fact had to go into a
+   second tinted band stacked underneath, and two tinted bands touching read as
+   one broken component rather than as two facts.
+
+   TINT IS A SEVERITY CLAIM, so the card makes at most one. While every row
+   shares a hue the whole surface takes it; the moment two rows disagree the
+   surface goes neutral and the dots carry the colour instead. That is why
+   --alarm and --draft are separate modifiers and the neutral form is the base
+   rule: which one applies is computed from the rows, never from whichever kind
+   of finding happens to sort first. */
+
+/* The card is the PHONE form. Everything below describes the card itself; the
+   one thing that describes WHEN it applies is `display`, which is none here
+   and flex in the 520px block at the end of this file. Keeping the switch to
+   a single property in a single place is what stops a later rule in this
+   block from quietly turning the card back on at every width — which is
+   exactly what a `display: flex` sitting among the card's own layout rules
+   did on the first attempt. */
+.status-strip-card {
+  display: none;
+  flex-direction: column;
+  border: 1px solid var(--border-strong);
+  border-radius: 10px;
+  overflow: clip;
+  background: var(--card-bg);
+}
+
+.status-strip-card-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+  padding: 9px 14px;
+  background: var(--code-bg);
+  border-bottom: 1px solid var(--border);
+}
+
+.status-strip-card-eyebrow {
+  font: 600 11px/14px var(--font-sans);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+  color: var(--muted);
+}
+
+/* Mono, like every other machine-produced count in this viewer: the number is
+   read off the findings, not written by anyone. */
+.status-strip-card-count {
+  font: 11px/14px var(--font-mono, var(--font-sans));
+  color: var(--faint);
+}
+
+/* A button, not a row with a click handler. Each row is one way into the
+   Issues screen and has to be reachable by keyboard and announced as the
+   control it is. */
+.status-strip-finding {
+  display: flex;
+  align-items: flex-start;
+  gap: 10px;
+  width: 100%;
+  padding: 11px 14px;
+  border: 0;
+  background: none;
+  text-align: left;
+  cursor: pointer;
+  color: var(--ink);
+}
+
+.status-strip-finding + .status-strip-finding {
+  border-top: 1px solid var(--border);
+}
+
+.status-strip-finding-dot {
+  width: 7px;
+  height: 7px;
+  flex-shrink: 0;
+  margin-top: 6px;
+  border-radius: 999px;
+  background: var(--warn);
+}
+
+.status-strip-finding[data-tone="draft"] .status-strip-finding-dot {
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-finding-text {
+  flex: 1;
+  min-width: 0;
+  font: 13px/19px var(--font-sans);
+  color: var(--ink);
+}
+
+/* EH1-0: a 12px "\203a" glyph, in the card's hue (the tinted forms below
+   recolour it), optically dropped 2px to sit on the sentence's first line. */
+.status-strip-finding-chevron {
+  flex-shrink: 0;
+  margin-top: 2px;
+  color: var(--faint);
+  font: 400 12px/16px var(--font-sans);
+}
+
+/* The two tinted forms. The alphas are EH1-0's own (#9A6A1612 / #9A6A1645 /
+   #9A6A163D over the draft token), written as color-mix so the dark scheme
+   redefines them from its own hue instead of carrying a second literal. */
+.status-strip-card--alarm {
+  background: color-mix(in srgb, var(--warn) 7%, transparent);
+  border-color: color-mix(in srgb, var(--warn) 27%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-eyebrow,
+.status-strip-card--alarm .status-strip-card-count,
+.status-strip-card--alarm .status-strip-finding-chevron {
+  color: var(--warn);
+}
+
+.status-strip-card--alarm .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--draft {
+  background: color-mix(in srgb, var(--status-draft, #976600) 7%, transparent);
+  border-color: color-mix(in srgb, var(--status-draft, #976600) 27%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-eyebrow,
+.status-strip-card--draft .status-strip-card-count,
+.status-strip-card--draft .status-strip-finding-chevron {
+  color: var(--status-draft, #976600);
+}
+
+.status-strip-card--draft .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+/* The per-claim edit surface (Paper DON-0 / C9H-0).
+
+   It is a BAR ABOVE THE BODY with a two-segment switch, not a disclosure, and
+   the difference is which state is the default. A disclosure says the diff is
+   an aside a reader opens if they want it; the board says the opposite — a
+   claim that was approved and then rewritten opens SHOWING what moved,
+   because that is the thing about it a reader has to know before they read a
+   word of it. "Current" is the escape hatch, not the resting state. */
+/* While the changes are showing, the claim's own body is hidden by this class
+   on the CARD. See paintApprovedEdit for why the state does not live on the
+   body: the long-body disclosure re-wraps it after first paint, so the body
+   is not the same node before and after. */
+.claim--edit-view > .claim-body,
+.claim--edit-view > .claim-body-disclosure:not(.claim-body-disclosure--edit),
+.claim--edit-view > .claim-empty,
+.claim--edit-view > .claim-mockup-body {
+  display: none;
+}
+
+/* THE BAR WRAPS BY CONTENT, NOT BY VIEWPORT.
+   Its width is the reading column's, not the window's — about 690px at 1440
+   — so a media query cannot see the moment it runs out of room. The Current
+   state's line ("1 passage differs from the approval of 20 Sep") is the long
+   one, and at that column it overran by a few pixels and broke mid-date:
+   "...the approval of 20 / Sep".
+   Wrapping instead of shrinking is deliberate. The three ways to make it fit
+   were truncating the sentence, stepping the type down, or letting the row
+   become two rows; the first hides a fact, the second makes a 12px mono line
+   smaller than anything else on the card, and the third costs 8px of height
+   in the rare case. The switch is what moves, because it is the only part
+   with no sentence in it. */
+/* Paper CKF-0: the bar is the draft tint (--color-draft-bg), radius 6,
+   9px/13px, and it sits in the card's own 16px rhythm — no margin of its
+   own. Title and meta share a baseline with 10px between them. */
+.claim-edit-bar {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 8px 10px;
+  margin: 0;
+  padding: 9px 13px;
+  border-radius: 6px;
+  background: var(--status-draft-bg, rgba(154, 106, 22, .11));
+  font-family: var(--font-sans);
+}
+
+.claim-edit-bar-title {
+  flex: 0 0 auto;
+  font: 600 13px/18px var(--font-sans);
+  color: var(--status-draft, #976600);
+}
+
+/* Mono, like every other machine-read fact in this viewer: the date, the
+   actor and the passage count are read off the ledger, not written by
+   anyone. */
+/* The meta's own width is its CONTENT's, which is what makes the switch the
+   first thing to wrap: flex lays the row out from the items' base sizes, so
+   title + this sentence + switch overflowing moves the switch — the only item
+   with no sentence in it — down to its own row, and the sentence gets the
+   whole first row instead of being squeezed into what was left over.
+   It stays shrinkable on purpose. Refusing to shrink made it overrun the bar
+   entirely at 900px, where even a full row is not enough; wrapping inside
+   itself is a worse line but a contained one, and it is the last resort
+   rather than the first thing that happens. */
+.claim-edit-bar-meta {
+  flex: 1 1 auto;
+  min-width: 0;
+  font: 11px/16px var(--font-mono, var(--font-sans));
+  color: var(--muted);
+}
+
+/* The switch. The selected segment is a filled dark pill, which is the one
+   place in this panel that spends a strong value — everything else is a tint,
+   and the reader has to be able to see which of two states they are in from
+   across the page. */
+/* Paper CKJ-0: a card-ground pill with a 1px hairline, 2px inset, segments
+   3px/11px at 11/14. */
+.claim-edit-switch {
+  display: inline-flex;
+  align-items: center;
+  flex: 0 0 auto;
+  /* Pinned right on the first row, and still right when it wraps to its own. */
+  margin-left: auto;
+  gap: 0;
+  padding: 2px;
+  border: 1px solid var(--border);
+  border-radius: 999px;
+  background: var(--card-bg);
+}
+
+.claim-edit-switch-seg {
+  border: 0;
+  border-radius: 999px;
+  padding: 3px 11px;
+  background: none;
+  cursor: pointer;
+  font: 500 11px/14px var(--font-sans);
+  color: var(--muted);
+}
+
+.claim-edit-switch-seg[aria-pressed="true"] {
+  background: var(--ink);
+  color: var(--card-bg);
+  font-weight: 600;
+}
+
+/* The passages. Typography is the claim body's own — this IS the claim body,
+   shown a passage at a time — so a reader moving between Changes and Current
+   is reading the same prose in the same face, not a second document. */
+.claim-edit-diff,
+.claim-edit-current {
+  font-family: var(--font-serif);
+  font-size: 17px;
+  line-height: 28px;
+  max-width: 760px;
+}
+
+/* Passages keep the body's 16px rhythm between them (the card's own gap).
+   Margins rather than flex gap, because the four-line clamp turns this box
+   into a -webkit-box and a flex gap would vanish with it. */
+.claim-edit-passage {
+  margin: 0;
+}
+
+.claim-edit-passage + .claim-edit-passage {
+  margin-top: 16px;
+}
+
+.claim-edit-passage > :first-child {
+  margin-top: 0;
+}
+
+.claim-edit-passage > :last-child {
+  margin-bottom: 0;
+}
+
+/* Paper CK7-0 / CKP-0: a changed passage is a tinted row, radius 4, with a
+   3px rule INSIDE it (12px in from the edge, 12px before the text) that runs
+   the height of the text, old above new with 2px between them and 4px above
+   the pair. The prose itself takes the hue: blocked for the approved
+   passage, locked for the current one. Colour is not the only carrier — the
+   removed passage is struck through and the added one is not, so the two
+   stay apart in a monochrome print, under a colour-vision deficiency, and
+   in the dark scheme where the two tints are close in luminance. */
+.claim-edit-passage--removed,
+.claim-edit-passage--added,
+.claim-edit-passage--current {
+  position: relative;
+  border-radius: 4px;
+}
+
+.claim-edit-passage--removed,
+.claim-edit-passage--added {
+  padding: 7px 12px 7px 27px;
+}
+
+.claim-edit-passage--removed::before,
+.claim-edit-passage--added::before,
+.claim-edit-passage--current::before {
+  content: "";
+  position: absolute;
+  left: 12px;
+  top: 7px;
+  bottom: 7px;
+  width: 3px;
+  border-radius: 2px;
+  background: currentColor;
+}
+
+.claim-edit-passage--removed {
+  color: var(--warn);
+  background: var(--warn-bg, rgba(158, 59, 54, .10));
+  text-decoration: line-through 1px;
+  text-underline-position: from-font;
+}
+
+.claim-edit-passage--added {
+  color: var(--accent);
+  background: var(--accent-bg, rgba(44, 107, 82, .10));
+}
+
+.claim-edit-passage + .claim-edit-passage--removed {
+  margin-top: 20px;
+}
+
+.claim-edit-passage--removed + .claim-edit-passage--added {
+  margin-top: 2px;
+}
+
+/* The Current state (Paper DPZ-0): the passage that differs carries only a
+   draft-coloured rule at the left edge — no tint, prose in ink — so the
+   claim reads as ordinary prose with the seam marked. */
+.claim-edit-passage--current {
+  padding: 0 0 0 15px;
+}
+
+.claim-edit-passage--current::before {
+  left: 0;
+  top: 0;
+  bottom: 0;
+  background: var(--status-draft, #976600);
+}
+
+/* The words that moved, inside the passage they moved in. They stay in the
+   DOM for the passage's aria-label and for a reader who selects the text,
+   but Paper draws the passage as one tinted, coloured, struck (or not)
+   block and paints nothing on the individual words — the struck passage and
+   the coloured one are the comparison. */
+.claim-edit-word--removed,
+.claim-edit-word--added {
+  background: none;
+  color: inherit;
+  padding: 0;
+}
+
+.claim-edit-note {
+  margin: 8px 0 0;
+}
+
+.claim-edit-note-text,
+.claim-edit-note-hint,
+.claim-edit-field-note {
+  margin: 0;
+  font: 400 13px/19px var(--font-sans);
+  color: var(--muted);
+}
+
+/* The hint names the command that ENDS this state. A record written before
+   the approved wording was kept says "there is nothing to compare against",
+   and until this line existed that was the end of the sentence — true, and
+   with no way out of it. The wording is usually still in the project's own
+   git history, so the panel that reports the gap also names the verb that
+   closes it. */
+.claim-edit-note-hint {
+  margin-top: 4px;
+  color: var(--faint);
+}
+
+/* The fields that moved. This block is TEXT, not prose: it shows the YAML the
+   claim file holds, so it is monospace and pre-wrapped, and the tints are the
+   same two the passage diff uses so a reader does not learn a second colour
+   language halfway down one panel. */
+.claim-edit-fields {
+  margin: 8px 0 0;
+}
+
+.claim-edit-fields-toggle {
+  appearance: none;
+  -webkit-appearance: none;
+  background: none;
+  border: 0;
+  padding: 0;
+  cursor: pointer;
+  font: 500 13px/19px var(--font-sans);
+  color: var(--accent);
+  text-align: left;
+}
+
+.claim-edit-fields-toggle:hover { text-decoration: underline; }
+
+.claim-edit-fields-list {
+  margin-top: 6px;
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+}
+
+.claim-edit-field-name {
+  display: block;
+  font: 600 12px/18px var(--font-mono);
+  color: var(--muted);
+}
+
+.claim-edit-field-hunk {
+  margin: 3px 0 0;
+  padding: 6px 8px;
+  border-radius: 6px;
+  border-left: 3px solid transparent;
+  background: var(--code-bg);
+  font: 400 12px/18px var(--font-mono);
+  /* pre-wrap and not pre: a claims file's embodiment block is indented deep
+     enough to overflow a narrow column, and a horizontal scrollbar inside a
+     panel inside a claim is three nested scroll regions. Wrapping keeps the
+     indentation and loses only the guarantee that a line is one line. */
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+}
+
+.claim-edit-field-hunk--removed {
+  border-left-color: var(--warn);
+  background: color-mix(in srgb, var(--warn) 7%, var(--code-bg));
+}
+
+.claim-edit-field-hunk--added {
+  border-left-color: var(--border-strong);
+}
+
+@media (max-width: 520px) {
+  /* The phone swaps the whole banner: the desktop band's sentence-and-action
+     row goes away and the card takes its place, so .status-strip stops being
+     a painted surface and becomes the gutter that positions the card.
+     .reading-canvas > .status-strip is named alongside it because that rule
+     is two-class and unqueried, and would otherwise outrank this one and
+     restore the band's border underneath the card. */
+  .status-strip-head {
+    display: none;
+  }
+
+  .status-strip-card {
+    display: flex;
+  }
+
+  .reading-canvas > .status-strip,
+  .status-strip,
+  .status-strip--lint,
+  .status-strip--integrity {
+    background: none;
+    border: 0;
+    border-radius: 0;
+    margin: 0;
+    /* The gutter is the reading card's own 16px, not the desktop 32px —
+       EH1-0's mobile frame draws the card at mx-4 inside a 390px canvas. */
+    padding: 14px 16px 0;
+  }
+
+  /* The edit bar stacks into three rows (Paper CHK-0): the sentence, the
+     machine line under it, and the switch full width with two equal halves.
+     Side by side at this width the sentence wrapped to two lines, the mono
+     meta broke mid-word, and the switch was squeezed against them — three
+     things competing for a 343px row that only ever held one comfortably. */
+  .claim-edit-bar {
+    flex-direction: column;
+    align-items: stretch;
+    gap: 8px;
+    padding: 10px 12px;
+  }
+
+  .claim-edit-bar-title {
+    /* CKY-0: the title and its machine line are 2px apart on the phone. */
+    margin-bottom: -6px;
+  }
+
+  .claim-edit-bar-meta {
+    /* The phone stack gives every part its own row already. */
+    flex: none;
+    line-height: 15px;
+  }
+
+  .claim-edit-switch {
+    display: flex;
+    margin-left: 0;
+  }
+
+  .claim-edit-switch-seg {
+    flex: 1;
+    padding: 5px 10px;
+    font-size: 12px;
+    line-height: 16px;
+    text-align: center;
+  }
+
+  /* CKP-0: the rule and the text sit 10px in on a phone, not 12. */
+  .claim-edit-passage--removed,
+  .claim-edit-passage--added {
+    padding: 7px 10px 7px 23px;
+  }
+
+  .claim-edit-passage--removed::before,
+  .claim-edit-passage--added::before {
+    left: 10px;
+  }
+
+  .claim-edit-passage + .claim-edit-passage--removed {
+    margin-top: 18px;
+  }
+}
+
+
 @media print {
   :root { color-scheme: light; }

@@ -11115,10 +11685,6 @@
     display: block;
   }

-  .claim-links-summary {
-    cursor: auto;
-  }
-
   /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
@@ -11347,7 +11913,7 @@
     <div class="issues-body">
       <div id="issuesFindingsCard" class="issues-findings-card"></div>
       <aside id="issuesRailPanel" class="issues-rail-panel">
-        <p class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
+        <p id="issuesRailHeading" class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
         <p id="issuesRailCaveat" class="issues-rail-caveat"></p>
         <div id="issuesRailRows" class="issues-rail-rows"></div>
       </aside>
@@ -11374,9 +11940,10 @@


   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
-    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
+    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody" data-tone="alarm">
       <span id="statusStripSummary" class="status-strip-summary">
         <svg class="dx-icon status-strip-icon" aria-hidden="true"><use href="#dx-icon-triangle-alert"/></svg>
+        <span class="status-strip-dot" aria-hidden="true"></span>
         <span class="status-strip-summary-text">
           <strong id="statusStripTitle" class="status-strip-title"></strong>
         </span>
@@ -11386,6 +11953,13 @@
         <svg class="dx-icon status-strip-action-chevron" aria-hidden="true"><use href="#dx-icon-chevron-right"/></svg>
       </span>
     </button>
+    <div id="statusStripCard" class="status-strip-card">
+      <div class="status-strip-card-head">
+        <span id="statusStripCardEyebrow" class="status-strip-card-eyebrow">Needs you here</span>
+        <span id="statusStripCardCount" class="status-strip-card-count"></span>
+      </div>
+      <div id="statusStripFindings" class="status-strip-card-findings"></div>
+    </div>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>

@@ -11434,8 +12008,6 @@
       // each SSE re-render. It attaches NO listeners — those are delegated once
       // on surviving nodes below, so re-running this never double-binds.
       function initViewer() {
-        // Fragment swaps replace <main>; drop the stale mount pointer.
-        mountedSurfaceID = null;
         // Two-level show/hide: one .module-section per sidebar .sec-tab, and
         // (when a module has more than one facet) one .claim-group per .subtab
         // inside that module's .sub-nav. A single-facet module still has exactly
@@ -11562,7 +12134,6 @@
       // surface on first paint; smaller fixtures mount every surface so print,
       // theme probes, and live-reload witnesses that call getElementById keep
       // working without visiting every facet first.
-      var mountedSurfaceID = null;
       var SOFT_MOUNT_MIN_CLAIMS = 80;

       function claimCorpusSize() {
@@ -11590,7 +12161,6 @@
         var host = group.querySelector(':scope > [data-dossierx-surface-host]');
         var tmpl = group.querySelector(':scope > template.dossierx-surface-template');
         if (!host || !tmpl) {
-          mountedSurfaceID = surfaceID;
           return;
         }
         if (!host.querySelector('.claim')) {
@@ -11598,13 +12168,31 @@
           if (typeof window.dossierxEnhanceSystemRecord === 'function') {
             window.dossierxEnhanceSystemRecord();
           }
+          // The edited-since-approval chips and panels are attached HERE, at
+          // mount, and BEFORE the clamps below — not left to the next
+          // renderStatusStrip pass.
+          //
+          // They insert a node above each edited claim's body, so attaching
+          // them later moves everything under them. Two things read that
+          // height and cannot be allowed to read it early: a deep link has
+          // already scrolled to its claim, so the panels appearing afterwards
+          // slid the page out from under the sticky header; and the
+          // source-note clamps measure scrollHeight against clientHeight, so
+          // a mid-measurement insertion decides "show more" from a layout
+          // that is about to change. Decorating first means the surface is at
+          // its final height before anything measures or scrolls it.
+          renderApprovedEdits();
           // Source-note clamps observe live nodes only; re-arm after every
           // first-time mount so a freshly revealed surface is measured.
           if (typeof mountSourceNoteClamps === 'function') {
             mountSourceNoteClamps();
           }
         }
-        mountedSurfaceID = surfaceID;
+        // The no-template path (an eagerly rendered surface) never enters the
+        // block above, and still has claims to decorate. renderApprovedEdits
+        // is idempotent — every node it creates is either replaced wholesale
+        // or guarded — so the second call costs nothing on the path that did.
+        renderApprovedEdits();
       }

       function mountAllSurfaces() {
@@ -11866,10 +12454,8 @@
         if (!toastEl) { return; }
         toastEl.textContent = msg; // textContent — error text is never trusted markup
         toastEl.hidden = false;
-        toastEl.classList.add('comments-toast--show');
         if (toastTimer) { window.clearTimeout(toastTimer); }
         toastTimer = window.setTimeout(function () {
-          toastEl.classList.remove('comments-toast--show');
           toastEl.hidden = true;
         }, 4000);
       }
@@ -12710,21 +13296,30 @@
       // claim has no edges".

       var stripEl = document.getElementById('statusStrip');
-      var stripToggle = document.getElementById('statusStripToggle');
-      var stripSummary = document.getElementById('statusStripSummary');
-      var stripTitle = document.getElementById('statusStripTitle');
       // retry fix 21 (this lane's third attempt): #statusStripNote (the
       // "Critical 0 · Needs you 3 · ..." tally) is REMOVED per the
       // coordinator's ruling — no 02/03/04 board draws it, the chips
       // already carry every count it repeated — and dropped from shell.html
       // along with its element lookup here.
-      var stripAction = document.getElementById('statusStripAction');
+      //
+      // THE HEAD HAS TWO FORMS, ONE PER WIDTH, and both are written on every
+      // paint. #statusStripToggle is the desktop band — one summary sentence
+      // with "Show issues" beside it — and #statusStripCard is the phone form
+      // (Paper EH1-0), a card with one ROW PER FINDING. CSS shows one; see
+      // shell.html for why the choice is a media query and not a width read
+      // in here.
+      //
+      // Both are filled unconditionally rather than behind a matchMedia
+      // check. The cost is a few DOM nodes nobody sees; the alternative is a
+      // resize listener and a breakpoint duplicated out of the stylesheet,
+      // which is one more thing to keep in step for no gain.
+      var stripToggle = document.getElementById('statusStripToggle');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripCard = document.getElementById('statusStripCard');
+      var stripCardCount = document.getElementById('statusStripCardCount');
+      var stripFindings = document.getElementById('statusStripFindings');
       var stripBody = document.getElementById('statusStripBody');
       var lastStatusData = null;
-      // Paper starts every status strip collapsed. stripExpanded then stays
-      // sticky across refreshes, so a poll (or an SSE tick) never takes the
-      // disclosure state away from the reader.
-      var stripExpanded = false;

       function countLabel(n, word) {
         return n + ' ' + word + (n === 1 ? '' : 's');
@@ -12931,12 +13526,51 @@
       // demoted slug, and the fixed-width count slot — rather than three
       // loose wrapping siblings. See style.css's .status-finding-dot /
       // .status-finding-text for the layout this DOM shape enables.
+      // editChangeSummary is the mono line under an Issues row for a claim
+      // rewritten since approval (Paper BR5-0 / EUD-0): what moved, and when
+      // the approval it moved from was given.
+      function editChangeSummary(change) {
+        var parts = [];
+        if (!change.content_retained) {
+          parts.push('approved wording not retained');
+        } else if (change.changed_passages) {
+          parts.push(countLabel(change.changed_passages, 'passage') + ' changed');
+        } else if ((change.field_changes || []).length) {
+          parts.push(countLabel(change.field_changes.length, 'field') + ' moved');
+        }
+        var approved = editDateLabel(change.approved_at);
+        if (approved) { parts.push('approved ' + approved); }
+        return parts.join(' · ');
+      }
+
+      // goToClaimChanges leaves the Issues screen for the claim itself, with
+      // the Changes state showing — the row said "See changes", so landing
+      // on the claim with the diff put away would not be what it offered.
+      function goToClaimChanges(id, change) {
+        closeIssuesView();
+        if (window.location.hash !== '#' + id) { window.location.hash = '#' + id; }
+        var card = document.getElementById(id);
+        if (card && change) { paintApprovedEdit(card, change, true); }
+        if (card && typeof card.scrollIntoView === 'function') { card.scrollIntoView({ block: 'start' }); }
+      }
+
       function renderStatusGroup(group) {
         var row = el('li', 'status-finding status-finding--group');
         row.setAttribute('data-severity', group.severity);
+        // A row whose cause is an edit since approval is draft work, not an
+        // alarm (Paper EUD-0 draws its dot and action in the draft hue).
+        var edits = offlineApprovedEdits();
+        var editedOwner = group.ownerModuleClaimID && edits[group.ownerModuleClaimID] ? group.ownerModuleClaimID : '';
+        var isEditCause = group.severity === 'needs_you' && group.kind === 'unapproved_edit' && !!editedOwner;
+        row.setAttribute('data-tone', isEditCause ? 'draft' : 'alarm');
         row.appendChild(el('span', 'status-finding-dot'));
         var text = el('span', 'status-finding-text');
-        text.appendChild(textEl('span', 'status-finding-rule', group.title || 'Issue'));
+        // Paper EUD-0 names the claim and what happened to it in one line:
+        // "<Title> was approved and is being rewritten".
+        var title = isEditCause
+          ? readinessClaimLabel(editedOwner) + ' was approved and is being rewritten'
+          : (group.title || 'Issue');
+        text.appendChild(textEl('span', 'status-finding-rule', title));
         var ids = Object.keys(group.claimIDs).sort();
         // Paper 1KX-0: every row carries a second, demoted line under its
         // title holding the dotted claim id of the claim the reader must go
@@ -12954,11 +13588,52 @@
         if (ownerID) {
           text.appendChild(textEl('span', 'status-finding-claim', ownerID));
         }
+
+        // The third line (Paper board 15, F and I). A Needs-you row names its
+        // cause KIND in mono, because the sentence alone cannot distinguish
+        // "someone never wrote this" from "someone approved it and then
+        // moved it". A blocker row whose owner was approved and is being
+        // rewritten says so under the row it already had (BR5-0): no new
+        // group and no new severity, one line added to the existing row.
+        var detail = null;
+        if (group.severity === 'needs_you') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind', group.kind || 'review'));
+          if (isEditCause) {
+            detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          }
+        } else if (editedOwner && group.severity === 'blocker') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind status-finding-kind--edited', 'Edited · was approved'));
+          detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          var link = textEl('button', 'status-finding-link', 'See changes');
+          link.type = 'button';
+          link.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(editedOwner, edits[editedOwner]);
+          });
+          detail.appendChild(link);
+        }
+        if (detail) { text.appendChild(detail); }
         row.appendChild(text);
-        // retry fix 7: 04 §4.8 "Count label" / §6 "Row count" promotes the
-        // phrase INTO the count slot as a bare `N claims`, never `blocks N
-        // claim(s)` — that longer phrase was written for a full-width third
-        // line, which the fixed 104px slot (style.css) no longer is.
+
+        // The right-hand slot. A blocker row keeps its count (04 §4.8: the
+        // phrase promoted INTO the 104px slot as a bare `N claims`). A
+        // Needs-you row carries its way in instead (Paper ETC-0 / EUD-0):
+        // the thing waiting is one claim, and the count would always be 1.
+        if (group.severity === 'needs_you' && group.ownerModuleClaimID) {
+          var owner = group.ownerModuleClaimID;
+          var action = textEl('button', 'status-finding-action', isEditCause ? 'See changes' : 'Open claim');
+          action.type = 'button';
+          action.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(owner, isEditCause ? edits[owner] : null);
+          });
+          row.appendChild(action);
+          return row;
+        }
         var n = ids.length || group.count;
         row.appendChild(textEl('span', 'status-finding-msg', n + ' claim' + (n === 1 ? '' : 's')));
         return row;
@@ -13057,7 +13732,7 @@
         var label = card && card.querySelector('.k .label');
         if (label) {
           var copy = label.cloneNode(true);
-          copy.querySelectorAll('.pill, .k-id, .claim-collapse-chevron').forEach(function (node) { node.remove(); });
+          copy.querySelectorAll('.pill, .k-id').forEach(function (node) { node.remove(); });
           var rendered = (copy.textContent || '').replace(/\s+/g, ' ').trim();
           if (rendered) { return rendered; }
         }
@@ -13084,7 +13759,12 @@
           approval_content_drift: 'This claim changed after approval',
           approval_missing: 'This claim has no approval record',
           approval_released: 'This claim\'s approval was released',
-          approval_unknown: 'This claim\'s approval state is unknown'
+          approval_unknown: 'This claim\'s approval state is unknown',
+          // Deliberately not "changed after approval" (approval_content_drift's
+          // wording): that names a LOCKED claim whose bytes no longer match a
+          // STANDING approval, which is the tamper finding. This names the
+          // honest, intended act — unlock, rewrite, re-lock — caught mid-way.
+          unapproved_edit: 'This claim was approved, then rewritten'
         };
         return labels[record.kind] || String(record.kind || 'Readiness obstacle').replace(/_/g, ' ');
       }
@@ -13592,12 +14272,397 @@
         } catch (_) { return {}; }
       }

+      // ---- Unapproved edits -------------------------------------------------
+      //
+      // The state these three functions draw is the ordinary one, and it was
+      // the one the viewer could not show. A claim is locked; someone unlocks
+      // it, rewrites it, and has not locked it again yet. The new wording
+      // replaced the old in the file, so the reading view showed text with
+      // nothing to compare it against and a chip reading DRAFT — the same chip
+      // a claim nobody ever approved carries. "Never written" and "approved,
+      // then moved" are different jobs for a reviewer, and one word was being
+      // used for both.
+      //
+      // The engine now answers both questions: readiness emits an
+      // `unapproved_edit` cause (so the claim reaches the Issues screen on its
+      // own, without needing a dependent to notice for it), and the graph
+      // payload carries the line diff from the approved body to the current
+      // one. Everything below is presentation of those two facts. It computes
+      // nothing about whether an edit is allowed, and never writes.
+
+      // offlineApprovedEdits reads the per-claim approvaledit.Change records
+      // off the SAME rendered graph payload offlineReadiness reads. There is
+      // deliberately no /api/status path for this: a checked-in file:// export
+      // has no endpoint to ask, and a panel that appeared only under
+      // "dossierx serve" would be missing exactly where a reviewer reads a
+      // repository they did not build.
+      function offlineApprovedEdits() {
+        var node = document.getElementById('dossierx-graph');
+        if (!node) { return {}; }
+        try {
+          var payload = JSON.parse(node.textContent || '{}');
+          var out = {};
+          (payload.nodes || []).forEach(function (graphNode) {
+            if (graphNode.id && graphNode.approved_edit) { out[graphNode.id] = graphNode.approved_edit; }
+          });
+          return out;
+        } catch (_) { return {}; }
+      }
+
+      // editDateLabel turns an RFC3339 instant into the board's own short
+      // form ("12 Sep"). It is deliberately not a full date: the panel's job
+      // is to place the approval in the reader's recent memory, and a
+      // four-part date would be the widest thing on a row that is otherwise
+      // a sentence.
+      function editDateLabel(iso) {
+        if (!iso) { return ''; }
+        var d = new Date(iso);
+        if (isNaN(d.getTime())) { return String(iso).slice(0, 10); }
+        return d.getDate() + ' ' + ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun',
+          'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][d.getMonth()];
+      }
+
+      // approvedEditHasDiff reports whether there is a second wording to show
+      // at all.
+      //
+      // Two states look like an edit and have nothing to put behind a
+      // "Changes" tab. A record written before the approved wording was kept
+      // proves the text moved and does not carry what it moved from. A claim
+      // whose hash moved because its rests_on or build_role moved has an
+      // approved wording identical to its current one. In both, the panel has
+      // one sentence to say and no second version of the claim — so there is
+      // no switch, and the claim's own body stays on screen.
+      //
+      // Offering the switch anyway is what this replaces, and it was worse
+      // than a useless control: choosing "Changes" hid the body and put the
+      // sentence in its place, so the claim rendered with no content at all.
+      function approvedEditHasDiff(change) {
+        return !!(change.content_retained &&
+          (change.hunks || []).length &&
+          (change.changed_passages || 0) > 0);
+      }
+
+      // approvedEditDiff builds the passage-by-passage view: unchanged
+      // passages as ordinary prose, and each changed one as the approved
+      // passage above the current one, tinted and ruled.
+      //
+      // The HTML is produced by the ENGINE (internal/approvaledit), not here,
+      // because internal/render/markdown is this project's only markdown
+      // renderer and the viewer has none. Shipping the source and parsing it
+      // in the browser would mean growing a second renderer that could
+      // disagree with the first about the same claim, on the same page.
+      function approvedEditDiff(change) {
+        var body = el('div', 'claim-edit-diff');
+        (change.hunks || []).forEach(function (hunk) {
+          var cls = hunk.op === 'remove' ? 'claim-edit-passage claim-edit-passage--removed'
+            : hunk.op === 'add' ? 'claim-edit-passage claim-edit-passage--added'
+            : 'claim-edit-passage';
+          var block = el('div', cls);
+          // innerHTML, and only here. The value came from
+          // internal/render/markdown, which is the escaping boundary every
+          // other claim body in this viewer crosses — the same function, over
+          // the same author bytes, reached through the same payload as the
+          // readiness projection beside it. It is exactly as trusted as the
+          // body it sits above, and no more.
+          block.innerHTML = hunk.html || '';
+          if ((hunk.changed || []).length) {
+            block.setAttribute('aria-label',
+              (hunk.op === 'remove' ? 'Removed since approval: ' : 'Added since approval: ') +
+              hunk.changed.join(', '));
+          }
+          body.appendChild(block);
+        });
+        var fields = approvedEditFields(change);
+        if (fields) { body.appendChild(fields); }
+        return body;
+      }
+
+      // approvedEditCurrent builds the OTHER state of the same surface: the
+      // current wording, passage by passage, with the passage that differs
+      // from the approval carrying a draft-coloured rule and no tint (Paper
+      // DPZ-0: "the same claim with the diff put away, reading as ordinary
+      // prose, with an amber rule marking where the difference sits").
+      //
+      // It is rendered from the same hunks as the diff rather than by
+      // un-hiding the claim's own body, because the body cannot say WHERE the
+      // difference sits: the rule is the one thing this state adds, and the
+      // body has no passage boundaries to hang it on.
+      function approvedEditCurrent(change) {
+        var body = el('div', 'claim-edit-current');
+        (change.hunks || []).forEach(function (hunk) {
+          if (hunk.op === 'remove') { return; }
+          var block = el('div', hunk.op === 'add'
+            ? 'claim-edit-passage claim-edit-passage--current'
+            : 'claim-edit-passage');
+          // Same escaping boundary as approvedEditDiff: engine-rendered
+          // markdown, the same bytes the diff shows.
+          block.innerHTML = hunk.html || '';
+          if (hunk.op === 'add') {
+            block.setAttribute('aria-label', 'This passage differs from the approved wording');
+          }
+          body.appendChild(block);
+        });
+        return body;
+      }
+
+      // approvedEditFields draws the non-body fields that moved, each as its
+      // approved YAML above its current YAML with the words that moved marked.
+      //
+      // It is here because "Also changed: embodiment, audit_notes." was not an
+      // answer. For a claim whose prose never moved that sentence WAS the whole
+      // panel, and it named the change without showing it — the reader was told
+      // a field they cannot see had moved in a way they cannot see, and had to
+      // go and read the file to find out what they were being asked about. Six
+      // of the fourteen claims this feature was built for are in exactly that
+      // state.
+      //
+      // The blocks are drawn as text and not as prose. A field is YAML, so it
+      // is escaped by the engine (internal/approvaledit/fields.go) and shown in
+      // the monospace the file itself would show; running it through the
+      // markdown renderer would turn a list into a list and a `*` into
+      // emphasis, and stop it being the thing on disk.
+      function approvedEditFields(change) {
+        var changes = change.field_changes || [];
+        if (!changes.length) { return null; }
+        var wrap = el('div', 'claim-edit-fields');
+
+        var toggle = el('button', 'claim-edit-fields-toggle');
+        toggle.type = 'button';
+        var list = el('div', 'claim-edit-fields-list');
+        var open = true;
+        function paint() {
+          toggle.setAttribute('aria-expanded', String(open));
+          toggle.textContent = (open ? 'Hide' : 'Show') + ' the ' +
+            (changes.length === 1 ? 'field' : changes.length + ' fields') + ' that moved';
+          list.hidden = !open;
+        }
+        toggle.addEventListener('click', function (event) {
+          event.preventDefault();
+          open = !open;
+          paint();
+        });
+
+        changes.forEach(function (field) {
+          var row = el('div', 'claim-edit-field');
+          row.appendChild(textEl('span', 'claim-edit-field-name', field.field));
+          if (field.truncated) {
+            row.appendChild(textEl('p', 'claim-edit-field-note',
+              'This field moved. It is too large to show here; read it in the claim file.'));
+            list.appendChild(row);
+            return;
+          }
+          (field.hunks || []).forEach(function (hunk) {
+            var cls = hunk.op === 'remove' ? 'claim-edit-field-hunk claim-edit-field-hunk--removed'
+              : hunk.op === 'add' ? 'claim-edit-field-hunk claim-edit-field-hunk--added'
+              : 'claim-edit-field-hunk';
+            var block = el('pre', cls);
+            // innerHTML over engine-escaped text. internal/approvaledit's
+            // field renderer HTML-escapes the YAML before it marks anything,
+            // so the only tags that can be in here are the mark spans it
+            // wrote itself — a field holding <script> arrives as text.
+            block.innerHTML = hunk.html || '';
+            if ((hunk.changed || []).length) {
+              block.setAttribute('aria-label',
+                (hunk.op === 'remove' ? 'Removed from ' : 'Added to ') + field.field + ': ' +
+                hunk.changed.join(', '));
+            }
+            row.appendChild(block);
+          });
+          list.appendChild(row);
+        });
+
+        paint();
+        wrap.appendChild(toggle);
+        wrap.appendChild(list);
+        return wrap;
+      }
+
+      // approvedEditNote is what a claim with no second wording says instead of
+      // offering a switch. It sits under the bar, beside the claim's own body
+      // rather than in place of it.
+      function approvedEditNote(change) {
+        var wrap = el('div', 'claim-edit-note');
+        if (!change.content_retained) {
+          wrap.appendChild(textEl('p', 'claim-edit-note-text',
+            'This claim was approved before the approved wording was kept on the record, so there is nothing to compare against. The approval is released and the text has since changed.'));
+          wrap.appendChild(textEl('p', 'claim-edit-note-hint',
+            'Run dossierx claim recover-approved-content to look for the approved revision in this project\u2019s git history.'));
+          return wrap;
+        }
+        var fields = change.other_fields || [];
+        wrap.appendChild(textEl('p', 'claim-edit-note-text', fields.length
+          ? 'The wording is unchanged since approval. ' +
+            (fields.length === 1 ? '1 field moved.' : fields.length + ' fields moved.')
+          : 'The wording is unchanged since approval.'));
+        var diff = approvedEditFields(change);
+        if (diff) { wrap.appendChild(diff); }
+        return wrap;
+      }
+
+      // approvedEditBar is the control the board draws in place of the old
+      // disclosure: a tinted row above the body that says what the reader is
+      // looking at, and a two-segment switch between the two states.
+      //
+      // It is a BAR AND NOT A DISCLOSURE, and the difference is which state
+      // is the default. A disclosure says the diff is an aside a reader opens
+      // if they want it; the board says the opposite — a claim that was
+      // approved and then rewritten opens SHOWING what moved, because that is
+      // the thing about it a reader has to know before they read a word of
+      // it. "Current" is the escape hatch, not the resting state.
+      function approvedEditBar(card, change, showingChanges) {
+        var bar = el('div', 'claim-edit-bar');
+        var approved = editDateLabel(change.approved_at);
+        var hasDiff = approvedEditHasDiff(change);
+        bar.appendChild(textEl('span', 'claim-edit-bar-title',
+          !hasDiff || showingChanges ? 'Edited since it was approved' : 'Showing the current wording'));
+
+        var meta;
+        if (!hasDiff || showingChanges) {
+          meta = 'approved ' + approved + (change.approved_by ? ' \u00b7 ' + change.approved_by : '');
+        } else {
+          var passages = change.changed_passages || 0;
+          meta = passages === 1
+            ? '1 passage differs from the approval of ' + approved
+            : passages + ' passages differ from the approval of ' + approved;
+        }
+        bar.appendChild(textEl('span', 'claim-edit-bar-meta', meta));
+
+        // No second wording, no switch. A control whose two states show the
+        // same thing is not a control, and the one that used to be here did
+        // worse than nothing: picking "Changes" hid the claim's body to make
+        // room for a diff that did not exist.
+        if (!hasDiff) { return bar; }
+
+        var group = el('div', 'claim-edit-switch');
+        group.setAttribute('role', 'group');
+        group.setAttribute('aria-label', 'Which wording to show');
+        [['Changes', true], ['Current', false]].forEach(function (pair) {
+          var seg = el('button', 'claim-edit-switch-seg');
+          seg.type = 'button';
+          seg.textContent = pair[0];
+          seg.setAttribute('aria-pressed', String(showingChanges === pair[1]));
+          seg.addEventListener('click', function (event) {
+            event.preventDefault();
+            paintApprovedEdit(card, change, pair[1]);
+          });
+          group.appendChild(seg);
+        });
+        bar.appendChild(group);
+        return bar;
+      }
+
+      // paintApprovedEdit puts one claim card into one of the two states, and
+      // is the only thing that writes either of them, so the bar and the body
+      // can never disagree about which one is showing.
+      function paintApprovedEdit(card, change, showingChanges) {
+        var hasDiff = approvedEditHasDiff(change);
+        showingChanges = hasDiff && showingChanges;
+        var existingBar = card.querySelector(':scope > .claim-edit-bar');
+        // The diff may already be wrapped: the four-line disclosure wraps it
+        // exactly as it wraps a claim body (system-record.js's BODY_LIKE), so
+        // what has to be taken out is the wrapper when there is one.
+        var existingDiff = card.querySelector(
+          ':scope > .claim-edit-diff, :scope > .claim-edit-current, :scope > .claim-edit-note, :scope > .claim-body-disclosure--edit');
+
+        var bar = approvedEditBar(card, change, showingChanges);
+        if (existingBar) { existingBar.replaceWith(bar); }
+        else {
+          var head = card.querySelector(':scope > .k');
+          if (head && head.parentNode) { head.parentNode.insertBefore(bar, head.nextSibling); }
+          else { card.insertBefore(bar, card.firstChild); }
+        }
+        if (existingDiff) { existingDiff.remove(); }
+        if (!hasDiff) {
+          bar.parentNode.insertBefore(approvedEditNote(change), bar.nextSibling);
+        } else {
+          bar.parentNode.insertBefore(
+            showingChanges ? approvedEditDiff(change) : approvedEditCurrent(change), bar.nextSibling);
+          // Hand the new surface to the disclosure so it is measured and
+          // clamped like any other body. Without this the diff renders at
+          // full height while the claim beside it truncates at four lines,
+          // which is the same claim behaving as two components.
+          if (typeof window.dossierxEnhanceSystemRecord === 'function') {
+            window.dossierxEnhanceSystemRecord();
+          }
+        }
+        // While there is a second wording the claim's own body is off screen
+        // in BOTH states: Changes shows the diff and Current shows the same
+        // passages with the changed one ruled. The body comes back only for
+        // a claim with nothing to compare against (no switch).
+        //
+        // The body is hidden by a class ON THE CARD, and not by setting
+        // `hidden` on the body itself. Which element the body IS moves: the
+        // long-body disclosure wraps
+        // .claim-body inside .claim-body-disclosure after first paint, so a
+        // handle taken before the wrap and a handle taken after it are
+        // different nodes. Hiding whichever one a selector matched at the
+        // time left the first one hidden and the second one shown, and the
+        // switch stopped working on exactly the claims long enough to be
+        // wrapped. A class on the card is one state, in one place, whatever
+        // the body is wearing.
+        //
+        // It is also why the body is hidden rather than removed: it carries
+        // the source-note clamps and that disclosure, and tearing it out
+        // would take their state with it every time the reader flipped.
+        card.classList.toggle('claim--edit-view', hasDiff);
+      }
+
+      // renderApprovedEdits upgrades each edited claim's chip and puts its
+      // card into the Changes state. It is driven from mountSurface (so the
+      // surface reaches its final height before anything measures or scrolls
+      // it) and again from renderStatusStrip, so everything it creates is
+      // either guarded or left alone on a repaint.
+      function renderApprovedEdits() {
+        var edits = offlineApprovedEdits();
+        Object.keys(edits).forEach(function (id) {
+          var card = document.getElementById(id);
+          if (!card) { return; }
+          var change = edits[id];
+
+          // The chip. It stays .pill.pv — the claim IS a draft, and inventing
+          // a fourth colour for it would say the state is unrelated to the
+          // one the reader already knows. Only the word changes, because only
+          // the word was wrong: DRAFT is true and incomplete, and a reviewer
+          // deciding what to open next needs the part it leaves out.
+          var pill = card.querySelector('.k .label .pill');
+          if (pill && pill.getAttribute('data-dx-edited') !== '1') {
+            pill.setAttribute('data-dx-edited', '1');
+            pill.setAttribute('title', 'This claim held an approval, which was released when it was unlocked. What is written now differs from what was approved.');
+            // No padlock. Paper (CDJ-0, CIY-0) draws the chip as the word
+            // alone: the open padlock is DRAFT's glyph, and this chip exists
+            // to say the state is NOT plain draft.
+            pill.textContent = '';
+            pill.appendChild(textEl('span', '', 'Edited \u00b7 was approved'));
+          }
+
+          // The bar and the body state, painted only when the bar is not
+          // already there: a repaint must not take the reader's own "Current"
+          // choice away from them on the next poll.
+          if (!card.querySelector(':scope > .claim-edit-bar')) {
+            paintApprovedEdit(card, change, true);
+          }
+        });
+      }
+
+      // approvedEditIDsIn returns the claims in the id set that have been
+      // rewritten since the approval their unlock released.
+      //
+      // It is scoped to the facet on screen, by the SAME id set the rest of
+      // the strip scopes itself with — a count of claims on a page the reader
+      // is not looking at is a count they cannot act on. The set is a
+      // null-prototype object keyed by id (activeFacetClaimIDs), so this
+      // indexes it rather than calling Set or Array methods it does not have.
+      function approvedEditIDsIn(claimIDs) {
+        var ids = claimIDs || {};
+        return Object.keys(offlineApprovedEdits()).filter(function (id) { return ids[id]; });
+      }
+
       // renderStatusStrip paints one /api/status payload. An empty verdict hides
       // the strip entirely rather than showing a green badge: the viewer already
       // reads as "fine" by default, and a persistent all-clear chip would be one
       // more thing to stop noticing.
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary || !stripTitle) { return; }
+        if (!stripEl || !stripBody || !stripCard || !stripFindings || !stripTitle) { return; }
         lastStatusData = data || {};
         var claimIDs = activeFacetClaimIDs();
         var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
@@ -13620,6 +14685,7 @@
         // PREVIOUS pass's state, since this function only writes it below.
         var bannerShowing = groups.length > 0 && actionable;
         renderClaimReadiness(lastStatusData.readiness || offlineReadiness(), bannerShowing);
+        renderApprovedEdits();
         if (!groups.length || !actionable) {
           stripEl.hidden = true;
           stripEl.classList.remove('status-strip--integrity', 'status-strip--lint');
@@ -13713,12 +14779,18 @@
           recordEl.classList.add('status-group--approval-record');
           stripBody.appendChild(recordEl);
         }
+        // Under the Needs-you filter the group's weight phrase counts what is
+        // waiting on the reader, not what is blocked (Paper EMP-0: "3 of 4
+        // need you here").
+        var needsTotal = countSeverity(groups, 'needs_you');
         moduleOrder.forEach(function (moduleID) {
           var rows = byModule[moduleID].slice().sort(function (a, b) {
             return statusGroupClaimCount(b) - statusGroupClaimCount(a);
           });
           var weight = uniqueClaimCount(rows);
-          var note = 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
+          var note = stripSeverityFilter === 'needs_you'
+            ? weight + ' of ' + needsTotal + ' need you here'
+            : 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
           stripBody.appendChild(findingGroup(moduleLabel(moduleID), rows.map(renderStatusGroup), note));
         });

@@ -13728,38 +14800,189 @@
         // used to repeat as a second string. statusChipLine, the function
         // that built that string, is now dead (no other call site) and is
         // deleted.
-        var headline = blockerHeadline(groups);
-        var needsYou = countSeverity(groups, 'needs_you') > 0;
-        if (ledger.length) {
-          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
-        } else if (lintErrors.length && !headline && !needsYou) {
-          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
-        } else if (headline) {
-          stripTitle.textContent = headline;
-        } else {
-          stripTitle.textContent = countLabel(groups.length, 'grouped issue') + ' in this facet';
-        }
+        //
+        // Both forms of the head, every paint. The desktop band carries the
+        // single headline sentence it always did; the phone card carries one
+        // row per finding. They are derived from the SAME groups, ledger and
+        // lint lists a line apart, so the two can differ in shape and never
+        // in what they say.
+        var rows = statusStripRows(groups, ledger, lintErrors, claimIDs);
+        renderStatusStripCard(rows);
+        // The band gets the same rows, one full-bleed row each (Paper board
+        // 15, C2M-0 / C92-0), in the same order — most serious first — so the
+        // two forms differ in shape and never in what they say.
+        renderStatusStripBand(rows);

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        // Paper's default reading view always starts with the findings body
-        // collapsed. Critical and needs-you counts still determine the
-        // truthful summary/chips above; they do not take control of the
-        // reader's disclosure state. Once the reader opens the strip, the
-        // existing stripExpanded state remains sticky across later polls.
-        setStripExpanded(stripExpanded);
+        // #statusStripBody is populated above and stays hidden: the Issues
+        // screen MOVES its children into itself (issuesSyncFromStrip), so it
+        // must exist and must not be shown here.
+        if (stripBody) { stripBody.hidden = true; }
         positionStatusStrip();
         stripEl.hidden = false;
       }

-      function setStripExpanded(expanded) {
-        stripExpanded = !!expanded;
-        if (stripBody) { stripBody.hidden = !stripExpanded; }
-        if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
-        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
-        if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
+      // statusStripRows turns the facet's findings into the card's rows —
+      // one row per KIND of thing waiting, never one per claim.
+      //
+      // The rows are the summary sentences the strip already computed, each
+      // now carrying its own hue instead of four of them competing to be the
+      // one headline. `tone` is the row's colour family, and it is the only
+      // thing the card's tint rule reads: 'alarm' for anything the approval
+      // record or the dependency chain is refusing, 'draft' for work in
+      // progress that nobody has approved yet.
+      function statusStripRows(groups, ledger, lintErrors, claimIDs) {
+        var rows = [];
+        if (ledger.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(ledger.length, 'approval record issue') + ' in this facet need' +
+              (ledger.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        var headline = blockerHeadline(groups);
+        if (headline) {
+          rows.push({ tone: 'alarm', severity: 'blocker', text: headline });
+        }
+        if (lintErrors.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(lintErrors.length, 'issue') + ' in this facet need' +
+              (lintErrors.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        // Claims rewritten since approval. This is the row the old
+        // single-sentence head had no space for, and the reason it became a
+        // card: it is amber where everything above it is red, and one tinted
+        // surface cannot make two severity claims at once.
+        var edited = approvedEditIDsIn(claimIDs);
+        if (edited.length) {
+          rows.push({
+            tone: 'draft', severity: 'needs_you',
+            text: countLabel(edited.length, 'claim') + ' ' + (edited.length === 1 ? 'has' : 'have') +
+              ' edits that have not been approved'
+          });
+        }
+        if (!rows.length) {
+          rows.push({
+            tone: 'alarm', severity: '',
+            text: countLabel(groups.length, 'grouped issue') + ' in this facet'
+          });
+        }
+        return rows;
       }

+      // renderStatusStripCard paints the head. Tinted while every row shares
+      // a tone, neutral the moment they disagree — Paper EH1-0's own rule,
+      // and the reason it is a rule: a tinted surface IS a severity claim,
+      // so a card holding a red fact and an amber one has to stop making it
+      // and let the dots carry the colour instead.
+      // statusStripRowAction names the way in each row offers. A blocked row
+      // opens the Issues screen as it always did; an edited-since-approval
+      // row says what it is for (Paper CDS-0: "Review changes").
+      function statusStripRowAction(row) {
+        return row.tone === 'draft' ? 'Review changes' : 'Show issues';
+      }
+
+      // openIssuesForBandRow: a blocked row opens the screen UNFILTERED — its
+      // sentence names one finding but the band stands for the whole facet.
+      // An edited row names exactly what it filters to (the claims waiting on
+      // the reader), which is what earns it the filter, same as a card row.
+      function openIssuesForBandRow(tone, severity) {
+        stripSeverityFilter = tone === 'draft' ? (severity || '') : '';
+        renderStatusStrip(lastStatusData);
+        openIssuesView();
+      }
+
+      // renderStatusStripBand paints the DESKTOP form: the static first row
+      // (#statusStripToggle, so the ids the rest of the viewer and its tests
+      // address stay put) takes the first finding, and one more
+      // .status-strip-head button is appended per further finding. Each row
+      // carries data-tone, which is the only thing the stylesheet reads to
+      // pick its tint, its icon and its action colour.
+      function renderStatusStripBand(rows) {
+        if (!stripToggle || !stripTitle || !stripEl) { return; }
+        var first = rows[0];
+        stripToggle.setAttribute('data-tone', first.tone);
+        stripToggle.setAttribute('data-severity', first.severity || '');
+        stripTitle.textContent = first.text;
+        var firstAction = document.getElementById('statusStripAction');
+        if (firstAction) { firstAction.textContent = statusStripRowAction(first); }
+
+        stripEl.querySelectorAll(':scope > .status-strip-head--row').forEach(function (node) { node.remove(); });
+        rows.slice(1).forEach(function (row) {
+          var btn = el('button', 'status-strip-head status-strip-head--row');
+          btn.type = 'button';
+          btn.setAttribute('data-tone', row.tone);
+          btn.setAttribute('data-severity', row.severity || '');
+          var summary = el('span', 'status-strip-summary');
+          var icon = dxIcon('triangle-alert');
+          icon.classList.add('status-strip-icon');
+          summary.appendChild(icon);
+          summary.appendChild(el('span', 'status-strip-dot'));
+          var text = el('span', 'status-strip-summary-text');
+          text.appendChild(textEl('strong', 'status-strip-title', row.text));
+          summary.appendChild(text);
+          btn.appendChild(summary);
+          var actionRow = el('span', 'status-strip-action-row');
+          actionRow.appendChild(textEl('span', 'status-strip-action', statusStripRowAction(row)));
+          btn.appendChild(actionRow);
+          btn.addEventListener('click', function (event) {
+            event.preventDefault();
+            openIssuesForBandRow(row.tone, row.severity);
+          });
+          // After the previous row and before the phone card, so the band is
+          // one stack of rows whatever the count.
+          var before = stripCard || stripBody;
+          if (before && before.parentNode === stripEl) { stripEl.insertBefore(btn, before); }
+          else { stripEl.appendChild(btn); }
+        });
+      }
+
+      function renderStatusStripCard(rows) {
+        if (!stripCard || !stripFindings) { return; }
+        var tones = {};
+        rows.forEach(function (row) { tones[row.tone] = true; });
+        var toneKeys = Object.keys(tones);
+        var tinted = toneKeys.length === 1 ? toneKeys[0] : '';
+        stripCard.classList.toggle('status-strip-card--alarm', tinted === 'alarm');
+        stripCard.classList.toggle('status-strip-card--draft', tinted === 'draft');
+        if (stripCardCount) { stripCardCount.textContent = String(rows.length); }
+
+        stripFindings.textContent = '';
+        rows.forEach(function (row) {
+          // A button, not a div with a handler: each row is a way into the
+          // Issues screen and has to be reachable by keyboard and named to a
+          // screen reader like the one control it is.
+          var el_ = el('button', 'status-strip-finding');
+          el_.type = 'button';
+          el_.setAttribute('data-tone', row.tone);
+          var dot = el('span', 'status-strip-finding-dot');
+          dot.setAttribute('aria-hidden', 'true');
+          el_.appendChild(dot);
+          el_.appendChild(textEl('span', 'status-strip-finding-text', row.text));
+          // A text glyph, not the 14px icon: EH1-0 draws the row's way-in as
+          // a 12px "›" in the card's own hue.
+          var chev = textEl('span', 'status-strip-finding-chevron', '›');
+          chev.setAttribute('aria-hidden', 'true');
+          el_.appendChild(chev);
+          el_.addEventListener('click', function (event) {
+            event.preventDefault();
+            // Filtered to the row's own severity. On a real corpus the
+            // unfiltered screen is hundreds of dependency rows and the thing
+            // this row named is somewhere inside them; a way in that lands a
+            // reader in a list they then have to search has not answered the
+            // question it asked. Same stripSeverityFilter the chips set, so
+            // the chip is pressed on arrival and one more click clears it.
+            stripSeverityFilter = row.severity || '';
+            renderStatusStrip(lastStatusData);
+            openIssuesView();
+          });
+          stripFindings.appendChild(el_);
+        });
+      }
+
       // refreshStatus polls the endpoint. A FAILED poll deliberately leaves the
       // last verdict on screen untouched: a transient fetch failure (the server
       // restarting mid-reload) must never be able to clear an integrity warning,
@@ -13773,25 +14996,20 @@
       }

       // R09.6 (Paper ZP-0): "Issues is its own screen, not an expansion. The
-      // banner is the only way in." Paper backs that with node evidence:
-      // 56T-0, 574-0 and 6P-0 draw ONE state — the sentence plus "Show
-      // issues" — the desktop banner is a single flex ROW with no slot for a
-      // body, the string "Hide issues" does not exist anywhere in the design
-      // file, and 04 section 4 records that no board in the group draws an
-      // expanded state. The inline expand listener that lived here is gone;
-      // the toggle's only job is opening the Issues screen.
+      // banner is the only way in." No board in the group draws an expanded
+      // banner, and the string "Hide issues" does not exist anywhere in the
+      // design file, so the strip has no inline disclosure at all.
       //
-      // setStripExpanded is deliberately KEPT: renderStatusStrip still calls
-      // it every poll with a permanently-false stripExpanded, and that is
-      // what holds #statusStripBody hidden, aria-expanded="false" and the
-      // action reading "Show issues".
+      // EH1-0 then replaced the one sentence with one row per finding, and
+      // the entry point went with it: each row opens the Issues screen,
+      // filtered to that row's own severity. "The banner is the only way in"
+      // still holds — the banner simply has more than one door now, because
+      // a facet routinely has more than one kind of thing waiting.
       //
       // #statusStripBody is never emptied, removed or left unpopulated:
       // renderStatusStrip still builds into it, issuesSyncFromStrip still
       // MOVES its children into the Issues screen, and closeIssuesView still
-      // moves them back. Only its visibility to the reader is withdrawn — the
-      // children now return into a hidden body instead of an expanded one,
-      // which is the defect this removes.
+      // moves them back. It is simply never shown in place.

       // ================================================================
       // Issues view (04-issues-screen.md) — lane L8a
@@ -13800,9 +15018,8 @@
       // The screen's own chrome around lane L8's status-strip vocabulary:
       // breadcrumb, title/subtitle, scope and sort controls, the findings
       // card and the "WHERE THE BLOCKERS LIVE" rail (04 §3, §4.1-4.3, §4.5,
-      // §4.6, §4.9). R09.6: reached ONLY from "Show issues" — the listener
-      // just below runs AFTER the one directly above (source order), so by
-      // the time it fires `stripExpanded` already holds L8's own new value.
+      // §4.6, §4.9). R09.6: reached ONLY from a row of the banner card, each
+      // of which wires openIssuesView itself as it is built.
       //
       // #issuesFilters and #issuesFindingsCard never build their own copy of
       // the severity chips or the grouped findings list: issuesSyncFromStrip
@@ -13907,11 +15124,59 @@
         var m = issuesModuleWeights(issuesScope).claims;
         issuesFindingsCard.querySelectorAll('.status-group-head-note').forEach(function (note) {
           var match = /^blocks (\d+) of \d+ claims? here$/.exec(note.textContent || '');
-          if (!match) { return; }
-          note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+          if (match) {
+            note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+            return;
+          }
+          // The Needs-you phrase: the same denominator rule, over the rows
+          // the filter is showing (issuesGroupsForScope already applies it).
+          var waiting = /^(\d+) of \d+ need you here$/.exec(note.textContent || '');
+          if (waiting) { note.textContent = waiting[1] + ' of ' + m + ' need you here'; }
         });
       }

+      // issuesCauseWeights is the rail's OTHER ranking (Paper EKL-0, "WHAT IS
+      // WAITING"): under the Needs-you filter the question is what kind of
+      // work is waiting rather than where the blockers live, so the rail
+      // counts causes, by kind, and colours each bar by the kind's hue. A
+      // claim waiting for two reasons is counted under each, and the caveat
+      // says so.
+      var ISSUES_CAUSE_LABELS = {
+        unapproved_edit: 'Unapproved edits',
+        direct_dependency_change: 'Dependency changed',
+        upstream_dependency_review: 'Upstream review',
+        own_flag: 'Flagged for review',
+        own_thread: 'Open review thread',
+        approval_content_drift: 'Approved wording drifted',
+        approval_released: 'Approval released',
+        approval_missing: 'Approval record missing',
+        approval_unknown: 'Approval state unknown'
+      };
+      function issuesCauseWeights(scope) {
+        var groups = issuesGroupsForScope(scope);
+        var byKind = {};
+        var order = [];
+        groups.forEach(function (g) {
+          var kind = g.kind || 'review';
+          if (!Object.prototype.hasOwnProperty.call(byKind, kind)) { byKind[kind] = []; order.push(kind); }
+          byKind[kind].push(g);
+        });
+        var rows = order.map(function (kind) {
+          return {
+            id: kind,
+            label: ISSUES_CAUSE_LABELS[kind] || kind.replace(/_/g, ' '),
+            tone: kind === 'unapproved_edit' ? 'draft' : 'alarm',
+            weight: uniqueClaimCount(byKind[kind])
+          };
+        });
+        rows.sort(function (a, b) { return b.weight - a.weight; });
+        return {
+          rows: rows,
+          claims: uniqueClaimCount(groups),
+          causes: rows.reduce(function (sum, r) { return sum + r.weight; }, 0)
+        };
+      }
+
       // renderIssuesRail (04 §4.9): a ranking with bars, proportional to the
       // facet's blocked-claim TOTAL (§4.9's own measured widths — retry fix
       // 4, coordinator ruling: "rail bars use the spec's rounding basis" —
@@ -13931,10 +15196,15 @@
       // corpus size or scope.
       function renderIssuesRail() {
         if (!issuesRailRowsEl) { return; }
-        var data = issuesModuleWeights(issuesScope);
+        var waiting = stripSeverityFilter === 'needs_you';
+        var data = waiting ? issuesCauseWeights(issuesScope) : issuesModuleWeights(issuesScope);
+        var heading = document.getElementById('issuesRailHeading');
+        if (heading) { heading.textContent = waiting ? 'WHAT IS WAITING' : 'WHERE THE BLOCKERS LIVE'; }
         issuesRailRowsEl.textContent = '';
         var shown = data.rows.slice(0, ISSUES_RAIL_MAX_ROWS);
-        var total = data.claims;
+        // Module bars are proportional to the blocked-claim total; cause bars
+        // to the cause total (EKL-0: 2 of 4 causes draws round(50%)).
+        var total = waiting ? data.causes : data.claims;
         shown.forEach(function (row) {
           var wrap = el('div', 'issues-rail-row');
           var labelRow = el('div', 'issues-rail-row-label');
@@ -13943,6 +15213,7 @@
           wrap.appendChild(labelRow);
           var track = el('div', 'issues-rail-bar-track');
           var fill = el('div', 'issues-rail-bar-fill');
+          if (row.tone) { fill.setAttribute('data-tone', row.tone); }
           fill.style.width = (total ? Math.round((row.weight / total) * 100) : 0) + '%';
           track.appendChild(fill);
           wrap.appendChild(track);
@@ -13952,10 +15223,14 @@
           issuesRailRowsEl.appendChild(textEl('p', 'issues-rail-more',
             (data.rows.length - ISSUES_RAIL_MAX_ROWS) + ' more not shown'));
         }
-        var caveat = data.claims > 0
-          ? (countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
-             ' — a claim blocked through two modules counts under both.')
-          : '';
+        var caveat = '';
+        if (waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ', ' + countLabel(data.causes, 'cause') +
+            ' — a claim can be waiting for more than one reason and is counted under each.';
+        } else if (!waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
+            ' — a claim blocked through two modules counts under both.';
+        }
         if (issuesRailCaveatEl) { issuesRailCaveatEl.textContent = caveat; }
         if (issuesCaveatEl) { issuesCaveatEl.textContent = caveat; }
         // §8 item 5: an empty ranking is a ranking of nothing — the panel is
@@ -13981,7 +15256,13 @@
             (lastStatusData && lastStatusData.readiness) || offlineReadiness(),
             activeFacetClaimIDs(), [], [], [], []
           );
-          issuesSubtitleEl.textContent = blockerHeadline(groups) || 'Nothing in this facet is blocked.';
+          // Under the Needs-you filter the sentence is about the reader's
+          // work, not the facet's blockers (Paper ENQ-0).
+          var waiting = stripSeverityFilter === 'needs_you' ? countSeverity(groups, 'needs_you') : 0;
+          issuesSubtitleEl.textContent = waiting
+            ? countLabel(waiting, 'claim') + (waiting === 1 ? ' is' : ' are') +
+              ' waiting on you in this facet. Each one names what moved and what it was measured against.'
+            : (blockerHeadline(groups) || 'Nothing in this facet is blocked.');
         }
       }

@@ -14029,6 +15310,10 @@
         }
         issuesApplyScopeDenominator();
         renderIssuesRail();
+        // The subtitle follows the filter (a chip click repaints the strip,
+        // which lands here through the observer), so it is re-read on every
+        // sync and not only when the screen opens.
+        issuesSyncHeader();
         if (issuesStripObserver) { issuesStripObserver.observe(stripBody, { childList: true }); }
       }

@@ -14141,14 +15426,20 @@
       if (issuesSortControl) {
         issuesSortControl.addEventListener('click', function (event) { event.preventDefault(); });
       }
-      // "Show issues" (R09.6) opens this view — UNCONDITIONALLY on every
-      // click of this control, whichever label it currently reads. The
-      // strip's own inline expand/collapse (the listener directly above,
-      // L8's, toggling stripExpanded/#statusStripBody) changes the sticky
-      // disclosure state first. R09.6 names this same control as the Issues
-      // entry point, so this listener does not gate on that resulting state.
+      // R09.6's "the banner is the only way in" holds for both forms. The
+      // desktop band has one door, bound here; the phone card has one per
+      // finding, each wired as it is built (see renderStatusStripCard)
+      // because each carries its own severity filter.
+      //
+      // The band's door opens the screen UNFILTERED, and that is not an
+      // oversight: its sentence names one finding but the band stands for the
+      // whole facet, so narrowing to that one row's severity would hide the
+      // others behind a filter the reader did not choose. A card row names
+      // exactly what it filters to, which is what earns it the filter.
       if (stripToggle) {
-        stripToggle.addEventListener('click', openIssuesView);
+        stripToggle.addEventListener('click', function () {
+          openIssuesForBandRow(stripToggle.getAttribute('data-tone'), stripToggle.getAttribute('data-severity'));
+        });
       }
       // Any ordinary navigation — a sidebar tab, a subtab, a citation link —
       // leaves this screen the same way the back chevron does. Capture phase
@@ -15019,8 +16310,23 @@
     window.scrollTo({ top: Math.max(0, Math.round(window.scrollY + top - offset)), behavior: 'instant' });
   }

+  // BODY_LIKE is what the four-line disclosure clamps: a claim's own body, and
+  // the passage-by-passage diff that stands in for it while the reader is
+  // looking at what changed since approval.
+  //
+  // The diff is in this set because it IS the body, shown a passage at a time
+  // — so a claim that clamps to four lines with "... more" in one state and
+  // runs to full height in the other is the same claim behaving as two
+  // different components. Sharing the mechanism rather than copying it also
+  // means the toggle's label, its aria wiring and its scroll compensation
+  // cannot drift between the two.
+  // .claim-edit-current is the other state of the same surface: the current
+  // wording rendered from the same passages, with the changed one ruled. It
+  // stands in for the body exactly as the diff does, so it clamps like it.
+  var BODY_LIKE = '.claim-body, .claim-edit-diff, .claim-edit-current';
+
   function setClaimBodyExpanded(wrapper, expanded) {
-    var body = wrapper && wrapper.querySelector(':scope > .claim-body');
+    var body = wrapper && wrapper.querySelector(':scope > .claim-body, :scope > .claim-edit-diff, :scope > .claim-edit-current');
     var toggle = wrapper && wrapper.querySelector(':scope > .claim-body-disclosure__toggle');
     if (!body || !toggle || toggle.hidden) { return; }
     wrapper.classList.toggle('claim-body-disclosure--expanded', expanded);
@@ -15033,13 +16339,19 @@
   }

   function syncClaimBodyDisclosures() {
-    document.querySelectorAll('.claim-body').forEach(function (body) {
+    document.querySelectorAll(BODY_LIKE).forEach(function (body) {
       var wrapper = body.parentElement && body.parentElement.classList.contains('claim-body-disclosure')
         ? body.parentElement
         : null;
       if (!wrapper) {
         wrapper = document.createElement('div');
         wrapper.className = 'claim-body-disclosure';
+        // The diff's wrapper is marked, because the card hides the claim's
+        // own body while the changes are showing and must not hide the diff
+        // along with it — both are a .claim-body-disclosure by then.
+        if (body.classList.contains('claim-edit-diff') || body.classList.contains('claim-edit-current')) {
+          wrapper.classList.add('claim-body-disclosure--edit');
+        }
         body.parentNode.insertBefore(wrapper, body);
         wrapper.appendChild(body);
         if (!body.id) { body.id = 'claim-body-' + (++claimBodyDisclosureSequence); }
@@ -15095,7 +16407,6 @@

   function enhanceConformanceFooters() {
     document.querySelectorAll('details.claim-conformance').forEach(function (original) {
-      if (original.dataset.footerEnhanced === 'true') { return; }
       var claim = original.closest('.claim');
       var footer = claim && claim.querySelector('.claim-footer');
       var summary = original.querySelector(':scope > summary');
@@ -15200,13 +16511,6 @@
   }

   function renderToc() {
-    // Theme-parity's hover probe forces :hover through CDP node ids. This
-    // function replaceChildren()s the TOC list on every enhance pass (the
-    // layout MutationObserver retriggers enhance when addModuleHeaders
-    // rewrites the module head), so a node id dies between QuerySelector
-    // and ForcePseudoState. The harness sets this flag for the duration of
-    // that probe; it is otherwise unset.
-    if (window.__dxParityFreezeToc) { return; }
     var toc = document.getElementById('systemFacetToc');
     if (!toc) {
       toc = document.createElement('aside');
@@ -15900,19 +17204,6 @@
     return { from: from, to: to, type: type };
   }

-  // normalizeEdges maps a whole edge list, dropping unusable entries.
-  function normalizeEdges(edges) {
-    var list = asArray(edges);
-    var out = [];
-    for (var i = 0; i < list.length; i++) {
-      var e = normalizeEdge(list[i]);
-      if (e) {
-        out.push(e);
-      }
-    }
-    return out;
-  }
-
   // idSet builds a lookup Set from an id array. Internal only.
   function idSet(ids) {
     var s = new Set();
@@ -17653,7 +18944,6 @@
   var parseTimeHash = ''; // the graph state segment seen at parse time
   var originClaimId = ''; // OPEN_ID_ATTR off the trigger that opened the pane, '' from the nav trigger
   var collapseOverride = false; // the reader insisted on every claim
-  var notices = []; // {text, kind, action} rendered above the canvas
   var dragging = null; // {id} while a node is being dragged
   var panning = null; // {x, y} while the background is being dragged
   var movedWhileDown = false;

testdata/fixture-conformance-v1/build/viewer/index.html  (+1555 -265)
    size            22311 lines / 1230631 bytes -> 23601 lines / 1288309 bytes
@@ -5365,6 +5365,15 @@
    form; only the bottom hairline separates it from the body beneath. The
    520px inset-card form below is a DIFFERENT, deliberately distinct board
    (R-H.1) and keeps its own margin/border/radius, untouched by this. */
+/* THIS IS THE DESKTOP FORM and is unchanged: a full-bleed band carrying one
+   summary sentence with "Show issues" beside it. At 1440 a row has the space
+   to carry a sentence and an action without either crowding the other.
+
+   The PHONE form is a different component — a card with one row per finding
+   (Paper EH1-0) — and lives under "The banner card" near the end of this
+   file. Both are in the DOM; the 520px block picks one. Everything from here
+   to .status-strip-action-chevron therefore applies to the desktop form only,
+   and the mobile block switches all of it off rather than re-tuning it. */
 .status-strip {
   width: 100%;
   background: var(--card-bg);
@@ -5377,25 +5386,26 @@
   display: none;
 }

+/* The strip itself is card ground. Its TINT lives on each row (below,
+   keyed by data-tone), because a band with two findings on it carries two
+   hues and a surface cannot make two severity claims at once (Paper board
+   15, C2M-0 red over C92-0 amber, hairline-divided). */
 .status-strip--lint {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
 }

 .status-strip--integrity {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
   box-shadow: none;
 }

-/* retry fix 20: 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop
-   banner at 13px/32px, not 13px/14px — 32px is the reading card's own
-   gutter (--card-gutter, G8), reproduced here as a literal because
-   .status-strip mounts as a sibling of the record header, never as a
-   descendant of .card/.claim-tree, so the custom property does not cascade
-   to it. The 520px inset-card form re-pins this back down to 14px in this
-   lane's own appended mobile block, matching the value this rule carried
-   (and was verified against) before this fix. */
+/* 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop banner at
+   13px/32px — 32px is the reading card's own gutter (--card-gutter, G8),
+   reproduced here as a literal because .status-strip mounts as a sibling of
+   the record header, never as a descendant of .card/.claim-tree, so the
+   custom property does not cascade to it. */
 .status-strip-head {
   display: flex;
   align-items: center;
@@ -5409,11 +5419,9 @@
   cursor: pointer;
 }

-/* retry fix 14: 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning
-   triangle ahead of the banner sentence at every width, in both schemes — it
-   was absent everywhere. .status-strip-summary becomes the icon's row; the
-   title/note stack (unchanged) moves into its own -text wrapper so the two
-   IDs the runtime writes to keep working untouched. */
+/* 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning triangle
+   ahead of the banner sentence. .status-strip-summary is the icon's row; the
+   title stack sits in its own -text wrapper. */
 .status-strip-summary {
   display: flex;
   align-items: flex-start;
@@ -5430,20 +5438,70 @@
   flex-shrink: 0;
 }

+/* One row per finding, each its own tint (Paper C2M-0: blocked ground
+   #9E3B360F; CDR-0: edited ground #9A6A1612), divided by the hairline. The
+   icon slot is 15px either way: the triangle for an alarm row, a 7px dot in
+   the draft hue for an edited row, so the sentences of stacked rows share
+   one left edge. */
+.status-strip-head[data-tone="alarm"] { background: var(--status-strip-bg); }
+.status-strip-head[data-tone="draft"] { background: var(--status-draft-bg, rgba(154, 106, 22, .11)); }
+.status-strip-head + .status-strip-head { border-top: 1px solid var(--border); }
+
+.status-strip-dot {
+  display: none;
+  width: 15px;
+  height: 15px;
+  flex-shrink: 0;
+  align-items: center;
+  justify-content: center;
+}
+
+.status-strip-dot::before {
+  content: "";
+  width: 7px;
+  height: 7px;
+  border-radius: 999px;
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-head[data-tone="draft"] .status-strip-icon { display: none; }
+.status-strip-head[data-tone="draft"] .status-strip-dot { display: inline-flex; }
+.status-strip-head[data-tone="draft"] .status-strip-action { color: var(--status-draft, #976600); }
+
 .status-strip-summary-text {
   display: grid;
   gap: 3px;
   min-width: 0;
 }

-/* retry fix 21: 02 §4.10 "Body" / 03 §4.5 "Body text" measure the banner
-   sentence at Inter 14/18 weight 400, not 13px weight 680 — the sentence
-   reads as body prose, not an emphasized heading. */
+/* 02 §4.10 "Body" / 03 §4.5 "Body text": the banner sentence is Inter 14/18
+   weight 400 — it reads as body prose, not as an emphasized heading. */
 .status-strip-title {
   font: 400 14px/18px var(--font-sans);
   color: var(--ink);
 }

+/* 02 §4.10/§4.19, 03 §4.5/§7.5, R-H.1: "Show issues" is the ALARM half of the
+   banner (the body sentence stays --color-ink) and reads in the same red as
+   the icon, never the generic --accent link colour. */
+.status-strip-action {
+  color: var(--warn);
+  font: 500 13px/16px var(--font-sans);
+  letter-spacing: 0;
+  text-transform: none;
+  white-space: nowrap;
+}
+
+.status-strip-action-row {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+}
+
+/* § 4.10 draws no trailing chevron on the desktop banner. */
+.status-strip-action-chevron {
+  display: none;
+}
 /* .status-strip-note (the "Critical 0 · Needs you 3 · ..." tally line) is
    REMOVED per the coordinator's ruling on this retry: no 02/03/04 board
    draws it, and the severity chips already carry every count it repeated.
@@ -5458,30 +5516,12 @@
    500, 13/16 — R-H.1's weight 600 at 13/16 is the PHONE step (added in
    L8's own 520 block below), not the desktop default. --warn is correct at
    both widths and is unchanged. */
-.status-strip-action {
-  color: var(--warn);
-  font: 500 13px/16px var(--font-sans);
-  letter-spacing: 0;
-  text-transform: none;
-  white-space: nowrap;
-}
-
 /* retry fix 15: the action text (#statusStripAction, whose textContent the
    runtime overwrites on every toggle) and its trailing chevron are separate
    elements inside this row wrapper — the row, not the text span, is what
    the 520 block turns into R-H.1's divided action row, so a re-render can
    never wipe the chevron out from under it. The chevron itself is hidden
    above 520 (§ 4.10 draws no trailing chevron on the desktop banner). */
-.status-strip-action-row {
-  display: inline-flex;
-  align-items: center;
-  gap: 6px;
-}
-
-.status-strip-action-chevron {
-  display: none;
-}
-
 /* The detail list is capped and scrolls internally: a project with forty lint
    errors must not grow a dock that swallows the page it is describing. */
 /* retry fix 16: the promoted list is 04 §3/§9 item 4's bottom-open card —
@@ -5744,6 +5784,81 @@
   font-weight: 600;
 }

+/* Paper board 15 (BR5-0 / ETC-0 / EUD-0): a row's third line — the cause
+   kind in mono, a mono summary of what moved, and for a blocker row whose
+   owner was approved and is being rewritten, the way to its diff. */
+.status-finding-detail {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 9px;
+  padding-top: 1px;
+}
+
+.status-finding-kind {
+  padding: 2px 7px;
+  border-radius: 999px;
+  font: 500 10px/12px var(--font-mono);
+  background: var(--warn-bg);
+  color: var(--warn);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-kind,
+.status-finding-kind--edited {
+  background: var(--status-draft-bg);
+  color: var(--status-draft);
+}
+
+/* BR5-0: the chip under a blocker row is the claim chip's word, at the
+   row's scale — Inter 10/14 600, 0.06em, uppercase. */
+.status-finding-kind--edited {
+  font: 600 10px/14px var(--font-sans);
+  letter-spacing: .06em;
+  text-transform: uppercase;
+}
+
+.status-finding-meta {
+  font: 400 11px/14px var(--font-mono);
+  color: var(--muted);
+}
+
+.status-finding-link {
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  font: 600 11px/14px var(--font-sans);
+  color: var(--link);
+}
+
+.status-finding-link:hover { text-decoration: underline; }
+
+/* EUD-0: a Needs-you row's right-hand slot is its way in, in the row's hue,
+   where a blocker row keeps its count. */
+.status-finding-action {
+  flex: 0 0 auto;
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  text-align: right;
+  font: 600 14px/18px var(--font-sans);
+  color: var(--warn);
+}
+
+.status-finding-action:hover { text-decoration: underline; }
+
+/* A row caused by an edit since approval is draft work, not an alarm: its
+   dot and its action take the draft hue (EUD-0). */
+.status-finding--group[data-tone="draft"] .status-finding-dot,
+.status-finding--group[data-severity="needs_you"][data-tone="draft"] .status-finding-dot {
+  background: var(--status-draft);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-action {
+  color: var(--status-draft);
+}
+
 /* file:// has no /api/status and therefore no ledger_findings at all (04 §8
    item 11 / §9 item 5) — stated in the body rather than silently omitted. */
 .status-strip-static-note {
@@ -5870,7 +5985,7 @@
   .comment-reply-trigger,
   .comment-composer-submit,
   .comment-composer-input,
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
   }
@@ -5934,12 +6049,15 @@
     border-left: 0;
     /* R-J.2 / OD14a.7 / M4, verifier RETRY item 4(iii): a dark-only 1px top
        hairline (the light sheet relies on its 16px corners and the scrim
-       alone). `light-dark()` expresses "present only under dark" without a
-       second selector-scoped dark query or a new dark-only token. A
+       alone). --sheet-hairline is the engine-owned token declared for
+       exactly this edge (:root and the two dark blocks): `transparent` in
+       light, var(--border) in dark, on both the OS path and the explicit
+       Dark toggle. It used to be a `light-dark()` literal while the token
+       sat unread; the token is the single place the edge is decided. A
        component-scoped `@media screen and (prefers-color-scheme: dark)`
-       block is unavailable
-       here (theme_tokens_test.go's ExactlyOneScreenScopedDarkQuery). */
-    border-top: 1px solid light-dark(transparent, var(--border));
+       block is unavailable here (theme_tokens_test.go's
+       ExactlyOneScreenScopedDarkQuery). */
+    border-top: 1px solid var(--sheet-hairline);
     border-top-left-radius: 16px;
     border-top-right-radius: 16px;
     border-radius: 16px 16px 0 0;
@@ -6015,25 +6133,10 @@
     max-width: 100%;
     box-sizing: border-box;
   }
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
-  }
-  .status-strip-head {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) 10px;
-    align-items: flex-start;
-  }
-  .status-strip-summary {
-    grid-column: 1;
-    grid-row: 1;
-  }
-  .status-strip-action-row {
-    grid-column: 1;
-    grid-row: 2;
-    justify-self: start;
-  }
-}
+  }}

 /* The claim head line. v0.4.1 gives it two children instead of loose text:
    a <span class="label"> carrying the label plus its status pill, and the
@@ -6576,24 +6679,9 @@
   background: var(--accent-bg);
 }

-/* nav-group-label separates the Tracks run from the modules above it. The
-   sidebar is otherwise a flat list of .sec-tab buttons, and a track tab looks
-   exactly like a module tab because mechanically it IS one — so without a
-   caption a reader would have no way to tell that the axis changed halfway
-   down. It borrows the .eyebrow register (mono, uppercase, --faint) rather
-   than inventing one, because it is the same kind of thing: a quiet caption
-   over the content beneath it, never a control. The top border and margin are
-   what make it read as a division rather than as a first entry in the run. */
-.nav-group-label {
-  margin: 14px 0 4px;
-  padding: 10px 20px 0;
-  border-top: 1px solid var(--border);
-  font-family: var(--font-mono);
-  font-size: 9px;
-  letter-spacing: .14em;
-  text-transform: uppercase;
-  color: var(--faint);
-}
+/* .nav-group-label (the caption over the Tracks run) is gone: the sidebar
+   groups modules and tracks in .system-nav-group <details> (shell.html), and
+   nothing has emitted the class since the system-record redesign. */

 /* Secondary horizontal facet tab strip inside a module's content area,
    ported verbatim (selector + declarations) from the reference docs
@@ -7222,14 +7310,6 @@
   color: var(--link);
 }

-.nav-group-label {
-  margin: 17px 0 5px;
-  padding: 12px 10px 0;
-  color: var(--faint);
-  font-size: 9px;
-  letter-spacing: .12em;
-}
-
 .sidebar-footer {
   padding: 11px 18px 15px;
   color: var(--faint);
@@ -8015,9 +8095,23 @@
   max-width: 760px;
 }

-.claim-body-disclosure > .claim-body { max-width: none; }
+.claim-body-disclosure > .claim-body,
+.claim-body-disclosure > .claim-edit-diff,
+.claim-body-disclosure > .claim-edit-current { max-width: none; }

-.claim-body-disclosure--collapsed > .claim-body {
+/* .claim-edit-diff is clamped alongside .claim-body because it IS the body,
+   shown a passage at a time (see system-record.js's BODY_LIKE). A claim that
+   truncates to four lines in one state and runs to full height in the other
+   is the same claim behaving as two components.
+   The clamp counts LINE BOXES and is not a height: the diff's passages carry
+   their own margins, and a tinted one carries padding, so a collapsed diff is
+   four lines of prose PLUS whatever box the first passage sits in. It is
+   therefore a little taller than a collapsed body and not a fixed multiple of
+   the line height — which is the honest description, and what the test
+   asserts rather than an exact 4x that does not hold. */
+.claim-body-disclosure--collapsed > .claim-body,
+.claim-body-disclosure--collapsed > .claim-edit-diff,
+.claim-body-disclosure--collapsed > .claim-edit-current {
   display: -webkit-box;
   overflow: hidden;
   -webkit-box-orient: vertical;
@@ -9762,31 +9856,10 @@
     border-radius: 10px;
     border-color: color-mix(in srgb, var(--warn) 26%, var(--border));
   }
-  /* retry fix 29 (this lane's third attempt): the desktop banner's own
-     padding moved from a literal 14px to the reading card's 32px gutter
-     (retry fix 20 above), but the 520px form stays the inset-card treatment
-     R-H.1 always specified. 02 §4.19 "Body block" gives the mobile form's
-     own padding as 12px/14px — one px shallower than the pre-retry-3 value
-     this rule carried, which was never checked against §4.19 directly. */
-  .status-strip-head {
-    padding: 12px 14px;
-  }
-  /* retry fix 33 (this lane's third attempt, 02 §4.19 "Icon"): the mobile
-     icon's optical offset is 2px, not the desktop row's 1px. */
-  .status-strip-icon {
-    margin-top: 2px;
-  }
-  /* retry fix 34 (this lane's third attempt, 02 §4.19 "Body text"): the
-     mobile banner sentence is Inter 13/19, a full step down from the
-     desktop 14/18 fix 21 (above) set — §4.19 is a distinct node (`574-0`)
-     from §4.10's `6T-0`, not the same rule at a smaller viewport. */
-  .status-strip-title {
-    font-size: 13px;
-    line-height: 19px;
-  }
-  .status-strip--open {
-    border-radius: 10px 10px 0 0;
-  }
+  /* The banner's own head padding, icon offset and sentence size used to be
+     re-pinned here for the phone. All three described the single-sentence
+     head that Paper EH1-0 replaced with a card, and their rules went with
+     it; the card carries its own metrics at both widths. */
   .status-strip-body {
     border-top-color: color-mix(in srgb, var(--warn) 18%, var(--border));
   }
@@ -9861,32 +9934,6 @@
     height: 30px;
     padding-inline: 12px;
   }
-  /* retry fix 15 (R-H.1, 02 §4.19): the action gets its own divided row —
-     a 1px top hairline at ~18% blocked tint, a 38px row and 14px inline
-     padding, bled to the head's own inline/bottom padding edges so the tap
-     target is the card's full width. `grid-column: 1 / -1` reclaims the
-     10px caret column too, since the divider must span edge to edge. */
-  .status-strip-action-row {
-    grid-column: 1 / -1;
-    justify-self: stretch;
-    display: flex;
-    align-items: center;
-    justify-content: space-between;
-    height: 38px;
-    margin: 8px -14px -13px;
-    padding-inline: 14px;
-    border-top: 1px solid color-mix(in srgb, var(--warn) 18%, transparent);
-  }
-  .status-strip-action {
-    font-weight: 600; /* retry fix 13: the 600 step is the PHONE form only. */
-  }
-  .status-strip-action-chevron {
-    display: inline-block;
-    width: 13px;
-    height: 13px;
-    color: var(--warn);
-    flex-shrink: 0;
-  }
 }

 /* retry fix 17 (LANES.md OD3, and fix 11's technique): .status-severity-chip
@@ -10287,13 +10334,19 @@
   background: var(--paper);
 }

-/* 04 §4.9: the bar is always draft amber, ranking VOLUME, not severity —
-   never colour these bars per row severity. */
+/* 04 §4.9: the module ranking's bar is always draft amber, ranking VOLUME,
+   not severity. The cause ranking under the Needs-you filter (Paper EKL-0,
+   "WHAT IS WAITING") is the one exception: each bar takes its cause's hue,
+   because there the rows ARE kinds of work and the hue is what tells them
+   apart. */
 .issues-rail-bar-fill {
   height: 5px;
   background: var(--status-draft);
 }

+.issues-rail-bar-fill[data-tone="alarm"] { background: var(--warn); }
+.issues-rail-bar-fill[data-tone="draft"] { background: var(--status-draft); }
+
 /* 04 §8 item 8: the rail is a ranking, so it truncates rather than scrolls. */
 .issues-rail-more {
   margin: 0;
@@ -11001,9 +11054,7 @@
   font: 13px/16px var(--font-sans);
 }
 .reading-canvas .claim-footer .comment-chip .dx-icon { width: 14px; height: 14px; }
-@media screen and (min-width: 861px) {
-  .reading-canvas .status-strip-action-chevron { display: none; }
-}
+@media screen and (min-width: 861px) {}
 @media screen and (max-width: 520px) {
   /* `display: flow-root` gives the canvas a block-formatting context without
      the side effects of restoring a border or `overflow: clip`. Without one,
@@ -11048,15 +11099,6 @@
      carries a 20px margin-bottom standing in for both and .reading-canvas has
      no border or padding at this tier, so the banner's own 14px collapses
      into that 20px and the reader gets 20px. State the no-tab-row case. */
-  .reading-canvas .status-strip-head { padding: 12px 14px; }
-  /* 56U-0: the triangle hangs on the FIRST text line (align-items: start,
-     10px gap), not centred against a three-line sentence. */
-  .reading-canvas .status-strip-summary { gap: 10px; align-items: flex-start; }
-  /* 56Y-0: Inter 13/19 — a full step down from the desktop 14/18. */
-  .reading-canvas .status-strip-title { font-size: 13px; line-height: 19px; }
-  /* 56Z-0: the divided action row bleeds to the head's own 14px inline and
-     12px bottom padding edges; Paper draws no extra gap above the rule. */
-  .reading-canvas .status-strip-action-row { margin: 12px -14px -12px; }
   .reading-canvas .claim-footer {
     margin-top: 6px;
     padding-top: 13px;
@@ -11097,6 +11139,534 @@
   .reading-canvas .claim-footer-chip--blocked .claim-footer-chip-count { color: var(--warn); }
 }

+/* ---- The banner card, and unapproved edits -------------------------------
+
+   ONE CARD, A ROW PER FINDING (Paper node EH1-0).
+
+   The banner used to be a single sentence with a "Show issues" action beside
+   it. That shape holds exactly one statement, and a facet routinely has two
+   kinds of thing waiting: claims blocked by unapproved dependencies, and
+   claims rewritten since they were approved. The second fact had to go into a
+   second tinted band stacked underneath, and two tinted bands touching read as
+   one broken component rather than as two facts.
+
+   TINT IS A SEVERITY CLAIM, so the card makes at most one. While every row
+   shares a hue the whole surface takes it; the moment two rows disagree the
+   surface goes neutral and the dots carry the colour instead. That is why
+   --alarm and --draft are separate modifiers and the neutral form is the base
+   rule: which one applies is computed from the rows, never from whichever kind
+   of finding happens to sort first. */
+
+/* The card is the PHONE form. Everything below describes the card itself; the
+   one thing that describes WHEN it applies is `display`, which is none here
+   and flex in the 520px block at the end of this file. Keeping the switch to
+   a single property in a single place is what stops a later rule in this
+   block from quietly turning the card back on at every width — which is
+   exactly what a `display: flex` sitting among the card's own layout rules
+   did on the first attempt. */
+.status-strip-card {
+  display: none;
+  flex-direction: column;
+  border: 1px solid var(--border-strong);
+  border-radius: 10px;
+  overflow: clip;
+  background: var(--card-bg);
+}
+
+.status-strip-card-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+  padding: 9px 14px;
+  background: var(--code-bg);
+  border-bottom: 1px solid var(--border);
+}
+
+.status-strip-card-eyebrow {
+  font: 600 11px/14px var(--font-sans);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+  color: var(--muted);
+}
+
+/* Mono, like every other machine-produced count in this viewer: the number is
+   read off the findings, not written by anyone. */
+.status-strip-card-count {
+  font: 11px/14px var(--font-mono, var(--font-sans));
+  color: var(--faint);
+}
+
+/* A button, not a row with a click handler. Each row is one way into the
+   Issues screen and has to be reachable by keyboard and announced as the
+   control it is. */
+.status-strip-finding {
+  display: flex;
+  align-items: flex-start;
+  gap: 10px;
+  width: 100%;
+  padding: 11px 14px;
+  border: 0;
+  background: none;
+  text-align: left;
+  cursor: pointer;
+  color: var(--ink);
+}
+
+.status-strip-finding + .status-strip-finding {
+  border-top: 1px solid var(--border);
+}
+
+.status-strip-finding-dot {
+  width: 7px;
+  height: 7px;
+  flex-shrink: 0;
+  margin-top: 6px;
+  border-radius: 999px;
+  background: var(--warn);
+}
+
+.status-strip-finding[data-tone="draft"] .status-strip-finding-dot {
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-finding-text {
+  flex: 1;
+  min-width: 0;
+  font: 13px/19px var(--font-sans);
+  color: var(--ink);
+}
+
+/* EH1-0: a 12px "\203a" glyph, in the card's hue (the tinted forms below
+   recolour it), optically dropped 2px to sit on the sentence's first line. */
+.status-strip-finding-chevron {
+  flex-shrink: 0;
+  margin-top: 2px;
+  color: var(--faint);
+  font: 400 12px/16px var(--font-sans);
+}
+
+/* The two tinted forms. The alphas are EH1-0's own (#9A6A1612 / #9A6A1645 /
+   #9A6A163D over the draft token), written as color-mix so the dark scheme
+   redefines them from its own hue instead of carrying a second literal. */
+.status-strip-card--alarm {
+  background: color-mix(in srgb, var(--warn) 7%, transparent);
+  border-color: color-mix(in srgb, var(--warn) 27%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-eyebrow,
+.status-strip-card--alarm .status-strip-card-count,
+.status-strip-card--alarm .status-strip-finding-chevron {
+  color: var(--warn);
+}
+
+.status-strip-card--alarm .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--draft {
+  background: color-mix(in srgb, var(--status-draft, #976600) 7%, transparent);
+  border-color: color-mix(in srgb, var(--status-draft, #976600) 27%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-eyebrow,
+.status-strip-card--draft .status-strip-card-count,
+.status-strip-card--draft .status-strip-finding-chevron {
+  color: var(--status-draft, #976600);
+}
+
+.status-strip-card--draft .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+/* The per-claim edit surface (Paper DON-0 / C9H-0).
+
+   It is a BAR ABOVE THE BODY with a two-segment switch, not a disclosure, and
+   the difference is which state is the default. A disclosure says the diff is
+   an aside a reader opens if they want it; the board says the opposite — a
+   claim that was approved and then rewritten opens SHOWING what moved,
+   because that is the thing about it a reader has to know before they read a
+   word of it. "Current" is the escape hatch, not the resting state. */
+/* While the changes are showing, the claim's own body is hidden by this class
+   on the CARD. See paintApprovedEdit for why the state does not live on the
+   body: the long-body disclosure re-wraps it after first paint, so the body
+   is not the same node before and after. */
+.claim--edit-view > .claim-body,
+.claim--edit-view > .claim-body-disclosure:not(.claim-body-disclosure--edit),
+.claim--edit-view > .claim-empty,
+.claim--edit-view > .claim-mockup-body {
+  display: none;
+}
+
+/* THE BAR WRAPS BY CONTENT, NOT BY VIEWPORT.
+   Its width is the reading column's, not the window's — about 690px at 1440
+   — so a media query cannot see the moment it runs out of room. The Current
+   state's line ("1 passage differs from the approval of 20 Sep") is the long
+   one, and at that column it overran by a few pixels and broke mid-date:
+   "...the approval of 20 / Sep".
+   Wrapping instead of shrinking is deliberate. The three ways to make it fit
+   were truncating the sentence, stepping the type down, or letting the row
+   become two rows; the first hides a fact, the second makes a 12px mono line
+   smaller than anything else on the card, and the third costs 8px of height
+   in the rare case. The switch is what moves, because it is the only part
+   with no sentence in it. */
+/* Paper CKF-0: the bar is the draft tint (--color-draft-bg), radius 6,
+   9px/13px, and it sits in the card's own 16px rhythm — no margin of its
+   own. Title and meta share a baseline with 10px between them. */
+.claim-edit-bar {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 8px 10px;
+  margin: 0;
+  padding: 9px 13px;
+  border-radius: 6px;
+  background: var(--status-draft-bg, rgba(154, 106, 22, .11));
+  font-family: var(--font-sans);
+}
+
+.claim-edit-bar-title {
+  flex: 0 0 auto;
+  font: 600 13px/18px var(--font-sans);
+  color: var(--status-draft, #976600);
+}
+
+/* Mono, like every other machine-read fact in this viewer: the date, the
+   actor and the passage count are read off the ledger, not written by
+   anyone. */
+/* The meta's own width is its CONTENT's, which is what makes the switch the
+   first thing to wrap: flex lays the row out from the items' base sizes, so
+   title + this sentence + switch overflowing moves the switch — the only item
+   with no sentence in it — down to its own row, and the sentence gets the
+   whole first row instead of being squeezed into what was left over.
+   It stays shrinkable on purpose. Refusing to shrink made it overrun the bar
+   entirely at 900px, where even a full row is not enough; wrapping inside
+   itself is a worse line but a contained one, and it is the last resort
+   rather than the first thing that happens. */
+.claim-edit-bar-meta {
+  flex: 1 1 auto;
+  min-width: 0;
+  font: 11px/16px var(--font-mono, var(--font-sans));
+  color: var(--muted);
+}
+
+/* The switch. The selected segment is a filled dark pill, which is the one
+   place in this panel that spends a strong value — everything else is a tint,
+   and the reader has to be able to see which of two states they are in from
+   across the page. */
+/* Paper CKJ-0: a card-ground pill with a 1px hairline, 2px inset, segments
+   3px/11px at 11/14. */
+.claim-edit-switch {
+  display: inline-flex;
+  align-items: center;
+  flex: 0 0 auto;
+  /* Pinned right on the first row, and still right when it wraps to its own. */
+  margin-left: auto;
+  gap: 0;
+  padding: 2px;
+  border: 1px solid var(--border);
+  border-radius: 999px;
+  background: var(--card-bg);
+}
+
+.claim-edit-switch-seg {
+  border: 0;
+  border-radius: 999px;
+  padding: 3px 11px;
+  background: none;
+  cursor: pointer;
+  font: 500 11px/14px var(--font-sans);
+  color: var(--muted);
+}
+
+.claim-edit-switch-seg[aria-pressed="true"] {
+  background: var(--ink);
+  color: var(--card-bg);
+  font-weight: 600;
+}
+
+/* The passages. Typography is the claim body's own — this IS the claim body,
+   shown a passage at a time — so a reader moving between Changes and Current
+   is reading the same prose in the same face, not a second document. */
+.claim-edit-diff,
+.claim-edit-current {
+  font-family: var(--font-serif);
+  font-size: 17px;
+  line-height: 28px;
+  max-width: 760px;
+}
+
+/* Passages keep the body's 16px rhythm between them (the card's own gap).
+   Margins rather than flex gap, because the four-line clamp turns this box
+   into a -webkit-box and a flex gap would vanish with it. */
+.claim-edit-passage {
+  margin: 0;
+}
+
+.claim-edit-passage + .claim-edit-passage {
+  margin-top: 16px;
+}
+
+.claim-edit-passage > :first-child {
+  margin-top: 0;
+}
+
+.claim-edit-passage > :last-child {
+  margin-bottom: 0;
+}
+
+/* Paper CK7-0 / CKP-0: a changed passage is a tinted row, radius 4, with a
+   3px rule INSIDE it (12px in from the edge, 12px before the text) that runs
+   the height of the text, old above new with 2px between them and 4px above
+   the pair. The prose itself takes the hue: blocked for the approved
+   passage, locked for the current one. Colour is not the only carrier — the
+   removed passage is struck through and the added one is not, so the two
+   stay apart in a monochrome print, under a colour-vision deficiency, and
+   in the dark scheme where the two tints are close in luminance. */
+.claim-edit-passage--removed,
+.claim-edit-passage--added,
+.claim-edit-passage--current {
+  position: relative;
+  border-radius: 4px;
+}
+
+.claim-edit-passage--removed,
+.claim-edit-passage--added {
+  padding: 7px 12px 7px 27px;
+}
+
+.claim-edit-passage--removed::before,
+.claim-edit-passage--added::before,
+.claim-edit-passage--current::before {
+  content: "";
+  position: absolute;
+  left: 12px;
+  top: 7px;
+  bottom: 7px;
+  width: 3px;
+  border-radius: 2px;
+  background: currentColor;
+}
+
+.claim-edit-passage--removed {
+  color: var(--warn);
+  background: var(--warn-bg, rgba(158, 59, 54, .10));
+  text-decoration: line-through 1px;
+  text-underline-position: from-font;
+}
+
+.claim-edit-passage--added {
+  color: var(--accent);
+  background: var(--accent-bg, rgba(44, 107, 82, .10));
+}
+
+.claim-edit-passage + .claim-edit-passage--removed {
+  margin-top: 20px;
+}
+
+.claim-edit-passage--removed + .claim-edit-passage--added {
+  margin-top: 2px;
+}
+
+/* The Current state (Paper DPZ-0): the passage that differs carries only a
+   draft-coloured rule at the left edge — no tint, prose in ink — so the
+   claim reads as ordinary prose with the seam marked. */
+.claim-edit-passage--current {
+  padding: 0 0 0 15px;
+}
+
+.claim-edit-passage--current::before {
+  left: 0;
+  top: 0;
+  bottom: 0;
+  background: var(--status-draft, #976600);
+}
+
+/* The words that moved, inside the passage they moved in. They stay in the
+   DOM for the passage's aria-label and for a reader who selects the text,
+   but Paper draws the passage as one tinted, coloured, struck (or not)
+   block and paints nothing on the individual words — the struck passage and
+   the coloured one are the comparison. */
+.claim-edit-word--removed,
+.claim-edit-word--added {
+  background: none;
+  color: inherit;
+  padding: 0;
+}
+
+.claim-edit-note {
+  margin: 8px 0 0;
+}
+
+.claim-edit-note-text,
+.claim-edit-note-hint,
+.claim-edit-field-note {
+  margin: 0;
+  font: 400 13px/19px var(--font-sans);
+  color: var(--muted);
+}
+
+/* The hint names the command that ENDS this state. A record written before
+   the approved wording was kept says "there is nothing to compare against",
+   and until this line existed that was the end of the sentence — true, and
+   with no way out of it. The wording is usually still in the project's own
+   git history, so the panel that reports the gap also names the verb that
+   closes it. */
+.claim-edit-note-hint {
+  margin-top: 4px;
+  color: var(--faint);
+}
+
+/* The fields that moved. This block is TEXT, not prose: it shows the YAML the
+   claim file holds, so it is monospace and pre-wrapped, and the tints are the
+   same two the passage diff uses so a reader does not learn a second colour
+   language halfway down one panel. */
+.claim-edit-fields {
+  margin: 8px 0 0;
+}
+
+.claim-edit-fields-toggle {
+  appearance: none;
+  -webkit-appearance: none;
+  background: none;
+  border: 0;
+  padding: 0;
+  cursor: pointer;
+  font: 500 13px/19px var(--font-sans);
+  color: var(--accent);
+  text-align: left;
+}
+
+.claim-edit-fields-toggle:hover { text-decoration: underline; }
+
+.claim-edit-fields-list {
+  margin-top: 6px;
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+}
+
+.claim-edit-field-name {
+  display: block;
+  font: 600 12px/18px var(--font-mono);
+  color: var(--muted);
+}
+
+.claim-edit-field-hunk {
+  margin: 3px 0 0;
+  padding: 6px 8px;
+  border-radius: 6px;
+  border-left: 3px solid transparent;
+  background: var(--code-bg);
+  font: 400 12px/18px var(--font-mono);
+  /* pre-wrap and not pre: a claims file's embodiment block is indented deep
+     enough to overflow a narrow column, and a horizontal scrollbar inside a
+     panel inside a claim is three nested scroll regions. Wrapping keeps the
+     indentation and loses only the guarantee that a line is one line. */
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+}
+
+.claim-edit-field-hunk--removed {
+  border-left-color: var(--warn);
+  background: color-mix(in srgb, var(--warn) 7%, var(--code-bg));
+}
+
+.claim-edit-field-hunk--added {
+  border-left-color: var(--border-strong);
+}
+
+@media (max-width: 520px) {
+  /* The phone swaps the whole banner: the desktop band's sentence-and-action
+     row goes away and the card takes its place, so .status-strip stops being
+     a painted surface and becomes the gutter that positions the card.
+     .reading-canvas > .status-strip is named alongside it because that rule
+     is two-class and unqueried, and would otherwise outrank this one and
+     restore the band's border underneath the card. */
+  .status-strip-head {
+    display: none;
+  }
+
+  .status-strip-card {
+    display: flex;
+  }
+
+  .reading-canvas > .status-strip,
+  .status-strip,
+  .status-strip--lint,
+  .status-strip--integrity {
+    background: none;
+    border: 0;
+    border-radius: 0;
+    margin: 0;
+    /* The gutter is the reading card's own 16px, not the desktop 32px —
+       EH1-0's mobile frame draws the card at mx-4 inside a 390px canvas. */
+    padding: 14px 16px 0;
+  }
+
+  /* The edit bar stacks into three rows (Paper CHK-0): the sentence, the
+     machine line under it, and the switch full width with two equal halves.
+     Side by side at this width the sentence wrapped to two lines, the mono
+     meta broke mid-word, and the switch was squeezed against them — three
+     things competing for a 343px row that only ever held one comfortably. */
+  .claim-edit-bar {
+    flex-direction: column;
+    align-items: stretch;
+    gap: 8px;
+    padding: 10px 12px;
+  }
+
+  .claim-edit-bar-title {
+    /* CKY-0: the title and its machine line are 2px apart on the phone. */
+    margin-bottom: -6px;
+  }
+
+  .claim-edit-bar-meta {
+    /* The phone stack gives every part its own row already. */
+    flex: none;
+    line-height: 15px;
+  }
+
+  .claim-edit-switch {
+    display: flex;
+    margin-left: 0;
+  }
+
+  .claim-edit-switch-seg {
+    flex: 1;
+    padding: 5px 10px;
+    font-size: 12px;
+    line-height: 16px;
+    text-align: center;
+  }
+
+  /* CKP-0: the rule and the text sit 10px in on a phone, not 12. */
+  .claim-edit-passage--removed,
+  .claim-edit-passage--added {
+    padding: 7px 10px 7px 23px;
+  }
+
+  .claim-edit-passage--removed::before,
+  .claim-edit-passage--added::before {
+    left: 10px;
+  }
+
+  .claim-edit-passage + .claim-edit-passage--removed {
+    margin-top: 18px;
+  }
+}
+
+
 @media print {
   :root { color-scheme: light; }

@@ -11115,10 +11685,6 @@
     display: block;
   }

-  .claim-links-summary {
-    cursor: auto;
-  }
-
   /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
@@ -11574,7 +12140,7 @@
   <div class="k" data-claim-id="widget.contract.mismatch" title="widget.contract.mismatch"><span class="label"><span class="k-title">Mismatch</span> <span class="pill pv"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-lock-open"/></svg>Draft</span><span class="k-id">widget.contract.mismatch</span></span></div>
 <div class="claim-body"><p>Set and scalar observations differ from their authored expectations.</p></div>

-<div class="claim-footer"><details class="claim-links" name="claim-footer-widget.contract.mismatch"><summary class="claim-footer-chip claim-footer-chip--relationships"><span class="claim-footer-chip-label">0 relationships</span><svg class="claim-footer__chevron" aria-hidden="true" viewBox="0 0 24 24" fill="none"><path d="m6 9 6 6 6-6"/></svg></summary></details><div class="claim-footer-panel claim-links-panel"><div class="claim-footer-panel-head"><span class="claim-footer-eyebrow">RELATIONSHIPS</span><span class="claim-footer-rule" aria-hidden="true"></span><span class="claim-footer-panel-count">0</span></div><div class="claim-relationship-direction"><div class="claim-relationship-direction-head"><span class="claim-relationship-arrow" aria-hidden="true">↑</span><span class="claim-relationship-direction-label">GOVERNED BY</span></div><ul class="claim-edges claim-relationship-list"><li class="claim-governed governed-none claim-relationship-none">none<span class="claim-governed-reason"> — neutral fixture</span></li></ul></div></div><span class="claim-footer-chip claim-footer-chip--sources claim-footer-chip--empty"><span class="claim-footer-chip-label">No sources</span></span><details class="claim-conformance" name="claim-footer-widget.contract.mismatch" data-conformance-mode="compare" data-claim-id="widget.contract.mismatch" data-implementation-ready="false" open><summary class="claim-conformance-head"><strong>2 checks</strong> <span class="pill pw">Not ready</span></summary><p class="claim-conformance-scope">Ready here means every declared check matches. Claim and release readiness remain separate.</p><div class="claim-conformance-eyebrow"><span class="claim-conformance-eyebrow-label">Implementation checks</span><span class="claim-conformance-eyebrow-rule" aria-hidden="true"></span><span class="claim-conformance-eyebrow-count">2</span></div><article class="claim-conformance-check" data-check-id="public-values" data-conformance-state="mismatch" data-shape="set" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Public Values</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Mismatch</span></div><p class="claim-conformance-explain">expected and observed sets differ</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">which of the 2 declared members the run actually exercised</span></div><div class="claim-conformance-row claim-conformance-row--found"><span class="claim-conformance-row-label">FOUND</span><div class="claim-conformance-row-value"><div class="claim-conformance-coverage" role="list"><span class="claim-conformance-chip claim-conformance-chip--missing" role="listitem" aria-label="blocked" title="blocked">1</span><span class="claim-conformance-chip" role="listitem" aria-label="ready" title="ready">2</span></div><div class="claim-conformance-legend"><span class="claim-conformance-legend-count">1 of 2 exercised</span><span class="claim-conformance-legend-swatch" aria-hidden="true"></span><span class="claim-conformance-legend-label">never run</span></div></div></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>public-values</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>set</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://mismatch</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <span class="claim-conformance-values"><code>blocked</code>, <code>ready</code></span></p><p class="claim-conformance-line claim-conformance-line--wide"><span>observed:</span> <span class="claim-conformance-values"><code>paused</code>, <code>ready</code></span></p><p class="claim-conformance-line claim-conformance-line--blocked"><span>missing:</span> <span class="claim-conformance-values"><code>blocked</code></span></p><p class="claim-conformance-line"><span>extra:</span> <span class="claim-conformance-values"><code>paused</code></span></p><p class="claim-conformance-line"><span>detail:</span> <code>expected and observed sets differ</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;public-values&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;set&#34;,&#34;target&#34;:&#34;widget://mismatch&#34;,&#34;expected&#34;:[&#34;blocked&#34;,&#34;ready&#34;],&#34;observed&#34;:[&#34;paused&#34;,&#34;ready&#34;],&#34;missing&#34;:[&#34;blocked&#34;],&#34;extra&#34;:[&#34;paused&#34;],&#34;detail&#34;:&#34;expected and observed sets differ&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article><article class="claim-conformance-check" data-check-id="schema-version" data-conformance-state="mismatch" data-shape="scalar" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Schema Version</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Mismatch</span></div><p class="claim-conformance-explain">expected and observed scalars differ</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">the value this target reports</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">FOUND</span><span class="claim-conformance-row-value">4</span></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>schema-version</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>scalar</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://mismatch-version</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <code>3</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>observed:</span> <code>4</code></p><p class="claim-conformance-line claim-conformance-line--empty"><span>extra:</span> <code>—</code></p><p class="claim-conformance-line"><span>detail:</span> <code>expected and observed scalars differ</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;schema-version&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;scalar&#34;,&#34;target&#34;:&#34;widget://mismatch-version&#34;,&#34;expected&#34;:&#34;3&#34;,&#34;observed&#34;:&#34;4&#34;,&#34;detail&#34;:&#34;expected and observed scalars differ&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article></details><span class="claim-comments-slot" hidden><button type="button" class="comment-chip comment-chip--empty" data-claim-id="widget.contract.mismatch" aria-controls="commentsPanel" aria-expanded="false" aria-label="add the first comment on this claim"><span class="comment-chip-glyph" aria-hidden="true"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-message-circle"/></svg></span> <span class="comment-chip-count">0</span></button></span></div>
+<div class="claim-footer"><details class="claim-links" name="claim-footer-widget.contract.mismatch"><summary class="claim-footer-chip claim-footer-chip--relationships"><span class="claim-footer-chip-label">0 relationships</span><svg class="claim-footer__chevron" aria-hidden="true" viewBox="0 0 24 24" fill="none"><path d="m6 9 6 6 6-6"/></svg></summary></details><div class="claim-footer-panel claim-links-panel"><div class="claim-footer-panel-head"><span class="claim-footer-eyebrow">RELATIONSHIPS</span><span class="claim-footer-rule" aria-hidden="true"></span><span class="claim-footer-panel-count">0</span></div><div class="claim-relationship-direction"><div class="claim-relationship-direction-head"><span class="claim-relationship-arrow" aria-hidden="true">↑</span><span class="claim-relationship-direction-label">GOVERNED BY</span></div><ul class="claim-edges claim-relationship-list"><li class="claim-governed governed-none claim-relationship-none">none<span class="claim-governed-reason"> — neutral fixture</span></li></ul></div></div><span class="claim-footer-chip claim-footer-chip--sources claim-footer-chip--empty"><span class="claim-footer-chip-label">No sources</span></span><details class="claim-conformance" name="claim-footer-widget.contract.mismatch" data-conformance-mode="compare" data-claim-id="widget.contract.mismatch" data-implementation-ready="false"><summary class="claim-conformance-head"><strong>2 checks</strong> <span class="pill pw">Not ready</span></summary><p class="claim-conformance-scope">Ready here means every declared check matches. Claim and release readiness remain separate.</p><div class="claim-conformance-eyebrow"><span class="claim-conformance-eyebrow-label">Implementation checks</span><span class="claim-conformance-eyebrow-rule" aria-hidden="true"></span><span class="claim-conformance-eyebrow-count">2</span></div><article class="claim-conformance-check" data-check-id="public-values" data-conformance-state="mismatch" data-shape="set" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Public Values</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Mismatch</span></div><p class="claim-conformance-explain">expected and observed sets differ</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">which of the 2 declared members the run actually exercised</span></div><div class="claim-conformance-row claim-conformance-row--found"><span class="claim-conformance-row-label">FOUND</span><div class="claim-conformance-row-value"><div class="claim-conformance-coverage" role="list"><span class="claim-conformance-chip claim-conformance-chip--missing" role="listitem" aria-label="blocked" title="blocked">1</span><span class="claim-conformance-chip" role="listitem" aria-label="ready" title="ready">2</span></div><div class="claim-conformance-legend"><span class="claim-conformance-legend-count">1 of 2 exercised</span><span class="claim-conformance-legend-swatch" aria-hidden="true"></span><span class="claim-conformance-legend-label">never run</span></div></div></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>public-values</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>set</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://mismatch</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <span class="claim-conformance-values"><code>blocked</code>, <code>ready</code></span></p><p class="claim-conformance-line claim-conformance-line--wide"><span>observed:</span> <span class="claim-conformance-values"><code>paused</code>, <code>ready</code></span></p><p class="claim-conformance-line claim-conformance-line--blocked"><span>missing:</span> <span class="claim-conformance-values"><code>blocked</code></span></p><p class="claim-conformance-line"><span>extra:</span> <span class="claim-conformance-values"><code>paused</code></span></p><p class="claim-conformance-line"><span>detail:</span> <code>expected and observed sets differ</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;public-values&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;set&#34;,&#34;target&#34;:&#34;widget://mismatch&#34;,&#34;expected&#34;:[&#34;blocked&#34;,&#34;ready&#34;],&#34;observed&#34;:[&#34;paused&#34;,&#34;ready&#34;],&#34;missing&#34;:[&#34;blocked&#34;],&#34;extra&#34;:[&#34;paused&#34;],&#34;detail&#34;:&#34;expected and observed sets differ&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article><article class="claim-conformance-check" data-check-id="schema-version" data-conformance-state="mismatch" data-shape="scalar" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Schema Version</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Mismatch</span></div><p class="claim-conformance-explain">expected and observed scalars differ</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">the value this target reports</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">FOUND</span><span class="claim-conformance-row-value">4</span></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>schema-version</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>scalar</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://mismatch-version</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <code>3</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>observed:</span> <code>4</code></p><p class="claim-conformance-line claim-conformance-line--empty"><span>extra:</span> <code>—</code></p><p class="claim-conformance-line"><span>detail:</span> <code>expected and observed scalars differ</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;schema-version&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;scalar&#34;,&#34;target&#34;:&#34;widget://mismatch-version&#34;,&#34;expected&#34;:&#34;3&#34;,&#34;observed&#34;:&#34;4&#34;,&#34;detail&#34;:&#34;expected and observed scalars differ&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article></details><span class="claim-comments-slot" hidden><button type="button" class="comment-chip comment-chip--empty" data-claim-id="widget.contract.mismatch" aria-controls="commentsPanel" aria-expanded="false" aria-label="add the first comment on this claim"><span class="comment-chip-glyph" aria-hidden="true"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-message-circle"/></svg></span> <span class="comment-chip-count">0</span></button></span></div>
 </section>


@@ -11582,7 +12148,7 @@
   <div class="k" data-claim-id="widget.contract.owed" title="widget.contract.owed"><span class="label"><span class="k-title">Owed</span> <span class="pill pv"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-lock-open"/></svg>Draft</span><span class="k-id">widget.contract.owed</span></span></div>
 <div class="claim-body"><p>No observation exists for this target.</p></div>

-<div class="claim-footer"><details class="claim-links" name="claim-footer-widget.contract.owed"><summary class="claim-footer-chip claim-footer-chip--relationships"><span class="claim-footer-chip-label">0 relationships</span><svg class="claim-footer__chevron" aria-hidden="true" viewBox="0 0 24 24" fill="none"><path d="m6 9 6 6 6-6"/></svg></summary></details><div class="claim-footer-panel claim-links-panel"><div class="claim-footer-panel-head"><span class="claim-footer-eyebrow">RELATIONSHIPS</span><span class="claim-footer-rule" aria-hidden="true"></span><span class="claim-footer-panel-count">0</span></div><div class="claim-relationship-direction"><div class="claim-relationship-direction-head"><span class="claim-relationship-arrow" aria-hidden="true">↑</span><span class="claim-relationship-direction-label">GOVERNED BY</span></div><ul class="claim-edges claim-relationship-list"><li class="claim-governed governed-none claim-relationship-none">none<span class="claim-governed-reason"> — neutral fixture</span></li></ul></div></div><span class="claim-footer-chip claim-footer-chip--sources claim-footer-chip--empty"><span class="claim-footer-chip-label">No sources</span></span><details class="claim-conformance" name="claim-footer-widget.contract.owed" data-conformance-mode="compare" data-claim-id="widget.contract.owed" data-implementation-ready="false" open><summary class="claim-conformance-head"><strong>1 check</strong> <span class="pill pw">Not ready</span></summary><p class="claim-conformance-scope">Ready here means every declared check matches. Claim and release readiness remain separate.</p><div class="claim-conformance-eyebrow"><span class="claim-conformance-eyebrow-label">Implementation checks</span><span class="claim-conformance-eyebrow-rule" aria-hidden="true"></span><span class="claim-conformance-eyebrow-count">1</span></div><article class="claim-conformance-check" data-check-id="current-value" data-conformance-state="owed" data-shape="scalar" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Current Value</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Owed</span></div><p class="claim-conformance-explain">no observation for the declared adapter and target</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">the value this target reports</span></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>current-value</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>scalar</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://owed</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <code>ready</code></p><p class="claim-conformance-line claim-conformance-line--empty"><span>extra:</span> <code>—</code></p><p class="claim-conformance-line"><span>detail:</span> <code>no observation for the declared adapter and target</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;current-value&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;scalar&#34;,&#34;target&#34;:&#34;widget://owed&#34;,&#34;expected&#34;:&#34;ready&#34;,&#34;detail&#34;:&#34;no observation for the declared adapter and target&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article></details><span class="claim-comments-slot" hidden><button type="button" class="comment-chip comment-chip--empty" data-claim-id="widget.contract.owed" aria-controls="commentsPanel" aria-expanded="false" aria-label="add the first comment on this claim"><span class="comment-chip-glyph" aria-hidden="true"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-message-circle"/></svg></span> <span class="comment-chip-count">0</span></button></span></div>
+<div class="claim-footer"><details class="claim-links" name="claim-footer-widget.contract.owed"><summary class="claim-footer-chip claim-footer-chip--relationships"><span class="claim-footer-chip-label">0 relationships</span><svg class="claim-footer__chevron" aria-hidden="true" viewBox="0 0 24 24" fill="none"><path d="m6 9 6 6 6-6"/></svg></summary></details><div class="claim-footer-panel claim-links-panel"><div class="claim-footer-panel-head"><span class="claim-footer-eyebrow">RELATIONSHIPS</span><span class="claim-footer-rule" aria-hidden="true"></span><span class="claim-footer-panel-count">0</span></div><div class="claim-relationship-direction"><div class="claim-relationship-direction-head"><span class="claim-relationship-arrow" aria-hidden="true">↑</span><span class="claim-relationship-direction-label">GOVERNED BY</span></div><ul class="claim-edges claim-relationship-list"><li class="claim-governed governed-none claim-relationship-none">none<span class="claim-governed-reason"> — neutral fixture</span></li></ul></div></div><span class="claim-footer-chip claim-footer-chip--sources claim-footer-chip--empty"><span class="claim-footer-chip-label">No sources</span></span><details class="claim-conformance" name="claim-footer-widget.contract.owed" data-conformance-mode="compare" data-claim-id="widget.contract.owed" data-implementation-ready="false"><summary class="claim-conformance-head"><strong>1 check</strong> <span class="pill pw">Not ready</span></summary><p class="claim-conformance-scope">Ready here means every declared check matches. Claim and release readiness remain separate.</p><div class="claim-conformance-eyebrow"><span class="claim-conformance-eyebrow-label">Implementation checks</span><span class="claim-conformance-eyebrow-rule" aria-hidden="true"></span><span class="claim-conformance-eyebrow-count">1</span></div><article class="claim-conformance-check" data-check-id="current-value" data-conformance-state="owed" data-shape="scalar" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Current Value</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Owed</span></div><p class="claim-conformance-explain">no observation for the declared adapter and target</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">the value this target reports</span></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>current-value</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>scalar</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://owed</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <code>ready</code></p><p class="claim-conformance-line claim-conformance-line--empty"><span>extra:</span> <code>—</code></p><p class="claim-conformance-line"><span>detail:</span> <code>no observation for the declared adapter and target</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;current-value&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;scalar&#34;,&#34;target&#34;:&#34;widget://owed&#34;,&#34;expected&#34;:&#34;ready&#34;,&#34;detail&#34;:&#34;no observation for the declared adapter and target&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article></details><span class="claim-comments-slot" hidden><button type="button" class="comment-chip comment-chip--empty" data-claim-id="widget.contract.owed" aria-controls="commentsPanel" aria-expanded="false" aria-label="add the first comment on this claim"><span class="comment-chip-glyph" aria-hidden="true"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-message-circle"/></svg></span> <span class="comment-chip-count">0</span></button></span></div>
 </section>


@@ -11590,7 +12156,7 @@
   <div class="k" data-claim-id="widget.contract.uncheckable" title="widget.contract.uncheckable"><span class="label"><span class="k-title">Uncheckable</span> <span class="pill pv"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-lock-open"/></svg>Draft</span><span class="k-id">widget.contract.uncheckable</span></span></div>
 <div class="claim-body"><p>The adapter reported a per-target failure.</p></div>

-<div class="claim-footer"><details class="claim-links" name="claim-footer-widget.contract.uncheckable"><summary class="claim-footer-chip claim-footer-chip--relationships"><span class="claim-footer-chip-label">0 relationships</span><svg class="claim-footer__chevron" aria-hidden="true" viewBox="0 0 24 24" fill="none"><path d="m6 9 6 6 6-6"/></svg></summary></details><div class="claim-footer-panel claim-links-panel"><div class="claim-footer-panel-head"><span class="claim-footer-eyebrow">RELATIONSHIPS</span><span class="claim-footer-rule" aria-hidden="true"></span><span class="claim-footer-panel-count">0</span></div><div class="claim-relationship-direction"><div class="claim-relationship-direction-head"><span class="claim-relationship-arrow" aria-hidden="true">↑</span><span class="claim-relationship-direction-label">GOVERNED BY</span></div><ul class="claim-edges claim-relationship-list"><li class="claim-governed governed-none claim-relationship-none">none<span class="claim-governed-reason"> — neutral fixture</span></li></ul></div></div><span class="claim-footer-chip claim-footer-chip--sources claim-footer-chip--empty"><span class="claim-footer-chip-label">No sources</span></span><details class="claim-conformance" name="claim-footer-widget.contract.uncheckable" data-conformance-mode="compare" data-claim-id="widget.contract.uncheckable" data-implementation-ready="false" open><summary class="claim-conformance-head"><strong>1 check</strong> <span class="pill pw">Not ready</span></summary><p class="claim-conformance-scope">Ready here means every declared check matches. Claim and release readiness remain separate.</p><div class="claim-conformance-eyebrow"><span class="claim-conformance-eyebrow-label">Implementation checks</span><span class="claim-conformance-eyebrow-rule" aria-hidden="true"></span><span class="claim-conformance-eyebrow-count">1</span></div><article class="claim-conformance-check" data-check-id="current-value" data-conformance-state="uncheckable" data-shape="scalar" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Current Value</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Uncheckable</span></div><p class="claim-conformance-explain">adapter reported an observation error</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">the value this target reports</span></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>current-value</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>scalar</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://uncheckable</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <code>ready</code></p><p class="claim-conformance-line claim-conformance-line--empty"><span>extra:</span> <code>—</code></p><p class="claim-conformance-line claim-conformance-line--blocked"><span>observation error:</span> <code>fixture_unavailable</code></p><p class="claim-conformance-line"><span>adapter message:</span> <code>the neutral fixture has no snapshot</code></p><p class="claim-conformance-line"><span>detail:</span> <code>adapter reported an observation error</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;current-value&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;scalar&#34;,&#34;target&#34;:&#34;widget://uncheckable&#34;,&#34;expected&#34;:&#34;ready&#34;,&#34;observation_error&#34;:{&#34;code&#34;:&#34;fixture_unavailable&#34;,&#34;message&#34;:&#34;the neutral fixture has no snapshot&#34;},&#34;detail&#34;:&#34;adapter reported an observation error&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article></details><span class="claim-comments-slot" hidden><button type="button" class="comment-chip comment-chip--empty" data-claim-id="widget.contract.uncheckable" aria-controls="commentsPanel" aria-expanded="false" aria-label="add the first comment on this claim"><span class="comment-chip-glyph" aria-hidden="true"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-message-circle"/></svg></span> <span class="comment-chip-count">0</span></button></span></div>
+<div class="claim-footer"><details class="claim-links" name="claim-footer-widget.contract.uncheckable"><summary class="claim-footer-chip claim-footer-chip--relationships"><span class="claim-footer-chip-label">0 relationships</span><svg class="claim-footer__chevron" aria-hidden="true" viewBox="0 0 24 24" fill="none"><path d="m6 9 6 6 6-6"/></svg></summary></details><div class="claim-footer-panel claim-links-panel"><div class="claim-footer-panel-head"><span class="claim-footer-eyebrow">RELATIONSHIPS</span><span class="claim-footer-rule" aria-hidden="true"></span><span class="claim-footer-panel-count">0</span></div><div class="claim-relationship-direction"><div class="claim-relationship-direction-head"><span class="claim-relationship-arrow" aria-hidden="true">↑</span><span class="claim-relationship-direction-label">GOVERNED BY</span></div><ul class="claim-edges claim-relationship-list"><li class="claim-governed governed-none claim-relationship-none">none<span class="claim-governed-reason"> — neutral fixture</span></li></ul></div></div><span class="claim-footer-chip claim-footer-chip--sources claim-footer-chip--empty"><span class="claim-footer-chip-label">No sources</span></span><details class="claim-conformance" name="claim-footer-widget.contract.uncheckable" data-conformance-mode="compare" data-claim-id="widget.contract.uncheckable" data-implementation-ready="false"><summary class="claim-conformance-head"><strong>1 check</strong> <span class="pill pw">Not ready</span></summary><p class="claim-conformance-scope">Ready here means every declared check matches. Claim and release readiness remain separate.</p><div class="claim-conformance-eyebrow"><span class="claim-conformance-eyebrow-label">Implementation checks</span><span class="claim-conformance-eyebrow-rule" aria-hidden="true"></span><span class="claim-conformance-eyebrow-count">1</span></div><article class="claim-conformance-check" data-check-id="current-value" data-conformance-state="uncheckable" data-shape="scalar" data-implementation-ready="false"><div class="claim-conformance-check-head"><span class="claim-conformance-check-title">Current Value</span><span class="claim-conformance-verdict"><span class="claim-conformance-verdict-dot" aria-hidden="true"></span>Uncheckable</span></div><p class="claim-conformance-explain">adapter reported an observation error</p><div class="claim-conformance-row"><span class="claim-conformance-row-label">EXAMINED</span><span class="claim-conformance-row-value">the verification suite for this claim</span></div><div class="claim-conformance-row"><span class="claim-conformance-row-label">COMPARED</span><span class="claim-conformance-row-value">the value this target reports</span></div><details class="claim-conformance-disclosure"><summary class="claim-conformance-disclosure-trigger"><svg class="claim-conformance-chevron" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><path d="m9 18 6-6-6-6" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg><span>How this was checked</span><span class="claim-conformance-disclosure-spacer" aria-hidden="true"></span><span class="claim-conformance-snapshot">snapshot neutral-fixture-1</span></summary><div class="claim-conformance-machine"><p class="claim-conformance-line"><span>check:</span> <code>current-value</code></p><p class="claim-conformance-line"><span>adapter:</span> <code>neutral/v1</code></p><p class="claim-conformance-line"><span>shape:</span> <code>scalar</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>target:</span> <code>widget://uncheckable</code></p><p class="claim-conformance-line claim-conformance-line--wide"><span>expected:</span> <code>ready</code></p><p class="claim-conformance-line claim-conformance-line--empty"><span>extra:</span> <code>—</code></p><p class="claim-conformance-line claim-conformance-line--blocked"><span>observation error:</span> <code>fixture_unavailable</code></p><p class="claim-conformance-line"><span>adapter message:</span> <code>the neutral fixture has no snapshot</code></p><p class="claim-conformance-line"><span>detail:</span> <code>adapter reported an observation error</code></p><button type="button" class="claim-conformance-copy" data-copy-json="{&#34;check&#34;:&#34;current-value&#34;,&#34;adapter&#34;:&#34;neutral/v1&#34;,&#34;shape&#34;:&#34;scalar&#34;,&#34;target&#34;:&#34;widget://uncheckable&#34;,&#34;expected&#34;:&#34;ready&#34;,&#34;observation_error&#34;:{&#34;code&#34;:&#34;fixture_unavailable&#34;,&#34;message&#34;:&#34;the neutral fixture has no snapshot&#34;},&#34;detail&#34;:&#34;adapter reported an observation error&#34;}"><svg class="claim-conformance-copy-icon" viewBox="0 0 24 24" width="12" height="12" aria-hidden="true"><rect x="8" y="8" width="14" height="14" rx="2" fill="none" stroke="currentColor" stroke-width="2"/><path d="M4 16V6a2 2 0 0 1 2-2h10" fill="none" stroke="currentColor" stroke-width="2"/></svg><span class="claim-conformance-copy-label">Copy as JSON</span><span class="claim-conformance-copy-caption">the same envelope dossierx check returns</span></button></div></details></article></details><span class="claim-comments-slot" hidden><button type="button" class="comment-chip comment-chip--empty" data-claim-id="widget.contract.uncheckable" aria-controls="commentsPanel" aria-expanded="false" aria-label="add the first comment on this claim"><span class="comment-chip-glyph" aria-hidden="true"><svg class="dx-icon" aria-hidden="true"><use href="#dx-icon-message-circle"/></svg></span> <span class="comment-chip-count">0</span></button></span></div>
 </section>

               </section>
@@ -11637,7 +12203,7 @@
     <div class="issues-body">
       <div id="issuesFindingsCard" class="issues-findings-card"></div>
       <aside id="issuesRailPanel" class="issues-rail-panel">
-        <p class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
+        <p id="issuesRailHeading" class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
         <p id="issuesRailCaveat" class="issues-rail-caveat"></p>
         <div id="issuesRailRows" class="issues-rail-rows"></div>
       </aside>
@@ -11664,9 +12230,10 @@


   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
-    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
+    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody" data-tone="alarm">
       <span id="statusStripSummary" class="status-strip-summary">
         <svg class="dx-icon status-strip-icon" aria-hidden="true"><use href="#dx-icon-triangle-alert"/></svg>
+        <span class="status-strip-dot" aria-hidden="true"></span>
         <span class="status-strip-summary-text">
           <strong id="statusStripTitle" class="status-strip-title"></strong>
         </span>
@@ -11676,6 +12243,13 @@
         <svg class="dx-icon status-strip-action-chevron" aria-hidden="true"><use href="#dx-icon-chevron-right"/></svg>
       </span>
     </button>
+    <div id="statusStripCard" class="status-strip-card">
+      <div class="status-strip-card-head">
+        <span id="statusStripCardEyebrow" class="status-strip-card-eyebrow">Needs you here</span>
+        <span id="statusStripCardCount" class="status-strip-card-count"></span>
+      </div>
+      <div id="statusStripFindings" class="status-strip-card-findings"></div>
+    </div>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>

@@ -11759,8 +12333,6 @@
       // each SSE re-render. It attaches NO listeners — those are delegated once
       // on surviving nodes below, so re-running this never double-binds.
       function initViewer() {
-        // Fragment swaps replace <main>; drop the stale mount pointer.
-        mountedSurfaceID = null;
         // Two-level show/hide: one .module-section per sidebar .sec-tab, and
         // (when a module has more than one facet) one .claim-group per .subtab
         // inside that module's .sub-nav. A single-facet module still has exactly
@@ -11887,7 +12459,6 @@
       // surface on first paint; smaller fixtures mount every surface so print,
       // theme probes, and live-reload witnesses that call getElementById keep
       // working without visiting every facet first.
-      var mountedSurfaceID = null;
       var SOFT_MOUNT_MIN_CLAIMS = 80;

       function claimCorpusSize() {
@@ -11915,7 +12486,6 @@
         var host = group.querySelector(':scope > [data-dossierx-surface-host]');
         var tmpl = group.querySelector(':scope > template.dossierx-surface-template');
         if (!host || !tmpl) {
-          mountedSurfaceID = surfaceID;
           return;
         }
         if (!host.querySelector('.claim')) {
@@ -11923,13 +12493,31 @@
           if (typeof window.dossierxEnhanceSystemRecord === 'function') {
             window.dossierxEnhanceSystemRecord();
           }
+          // The edited-since-approval chips and panels are attached HERE, at
+          // mount, and BEFORE the clamps below — not left to the next
+          // renderStatusStrip pass.
+          //
+          // They insert a node above each edited claim's body, so attaching
+          // them later moves everything under them. Two things read that
+          // height and cannot be allowed to read it early: a deep link has
+          // already scrolled to its claim, so the panels appearing afterwards
+          // slid the page out from under the sticky header; and the
+          // source-note clamps measure scrollHeight against clientHeight, so
+          // a mid-measurement insertion decides "show more" from a layout
+          // that is about to change. Decorating first means the surface is at
+          // its final height before anything measures or scrolls it.
+          renderApprovedEdits();
           // Source-note clamps observe live nodes only; re-arm after every
           // first-time mount so a freshly revealed surface is measured.
           if (typeof mountSourceNoteClamps === 'function') {
             mountSourceNoteClamps();
           }
         }
-        mountedSurfaceID = surfaceID;
+        // The no-template path (an eagerly rendered surface) never enters the
+        // block above, and still has claims to decorate. renderApprovedEdits
+        // is idempotent — every node it creates is either replaced wholesale
+        // or guarded — so the second call costs nothing on the path that did.
+        renderApprovedEdits();
       }

       function mountAllSurfaces() {
@@ -12191,10 +12779,8 @@
         if (!toastEl) { return; }
         toastEl.textContent = msg; // textContent — error text is never trusted markup
         toastEl.hidden = false;
-        toastEl.classList.add('comments-toast--show');
         if (toastTimer) { window.clearTimeout(toastTimer); }
         toastTimer = window.setTimeout(function () {
-          toastEl.classList.remove('comments-toast--show');
           toastEl.hidden = true;
         }, 4000);
       }
@@ -13035,21 +13621,30 @@
       // claim has no edges".

       var stripEl = document.getElementById('statusStrip');
-      var stripToggle = document.getElementById('statusStripToggle');
-      var stripSummary = document.getElementById('statusStripSummary');
-      var stripTitle = document.getElementById('statusStripTitle');
       // retry fix 21 (this lane's third attempt): #statusStripNote (the
       // "Critical 0 · Needs you 3 · ..." tally) is REMOVED per the
       // coordinator's ruling — no 02/03/04 board draws it, the chips
       // already carry every count it repeated — and dropped from shell.html
       // along with its element lookup here.
-      var stripAction = document.getElementById('statusStripAction');
+      //
+      // THE HEAD HAS TWO FORMS, ONE PER WIDTH, and both are written on every
+      // paint. #statusStripToggle is the desktop band — one summary sentence
+      // with "Show issues" beside it — and #statusStripCard is the phone form
+      // (Paper EH1-0), a card with one ROW PER FINDING. CSS shows one; see
+      // shell.html for why the choice is a media query and not a width read
+      // in here.
+      //
+      // Both are filled unconditionally rather than behind a matchMedia
+      // check. The cost is a few DOM nodes nobody sees; the alternative is a
+      // resize listener and a breakpoint duplicated out of the stylesheet,
+      // which is one more thing to keep in step for no gain.
+      var stripToggle = document.getElementById('statusStripToggle');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripCard = document.getElementById('statusStripCard');
+      var stripCardCount = document.getElementById('statusStripCardCount');
+      var stripFindings = document.getElementById('statusStripFindings');
       var stripBody = document.getElementById('statusStripBody');
       var lastStatusData = null;
-      // Paper starts every status strip collapsed. stripExpanded then stays
-      // sticky across refreshes, so a poll (or an SSE tick) never takes the
-      // disclosure state away from the reader.
-      var stripExpanded = false;

       function countLabel(n, word) {
         return n + ' ' + word + (n === 1 ? '' : 's');
@@ -13256,12 +13851,51 @@
       // demoted slug, and the fixed-width count slot — rather than three
       // loose wrapping siblings. See style.css's .status-finding-dot /
       // .status-finding-text for the layout this DOM shape enables.
+      // editChangeSummary is the mono line under an Issues row for a claim
+      // rewritten since approval (Paper BR5-0 / EUD-0): what moved, and when
+      // the approval it moved from was given.
+      function editChangeSummary(change) {
+        var parts = [];
+        if (!change.content_retained) {
+          parts.push('approved wording not retained');
+        } else if (change.changed_passages) {
+          parts.push(countLabel(change.changed_passages, 'passage') + ' changed');
+        } else if ((change.field_changes || []).length) {
+          parts.push(countLabel(change.field_changes.length, 'field') + ' moved');
+        }
+        var approved = editDateLabel(change.approved_at);
+        if (approved) { parts.push('approved ' + approved); }
+        return parts.join(' · ');
+      }
+
+      // goToClaimChanges leaves the Issues screen for the claim itself, with
+      // the Changes state showing — the row said "See changes", so landing
+      // on the claim with the diff put away would not be what it offered.
+      function goToClaimChanges(id, change) {
+        closeIssuesView();
+        if (window.location.hash !== '#' + id) { window.location.hash = '#' + id; }
+        var card = document.getElementById(id);
+        if (card && change) { paintApprovedEdit(card, change, true); }
+        if (card && typeof card.scrollIntoView === 'function') { card.scrollIntoView({ block: 'start' }); }
+      }
+
       function renderStatusGroup(group) {
         var row = el('li', 'status-finding status-finding--group');
         row.setAttribute('data-severity', group.severity);
+        // A row whose cause is an edit since approval is draft work, not an
+        // alarm (Paper EUD-0 draws its dot and action in the draft hue).
+        var edits = offlineApprovedEdits();
+        var editedOwner = group.ownerModuleClaimID && edits[group.ownerModuleClaimID] ? group.ownerModuleClaimID : '';
+        var isEditCause = group.severity === 'needs_you' && group.kind === 'unapproved_edit' && !!editedOwner;
+        row.setAttribute('data-tone', isEditCause ? 'draft' : 'alarm');
         row.appendChild(el('span', 'status-finding-dot'));
         var text = el('span', 'status-finding-text');
-        text.appendChild(textEl('span', 'status-finding-rule', group.title || 'Issue'));
+        // Paper EUD-0 names the claim and what happened to it in one line:
+        // "<Title> was approved and is being rewritten".
+        var title = isEditCause
+          ? readinessClaimLabel(editedOwner) + ' was approved and is being rewritten'
+          : (group.title || 'Issue');
+        text.appendChild(textEl('span', 'status-finding-rule', title));
         var ids = Object.keys(group.claimIDs).sort();
         // Paper 1KX-0: every row carries a second, demoted line under its
         // title holding the dotted claim id of the claim the reader must go
@@ -13279,11 +13913,52 @@
         if (ownerID) {
           text.appendChild(textEl('span', 'status-finding-claim', ownerID));
         }
+
+        // The third line (Paper board 15, F and I). A Needs-you row names its
+        // cause KIND in mono, because the sentence alone cannot distinguish
+        // "someone never wrote this" from "someone approved it and then
+        // moved it". A blocker row whose owner was approved and is being
+        // rewritten says so under the row it already had (BR5-0): no new
+        // group and no new severity, one line added to the existing row.
+        var detail = null;
+        if (group.severity === 'needs_you') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind', group.kind || 'review'));
+          if (isEditCause) {
+            detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          }
+        } else if (editedOwner && group.severity === 'blocker') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind status-finding-kind--edited', 'Edited · was approved'));
+          detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          var link = textEl('button', 'status-finding-link', 'See changes');
+          link.type = 'button';
+          link.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(editedOwner, edits[editedOwner]);
+          });
+          detail.appendChild(link);
+        }
+        if (detail) { text.appendChild(detail); }
         row.appendChild(text);
-        // retry fix 7: 04 §4.8 "Count label" / §6 "Row count" promotes the
-        // phrase INTO the count slot as a bare `N claims`, never `blocks N
-        // claim(s)` — that longer phrase was written for a full-width third
-        // line, which the fixed 104px slot (style.css) no longer is.
+
+        // The right-hand slot. A blocker row keeps its count (04 §4.8: the
+        // phrase promoted INTO the 104px slot as a bare `N claims`). A
+        // Needs-you row carries its way in instead (Paper ETC-0 / EUD-0):
+        // the thing waiting is one claim, and the count would always be 1.
+        if (group.severity === 'needs_you' && group.ownerModuleClaimID) {
+          var owner = group.ownerModuleClaimID;
+          var action = textEl('button', 'status-finding-action', isEditCause ? 'See changes' : 'Open claim');
+          action.type = 'button';
+          action.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(owner, isEditCause ? edits[owner] : null);
+          });
+          row.appendChild(action);
+          return row;
+        }
         var n = ids.length || group.count;
         row.appendChild(textEl('span', 'status-finding-msg', n + ' claim' + (n === 1 ? '' : 's')));
         return row;
@@ -13382,7 +14057,7 @@
         var label = card && card.querySelector('.k .label');
         if (label) {
           var copy = label.cloneNode(true);
-          copy.querySelectorAll('.pill, .k-id, .claim-collapse-chevron').forEach(function (node) { node.remove(); });
+          copy.querySelectorAll('.pill, .k-id').forEach(function (node) { node.remove(); });
           var rendered = (copy.textContent || '').replace(/\s+/g, ' ').trim();
           if (rendered) { return rendered; }
         }
@@ -13409,7 +14084,12 @@
           approval_content_drift: 'This claim changed after approval',
           approval_missing: 'This claim has no approval record',
           approval_released: 'This claim\'s approval was released',
-          approval_unknown: 'This claim\'s approval state is unknown'
+          approval_unknown: 'This claim\'s approval state is unknown',
+          // Deliberately not "changed after approval" (approval_content_drift's
+          // wording): that names a LOCKED claim whose bytes no longer match a
+          // STANDING approval, which is the tamper finding. This names the
+          // honest, intended act — unlock, rewrite, re-lock — caught mid-way.
+          unapproved_edit: 'This claim was approved, then rewritten'
         };
         return labels[record.kind] || String(record.kind || 'Readiness obstacle').replace(/_/g, ' ');
       }
@@ -13917,12 +14597,397 @@
         } catch (_) { return {}; }
       }

+      // ---- Unapproved edits -------------------------------------------------
+      //
+      // The state these three functions draw is the ordinary one, and it was
+      // the one the viewer could not show. A claim is locked; someone unlocks
+      // it, rewrites it, and has not locked it again yet. The new wording
+      // replaced the old in the file, so the reading view showed text with
+      // nothing to compare it against and a chip reading DRAFT — the same chip
+      // a claim nobody ever approved carries. "Never written" and "approved,
+      // then moved" are different jobs for a reviewer, and one word was being
+      // used for both.
+      //
+      // The engine now answers both questions: readiness emits an
+      // `unapproved_edit` cause (so the claim reaches the Issues screen on its
+      // own, without needing a dependent to notice for it), and the graph
+      // payload carries the line diff from the approved body to the current
+      // one. Everything below is presentation of those two facts. It computes
+      // nothing about whether an edit is allowed, and never writes.
+
+      // offlineApprovedEdits reads the per-claim approvaledit.Change records
+      // off the SAME rendered graph payload offlineReadiness reads. There is
+      // deliberately no /api/status path for this: a checked-in file:// export
+      // has no endpoint to ask, and a panel that appeared only under
+      // "dossierx serve" would be missing exactly where a reviewer reads a
+      // repository they did not build.
+      function offlineApprovedEdits() {
+        var node = document.getElementById('dossierx-graph');
+        if (!node) { return {}; }
+        try {
+          var payload = JSON.parse(node.textContent || '{}');
+          var out = {};
+          (payload.nodes || []).forEach(function (graphNode) {
+            if (graphNode.id && graphNode.approved_edit) { out[graphNode.id] = graphNode.approved_edit; }
+          });
+          return out;
+        } catch (_) { return {}; }
+      }
+
+      // editDateLabel turns an RFC3339 instant into the board's own short
+      // form ("12 Sep"). It is deliberately not a full date: the panel's job
+      // is to place the approval in the reader's recent memory, and a
+      // four-part date would be the widest thing on a row that is otherwise
+      // a sentence.
+      function editDateLabel(iso) {
+        if (!iso) { return ''; }
+        var d = new Date(iso);
+        if (isNaN(d.getTime())) { return String(iso).slice(0, 10); }
+        return d.getDate() + ' ' + ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun',
+          'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][d.getMonth()];
+      }
+
+      // approvedEditHasDiff reports whether there is a second wording to show
+      // at all.
+      //
+      // Two states look like an edit and have nothing to put behind a
+      // "Changes" tab. A record written before the approved wording was kept
+      // proves the text moved and does not carry what it moved from. A claim
+      // whose hash moved because its rests_on or build_role moved has an
+      // approved wording identical to its current one. In both, the panel has
+      // one sentence to say and no second version of the claim — so there is
+      // no switch, and the claim's own body stays on screen.
+      //
+      // Offering the switch anyway is what this replaces, and it was worse
+      // than a useless control: choosing "Changes" hid the body and put the
+      // sentence in its place, so the claim rendered with no content at all.
+      function approvedEditHasDiff(change) {
+        return !!(change.content_retained &&
+          (change.hunks || []).length &&
+          (change.changed_passages || 0) > 0);
+      }
+
+      // approvedEditDiff builds the passage-by-passage view: unchanged
+      // passages as ordinary prose, and each changed one as the approved
+      // passage above the current one, tinted and ruled.
+      //
+      // The HTML is produced by the ENGINE (internal/approvaledit), not here,
+      // because internal/render/markdown is this project's only markdown
+      // renderer and the viewer has none. Shipping the source and parsing it
+      // in the browser would mean growing a second renderer that could
+      // disagree with the first about the same claim, on the same page.
+      function approvedEditDiff(change) {
+        var body = el('div', 'claim-edit-diff');
+        (change.hunks || []).forEach(function (hunk) {
+          var cls = hunk.op === 'remove' ? 'claim-edit-passage claim-edit-passage--removed'
+            : hunk.op === 'add' ? 'claim-edit-passage claim-edit-passage--added'
+            : 'claim-edit-passage';
+          var block = el('div', cls);
+          // innerHTML, and only here. The value came from
+          // internal/render/markdown, which is the escaping boundary every
+          // other claim body in this viewer crosses — the same function, over
+          // the same author bytes, reached through the same payload as the
+          // readiness projection beside it. It is exactly as trusted as the
+          // body it sits above, and no more.
+          block.innerHTML = hunk.html || '';
+          if ((hunk.changed || []).length) {
+            block.setAttribute('aria-label',
+              (hunk.op === 'remove' ? 'Removed since approval: ' : 'Added since approval: ') +
+              hunk.changed.join(', '));
+          }
+          body.appendChild(block);
+        });
+        var fields = approvedEditFields(change);
+        if (fields) { body.appendChild(fields); }
+        return body;
+      }
+
+      // approvedEditCurrent builds the OTHER state of the same surface: the
+      // current wording, passage by passage, with the passage that differs
+      // from the approval carrying a draft-coloured rule and no tint (Paper
+      // DPZ-0: "the same claim with the diff put away, reading as ordinary
+      // prose, with an amber rule marking where the difference sits").
+      //
+      // It is rendered from the same hunks as the diff rather than by
+      // un-hiding the claim's own body, because the body cannot say WHERE the
+      // difference sits: the rule is the one thing this state adds, and the
+      // body has no passage boundaries to hang it on.
+      function approvedEditCurrent(change) {
+        var body = el('div', 'claim-edit-current');
+        (change.hunks || []).forEach(function (hunk) {
+          if (hunk.op === 'remove') { return; }
+          var block = el('div', hunk.op === 'add'
+            ? 'claim-edit-passage claim-edit-passage--current'
+            : 'claim-edit-passage');
+          // Same escaping boundary as approvedEditDiff: engine-rendered
+          // markdown, the same bytes the diff shows.
+          block.innerHTML = hunk.html || '';
+          if (hunk.op === 'add') {
+            block.setAttribute('aria-label', 'This passage differs from the approved wording');
+          }
+          body.appendChild(block);
+        });
+        return body;
+      }
+
+      // approvedEditFields draws the non-body fields that moved, each as its
+      // approved YAML above its current YAML with the words that moved marked.
+      //
+      // It is here because "Also changed: embodiment, audit_notes." was not an
+      // answer. For a claim whose prose never moved that sentence WAS the whole
+      // panel, and it named the change without showing it — the reader was told
+      // a field they cannot see had moved in a way they cannot see, and had to
+      // go and read the file to find out what they were being asked about. Six
+      // of the fourteen claims this feature was built for are in exactly that
+      // state.
+      //
+      // The blocks are drawn as text and not as prose. A field is YAML, so it
+      // is escaped by the engine (internal/approvaledit/fields.go) and shown in
+      // the monospace the file itself would show; running it through the
+      // markdown renderer would turn a list into a list and a `*` into
+      // emphasis, and stop it being the thing on disk.
+      function approvedEditFields(change) {
+        var changes = change.field_changes || [];
+        if (!changes.length) { return null; }
+        var wrap = el('div', 'claim-edit-fields');
+
+        var toggle = el('button', 'claim-edit-fields-toggle');
+        toggle.type = 'button';
+        var list = el('div', 'claim-edit-fields-list');
+        var open = true;
+        function paint() {
+          toggle.setAttribute('aria-expanded', String(open));
+          toggle.textContent = (open ? 'Hide' : 'Show') + ' the ' +
+            (changes.length === 1 ? 'field' : changes.length + ' fields') + ' that moved';
+          list.hidden = !open;
+        }
+        toggle.addEventListener('click', function (event) {
+          event.preventDefault();
+          open = !open;
+          paint();
+        });
+
+        changes.forEach(function (field) {
+          var row = el('div', 'claim-edit-field');
+          row.appendChild(textEl('span', 'claim-edit-field-name', field.field));
+          if (field.truncated) {
+            row.appendChild(textEl('p', 'claim-edit-field-note',
+              'This field moved. It is too large to show here; read it in the claim file.'));
+            list.appendChild(row);
+            return;
+          }
+          (field.hunks || []).forEach(function (hunk) {
+            var cls = hunk.op === 'remove' ? 'claim-edit-field-hunk claim-edit-field-hunk--removed'
+              : hunk.op === 'add' ? 'claim-edit-field-hunk claim-edit-field-hunk--added'
+              : 'claim-edit-field-hunk';
+            var block = el('pre', cls);
+            // innerHTML over engine-escaped text. internal/approvaledit's
+            // field renderer HTML-escapes the YAML before it marks anything,
+            // so the only tags that can be in here are the mark spans it
+            // wrote itself — a field holding <script> arrives as text.
+            block.innerHTML = hunk.html || '';
+            if ((hunk.changed || []).length) {
+              block.setAttribute('aria-label',
+                (hunk.op === 'remove' ? 'Removed from ' : 'Added to ') + field.field + ': ' +
+                hunk.changed.join(', '));
+            }
+            row.appendChild(block);
+          });
+          list.appendChild(row);
+        });
+
+        paint();
+        wrap.appendChild(toggle);
+        wrap.appendChild(list);
+        return wrap;
+      }
+
+      // approvedEditNote is what a claim with no second wording says instead of
+      // offering a switch. It sits under the bar, beside the claim's own body
+      // rather than in place of it.
+      function approvedEditNote(change) {
+        var wrap = el('div', 'claim-edit-note');
+        if (!change.content_retained) {
+          wrap.appendChild(textEl('p', 'claim-edit-note-text',
+            'This claim was approved before the approved wording was kept on the record, so there is nothing to compare against. The approval is released and the text has since changed.'));
+          wrap.appendChild(textEl('p', 'claim-edit-note-hint',
+            'Run dossierx claim recover-approved-content to look for the approved revision in this project\u2019s git history.'));
+          return wrap;
+        }
+        var fields = change.other_fields || [];
+        wrap.appendChild(textEl('p', 'claim-edit-note-text', fields.length
+          ? 'The wording is unchanged since approval. ' +
+            (fields.length === 1 ? '1 field moved.' : fields.length + ' fields moved.')
+          : 'The wording is unchanged since approval.'));
+        var diff = approvedEditFields(change);
+        if (diff) { wrap.appendChild(diff); }
+        return wrap;
+      }
+
+      // approvedEditBar is the control the board draws in place of the old
+      // disclosure: a tinted row above the body that says what the reader is
+      // looking at, and a two-segment switch between the two states.
+      //
+      // It is a BAR AND NOT A DISCLOSURE, and the difference is which state
+      // is the default. A disclosure says the diff is an aside a reader opens
+      // if they want it; the board says the opposite — a claim that was
+      // approved and then rewritten opens SHOWING what moved, because that is
+      // the thing about it a reader has to know before they read a word of
+      // it. "Current" is the escape hatch, not the resting state.
+      function approvedEditBar(card, change, showingChanges) {
+        var bar = el('div', 'claim-edit-bar');
+        var approved = editDateLabel(change.approved_at);
+        var hasDiff = approvedEditHasDiff(change);
+        bar.appendChild(textEl('span', 'claim-edit-bar-title',
+          !hasDiff || showingChanges ? 'Edited since it was approved' : 'Showing the current wording'));
+
+        var meta;
+        if (!hasDiff || showingChanges) {
+          meta = 'approved ' + approved + (change.approved_by ? ' \u00b7 ' + change.approved_by : '');
+        } else {
+          var passages = change.changed_passages || 0;
+          meta = passages === 1
+            ? '1 passage differs from the approval of ' + approved
+            : passages + ' passages differ from the approval of ' + approved;
+        }
+        bar.appendChild(textEl('span', 'claim-edit-bar-meta', meta));
+
+        // No second wording, no switch. A control whose two states show the
+        // same thing is not a control, and the one that used to be here did
+        // worse than nothing: picking "Changes" hid the claim's body to make
+        // room for a diff that did not exist.
+        if (!hasDiff) { return bar; }
+
+        var group = el('div', 'claim-edit-switch');
+        group.setAttribute('role', 'group');
+        group.setAttribute('aria-label', 'Which wording to show');
+        [['Changes', true], ['Current', false]].forEach(function (pair) {
+          var seg = el('button', 'claim-edit-switch-seg');
+          seg.type = 'button';
+          seg.textContent = pair[0];
+          seg.setAttribute('aria-pressed', String(showingChanges === pair[1]));
+          seg.addEventListener('click', function (event) {
+            event.preventDefault();
+            paintApprovedEdit(card, change, pair[1]);
+          });
+          group.appendChild(seg);
+        });
+        bar.appendChild(group);
+        return bar;
+      }
+
+      // paintApprovedEdit puts one claim card into one of the two states, and
+      // is the only thing that writes either of them, so the bar and the body
+      // can never disagree about which one is showing.
+      function paintApprovedEdit(card, change, showingChanges) {
+        var hasDiff = approvedEditHasDiff(change);
+        showingChanges = hasDiff && showingChanges;
+        var existingBar = card.querySelector(':scope > .claim-edit-bar');
+        // The diff may already be wrapped: the four-line disclosure wraps it
+        // exactly as it wraps a claim body (system-record.js's BODY_LIKE), so
+        // what has to be taken out is the wrapper when there is one.
+        var existingDiff = card.querySelector(
+          ':scope > .claim-edit-diff, :scope > .claim-edit-current, :scope > .claim-edit-note, :scope > .claim-body-disclosure--edit');
+
+        var bar = approvedEditBar(card, change, showingChanges);
+        if (existingBar) { existingBar.replaceWith(bar); }
+        else {
+          var head = card.querySelector(':scope > .k');
+          if (head && head.parentNode) { head.parentNode.insertBefore(bar, head.nextSibling); }
+          else { card.insertBefore(bar, card.firstChild); }
+        }
+        if (existingDiff) { existingDiff.remove(); }
+        if (!hasDiff) {
+          bar.parentNode.insertBefore(approvedEditNote(change), bar.nextSibling);
+        } else {
+          bar.parentNode.insertBefore(
+            showingChanges ? approvedEditDiff(change) : approvedEditCurrent(change), bar.nextSibling);
+          // Hand the new surface to the disclosure so it is measured and
+          // clamped like any other body. Without this the diff renders at
+          // full height while the claim beside it truncates at four lines,
+          // which is the same claim behaving as two components.
+          if (typeof window.dossierxEnhanceSystemRecord === 'function') {
+            window.dossierxEnhanceSystemRecord();
+          }
+        }
+        // While there is a second wording the claim's own body is off screen
+        // in BOTH states: Changes shows the diff and Current shows the same
+        // passages with the changed one ruled. The body comes back only for
+        // a claim with nothing to compare against (no switch).
+        //
+        // The body is hidden by a class ON THE CARD, and not by setting
+        // `hidden` on the body itself. Which element the body IS moves: the
+        // long-body disclosure wraps
+        // .claim-body inside .claim-body-disclosure after first paint, so a
+        // handle taken before the wrap and a handle taken after it are
+        // different nodes. Hiding whichever one a selector matched at the
+        // time left the first one hidden and the second one shown, and the
+        // switch stopped working on exactly the claims long enough to be
+        // wrapped. A class on the card is one state, in one place, whatever
+        // the body is wearing.
+        //
+        // It is also why the body is hidden rather than removed: it carries
+        // the source-note clamps and that disclosure, and tearing it out
+        // would take their state with it every time the reader flipped.
+        card.classList.toggle('claim--edit-view', hasDiff);
+      }
+
+      // renderApprovedEdits upgrades each edited claim's chip and puts its
+      // card into the Changes state. It is driven from mountSurface (so the
+      // surface reaches its final height before anything measures or scrolls
+      // it) and again from renderStatusStrip, so everything it creates is
+      // either guarded or left alone on a repaint.
+      function renderApprovedEdits() {
+        var edits = offlineApprovedEdits();
+        Object.keys(edits).forEach(function (id) {
+          var card = document.getElementById(id);
+          if (!card) { return; }
+          var change = edits[id];
+
+          // The chip. It stays .pill.pv — the claim IS a draft, and inventing
+          // a fourth colour for it would say the state is unrelated to the
+          // one the reader already knows. Only the word changes, because only
+          // the word was wrong: DRAFT is true and incomplete, and a reviewer
+          // deciding what to open next needs the part it leaves out.
+          var pill = card.querySelector('.k .label .pill');
+          if (pill && pill.getAttribute('data-dx-edited') !== '1') {
+            pill.setAttribute('data-dx-edited', '1');
+            pill.setAttribute('title', 'This claim held an approval, which was released when it was unlocked. What is written now differs from what was approved.');
+            // No padlock. Paper (CDJ-0, CIY-0) draws the chip as the word
+            // alone: the open padlock is DRAFT's glyph, and this chip exists
+            // to say the state is NOT plain draft.
+            pill.textContent = '';
+            pill.appendChild(textEl('span', '', 'Edited \u00b7 was approved'));
+          }
+
+          // The bar and the body state, painted only when the bar is not
+          // already there: a repaint must not take the reader's own "Current"
+          // choice away from them on the next poll.
+          if (!card.querySelector(':scope > .claim-edit-bar')) {
+            paintApprovedEdit(card, change, true);
+          }
+        });
+      }
+
+      // approvedEditIDsIn returns the claims in the id set that have been
+      // rewritten since the approval their unlock released.
+      //
+      // It is scoped to the facet on screen, by the SAME id set the rest of
+      // the strip scopes itself with — a count of claims on a page the reader
+      // is not looking at is a count they cannot act on. The set is a
+      // null-prototype object keyed by id (activeFacetClaimIDs), so this
+      // indexes it rather than calling Set or Array methods it does not have.
+      function approvedEditIDsIn(claimIDs) {
+        var ids = claimIDs || {};
+        return Object.keys(offlineApprovedEdits()).filter(function (id) { return ids[id]; });
+      }
+
       // renderStatusStrip paints one /api/status payload. An empty verdict hides
       // the strip entirely rather than showing a green badge: the viewer already
       // reads as "fine" by default, and a persistent all-clear chip would be one
       // more thing to stop noticing.
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary || !stripTitle) { return; }
+        if (!stripEl || !stripBody || !stripCard || !stripFindings || !stripTitle) { return; }
         lastStatusData = data || {};
         var claimIDs = activeFacetClaimIDs();
         var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
@@ -13945,6 +15010,7 @@
         // PREVIOUS pass's state, since this function only writes it below.
         var bannerShowing = groups.length > 0 && actionable;
         renderClaimReadiness(lastStatusData.readiness || offlineReadiness(), bannerShowing);
+        renderApprovedEdits();
         if (!groups.length || !actionable) {
           stripEl.hidden = true;
           stripEl.classList.remove('status-strip--integrity', 'status-strip--lint');
@@ -14038,12 +15104,18 @@
           recordEl.classList.add('status-group--approval-record');
           stripBody.appendChild(recordEl);
         }
+        // Under the Needs-you filter the group's weight phrase counts what is
+        // waiting on the reader, not what is blocked (Paper EMP-0: "3 of 4
+        // need you here").
+        var needsTotal = countSeverity(groups, 'needs_you');
         moduleOrder.forEach(function (moduleID) {
           var rows = byModule[moduleID].slice().sort(function (a, b) {
             return statusGroupClaimCount(b) - statusGroupClaimCount(a);
           });
           var weight = uniqueClaimCount(rows);
-          var note = 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
+          var note = stripSeverityFilter === 'needs_you'
+            ? weight + ' of ' + needsTotal + ' need you here'
+            : 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
           stripBody.appendChild(findingGroup(moduleLabel(moduleID), rows.map(renderStatusGroup), note));
         });

@@ -14053,38 +15125,189 @@
         // used to repeat as a second string. statusChipLine, the function
         // that built that string, is now dead (no other call site) and is
         // deleted.
-        var headline = blockerHeadline(groups);
-        var needsYou = countSeverity(groups, 'needs_you') > 0;
-        if (ledger.length) {
-          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
-        } else if (lintErrors.length && !headline && !needsYou) {
-          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
-        } else if (headline) {
-          stripTitle.textContent = headline;
-        } else {
-          stripTitle.textContent = countLabel(groups.length, 'grouped issue') + ' in this facet';
-        }
+        //
+        // Both forms of the head, every paint. The desktop band carries the
+        // single headline sentence it always did; the phone card carries one
+        // row per finding. They are derived from the SAME groups, ledger and
+        // lint lists a line apart, so the two can differ in shape and never
+        // in what they say.
+        var rows = statusStripRows(groups, ledger, lintErrors, claimIDs);
+        renderStatusStripCard(rows);
+        // The band gets the same rows, one full-bleed row each (Paper board
+        // 15, C2M-0 / C92-0), in the same order — most serious first — so the
+        // two forms differ in shape and never in what they say.
+        renderStatusStripBand(rows);

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        // Paper's default reading view always starts with the findings body
-        // collapsed. Critical and needs-you counts still determine the
-        // truthful summary/chips above; they do not take control of the
-        // reader's disclosure state. Once the reader opens the strip, the
-        // existing stripExpanded state remains sticky across later polls.
-        setStripExpanded(stripExpanded);
+        // #statusStripBody is populated above and stays hidden: the Issues
+        // screen MOVES its children into itself (issuesSyncFromStrip), so it
+        // must exist and must not be shown here.
+        if (stripBody) { stripBody.hidden = true; }
         positionStatusStrip();
         stripEl.hidden = false;
       }

-      function setStripExpanded(expanded) {
-        stripExpanded = !!expanded;
-        if (stripBody) { stripBody.hidden = !stripExpanded; }
-        if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
-        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
-        if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
+      // statusStripRows turns the facet's findings into the card's rows —
+      // one row per KIND of thing waiting, never one per claim.
+      //
+      // The rows are the summary sentences the strip already computed, each
+      // now carrying its own hue instead of four of them competing to be the
+      // one headline. `tone` is the row's colour family, and it is the only
+      // thing the card's tint rule reads: 'alarm' for anything the approval
+      // record or the dependency chain is refusing, 'draft' for work in
+      // progress that nobody has approved yet.
+      function statusStripRows(groups, ledger, lintErrors, claimIDs) {
+        var rows = [];
+        if (ledger.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(ledger.length, 'approval record issue') + ' in this facet need' +
+              (ledger.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        var headline = blockerHeadline(groups);
+        if (headline) {
+          rows.push({ tone: 'alarm', severity: 'blocker', text: headline });
+        }
+        if (lintErrors.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(lintErrors.length, 'issue') + ' in this facet need' +
+              (lintErrors.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        // Claims rewritten since approval. This is the row the old
+        // single-sentence head had no space for, and the reason it became a
+        // card: it is amber where everything above it is red, and one tinted
+        // surface cannot make two severity claims at once.
+        var edited = approvedEditIDsIn(claimIDs);
+        if (edited.length) {
+          rows.push({
+            tone: 'draft', severity: 'needs_you',
+            text: countLabel(edited.length, 'claim') + ' ' + (edited.length === 1 ? 'has' : 'have') +
+              ' edits that have not been approved'
+          });
+        }
+        if (!rows.length) {
+          rows.push({
+            tone: 'alarm', severity: '',
+            text: countLabel(groups.length, 'grouped issue') + ' in this facet'
+          });
+        }
+        return rows;
       }

+      // renderStatusStripCard paints the head. Tinted while every row shares
+      // a tone, neutral the moment they disagree — Paper EH1-0's own rule,
+      // and the reason it is a rule: a tinted surface IS a severity claim,
+      // so a card holding a red fact and an amber one has to stop making it
+      // and let the dots carry the colour instead.
+      // statusStripRowAction names the way in each row offers. A blocked row
+      // opens the Issues screen as it always did; an edited-since-approval
+      // row says what it is for (Paper CDS-0: "Review changes").
+      function statusStripRowAction(row) {
+        return row.tone === 'draft' ? 'Review changes' : 'Show issues';
+      }
+
+      // openIssuesForBandRow: a blocked row opens the screen UNFILTERED — its
+      // sentence names one finding but the band stands for the whole facet.
+      // An edited row names exactly what it filters to (the claims waiting on
+      // the reader), which is what earns it the filter, same as a card row.
+      function openIssuesForBandRow(tone, severity) {
+        stripSeverityFilter = tone === 'draft' ? (severity || '') : '';
+        renderStatusStrip(lastStatusData);
+        openIssuesView();
+      }
+
+      // renderStatusStripBand paints the DESKTOP form: the static first row
+      // (#statusStripToggle, so the ids the rest of the viewer and its tests
+      // address stay put) takes the first finding, and one more
+      // .status-strip-head button is appended per further finding. Each row
+      // carries data-tone, which is the only thing the stylesheet reads to
+      // pick its tint, its icon and its action colour.
+      function renderStatusStripBand(rows) {
+        if (!stripToggle || !stripTitle || !stripEl) { return; }
+        var first = rows[0];
+        stripToggle.setAttribute('data-tone', first.tone);
+        stripToggle.setAttribute('data-severity', first.severity || '');
+        stripTitle.textContent = first.text;
+        var firstAction = document.getElementById('statusStripAction');
+        if (firstAction) { firstAction.textContent = statusStripRowAction(first); }
+
+        stripEl.querySelectorAll(':scope > .status-strip-head--row').forEach(function (node) { node.remove(); });
+        rows.slice(1).forEach(function (row) {
+          var btn = el('button', 'status-strip-head status-strip-head--row');
+          btn.type = 'button';
+          btn.setAttribute('data-tone', row.tone);
+          btn.setAttribute('data-severity', row.severity || '');
+          var summary = el('span', 'status-strip-summary');
+          var icon = dxIcon('triangle-alert');
+          icon.classList.add('status-strip-icon');
+          summary.appendChild(icon);
+          summary.appendChild(el('span', 'status-strip-dot'));
+          var text = el('span', 'status-strip-summary-text');
+          text.appendChild(textEl('strong', 'status-strip-title', row.text));
+          summary.appendChild(text);
+          btn.appendChild(summary);
+          var actionRow = el('span', 'status-strip-action-row');
+          actionRow.appendChild(textEl('span', 'status-strip-action', statusStripRowAction(row)));
+          btn.appendChild(actionRow);
+          btn.addEventListener('click', function (event) {
+            event.preventDefault();
+            openIssuesForBandRow(row.tone, row.severity);
+          });
+          // After the previous row and before the phone card, so the band is
+          // one stack of rows whatever the count.
+          var before = stripCard || stripBody;
+          if (before && before.parentNode === stripEl) { stripEl.insertBefore(btn, before); }
+          else { stripEl.appendChild(btn); }
+        });
+      }
+
+      function renderStatusStripCard(rows) {
+        if (!stripCard || !stripFindings) { return; }
+        var tones = {};
+        rows.forEach(function (row) { tones[row.tone] = true; });
+        var toneKeys = Object.keys(tones);
+        var tinted = toneKeys.length === 1 ? toneKeys[0] : '';
+        stripCard.classList.toggle('status-strip-card--alarm', tinted === 'alarm');
+        stripCard.classList.toggle('status-strip-card--draft', tinted === 'draft');
+        if (stripCardCount) { stripCardCount.textContent = String(rows.length); }
+
+        stripFindings.textContent = '';
+        rows.forEach(function (row) {
+          // A button, not a div with a handler: each row is a way into the
+          // Issues screen and has to be reachable by keyboard and named to a
+          // screen reader like the one control it is.
+          var el_ = el('button', 'status-strip-finding');
+          el_.type = 'button';
+          el_.setAttribute('data-tone', row.tone);
+          var dot = el('span', 'status-strip-finding-dot');
+          dot.setAttribute('aria-hidden', 'true');
+          el_.appendChild(dot);
+          el_.appendChild(textEl('span', 'status-strip-finding-text', row.text));
+          // A text glyph, not the 14px icon: EH1-0 draws the row's way-in as
+          // a 12px "›" in the card's own hue.
+          var chev = textEl('span', 'status-strip-finding-chevron', '›');
+          chev.setAttribute('aria-hidden', 'true');
+          el_.appendChild(chev);
+          el_.addEventListener('click', function (event) {
+            event.preventDefault();
+            // Filtered to the row's own severity. On a real corpus the
+            // unfiltered screen is hundreds of dependency rows and the thing
+            // this row named is somewhere inside them; a way in that lands a
+            // reader in a list they then have to search has not answered the
+            // question it asked. Same stripSeverityFilter the chips set, so
+            // the chip is pressed on arrival and one more click clears it.
+            stripSeverityFilter = row.severity || '';
+            renderStatusStrip(lastStatusData);
+            openIssuesView();
+          });
+          stripFindings.appendChild(el_);
+        });
+      }
+
       // refreshStatus polls the endpoint. A FAILED poll deliberately leaves the
       // last verdict on screen untouched: a transient fetch failure (the server
       // restarting mid-reload) must never be able to clear an integrity warning,
@@ -14098,25 +15321,20 @@
       }

       // R09.6 (Paper ZP-0): "Issues is its own screen, not an expansion. The
-      // banner is the only way in." Paper backs that with node evidence:
-      // 56T-0, 574-0 and 6P-0 draw ONE state — the sentence plus "Show
-      // issues" — the desktop banner is a single flex ROW with no slot for a
-      // body, the string "Hide issues" does not exist anywhere in the design
-      // file, and 04 section 4 records that no board in the group draws an
-      // expanded state. The inline expand listener that lived here is gone;
-      // the toggle's only job is opening the Issues screen.
+      // banner is the only way in." No board in the group draws an expanded
+      // banner, and the string "Hide issues" does not exist anywhere in the
+      // design file, so the strip has no inline disclosure at all.
       //
-      // setStripExpanded is deliberately KEPT: renderStatusStrip still calls
-      // it every poll with a permanently-false stripExpanded, and that is
-      // what holds #statusStripBody hidden, aria-expanded="false" and the
-      // action reading "Show issues".
+      // EH1-0 then replaced the one sentence with one row per finding, and
+      // the entry point went with it: each row opens the Issues screen,
+      // filtered to that row's own severity. "The banner is the only way in"
+      // still holds — the banner simply has more than one door now, because
+      // a facet routinely has more than one kind of thing waiting.
       //
       // #statusStripBody is never emptied, removed or left unpopulated:
       // renderStatusStrip still builds into it, issuesSyncFromStrip still
       // MOVES its children into the Issues screen, and closeIssuesView still
-      // moves them back. Only its visibility to the reader is withdrawn — the
-      // children now return into a hidden body instead of an expanded one,
-      // which is the defect this removes.
+      // moves them back. It is simply never shown in place.

       // ================================================================
       // Issues view (04-issues-screen.md) — lane L8a
@@ -14125,9 +15343,8 @@
       // The screen's own chrome around lane L8's status-strip vocabulary:
       // breadcrumb, title/subtitle, scope and sort controls, the findings
       // card and the "WHERE THE BLOCKERS LIVE" rail (04 §3, §4.1-4.3, §4.5,
-      // §4.6, §4.9). R09.6: reached ONLY from "Show issues" — the listener
-      // just below runs AFTER the one directly above (source order), so by
-      // the time it fires `stripExpanded` already holds L8's own new value.
+      // §4.6, §4.9). R09.6: reached ONLY from a row of the banner card, each
+      // of which wires openIssuesView itself as it is built.
       //
       // #issuesFilters and #issuesFindingsCard never build their own copy of
       // the severity chips or the grouped findings list: issuesSyncFromStrip
@@ -14232,11 +15449,59 @@
         var m = issuesModuleWeights(issuesScope).claims;
         issuesFindingsCard.querySelectorAll('.status-group-head-note').forEach(function (note) {
           var match = /^blocks (\d+) of \d+ claims? here$/.exec(note.textContent || '');
-          if (!match) { return; }
-          note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+          if (match) {
+            note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+            return;
+          }
+          // The Needs-you phrase: the same denominator rule, over the rows
+          // the filter is showing (issuesGroupsForScope already applies it).
+          var waiting = /^(\d+) of \d+ need you here$/.exec(note.textContent || '');
+          if (waiting) { note.textContent = waiting[1] + ' of ' + m + ' need you here'; }
         });
       }

+      // issuesCauseWeights is the rail's OTHER ranking (Paper EKL-0, "WHAT IS
+      // WAITING"): under the Needs-you filter the question is what kind of
+      // work is waiting rather than where the blockers live, so the rail
+      // counts causes, by kind, and colours each bar by the kind's hue. A
+      // claim waiting for two reasons is counted under each, and the caveat
+      // says so.
+      var ISSUES_CAUSE_LABELS = {
+        unapproved_edit: 'Unapproved edits',
+        direct_dependency_change: 'Dependency changed',
+        upstream_dependency_review: 'Upstream review',
+        own_flag: 'Flagged for review',
+        own_thread: 'Open review thread',
+        approval_content_drift: 'Approved wording drifted',
+        approval_released: 'Approval released',
+        approval_missing: 'Approval record missing',
+        approval_unknown: 'Approval state unknown'
+      };
+      function issuesCauseWeights(scope) {
+        var groups = issuesGroupsForScope(scope);
+        var byKind = {};
+        var order = [];
+        groups.forEach(function (g) {
+          var kind = g.kind || 'review';
+          if (!Object.prototype.hasOwnProperty.call(byKind, kind)) { byKind[kind] = []; order.push(kind); }
+          byKind[kind].push(g);
+        });
+        var rows = order.map(function (kind) {
+          return {
+            id: kind,
+            label: ISSUES_CAUSE_LABELS[kind] || kind.replace(/_/g, ' '),
+            tone: kind === 'unapproved_edit' ? 'draft' : 'alarm',
+            weight: uniqueClaimCount(byKind[kind])
+          };
+        });
+        rows.sort(function (a, b) { return b.weight - a.weight; });
+        return {
+          rows: rows,
+          claims: uniqueClaimCount(groups),
+          causes: rows.reduce(function (sum, r) { return sum + r.weight; }, 0)
+        };
+      }
+
       // renderIssuesRail (04 §4.9): a ranking with bars, proportional to the
       // facet's blocked-claim TOTAL (§4.9's own measured widths — retry fix
       // 4, coordinator ruling: "rail bars use the spec's rounding basis" —
@@ -14256,10 +15521,15 @@
       // corpus size or scope.
       function renderIssuesRail() {
         if (!issuesRailRowsEl) { return; }
-        var data = issuesModuleWeights(issuesScope);
+        var waiting = stripSeverityFilter === 'needs_you';
+        var data = waiting ? issuesCauseWeights(issuesScope) : issuesModuleWeights(issuesScope);
+        var heading = document.getElementById('issuesRailHeading');
+        if (heading) { heading.textContent = waiting ? 'WHAT IS WAITING' : 'WHERE THE BLOCKERS LIVE'; }
         issuesRailRowsEl.textContent = '';
         var shown = data.rows.slice(0, ISSUES_RAIL_MAX_ROWS);
-        var total = data.claims;
+        // Module bars are proportional to the blocked-claim total; cause bars
+        // to the cause total (EKL-0: 2 of 4 causes draws round(50%)).
+        var total = waiting ? data.causes : data.claims;
         shown.forEach(function (row) {
           var wrap = el('div', 'issues-rail-row');
           var labelRow = el('div', 'issues-rail-row-label');
@@ -14268,6 +15538,7 @@
           wrap.appendChild(labelRow);
           var track = el('div', 'issues-rail-bar-track');
           var fill = el('div', 'issues-rail-bar-fill');
+          if (row.tone) { fill.setAttribute('data-tone', row.tone); }
           fill.style.width = (total ? Math.round((row.weight / total) * 100) : 0) + '%';
           track.appendChild(fill);
           wrap.appendChild(track);
@@ -14277,10 +15548,14 @@
           issuesRailRowsEl.appendChild(textEl('p', 'issues-rail-more',
             (data.rows.length - ISSUES_RAIL_MAX_ROWS) + ' more not shown'));
         }
-        var caveat = data.claims > 0
-          ? (countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
-             ' — a claim blocked through two modules counts under both.')
-          : '';
+        var caveat = '';
+        if (waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ', ' + countLabel(data.causes, 'cause') +
+            ' — a claim can be waiting for more than one reason and is counted under each.';
+        } else if (!waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
+            ' — a claim blocked through two modules counts under both.';
+        }
         if (issuesRailCaveatEl) { issuesRailCaveatEl.textContent = caveat; }
         if (issuesCaveatEl) { issuesCaveatEl.textContent = caveat; }
         // §8 item 5: an empty ranking is a ranking of nothing — the panel is
@@ -14306,7 +15581,13 @@
             (lastStatusData && lastStatusData.readiness) || offlineReadiness(),
             activeFacetClaimIDs(), [], [], [], []
           );
-          issuesSubtitleEl.textContent = blockerHeadline(groups) || 'Nothing in this facet is blocked.';
+          // Under the Needs-you filter the sentence is about the reader's
+          // work, not the facet's blockers (Paper ENQ-0).
+          var waiting = stripSeverityFilter === 'needs_you' ? countSeverity(groups, 'needs_you') : 0;
+          issuesSubtitleEl.textContent = waiting
+            ? countLabel(waiting, 'claim') + (waiting === 1 ? ' is' : ' are') +
+              ' waiting on you in this facet. Each one names what moved and what it was measured against.'
+            : (blockerHeadline(groups) || 'Nothing in this facet is blocked.');
         }
       }

@@ -14354,6 +15635,10 @@
         }
         issuesApplyScopeDenominator();
         renderIssuesRail();
+        // The subtitle follows the filter (a chip click repaints the strip,
+        // which lands here through the observer), so it is re-read on every
+        // sync and not only when the screen opens.
+        issuesSyncHeader();
         if (issuesStripObserver) { issuesStripObserver.observe(stripBody, { childList: true }); }
       }

@@ -14466,14 +15751,20 @@
       if (issuesSortControl) {
         issuesSortControl.addEventListener('click', function (event) { event.preventDefault(); });
       }
-      // "Show issues" (R09.6) opens this view — UNCONDITIONALLY on every
-      // click of this control, whichever label it currently reads. The
-      // strip's own inline expand/collapse (the listener directly above,
-      // L8's, toggling stripExpanded/#statusStripBody) changes the sticky
-      // disclosure state first. R09.6 names this same control as the Issues
-      // entry point, so this listener does not gate on that resulting state.
+      // R09.6's "the banner is the only way in" holds for both forms. The
+      // desktop band has one door, bound here; the phone card has one per
+      // finding, each wired as it is built (see renderStatusStripCard)
+      // because each carries its own severity filter.
+      //
+      // The band's door opens the screen UNFILTERED, and that is not an
+      // oversight: its sentence names one finding but the band stands for the
+      // whole facet, so narrowing to that one row's severity would hide the
+      // others behind a filter the reader did not choose. A card row names
+      // exactly what it filters to, which is what earns it the filter.
       if (stripToggle) {
-        stripToggle.addEventListener('click', openIssuesView);
+        stripToggle.addEventListener('click', function () {
+          openIssuesForBandRow(stripToggle.getAttribute('data-tone'), stripToggle.getAttribute('data-severity'));
+        });
       }
       // Any ordinary navigation — a sidebar tab, a subtab, a citation link —
       // leaves this screen the same way the back chevron does. Capture phase
@@ -15344,8 +16635,23 @@
     window.scrollTo({ top: Math.max(0, Math.round(window.scrollY + top - offset)), behavior: 'instant' });
   }

+  // BODY_LIKE is what the four-line disclosure clamps: a claim's own body, and
+  // the passage-by-passage diff that stands in for it while the reader is
+  // looking at what changed since approval.
+  //
+  // The diff is in this set because it IS the body, shown a passage at a time
+  // — so a claim that clamps to four lines with "... more" in one state and
+  // runs to full height in the other is the same claim behaving as two
+  // different components. Sharing the mechanism rather than copying it also
+  // means the toggle's label, its aria wiring and its scroll compensation
+  // cannot drift between the two.
+  // .claim-edit-current is the other state of the same surface: the current
+  // wording rendered from the same passages, with the changed one ruled. It
+  // stands in for the body exactly as the diff does, so it clamps like it.
+  var BODY_LIKE = '.claim-body, .claim-edit-diff, .claim-edit-current';
+
   function setClaimBodyExpanded(wrapper, expanded) {
-    var body = wrapper && wrapper.querySelector(':scope > .claim-body');
+    var body = wrapper && wrapper.querySelector(':scope > .claim-body, :scope > .claim-edit-diff, :scope > .claim-edit-current');
     var toggle = wrapper && wrapper.querySelector(':scope > .claim-body-disclosure__toggle');
     if (!body || !toggle || toggle.hidden) { return; }
     wrapper.classList.toggle('claim-body-disclosure--expanded', expanded);
@@ -15358,13 +16664,19 @@
   }

   function syncClaimBodyDisclosures() {
-    document.querySelectorAll('.claim-body').forEach(function (body) {
+    document.querySelectorAll(BODY_LIKE).forEach(function (body) {
       var wrapper = body.parentElement && body.parentElement.classList.contains('claim-body-disclosure')
         ? body.parentElement
         : null;
       if (!wrapper) {
         wrapper = document.createElement('div');
         wrapper.className = 'claim-body-disclosure';
+        // The diff's wrapper is marked, because the card hides the claim's
+        // own body while the changes are showing and must not hide the diff
+        // along with it — both are a .claim-body-disclosure by then.
+        if (body.classList.contains('claim-edit-diff') || body.classList.contains('claim-edit-current')) {
+          wrapper.classList.add('claim-body-disclosure--edit');
+        }
         body.parentNode.insertBefore(wrapper, body);
         wrapper.appendChild(body);
         if (!body.id) { body.id = 'claim-body-' + (++claimBodyDisclosureSequence); }
@@ -15420,7 +16732,6 @@

   function enhanceConformanceFooters() {
     document.querySelectorAll('details.claim-conformance').forEach(function (original) {
-      if (original.dataset.footerEnhanced === 'true') { return; }
       var claim = original.closest('.claim');
       var footer = claim && claim.querySelector('.claim-footer');
       var summary = original.querySelector(':scope > summary');
@@ -15525,13 +16836,6 @@
   }

   function renderToc() {
-    // Theme-parity's hover probe forces :hover through CDP node ids. This
-    // function replaceChildren()s the TOC list on every enhance pass (the
-    // layout MutationObserver retriggers enhance when addModuleHeaders
-    // rewrites the module head), so a node id dies between QuerySelector
-    // and ForcePseudoState. The harness sets this flag for the duration of
-    // that probe; it is otherwise unset.
-    if (window.__dxParityFreezeToc) { return; }
     var toc = document.getElementById('systemFacetToc');
     if (!toc) {
       toc = document.createElement('aside');
@@ -16225,19 +17529,6 @@
     return { from: from, to: to, type: type };
   }

-  // normalizeEdges maps a whole edge list, dropping unusable entries.
-  function normalizeEdges(edges) {
-    var list = asArray(edges);
-    var out = [];
-    for (var i = 0; i < list.length; i++) {
-      var e = normalizeEdge(list[i]);
-      if (e) {
-        out.push(e);
-      }
-    }
-    return out;
-  }
-
   // idSet builds a lookup Set from an id array. Internal only.
   function idSet(ids) {
     var s = new Set();
@@ -17978,7 +19269,6 @@
   var parseTimeHash = ''; // the graph state segment seen at parse time
   var originClaimId = ''; // OPEN_ID_ATTR off the trigger that opened the pane, '' from the nav trigger
   var collapseOverride = false; // the reader insisted on every claim
-  var notices = []; // {text, kind, action} rendered above the canvas
   var dragging = null; // {id} while a node is being dragged
   var panning = null; // {x, y} while the background is being dragged
   var movedWhileDown = false;

testdata/fixture-graph-demo/build/viewer/index.html  (+1552 -262)
    size            22700 lines / 1439097 bytes -> 23990 lines / 1496790 bytes
@@ -5365,6 +5365,15 @@
    form; only the bottom hairline separates it from the body beneath. The
    520px inset-card form below is a DIFFERENT, deliberately distinct board
    (R-H.1) and keeps its own margin/border/radius, untouched by this. */
+/* THIS IS THE DESKTOP FORM and is unchanged: a full-bleed band carrying one
+   summary sentence with "Show issues" beside it. At 1440 a row has the space
+   to carry a sentence and an action without either crowding the other.
+
+   The PHONE form is a different component — a card with one row per finding
+   (Paper EH1-0) — and lives under "The banner card" near the end of this
+   file. Both are in the DOM; the 520px block picks one. Everything from here
+   to .status-strip-action-chevron therefore applies to the desktop form only,
+   and the mobile block switches all of it off rather than re-tuning it. */
 .status-strip {
   width: 100%;
   background: var(--card-bg);
@@ -5377,25 +5386,26 @@
   display: none;
 }

+/* The strip itself is card ground. Its TINT lives on each row (below,
+   keyed by data-tone), because a band with two findings on it carries two
+   hues and a surface cannot make two severity claims at once (Paper board
+   15, C2M-0 red over C92-0 amber, hairline-divided). */
 .status-strip--lint {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
 }

 .status-strip--integrity {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
   box-shadow: none;
 }

-/* retry fix 20: 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop
-   banner at 13px/32px, not 13px/14px — 32px is the reading card's own
-   gutter (--card-gutter, G8), reproduced here as a literal because
-   .status-strip mounts as a sibling of the record header, never as a
-   descendant of .card/.claim-tree, so the custom property does not cascade
-   to it. The 520px inset-card form re-pins this back down to 14px in this
-   lane's own appended mobile block, matching the value this rule carried
-   (and was verified against) before this fix. */
+/* 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop banner at
+   13px/32px — 32px is the reading card's own gutter (--card-gutter, G8),
+   reproduced here as a literal because .status-strip mounts as a sibling of
+   the record header, never as a descendant of .card/.claim-tree, so the
+   custom property does not cascade to it. */
 .status-strip-head {
   display: flex;
   align-items: center;
@@ -5409,11 +5419,9 @@
   cursor: pointer;
 }

-/* retry fix 14: 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning
-   triangle ahead of the banner sentence at every width, in both schemes — it
-   was absent everywhere. .status-strip-summary becomes the icon's row; the
-   title/note stack (unchanged) moves into its own -text wrapper so the two
-   IDs the runtime writes to keep working untouched. */
+/* 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning triangle
+   ahead of the banner sentence. .status-strip-summary is the icon's row; the
+   title stack sits in its own -text wrapper. */
 .status-strip-summary {
   display: flex;
   align-items: flex-start;
@@ -5430,20 +5438,70 @@
   flex-shrink: 0;
 }

+/* One row per finding, each its own tint (Paper C2M-0: blocked ground
+   #9E3B360F; CDR-0: edited ground #9A6A1612), divided by the hairline. The
+   icon slot is 15px either way: the triangle for an alarm row, a 7px dot in
+   the draft hue for an edited row, so the sentences of stacked rows share
+   one left edge. */
+.status-strip-head[data-tone="alarm"] { background: var(--status-strip-bg); }
+.status-strip-head[data-tone="draft"] { background: var(--status-draft-bg, rgba(154, 106, 22, .11)); }
+.status-strip-head + .status-strip-head { border-top: 1px solid var(--border); }
+
+.status-strip-dot {
+  display: none;
+  width: 15px;
+  height: 15px;
+  flex-shrink: 0;
+  align-items: center;
+  justify-content: center;
+}
+
+.status-strip-dot::before {
+  content: "";
+  width: 7px;
+  height: 7px;
+  border-radius: 999px;
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-head[data-tone="draft"] .status-strip-icon { display: none; }
+.status-strip-head[data-tone="draft"] .status-strip-dot { display: inline-flex; }
+.status-strip-head[data-tone="draft"] .status-strip-action { color: var(--status-draft, #976600); }
+
 .status-strip-summary-text {
   display: grid;
   gap: 3px;
   min-width: 0;
 }

-/* retry fix 21: 02 §4.10 "Body" / 03 §4.5 "Body text" measure the banner
-   sentence at Inter 14/18 weight 400, not 13px weight 680 — the sentence
-   reads as body prose, not an emphasized heading. */
+/* 02 §4.10 "Body" / 03 §4.5 "Body text": the banner sentence is Inter 14/18
+   weight 400 — it reads as body prose, not as an emphasized heading. */
 .status-strip-title {
   font: 400 14px/18px var(--font-sans);
   color: var(--ink);
 }

+/* 02 §4.10/§4.19, 03 §4.5/§7.5, R-H.1: "Show issues" is the ALARM half of the
+   banner (the body sentence stays --color-ink) and reads in the same red as
+   the icon, never the generic --accent link colour. */
+.status-strip-action {
+  color: var(--warn);
+  font: 500 13px/16px var(--font-sans);
+  letter-spacing: 0;
+  text-transform: none;
+  white-space: nowrap;
+}
+
+.status-strip-action-row {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+}
+
+/* § 4.10 draws no trailing chevron on the desktop banner. */
+.status-strip-action-chevron {
+  display: none;
+}
 /* .status-strip-note (the "Critical 0 · Needs you 3 · ..." tally line) is
    REMOVED per the coordinator's ruling on this retry: no 02/03/04 board
    draws it, and the severity chips already carry every count it repeated.
@@ -5458,30 +5516,12 @@
    500, 13/16 — R-H.1's weight 600 at 13/16 is the PHONE step (added in
    L8's own 520 block below), not the desktop default. --warn is correct at
    both widths and is unchanged. */
-.status-strip-action {
-  color: var(--warn);
-  font: 500 13px/16px var(--font-sans);
-  letter-spacing: 0;
-  text-transform: none;
-  white-space: nowrap;
-}
-
 /* retry fix 15: the action text (#statusStripAction, whose textContent the
    runtime overwrites on every toggle) and its trailing chevron are separate
    elements inside this row wrapper — the row, not the text span, is what
    the 520 block turns into R-H.1's divided action row, so a re-render can
    never wipe the chevron out from under it. The chevron itself is hidden
    above 520 (§ 4.10 draws no trailing chevron on the desktop banner). */
-.status-strip-action-row {
-  display: inline-flex;
-  align-items: center;
-  gap: 6px;
-}
-
-.status-strip-action-chevron {
-  display: none;
-}
-
 /* The detail list is capped and scrolls internally: a project with forty lint
    errors must not grow a dock that swallows the page it is describing. */
 /* retry fix 16: the promoted list is 04 §3/§9 item 4's bottom-open card —
@@ -5744,6 +5784,81 @@
   font-weight: 600;
 }

+/* Paper board 15 (BR5-0 / ETC-0 / EUD-0): a row's third line — the cause
+   kind in mono, a mono summary of what moved, and for a blocker row whose
+   owner was approved and is being rewritten, the way to its diff. */
+.status-finding-detail {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 9px;
+  padding-top: 1px;
+}
+
+.status-finding-kind {
+  padding: 2px 7px;
+  border-radius: 999px;
+  font: 500 10px/12px var(--font-mono);
+  background: var(--warn-bg);
+  color: var(--warn);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-kind,
+.status-finding-kind--edited {
+  background: var(--status-draft-bg);
+  color: var(--status-draft);
+}
+
+/* BR5-0: the chip under a blocker row is the claim chip's word, at the
+   row's scale — Inter 10/14 600, 0.06em, uppercase. */
+.status-finding-kind--edited {
+  font: 600 10px/14px var(--font-sans);
+  letter-spacing: .06em;
+  text-transform: uppercase;
+}
+
+.status-finding-meta {
+  font: 400 11px/14px var(--font-mono);
+  color: var(--muted);
+}
+
+.status-finding-link {
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  font: 600 11px/14px var(--font-sans);
+  color: var(--link);
+}
+
+.status-finding-link:hover { text-decoration: underline; }
+
+/* EUD-0: a Needs-you row's right-hand slot is its way in, in the row's hue,
+   where a blocker row keeps its count. */
+.status-finding-action {
+  flex: 0 0 auto;
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  text-align: right;
+  font: 600 14px/18px var(--font-sans);
+  color: var(--warn);
+}
+
+.status-finding-action:hover { text-decoration: underline; }
+
+/* A row caused by an edit since approval is draft work, not an alarm: its
+   dot and its action take the draft hue (EUD-0). */
+.status-finding--group[data-tone="draft"] .status-finding-dot,
+.status-finding--group[data-severity="needs_you"][data-tone="draft"] .status-finding-dot {
+  background: var(--status-draft);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-action {
+  color: var(--status-draft);
+}
+
 /* file:// has no /api/status and therefore no ledger_findings at all (04 §8
    item 11 / §9 item 5) — stated in the body rather than silently omitted. */
 .status-strip-static-note {
@@ -5870,7 +5985,7 @@
   .comment-reply-trigger,
   .comment-composer-submit,
   .comment-composer-input,
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
   }
@@ -5934,12 +6049,15 @@
     border-left: 0;
     /* R-J.2 / OD14a.7 / M4, verifier RETRY item 4(iii): a dark-only 1px top
        hairline (the light sheet relies on its 16px corners and the scrim
-       alone). `light-dark()` expresses "present only under dark" without a
-       second selector-scoped dark query or a new dark-only token. A
+       alone). --sheet-hairline is the engine-owned token declared for
+       exactly this edge (:root and the two dark blocks): `transparent` in
+       light, var(--border) in dark, on both the OS path and the explicit
+       Dark toggle. It used to be a `light-dark()` literal while the token
+       sat unread; the token is the single place the edge is decided. A
        component-scoped `@media screen and (prefers-color-scheme: dark)`
-       block is unavailable
-       here (theme_tokens_test.go's ExactlyOneScreenScopedDarkQuery). */
-    border-top: 1px solid light-dark(transparent, var(--border));
+       block is unavailable here (theme_tokens_test.go's
+       ExactlyOneScreenScopedDarkQuery). */
+    border-top: 1px solid var(--sheet-hairline);
     border-top-left-radius: 16px;
     border-top-right-radius: 16px;
     border-radius: 16px 16px 0 0;
@@ -6015,25 +6133,10 @@
     max-width: 100%;
     box-sizing: border-box;
   }
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
-  }
-  .status-strip-head {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) 10px;
-    align-items: flex-start;
-  }
-  .status-strip-summary {
-    grid-column: 1;
-    grid-row: 1;
-  }
-  .status-strip-action-row {
-    grid-column: 1;
-    grid-row: 2;
-    justify-self: start;
-  }
-}
+  }}

 /* The claim head line. v0.4.1 gives it two children instead of loose text:
    a <span class="label"> carrying the label plus its status pill, and the
@@ -6576,24 +6679,9 @@
   background: var(--accent-bg);
 }

-/* nav-group-label separates the Tracks run from the modules above it. The
-   sidebar is otherwise a flat list of .sec-tab buttons, and a track tab looks
-   exactly like a module tab because mechanically it IS one — so without a
-   caption a reader would have no way to tell that the axis changed halfway
-   down. It borrows the .eyebrow register (mono, uppercase, --faint) rather
-   than inventing one, because it is the same kind of thing: a quiet caption
-   over the content beneath it, never a control. The top border and margin are
-   what make it read as a division rather than as a first entry in the run. */
-.nav-group-label {
-  margin: 14px 0 4px;
-  padding: 10px 20px 0;
-  border-top: 1px solid var(--border);
-  font-family: var(--font-mono);
-  font-size: 9px;
-  letter-spacing: .14em;
-  text-transform: uppercase;
-  color: var(--faint);
-}
+/* .nav-group-label (the caption over the Tracks run) is gone: the sidebar
+   groups modules and tracks in .system-nav-group <details> (shell.html), and
+   nothing has emitted the class since the system-record redesign. */

 /* Secondary horizontal facet tab strip inside a module's content area,
    ported verbatim (selector + declarations) from the reference docs
@@ -7222,14 +7310,6 @@
   color: var(--link);
 }

-.nav-group-label {
-  margin: 17px 0 5px;
-  padding: 12px 10px 0;
-  color: var(--faint);
-  font-size: 9px;
-  letter-spacing: .12em;
-}
-
 .sidebar-footer {
   padding: 11px 18px 15px;
   color: var(--faint);
@@ -8015,9 +8095,23 @@
   max-width: 760px;
 }

-.claim-body-disclosure > .claim-body { max-width: none; }
+.claim-body-disclosure > .claim-body,
+.claim-body-disclosure > .claim-edit-diff,
+.claim-body-disclosure > .claim-edit-current { max-width: none; }

-.claim-body-disclosure--collapsed > .claim-body {
+/* .claim-edit-diff is clamped alongside .claim-body because it IS the body,
+   shown a passage at a time (see system-record.js's BODY_LIKE). A claim that
+   truncates to four lines in one state and runs to full height in the other
+   is the same claim behaving as two components.
+   The clamp counts LINE BOXES and is not a height: the diff's passages carry
+   their own margins, and a tinted one carries padding, so a collapsed diff is
+   four lines of prose PLUS whatever box the first passage sits in. It is
+   therefore a little taller than a collapsed body and not a fixed multiple of
+   the line height — which is the honest description, and what the test
+   asserts rather than an exact 4x that does not hold. */
+.claim-body-disclosure--collapsed > .claim-body,
+.claim-body-disclosure--collapsed > .claim-edit-diff,
+.claim-body-disclosure--collapsed > .claim-edit-current {
   display: -webkit-box;
   overflow: hidden;
   -webkit-box-orient: vertical;
@@ -9762,31 +9856,10 @@
     border-radius: 10px;
     border-color: color-mix(in srgb, var(--warn) 26%, var(--border));
   }
-  /* retry fix 29 (this lane's third attempt): the desktop banner's own
-     padding moved from a literal 14px to the reading card's 32px gutter
-     (retry fix 20 above), but the 520px form stays the inset-card treatment
-     R-H.1 always specified. 02 §4.19 "Body block" gives the mobile form's
-     own padding as 12px/14px — one px shallower than the pre-retry-3 value
-     this rule carried, which was never checked against §4.19 directly. */
-  .status-strip-head {
-    padding: 12px 14px;
-  }
-  /* retry fix 33 (this lane's third attempt, 02 §4.19 "Icon"): the mobile
-     icon's optical offset is 2px, not the desktop row's 1px. */
-  .status-strip-icon {
-    margin-top: 2px;
-  }
-  /* retry fix 34 (this lane's third attempt, 02 §4.19 "Body text"): the
-     mobile banner sentence is Inter 13/19, a full step down from the
-     desktop 14/18 fix 21 (above) set — §4.19 is a distinct node (`574-0`)
-     from §4.10's `6T-0`, not the same rule at a smaller viewport. */
-  .status-strip-title {
-    font-size: 13px;
-    line-height: 19px;
-  }
-  .status-strip--open {
-    border-radius: 10px 10px 0 0;
-  }
+  /* The banner's own head padding, icon offset and sentence size used to be
+     re-pinned here for the phone. All three described the single-sentence
+     head that Paper EH1-0 replaced with a card, and their rules went with
+     it; the card carries its own metrics at both widths. */
   .status-strip-body {
     border-top-color: color-mix(in srgb, var(--warn) 18%, var(--border));
   }
@@ -9861,32 +9934,6 @@
     height: 30px;
     padding-inline: 12px;
   }
-  /* retry fix 15 (R-H.1, 02 §4.19): the action gets its own divided row —
-     a 1px top hairline at ~18% blocked tint, a 38px row and 14px inline
-     padding, bled to the head's own inline/bottom padding edges so the tap
-     target is the card's full width. `grid-column: 1 / -1` reclaims the
-     10px caret column too, since the divider must span edge to edge. */
-  .status-strip-action-row {
-    grid-column: 1 / -1;
-    justify-self: stretch;
-    display: flex;
-    align-items: center;
-    justify-content: space-between;
-    height: 38px;
-    margin: 8px -14px -13px;
-    padding-inline: 14px;
-    border-top: 1px solid color-mix(in srgb, var(--warn) 18%, transparent);
-  }
-  .status-strip-action {
-    font-weight: 600; /* retry fix 13: the 600 step is the PHONE form only. */
-  }
-  .status-strip-action-chevron {
-    display: inline-block;
-    width: 13px;
-    height: 13px;
-    color: var(--warn);
-    flex-shrink: 0;
-  }
 }

 /* retry fix 17 (LANES.md OD3, and fix 11's technique): .status-severity-chip
@@ -10287,13 +10334,19 @@
   background: var(--paper);
 }

-/* 04 §4.9: the bar is always draft amber, ranking VOLUME, not severity —
-   never colour these bars per row severity. */
+/* 04 §4.9: the module ranking's bar is always draft amber, ranking VOLUME,
+   not severity. The cause ranking under the Needs-you filter (Paper EKL-0,
+   "WHAT IS WAITING") is the one exception: each bar takes its cause's hue,
+   because there the rows ARE kinds of work and the hue is what tells them
+   apart. */
 .issues-rail-bar-fill {
   height: 5px;
   background: var(--status-draft);
 }

+.issues-rail-bar-fill[data-tone="alarm"] { background: var(--warn); }
+.issues-rail-bar-fill[data-tone="draft"] { background: var(--status-draft); }
+
 /* 04 §8 item 8: the rail is a ranking, so it truncates rather than scrolls. */
 .issues-rail-more {
   margin: 0;
@@ -11001,9 +11054,7 @@
   font: 13px/16px var(--font-sans);
 }
 .reading-canvas .claim-footer .comment-chip .dx-icon { width: 14px; height: 14px; }
-@media screen and (min-width: 861px) {
-  .reading-canvas .status-strip-action-chevron { display: none; }
-}
+@media screen and (min-width: 861px) {}
 @media screen and (max-width: 520px) {
   /* `display: flow-root` gives the canvas a block-formatting context without
      the side effects of restoring a border or `overflow: clip`. Without one,
@@ -11048,15 +11099,6 @@
      carries a 20px margin-bottom standing in for both and .reading-canvas has
      no border or padding at this tier, so the banner's own 14px collapses
      into that 20px and the reader gets 20px. State the no-tab-row case. */
-  .reading-canvas .status-strip-head { padding: 12px 14px; }
-  /* 56U-0: the triangle hangs on the FIRST text line (align-items: start,
-     10px gap), not centred against a three-line sentence. */
-  .reading-canvas .status-strip-summary { gap: 10px; align-items: flex-start; }
-  /* 56Y-0: Inter 13/19 — a full step down from the desktop 14/18. */
-  .reading-canvas .status-strip-title { font-size: 13px; line-height: 19px; }
-  /* 56Z-0: the divided action row bleeds to the head's own 14px inline and
-     12px bottom padding edges; Paper draws no extra gap above the rule. */
-  .reading-canvas .status-strip-action-row { margin: 12px -14px -12px; }
   .reading-canvas .claim-footer {
     margin-top: 6px;
     padding-top: 13px;
@@ -11097,6 +11139,534 @@
   .reading-canvas .claim-footer-chip--blocked .claim-footer-chip-count { color: var(--warn); }
 }

+/* ---- The banner card, and unapproved edits -------------------------------
+
+   ONE CARD, A ROW PER FINDING (Paper node EH1-0).
+
+   The banner used to be a single sentence with a "Show issues" action beside
+   it. That shape holds exactly one statement, and a facet routinely has two
+   kinds of thing waiting: claims blocked by unapproved dependencies, and
+   claims rewritten since they were approved. The second fact had to go into a
+   second tinted band stacked underneath, and two tinted bands touching read as
+   one broken component rather than as two facts.
+
+   TINT IS A SEVERITY CLAIM, so the card makes at most one. While every row
+   shares a hue the whole surface takes it; the moment two rows disagree the
+   surface goes neutral and the dots carry the colour instead. That is why
+   --alarm and --draft are separate modifiers and the neutral form is the base
+   rule: which one applies is computed from the rows, never from whichever kind
+   of finding happens to sort first. */
+
+/* The card is the PHONE form. Everything below describes the card itself; the
+   one thing that describes WHEN it applies is `display`, which is none here
+   and flex in the 520px block at the end of this file. Keeping the switch to
+   a single property in a single place is what stops a later rule in this
+   block from quietly turning the card back on at every width — which is
+   exactly what a `display: flex` sitting among the card's own layout rules
+   did on the first attempt. */
+.status-strip-card {
+  display: none;
+  flex-direction: column;
+  border: 1px solid var(--border-strong);
+  border-radius: 10px;
+  overflow: clip;
+  background: var(--card-bg);
+}
+
+.status-strip-card-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+  padding: 9px 14px;
+  background: var(--code-bg);
+  border-bottom: 1px solid var(--border);
+}
+
+.status-strip-card-eyebrow {
+  font: 600 11px/14px var(--font-sans);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+  color: var(--muted);
+}
+
+/* Mono, like every other machine-produced count in this viewer: the number is
+   read off the findings, not written by anyone. */
+.status-strip-card-count {
+  font: 11px/14px var(--font-mono, var(--font-sans));
+  color: var(--faint);
+}
+
+/* A button, not a row with a click handler. Each row is one way into the
+   Issues screen and has to be reachable by keyboard and announced as the
+   control it is. */
+.status-strip-finding {
+  display: flex;
+  align-items: flex-start;
+  gap: 10px;
+  width: 100%;
+  padding: 11px 14px;
+  border: 0;
+  background: none;
+  text-align: left;
+  cursor: pointer;
+  color: var(--ink);
+}
+
+.status-strip-finding + .status-strip-finding {
+  border-top: 1px solid var(--border);
+}
+
+.status-strip-finding-dot {
+  width: 7px;
+  height: 7px;
+  flex-shrink: 0;
+  margin-top: 6px;
+  border-radius: 999px;
+  background: var(--warn);
+}
+
+.status-strip-finding[data-tone="draft"] .status-strip-finding-dot {
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-finding-text {
+  flex: 1;
+  min-width: 0;
+  font: 13px/19px var(--font-sans);
+  color: var(--ink);
+}
+
+/* EH1-0: a 12px "\203a" glyph, in the card's hue (the tinted forms below
+   recolour it), optically dropped 2px to sit on the sentence's first line. */
+.status-strip-finding-chevron {
+  flex-shrink: 0;
+  margin-top: 2px;
+  color: var(--faint);
+  font: 400 12px/16px var(--font-sans);
+}
+
+/* The two tinted forms. The alphas are EH1-0's own (#9A6A1612 / #9A6A1645 /
+   #9A6A163D over the draft token), written as color-mix so the dark scheme
+   redefines them from its own hue instead of carrying a second literal. */
+.status-strip-card--alarm {
+  background: color-mix(in srgb, var(--warn) 7%, transparent);
+  border-color: color-mix(in srgb, var(--warn) 27%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-eyebrow,
+.status-strip-card--alarm .status-strip-card-count,
+.status-strip-card--alarm .status-strip-finding-chevron {
+  color: var(--warn);
+}
+
+.status-strip-card--alarm .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--draft {
+  background: color-mix(in srgb, var(--status-draft, #976600) 7%, transparent);
+  border-color: color-mix(in srgb, var(--status-draft, #976600) 27%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-eyebrow,
+.status-strip-card--draft .status-strip-card-count,
+.status-strip-card--draft .status-strip-finding-chevron {
+  color: var(--status-draft, #976600);
+}
+
+.status-strip-card--draft .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+/* The per-claim edit surface (Paper DON-0 / C9H-0).
+
+   It is a BAR ABOVE THE BODY with a two-segment switch, not a disclosure, and
+   the difference is which state is the default. A disclosure says the diff is
+   an aside a reader opens if they want it; the board says the opposite — a
+   claim that was approved and then rewritten opens SHOWING what moved,
+   because that is the thing about it a reader has to know before they read a
+   word of it. "Current" is the escape hatch, not the resting state. */
+/* While the changes are showing, the claim's own body is hidden by this class
+   on the CARD. See paintApprovedEdit for why the state does not live on the
+   body: the long-body disclosure re-wraps it after first paint, so the body
+   is not the same node before and after. */
+.claim--edit-view > .claim-body,
+.claim--edit-view > .claim-body-disclosure:not(.claim-body-disclosure--edit),
+.claim--edit-view > .claim-empty,
+.claim--edit-view > .claim-mockup-body {
+  display: none;
+}
+
+/* THE BAR WRAPS BY CONTENT, NOT BY VIEWPORT.
+   Its width is the reading column's, not the window's — about 690px at 1440
+   — so a media query cannot see the moment it runs out of room. The Current
+   state's line ("1 passage differs from the approval of 20 Sep") is the long
+   one, and at that column it overran by a few pixels and broke mid-date:
+   "...the approval of 20 / Sep".
+   Wrapping instead of shrinking is deliberate. The three ways to make it fit
+   were truncating the sentence, stepping the type down, or letting the row
+   become two rows; the first hides a fact, the second makes a 12px mono line
+   smaller than anything else on the card, and the third costs 8px of height
+   in the rare case. The switch is what moves, because it is the only part
+   with no sentence in it. */
+/* Paper CKF-0: the bar is the draft tint (--color-draft-bg), radius 6,
+   9px/13px, and it sits in the card's own 16px rhythm — no margin of its
+   own. Title and meta share a baseline with 10px between them. */
+.claim-edit-bar {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 8px 10px;
+  margin: 0;
+  padding: 9px 13px;
+  border-radius: 6px;
+  background: var(--status-draft-bg, rgba(154, 106, 22, .11));
+  font-family: var(--font-sans);
+}
+
+.claim-edit-bar-title {
+  flex: 0 0 auto;
+  font: 600 13px/18px var(--font-sans);
+  color: var(--status-draft, #976600);
+}
+
+/* Mono, like every other machine-read fact in this viewer: the date, the
+   actor and the passage count are read off the ledger, not written by
+   anyone. */
+/* The meta's own width is its CONTENT's, which is what makes the switch the
+   first thing to wrap: flex lays the row out from the items' base sizes, so
+   title + this sentence + switch overflowing moves the switch — the only item
+   with no sentence in it — down to its own row, and the sentence gets the
+   whole first row instead of being squeezed into what was left over.
+   It stays shrinkable on purpose. Refusing to shrink made it overrun the bar
+   entirely at 900px, where even a full row is not enough; wrapping inside
+   itself is a worse line but a contained one, and it is the last resort
+   rather than the first thing that happens. */
+.claim-edit-bar-meta {
+  flex: 1 1 auto;
+  min-width: 0;
+  font: 11px/16px var(--font-mono, var(--font-sans));
+  color: var(--muted);
+}
+
+/* The switch. The selected segment is a filled dark pill, which is the one
+   place in this panel that spends a strong value — everything else is a tint,
+   and the reader has to be able to see which of two states they are in from
+   across the page. */
+/* Paper CKJ-0: a card-ground pill with a 1px hairline, 2px inset, segments
+   3px/11px at 11/14. */
+.claim-edit-switch {
+  display: inline-flex;
+  align-items: center;
+  flex: 0 0 auto;
+  /* Pinned right on the first row, and still right when it wraps to its own. */
+  margin-left: auto;
+  gap: 0;
+  padding: 2px;
+  border: 1px solid var(--border);
+  border-radius: 999px;
+  background: var(--card-bg);
+}
+
+.claim-edit-switch-seg {
+  border: 0;
+  border-radius: 999px;
+  padding: 3px 11px;
+  background: none;
+  cursor: pointer;
+  font: 500 11px/14px var(--font-sans);
+  color: var(--muted);
+}
+
+.claim-edit-switch-seg[aria-pressed="true"] {
+  background: var(--ink);
+  color: var(--card-bg);
+  font-weight: 600;
+}
+
+/* The passages. Typography is the claim body's own — this IS the claim body,
+   shown a passage at a time — so a reader moving between Changes and Current
+   is reading the same prose in the same face, not a second document. */
+.claim-edit-diff,
+.claim-edit-current {
+  font-family: var(--font-serif);
+  font-size: 17px;
+  line-height: 28px;
+  max-width: 760px;
+}
+
+/* Passages keep the body's 16px rhythm between them (the card's own gap).
+   Margins rather than flex gap, because the four-line clamp turns this box
+   into a -webkit-box and a flex gap would vanish with it. */
+.claim-edit-passage {
+  margin: 0;
+}
+
+.claim-edit-passage + .claim-edit-passage {
+  margin-top: 16px;
+}
+
+.claim-edit-passage > :first-child {
+  margin-top: 0;
+}
+
+.claim-edit-passage > :last-child {
+  margin-bottom: 0;
+}
+
+/* Paper CK7-0 / CKP-0: a changed passage is a tinted row, radius 4, with a
+   3px rule INSIDE it (12px in from the edge, 12px before the text) that runs
+   the height of the text, old above new with 2px between them and 4px above
+   the pair. The prose itself takes the hue: blocked for the approved
+   passage, locked for the current one. Colour is not the only carrier — the
+   removed passage is struck through and the added one is not, so the two
+   stay apart in a monochrome print, under a colour-vision deficiency, and
+   in the dark scheme where the two tints are close in luminance. */
+.claim-edit-passage--removed,
+.claim-edit-passage--added,
+.claim-edit-passage--current {
+  position: relative;
+  border-radius: 4px;
+}
+
+.claim-edit-passage--removed,
+.claim-edit-passage--added {
+  padding: 7px 12px 7px 27px;
+}
+
+.claim-edit-passage--removed::before,
+.claim-edit-passage--added::before,
+.claim-edit-passage--current::before {
+  content: "";
+  position: absolute;
+  left: 12px;
+  top: 7px;
+  bottom: 7px;
+  width: 3px;
+  border-radius: 2px;
+  background: currentColor;
+}
+
+.claim-edit-passage--removed {
+  color: var(--warn);
+  background: var(--warn-bg, rgba(158, 59, 54, .10));
+  text-decoration: line-through 1px;
+  text-underline-position: from-font;
+}
+
+.claim-edit-passage--added {
+  color: var(--accent);
+  background: var(--accent-bg, rgba(44, 107, 82, .10));
+}
+
+.claim-edit-passage + .claim-edit-passage--removed {
+  margin-top: 20px;
+}
+
+.claim-edit-passage--removed + .claim-edit-passage--added {
+  margin-top: 2px;
+}
+
+/* The Current state (Paper DPZ-0): the passage that differs carries only a
+   draft-coloured rule at the left edge — no tint, prose in ink — so the
+   claim reads as ordinary prose with the seam marked. */
+.claim-edit-passage--current {
+  padding: 0 0 0 15px;
+}
+
+.claim-edit-passage--current::before {
+  left: 0;
+  top: 0;
+  bottom: 0;
+  background: var(--status-draft, #976600);
+}
+
+/* The words that moved, inside the passage they moved in. They stay in the
+   DOM for the passage's aria-label and for a reader who selects the text,
+   but Paper draws the passage as one tinted, coloured, struck (or not)
+   block and paints nothing on the individual words — the struck passage and
+   the coloured one are the comparison. */
+.claim-edit-word--removed,
+.claim-edit-word--added {
+  background: none;
+  color: inherit;
+  padding: 0;
+}
+
+.claim-edit-note {
+  margin: 8px 0 0;
+}
+
+.claim-edit-note-text,
+.claim-edit-note-hint,
+.claim-edit-field-note {
+  margin: 0;
+  font: 400 13px/19px var(--font-sans);
+  color: var(--muted);
+}
+
+/* The hint names the command that ENDS this state. A record written before
+   the approved wording was kept says "there is nothing to compare against",
+   and until this line existed that was the end of the sentence — true, and
+   with no way out of it. The wording is usually still in the project's own
+   git history, so the panel that reports the gap also names the verb that
+   closes it. */
+.claim-edit-note-hint {
+  margin-top: 4px;
+  color: var(--faint);
+}
+
+/* The fields that moved. This block is TEXT, not prose: it shows the YAML the
+   claim file holds, so it is monospace and pre-wrapped, and the tints are the
+   same two the passage diff uses so a reader does not learn a second colour
+   language halfway down one panel. */
+.claim-edit-fields {
+  margin: 8px 0 0;
+}
+
+.claim-edit-fields-toggle {
+  appearance: none;
+  -webkit-appearance: none;
+  background: none;
+  border: 0;
+  padding: 0;
+  cursor: pointer;
+  font: 500 13px/19px var(--font-sans);
+  color: var(--accent);
+  text-align: left;
+}
+
+.claim-edit-fields-toggle:hover { text-decoration: underline; }
+
+.claim-edit-fields-list {
+  margin-top: 6px;
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+}
+
+.claim-edit-field-name {
+  display: block;
+  font: 600 12px/18px var(--font-mono);
+  color: var(--muted);
+}
+
+.claim-edit-field-hunk {
+  margin: 3px 0 0;
+  padding: 6px 8px;
+  border-radius: 6px;
+  border-left: 3px solid transparent;
+  background: var(--code-bg);
+  font: 400 12px/18px var(--font-mono);
+  /* pre-wrap and not pre: a claims file's embodiment block is indented deep
+     enough to overflow a narrow column, and a horizontal scrollbar inside a
+     panel inside a claim is three nested scroll regions. Wrapping keeps the
+     indentation and loses only the guarantee that a line is one line. */
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+}
+
+.claim-edit-field-hunk--removed {
+  border-left-color: var(--warn);
+  background: color-mix(in srgb, var(--warn) 7%, var(--code-bg));
+}
+
+.claim-edit-field-hunk--added {
+  border-left-color: var(--border-strong);
+}
+
+@media (max-width: 520px) {
+  /* The phone swaps the whole banner: the desktop band's sentence-and-action
+     row goes away and the card takes its place, so .status-strip stops being
+     a painted surface and becomes the gutter that positions the card.
+     .reading-canvas > .status-strip is named alongside it because that rule
+     is two-class and unqueried, and would otherwise outrank this one and
+     restore the band's border underneath the card. */
+  .status-strip-head {
+    display: none;
+  }
+
+  .status-strip-card {
+    display: flex;
+  }
+
+  .reading-canvas > .status-strip,
+  .status-strip,
+  .status-strip--lint,
+  .status-strip--integrity {
+    background: none;
+    border: 0;
+    border-radius: 0;
+    margin: 0;
+    /* The gutter is the reading card's own 16px, not the desktop 32px —
+       EH1-0's mobile frame draws the card at mx-4 inside a 390px canvas. */
+    padding: 14px 16px 0;
+  }
+
+  /* The edit bar stacks into three rows (Paper CHK-0): the sentence, the
+     machine line under it, and the switch full width with two equal halves.
+     Side by side at this width the sentence wrapped to two lines, the mono
+     meta broke mid-word, and the switch was squeezed against them — three
+     things competing for a 343px row that only ever held one comfortably. */
+  .claim-edit-bar {
+    flex-direction: column;
+    align-items: stretch;
+    gap: 8px;
+    padding: 10px 12px;
+  }
+
+  .claim-edit-bar-title {
+    /* CKY-0: the title and its machine line are 2px apart on the phone. */
+    margin-bottom: -6px;
+  }
+
+  .claim-edit-bar-meta {
+    /* The phone stack gives every part its own row already. */
+    flex: none;
+    line-height: 15px;
+  }
+
+  .claim-edit-switch {
+    display: flex;
+    margin-left: 0;
+  }
+
+  .claim-edit-switch-seg {
+    flex: 1;
+    padding: 5px 10px;
+    font-size: 12px;
+    line-height: 16px;
+    text-align: center;
+  }
+
+  /* CKP-0: the rule and the text sit 10px in on a phone, not 12. */
+  .claim-edit-passage--removed,
+  .claim-edit-passage--added {
+    padding: 7px 10px 7px 23px;
+  }
+
+  .claim-edit-passage--removed::before,
+  .claim-edit-passage--added::before {
+    left: 10px;
+  }
+
+  .claim-edit-passage + .claim-edit-passage--removed {
+    margin-top: 18px;
+  }
+}
+
+
 @media print {
   :root { color-scheme: light; }

@@ -11115,10 +11685,6 @@
     display: block;
   }

-  .claim-links-summary {
-    cursor: auto;
-  }
-
   /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
@@ -12061,7 +12627,7 @@
     <div class="issues-body">
       <div id="issuesFindingsCard" class="issues-findings-card"></div>
       <aside id="issuesRailPanel" class="issues-rail-panel">
-        <p class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
+        <p id="issuesRailHeading" class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
         <p id="issuesRailCaveat" class="issues-rail-caveat"></p>
         <div id="issuesRailRows" class="issues-rail-rows"></div>
       </aside>
@@ -12088,9 +12654,10 @@


   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
-    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
+    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody" data-tone="alarm">
       <span id="statusStripSummary" class="status-strip-summary">
         <svg class="dx-icon status-strip-icon" aria-hidden="true"><use href="#dx-icon-triangle-alert"/></svg>
+        <span class="status-strip-dot" aria-hidden="true"></span>
         <span class="status-strip-summary-text">
           <strong id="statusStripTitle" class="status-strip-title"></strong>
         </span>
@@ -12100,6 +12667,13 @@
         <svg class="dx-icon status-strip-action-chevron" aria-hidden="true"><use href="#dx-icon-chevron-right"/></svg>
       </span>
     </button>
+    <div id="statusStripCard" class="status-strip-card">
+      <div class="status-strip-card-head">
+        <span id="statusStripCardEyebrow" class="status-strip-card-eyebrow">Needs you here</span>
+        <span id="statusStripCardCount" class="status-strip-card-count"></span>
+      </div>
+      <div id="statusStripFindings" class="status-strip-card-findings"></div>
+    </div>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>

@@ -12148,8 +12722,6 @@
       // each SSE re-render. It attaches NO listeners — those are delegated once
       // on surviving nodes below, so re-running this never double-binds.
       function initViewer() {
-        // Fragment swaps replace <main>; drop the stale mount pointer.
-        mountedSurfaceID = null;
         // Two-level show/hide: one .module-section per sidebar .sec-tab, and
         // (when a module has more than one facet) one .claim-group per .subtab
         // inside that module's .sub-nav. A single-facet module still has exactly
@@ -12276,7 +12848,6 @@
       // surface on first paint; smaller fixtures mount every surface so print,
       // theme probes, and live-reload witnesses that call getElementById keep
       // working without visiting every facet first.
-      var mountedSurfaceID = null;
       var SOFT_MOUNT_MIN_CLAIMS = 80;

       function claimCorpusSize() {
@@ -12304,7 +12875,6 @@
         var host = group.querySelector(':scope > [data-dossierx-surface-host]');
         var tmpl = group.querySelector(':scope > template.dossierx-surface-template');
         if (!host || !tmpl) {
-          mountedSurfaceID = surfaceID;
           return;
         }
         if (!host.querySelector('.claim')) {
@@ -12312,13 +12882,31 @@
           if (typeof window.dossierxEnhanceSystemRecord === 'function') {
             window.dossierxEnhanceSystemRecord();
           }
+          // The edited-since-approval chips and panels are attached HERE, at
+          // mount, and BEFORE the clamps below — not left to the next
+          // renderStatusStrip pass.
+          //
+          // They insert a node above each edited claim's body, so attaching
+          // them later moves everything under them. Two things read that
+          // height and cannot be allowed to read it early: a deep link has
+          // already scrolled to its claim, so the panels appearing afterwards
+          // slid the page out from under the sticky header; and the
+          // source-note clamps measure scrollHeight against clientHeight, so
+          // a mid-measurement insertion decides "show more" from a layout
+          // that is about to change. Decorating first means the surface is at
+          // its final height before anything measures or scrolls it.
+          renderApprovedEdits();
           // Source-note clamps observe live nodes only; re-arm after every
           // first-time mount so a freshly revealed surface is measured.
           if (typeof mountSourceNoteClamps === 'function') {
             mountSourceNoteClamps();
           }
         }
-        mountedSurfaceID = surfaceID;
+        // The no-template path (an eagerly rendered surface) never enters the
+        // block above, and still has claims to decorate. renderApprovedEdits
+        // is idempotent — every node it creates is either replaced wholesale
+        // or guarded — so the second call costs nothing on the path that did.
+        renderApprovedEdits();
       }

       function mountAllSurfaces() {
@@ -12580,10 +13168,8 @@
         if (!toastEl) { return; }
         toastEl.textContent = msg; // textContent — error text is never trusted markup
         toastEl.hidden = false;
-        toastEl.classList.add('comments-toast--show');
         if (toastTimer) { window.clearTimeout(toastTimer); }
         toastTimer = window.setTimeout(function () {
-          toastEl.classList.remove('comments-toast--show');
           toastEl.hidden = true;
         }, 4000);
       }
@@ -13424,21 +14010,30 @@
       // claim has no edges".

       var stripEl = document.getElementById('statusStrip');
-      var stripToggle = document.getElementById('statusStripToggle');
-      var stripSummary = document.getElementById('statusStripSummary');
-      var stripTitle = document.getElementById('statusStripTitle');
       // retry fix 21 (this lane's third attempt): #statusStripNote (the
       // "Critical 0 · Needs you 3 · ..." tally) is REMOVED per the
       // coordinator's ruling — no 02/03/04 board draws it, the chips
       // already carry every count it repeated — and dropped from shell.html
       // along with its element lookup here.
-      var stripAction = document.getElementById('statusStripAction');
+      //
+      // THE HEAD HAS TWO FORMS, ONE PER WIDTH, and both are written on every
+      // paint. #statusStripToggle is the desktop band — one summary sentence
+      // with "Show issues" beside it — and #statusStripCard is the phone form
+      // (Paper EH1-0), a card with one ROW PER FINDING. CSS shows one; see
+      // shell.html for why the choice is a media query and not a width read
+      // in here.
+      //
+      // Both are filled unconditionally rather than behind a matchMedia
+      // check. The cost is a few DOM nodes nobody sees; the alternative is a
+      // resize listener and a breakpoint duplicated out of the stylesheet,
+      // which is one more thing to keep in step for no gain.
+      var stripToggle = document.getElementById('statusStripToggle');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripCard = document.getElementById('statusStripCard');
+      var stripCardCount = document.getElementById('statusStripCardCount');
+      var stripFindings = document.getElementById('statusStripFindings');
       var stripBody = document.getElementById('statusStripBody');
       var lastStatusData = null;
-      // Paper starts every status strip collapsed. stripExpanded then stays
-      // sticky across refreshes, so a poll (or an SSE tick) never takes the
-      // disclosure state away from the reader.
-      var stripExpanded = false;

       function countLabel(n, word) {
         return n + ' ' + word + (n === 1 ? '' : 's');
@@ -13645,12 +14240,51 @@
       // demoted slug, and the fixed-width count slot — rather than three
       // loose wrapping siblings. See style.css's .status-finding-dot /
       // .status-finding-text for the layout this DOM shape enables.
+      // editChangeSummary is the mono line under an Issues row for a claim
+      // rewritten since approval (Paper BR5-0 / EUD-0): what moved, and when
+      // the approval it moved from was given.
+      function editChangeSummary(change) {
+        var parts = [];
+        if (!change.content_retained) {
+          parts.push('approved wording not retained');
+        } else if (change.changed_passages) {
+          parts.push(countLabel(change.changed_passages, 'passage') + ' changed');
+        } else if ((change.field_changes || []).length) {
+          parts.push(countLabel(change.field_changes.length, 'field') + ' moved');
+        }
+        var approved = editDateLabel(change.approved_at);
+        if (approved) { parts.push('approved ' + approved); }
+        return parts.join(' · ');
+      }
+
+      // goToClaimChanges leaves the Issues screen for the claim itself, with
+      // the Changes state showing — the row said "See changes", so landing
+      // on the claim with the diff put away would not be what it offered.
+      function goToClaimChanges(id, change) {
+        closeIssuesView();
+        if (window.location.hash !== '#' + id) { window.location.hash = '#' + id; }
+        var card = document.getElementById(id);
+        if (card && change) { paintApprovedEdit(card, change, true); }
+        if (card && typeof card.scrollIntoView === 'function') { card.scrollIntoView({ block: 'start' }); }
+      }
+
       function renderStatusGroup(group) {
         var row = el('li', 'status-finding status-finding--group');
         row.setAttribute('data-severity', group.severity);
+        // A row whose cause is an edit since approval is draft work, not an
+        // alarm (Paper EUD-0 draws its dot and action in the draft hue).
+        var edits = offlineApprovedEdits();
+        var editedOwner = group.ownerModuleClaimID && edits[group.ownerModuleClaimID] ? group.ownerModuleClaimID : '';
+        var isEditCause = group.severity === 'needs_you' && group.kind === 'unapproved_edit' && !!editedOwner;
+        row.setAttribute('data-tone', isEditCause ? 'draft' : 'alarm');
         row.appendChild(el('span', 'status-finding-dot'));
         var text = el('span', 'status-finding-text');
-        text.appendChild(textEl('span', 'status-finding-rule', group.title || 'Issue'));
+        // Paper EUD-0 names the claim and what happened to it in one line:
+        // "<Title> was approved and is being rewritten".
+        var title = isEditCause
+          ? readinessClaimLabel(editedOwner) + ' was approved and is being rewritten'
+          : (group.title || 'Issue');
+        text.appendChild(textEl('span', 'status-finding-rule', title));
         var ids = Object.keys(group.claimIDs).sort();
         // Paper 1KX-0: every row carries a second, demoted line under its
         // title holding the dotted claim id of the claim the reader must go
@@ -13668,11 +14302,52 @@
         if (ownerID) {
           text.appendChild(textEl('span', 'status-finding-claim', ownerID));
         }
+
+        // The third line (Paper board 15, F and I). A Needs-you row names its
+        // cause KIND in mono, because the sentence alone cannot distinguish
+        // "someone never wrote this" from "someone approved it and then
+        // moved it". A blocker row whose owner was approved and is being
+        // rewritten says so under the row it already had (BR5-0): no new
+        // group and no new severity, one line added to the existing row.
+        var detail = null;
+        if (group.severity === 'needs_you') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind', group.kind || 'review'));
+          if (isEditCause) {
+            detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          }
+        } else if (editedOwner && group.severity === 'blocker') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind status-finding-kind--edited', 'Edited · was approved'));
+          detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          var link = textEl('button', 'status-finding-link', 'See changes');
+          link.type = 'button';
+          link.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(editedOwner, edits[editedOwner]);
+          });
+          detail.appendChild(link);
+        }
+        if (detail) { text.appendChild(detail); }
         row.appendChild(text);
-        // retry fix 7: 04 §4.8 "Count label" / §6 "Row count" promotes the
-        // phrase INTO the count slot as a bare `N claims`, never `blocks N
-        // claim(s)` — that longer phrase was written for a full-width third
-        // line, which the fixed 104px slot (style.css) no longer is.
+
+        // The right-hand slot. A blocker row keeps its count (04 §4.8: the
+        // phrase promoted INTO the 104px slot as a bare `N claims`). A
+        // Needs-you row carries its way in instead (Paper ETC-0 / EUD-0):
+        // the thing waiting is one claim, and the count would always be 1.
+        if (group.severity === 'needs_you' && group.ownerModuleClaimID) {
+          var owner = group.ownerModuleClaimID;
+          var action = textEl('button', 'status-finding-action', isEditCause ? 'See changes' : 'Open claim');
+          action.type = 'button';
+          action.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(owner, isEditCause ? edits[owner] : null);
+          });
+          row.appendChild(action);
+          return row;
+        }
         var n = ids.length || group.count;
         row.appendChild(textEl('span', 'status-finding-msg', n + ' claim' + (n === 1 ? '' : 's')));
         return row;
@@ -13771,7 +14446,7 @@
         var label = card && card.querySelector('.k .label');
         if (label) {
           var copy = label.cloneNode(true);
-          copy.querySelectorAll('.pill, .k-id, .claim-collapse-chevron').forEach(function (node) { node.remove(); });
+          copy.querySelectorAll('.pill, .k-id').forEach(function (node) { node.remove(); });
           var rendered = (copy.textContent || '').replace(/\s+/g, ' ').trim();
           if (rendered) { return rendered; }
         }
@@ -13798,7 +14473,12 @@
           approval_content_drift: 'This claim changed after approval',
           approval_missing: 'This claim has no approval record',
           approval_released: 'This claim\'s approval was released',
-          approval_unknown: 'This claim\'s approval state is unknown'
+          approval_unknown: 'This claim\'s approval state is unknown',
+          // Deliberately not "changed after approval" (approval_content_drift's
+          // wording): that names a LOCKED claim whose bytes no longer match a
+          // STANDING approval, which is the tamper finding. This names the
+          // honest, intended act — unlock, rewrite, re-lock — caught mid-way.
+          unapproved_edit: 'This claim was approved, then rewritten'
         };
         return labels[record.kind] || String(record.kind || 'Readiness obstacle').replace(/_/g, ' ');
       }
@@ -14306,12 +14986,397 @@
         } catch (_) { return {}; }
       }

+      // ---- Unapproved edits -------------------------------------------------
+      //
+      // The state these three functions draw is the ordinary one, and it was
+      // the one the viewer could not show. A claim is locked; someone unlocks
+      // it, rewrites it, and has not locked it again yet. The new wording
+      // replaced the old in the file, so the reading view showed text with
+      // nothing to compare it against and a chip reading DRAFT — the same chip
+      // a claim nobody ever approved carries. "Never written" and "approved,
+      // then moved" are different jobs for a reviewer, and one word was being
+      // used for both.
+      //
+      // The engine now answers both questions: readiness emits an
+      // `unapproved_edit` cause (so the claim reaches the Issues screen on its
+      // own, without needing a dependent to notice for it), and the graph
+      // payload carries the line diff from the approved body to the current
+      // one. Everything below is presentation of those two facts. It computes
+      // nothing about whether an edit is allowed, and never writes.
+
+      // offlineApprovedEdits reads the per-claim approvaledit.Change records
+      // off the SAME rendered graph payload offlineReadiness reads. There is
+      // deliberately no /api/status path for this: a checked-in file:// export
+      // has no endpoint to ask, and a panel that appeared only under
+      // "dossierx serve" would be missing exactly where a reviewer reads a
+      // repository they did not build.
+      function offlineApprovedEdits() {
+        var node = document.getElementById('dossierx-graph');
+        if (!node) { return {}; }
+        try {
+          var payload = JSON.parse(node.textContent || '{}');
+          var out = {};
+          (payload.nodes || []).forEach(function (graphNode) {
+            if (graphNode.id && graphNode.approved_edit) { out[graphNode.id] = graphNode.approved_edit; }
+          });
+          return out;
+        } catch (_) { return {}; }
+      }
+
+      // editDateLabel turns an RFC3339 instant into the board's own short
+      // form ("12 Sep"). It is deliberately not a full date: the panel's job
+      // is to place the approval in the reader's recent memory, and a
+      // four-part date would be the widest thing on a row that is otherwise
+      // a sentence.
+      function editDateLabel(iso) {
+        if (!iso) { return ''; }
+        var d = new Date(iso);
+        if (isNaN(d.getTime())) { return String(iso).slice(0, 10); }
+        return d.getDate() + ' ' + ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun',
+          'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][d.getMonth()];
+      }
+
+      // approvedEditHasDiff reports whether there is a second wording to show
+      // at all.
+      //
+      // Two states look like an edit and have nothing to put behind a
+      // "Changes" tab. A record written before the approved wording was kept
+      // proves the text moved and does not carry what it moved from. A claim
+      // whose hash moved because its rests_on or build_role moved has an
+      // approved wording identical to its current one. In both, the panel has
+      // one sentence to say and no second version of the claim — so there is
+      // no switch, and the claim's own body stays on screen.
+      //
+      // Offering the switch anyway is what this replaces, and it was worse
+      // than a useless control: choosing "Changes" hid the body and put the
+      // sentence in its place, so the claim rendered with no content at all.
+      function approvedEditHasDiff(change) {
+        return !!(change.content_retained &&
+          (change.hunks || []).length &&
+          (change.changed_passages || 0) > 0);
+      }
+
+      // approvedEditDiff builds the passage-by-passage view: unchanged
+      // passages as ordinary prose, and each changed one as the approved
+      // passage above the current one, tinted and ruled.
+      //
+      // The HTML is produced by the ENGINE (internal/approvaledit), not here,
+      // because internal/render/markdown is this project's only markdown
+      // renderer and the viewer has none. Shipping the source and parsing it
+      // in the browser would mean growing a second renderer that could
+      // disagree with the first about the same claim, on the same page.
+      function approvedEditDiff(change) {
+        var body = el('div', 'claim-edit-diff');
+        (change.hunks || []).forEach(function (hunk) {
+          var cls = hunk.op === 'remove' ? 'claim-edit-passage claim-edit-passage--removed'
+            : hunk.op === 'add' ? 'claim-edit-passage claim-edit-passage--added'
+            : 'claim-edit-passage';
+          var block = el('div', cls);
+          // innerHTML, and only here. The value came from
+          // internal/render/markdown, which is the escaping boundary every
+          // other claim body in this viewer crosses — the same function, over
+          // the same author bytes, reached through the same payload as the
+          // readiness projection beside it. It is exactly as trusted as the
+          // body it sits above, and no more.
+          block.innerHTML = hunk.html || '';
+          if ((hunk.changed || []).length) {
+            block.setAttribute('aria-label',
+              (hunk.op === 'remove' ? 'Removed since approval: ' : 'Added since approval: ') +
+              hunk.changed.join(', '));
+          }
+          body.appendChild(block);
+        });
+        var fields = approvedEditFields(change);
+        if (fields) { body.appendChild(fields); }
+        return body;
+      }
+
+      // approvedEditCurrent builds the OTHER state of the same surface: the
+      // current wording, passage by passage, with the passage that differs
+      // from the approval carrying a draft-coloured rule and no tint (Paper
+      // DPZ-0: "the same claim with the diff put away, reading as ordinary
+      // prose, with an amber rule marking where the difference sits").
+      //
+      // It is rendered from the same hunks as the diff rather than by
+      // un-hiding the claim's own body, because the body cannot say WHERE the
+      // difference sits: the rule is the one thing this state adds, and the
+      // body has no passage boundaries to hang it on.
+      function approvedEditCurrent(change) {
+        var body = el('div', 'claim-edit-current');
+        (change.hunks || []).forEach(function (hunk) {
+          if (hunk.op === 'remove') { return; }
+          var block = el('div', hunk.op === 'add'
+            ? 'claim-edit-passage claim-edit-passage--current'
+            : 'claim-edit-passage');
+          // Same escaping boundary as approvedEditDiff: engine-rendered
+          // markdown, the same bytes the diff shows.
+          block.innerHTML = hunk.html || '';
+          if (hunk.op === 'add') {
+            block.setAttribute('aria-label', 'This passage differs from the approved wording');
+          }
+          body.appendChild(block);
+        });
+        return body;
+      }
+
+      // approvedEditFields draws the non-body fields that moved, each as its
+      // approved YAML above its current YAML with the words that moved marked.
+      //
+      // It is here because "Also changed: embodiment, audit_notes." was not an
+      // answer. For a claim whose prose never moved that sentence WAS the whole
+      // panel, and it named the change without showing it — the reader was told
+      // a field they cannot see had moved in a way they cannot see, and had to
+      // go and read the file to find out what they were being asked about. Six
+      // of the fourteen claims this feature was built for are in exactly that
+      // state.
+      //
+      // The blocks are drawn as text and not as prose. A field is YAML, so it
+      // is escaped by the engine (internal/approvaledit/fields.go) and shown in
+      // the monospace the file itself would show; running it through the
+      // markdown renderer would turn a list into a list and a `*` into
+      // emphasis, and stop it being the thing on disk.
+      function approvedEditFields(change) {
+        var changes = change.field_changes || [];
+        if (!changes.length) { return null; }
+        var wrap = el('div', 'claim-edit-fields');
+
+        var toggle = el('button', 'claim-edit-fields-toggle');
+        toggle.type = 'button';
+        var list = el('div', 'claim-edit-fields-list');
+        var open = true;
+        function paint() {
+          toggle.setAttribute('aria-expanded', String(open));
+          toggle.textContent = (open ? 'Hide' : 'Show') + ' the ' +
+            (changes.length === 1 ? 'field' : changes.length + ' fields') + ' that moved';
+          list.hidden = !open;
+        }
+        toggle.addEventListener('click', function (event) {
+          event.preventDefault();
+          open = !open;
+          paint();
+        });
+
+        changes.forEach(function (field) {
+          var row = el('div', 'claim-edit-field');
+          row.appendChild(textEl('span', 'claim-edit-field-name', field.field));
+          if (field.truncated) {
+            row.appendChild(textEl('p', 'claim-edit-field-note',
+              'This field moved. It is too large to show here; read it in the claim file.'));
+            list.appendChild(row);
+            return;
+          }
+          (field.hunks || []).forEach(function (hunk) {
+            var cls = hunk.op === 'remove' ? 'claim-edit-field-hunk claim-edit-field-hunk--removed'
+              : hunk.op === 'add' ? 'claim-edit-field-hunk claim-edit-field-hunk--added'
+              : 'claim-edit-field-hunk';
+            var block = el('pre', cls);
+            // innerHTML over engine-escaped text. internal/approvaledit's
+            // field renderer HTML-escapes the YAML before it marks anything,
+            // so the only tags that can be in here are the mark spans it
+            // wrote itself — a field holding <script> arrives as text.
+            block.innerHTML = hunk.html || '';
+            if ((hunk.changed || []).length) {
+              block.setAttribute('aria-label',
+                (hunk.op === 'remove' ? 'Removed from ' : 'Added to ') + field.field + ': ' +
+                hunk.changed.join(', '));
+            }
+            row.appendChild(block);
+          });
+          list.appendChild(row);
+        });
+
+        paint();
+        wrap.appendChild(toggle);
+        wrap.appendChild(list);
+        return wrap;
+      }
+
+      // approvedEditNote is what a claim with no second wording says instead of
+      // offering a switch. It sits under the bar, beside the claim's own body
+      // rather than in place of it.
+      function approvedEditNote(change) {
+        var wrap = el('div', 'claim-edit-note');
+        if (!change.content_retained) {
+          wrap.appendChild(textEl('p', 'claim-edit-note-text',
+            'This claim was approved before the approved wording was kept on the record, so there is nothing to compare against. The approval is released and the text has since changed.'));
+          wrap.appendChild(textEl('p', 'claim-edit-note-hint',
+            'Run dossierx claim recover-approved-content to look for the approved revision in this project\u2019s git history.'));
+          return wrap;
+        }
+        var fields = change.other_fields || [];
+        wrap.appendChild(textEl('p', 'claim-edit-note-text', fields.length
+          ? 'The wording is unchanged since approval. ' +
+            (fields.length === 1 ? '1 field moved.' : fields.length + ' fields moved.')
+          : 'The wording is unchanged since approval.'));
+        var diff = approvedEditFields(change);
+        if (diff) { wrap.appendChild(diff); }
+        return wrap;
+      }
+
+      // approvedEditBar is the control the board draws in place of the old
+      // disclosure: a tinted row above the body that says what the reader is
+      // looking at, and a two-segment switch between the two states.
+      //
+      // It is a BAR AND NOT A DISCLOSURE, and the difference is which state
+      // is the default. A disclosure says the diff is an aside a reader opens
+      // if they want it; the board says the opposite — a claim that was
+      // approved and then rewritten opens SHOWING what moved, because that is
+      // the thing about it a reader has to know before they read a word of
+      // it. "Current" is the escape hatch, not the resting state.
+      function approvedEditBar(card, change, showingChanges) {
+        var bar = el('div', 'claim-edit-bar');
+        var approved = editDateLabel(change.approved_at);
+        var hasDiff = approvedEditHasDiff(change);
+        bar.appendChild(textEl('span', 'claim-edit-bar-title',
+          !hasDiff || showingChanges ? 'Edited since it was approved' : 'Showing the current wording'));
+
+        var meta;
+        if (!hasDiff || showingChanges) {
+          meta = 'approved ' + approved + (change.approved_by ? ' \u00b7 ' + change.approved_by : '');
+        } else {
+          var passages = change.changed_passages || 0;
+          meta = passages === 1
+            ? '1 passage differs from the approval of ' + approved
+            : passages + ' passages differ from the approval of ' + approved;
+        }
+        bar.appendChild(textEl('span', 'claim-edit-bar-meta', meta));
+
+        // No second wording, no switch. A control whose two states show the
+        // same thing is not a control, and the one that used to be here did
+        // worse than nothing: picking "Changes" hid the claim's body to make
+        // room for a diff that did not exist.
+        if (!hasDiff) { return bar; }
+
+        var group = el('div', 'claim-edit-switch');
+        group.setAttribute('role', 'group');
+        group.setAttribute('aria-label', 'Which wording to show');
+        [['Changes', true], ['Current', false]].forEach(function (pair) {
+          var seg = el('button', 'claim-edit-switch-seg');
+          seg.type = 'button';
+          seg.textContent = pair[0];
+          seg.setAttribute('aria-pressed', String(showingChanges === pair[1]));
+          seg.addEventListener('click', function (event) {
+            event.preventDefault();
+            paintApprovedEdit(card, change, pair[1]);
+          });
+          group.appendChild(seg);
+        });
+        bar.appendChild(group);
+        return bar;
+      }
+
+      // paintApprovedEdit puts one claim card into one of the two states, and
+      // is the only thing that writes either of them, so the bar and the body
+      // can never disagree about which one is showing.
+      function paintApprovedEdit(card, change, showingChanges) {
+        var hasDiff = approvedEditHasDiff(change);
+        showingChanges = hasDiff && showingChanges;
+        var existingBar = card.querySelector(':scope > .claim-edit-bar');
+        // The diff may already be wrapped: the four-line disclosure wraps it
+        // exactly as it wraps a claim body (system-record.js's BODY_LIKE), so
+        // what has to be taken out is the wrapper when there is one.
+        var existingDiff = card.querySelector(
+          ':scope > .claim-edit-diff, :scope > .claim-edit-current, :scope > .claim-edit-note, :scope > .claim-body-disclosure--edit');
+
+        var bar = approvedEditBar(card, change, showingChanges);
+        if (existingBar) { existingBar.replaceWith(bar); }
+        else {
+          var head = card.querySelector(':scope > .k');
+          if (head && head.parentNode) { head.parentNode.insertBefore(bar, head.nextSibling); }
+          else { card.insertBefore(bar, card.firstChild); }
+        }
+        if (existingDiff) { existingDiff.remove(); }
+        if (!hasDiff) {
+          bar.parentNode.insertBefore(approvedEditNote(change), bar.nextSibling);
+        } else {
+          bar.parentNode.insertBefore(
+            showingChanges ? approvedEditDiff(change) : approvedEditCurrent(change), bar.nextSibling);
+          // Hand the new surface to the disclosure so it is measured and
+          // clamped like any other body. Without this the diff renders at
+          // full height while the claim beside it truncates at four lines,
+          // which is the same claim behaving as two components.
+          if (typeof window.dossierxEnhanceSystemRecord === 'function') {
+            window.dossierxEnhanceSystemRecord();
+          }
+        }
+        // While there is a second wording the claim's own body is off screen
+        // in BOTH states: Changes shows the diff and Current shows the same
+        // passages with the changed one ruled. The body comes back only for
+        // a claim with nothing to compare against (no switch).
+        //
+        // The body is hidden by a class ON THE CARD, and not by setting
+        // `hidden` on the body itself. Which element the body IS moves: the
+        // long-body disclosure wraps
+        // .claim-body inside .claim-body-disclosure after first paint, so a
+        // handle taken before the wrap and a handle taken after it are
+        // different nodes. Hiding whichever one a selector matched at the
+        // time left the first one hidden and the second one shown, and the
+        // switch stopped working on exactly the claims long enough to be
+        // wrapped. A class on the card is one state, in one place, whatever
+        // the body is wearing.
+        //
+        // It is also why the body is hidden rather than removed: it carries
+        // the source-note clamps and that disclosure, and tearing it out
+        // would take their state with it every time the reader flipped.
+        card.classList.toggle('claim--edit-view', hasDiff);
+      }
+
+      // renderApprovedEdits upgrades each edited claim's chip and puts its
+      // card into the Changes state. It is driven from mountSurface (so the
+      // surface reaches its final height before anything measures or scrolls
+      // it) and again from renderStatusStrip, so everything it creates is
+      // either guarded or left alone on a repaint.
+      function renderApprovedEdits() {
+        var edits = offlineApprovedEdits();
+        Object.keys(edits).forEach(function (id) {
+          var card = document.getElementById(id);
+          if (!card) { return; }
+          var change = edits[id];
+
+          // The chip. It stays .pill.pv — the claim IS a draft, and inventing
+          // a fourth colour for it would say the state is unrelated to the
+          // one the reader already knows. Only the word changes, because only
+          // the word was wrong: DRAFT is true and incomplete, and a reviewer
+          // deciding what to open next needs the part it leaves out.
+          var pill = card.querySelector('.k .label .pill');
+          if (pill && pill.getAttribute('data-dx-edited') !== '1') {
+            pill.setAttribute('data-dx-edited', '1');
+            pill.setAttribute('title', 'This claim held an approval, which was released when it was unlocked. What is written now differs from what was approved.');
+            // No padlock. Paper (CDJ-0, CIY-0) draws the chip as the word
+            // alone: the open padlock is DRAFT's glyph, and this chip exists
+            // to say the state is NOT plain draft.
+            pill.textContent = '';
+            pill.appendChild(textEl('span', '', 'Edited \u00b7 was approved'));
+          }
+
+          // The bar and the body state, painted only when the bar is not
+          // already there: a repaint must not take the reader's own "Current"
+          // choice away from them on the next poll.
+          if (!card.querySelector(':scope > .claim-edit-bar')) {
+            paintApprovedEdit(card, change, true);
+          }
+        });
+      }
+
+      // approvedEditIDsIn returns the claims in the id set that have been
+      // rewritten since the approval their unlock released.
+      //
+      // It is scoped to the facet on screen, by the SAME id set the rest of
+      // the strip scopes itself with — a count of claims on a page the reader
+      // is not looking at is a count they cannot act on. The set is a
+      // null-prototype object keyed by id (activeFacetClaimIDs), so this
+      // indexes it rather than calling Set or Array methods it does not have.
+      function approvedEditIDsIn(claimIDs) {
+        var ids = claimIDs || {};
+        return Object.keys(offlineApprovedEdits()).filter(function (id) { return ids[id]; });
+      }
+
       // renderStatusStrip paints one /api/status payload. An empty verdict hides
       // the strip entirely rather than showing a green badge: the viewer already
       // reads as "fine" by default, and a persistent all-clear chip would be one
       // more thing to stop noticing.
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary || !stripTitle) { return; }
+        if (!stripEl || !stripBody || !stripCard || !stripFindings || !stripTitle) { return; }
         lastStatusData = data || {};
         var claimIDs = activeFacetClaimIDs();
         var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
@@ -14334,6 +15399,7 @@
         // PREVIOUS pass's state, since this function only writes it below.
         var bannerShowing = groups.length > 0 && actionable;
         renderClaimReadiness(lastStatusData.readiness || offlineReadiness(), bannerShowing);
+        renderApprovedEdits();
         if (!groups.length || !actionable) {
           stripEl.hidden = true;
           stripEl.classList.remove('status-strip--integrity', 'status-strip--lint');
@@ -14427,12 +15493,18 @@
           recordEl.classList.add('status-group--approval-record');
           stripBody.appendChild(recordEl);
         }
+        // Under the Needs-you filter the group's weight phrase counts what is
+        // waiting on the reader, not what is blocked (Paper EMP-0: "3 of 4
+        // need you here").
+        var needsTotal = countSeverity(groups, 'needs_you');
         moduleOrder.forEach(function (moduleID) {
           var rows = byModule[moduleID].slice().sort(function (a, b) {
             return statusGroupClaimCount(b) - statusGroupClaimCount(a);
           });
           var weight = uniqueClaimCount(rows);
-          var note = 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
+          var note = stripSeverityFilter === 'needs_you'
+            ? weight + ' of ' + needsTotal + ' need you here'
+            : 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
           stripBody.appendChild(findingGroup(moduleLabel(moduleID), rows.map(renderStatusGroup), note));
         });

@@ -14442,38 +15514,189 @@
         // used to repeat as a second string. statusChipLine, the function
         // that built that string, is now dead (no other call site) and is
         // deleted.
-        var headline = blockerHeadline(groups);
-        var needsYou = countSeverity(groups, 'needs_you') > 0;
-        if (ledger.length) {
-          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
-        } else if (lintErrors.length && !headline && !needsYou) {
-          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
-        } else if (headline) {
-          stripTitle.textContent = headline;
-        } else {
-          stripTitle.textContent = countLabel(groups.length, 'grouped issue') + ' in this facet';
-        }
+        //
+        // Both forms of the head, every paint. The desktop band carries the
+        // single headline sentence it always did; the phone card carries one
+        // row per finding. They are derived from the SAME groups, ledger and
+        // lint lists a line apart, so the two can differ in shape and never
+        // in what they say.
+        var rows = statusStripRows(groups, ledger, lintErrors, claimIDs);
+        renderStatusStripCard(rows);
+        // The band gets the same rows, one full-bleed row each (Paper board
+        // 15, C2M-0 / C92-0), in the same order — most serious first — so the
+        // two forms differ in shape and never in what they say.
+        renderStatusStripBand(rows);

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        // Paper's default reading view always starts with the findings body
-        // collapsed. Critical and needs-you counts still determine the
-        // truthful summary/chips above; they do not take control of the
-        // reader's disclosure state. Once the reader opens the strip, the
-        // existing stripExpanded state remains sticky across later polls.
-        setStripExpanded(stripExpanded);
+        // #statusStripBody is populated above and stays hidden: the Issues
+        // screen MOVES its children into itself (issuesSyncFromStrip), so it
+        // must exist and must not be shown here.
+        if (stripBody) { stripBody.hidden = true; }
         positionStatusStrip();
         stripEl.hidden = false;
       }

-      function setStripExpanded(expanded) {
-        stripExpanded = !!expanded;
-        if (stripBody) { stripBody.hidden = !stripExpanded; }
-        if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
-        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
-        if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
+      // statusStripRows turns the facet's findings into the card's rows —
+      // one row per KIND of thing waiting, never one per claim.
+      //
+      // The rows are the summary sentences the strip already computed, each
+      // now carrying its own hue instead of four of them competing to be the
+      // one headline. `tone` is the row's colour family, and it is the only
+      // thing the card's tint rule reads: 'alarm' for anything the approval
+      // record or the dependency chain is refusing, 'draft' for work in
+      // progress that nobody has approved yet.
+      function statusStripRows(groups, ledger, lintErrors, claimIDs) {
+        var rows = [];
+        if (ledger.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(ledger.length, 'approval record issue') + ' in this facet need' +
+              (ledger.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        var headline = blockerHeadline(groups);
+        if (headline) {
+          rows.push({ tone: 'alarm', severity: 'blocker', text: headline });
+        }
+        if (lintErrors.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(lintErrors.length, 'issue') + ' in this facet need' +
+              (lintErrors.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        // Claims rewritten since approval. This is the row the old
+        // single-sentence head had no space for, and the reason it became a
+        // card: it is amber where everything above it is red, and one tinted
+        // surface cannot make two severity claims at once.
+        var edited = approvedEditIDsIn(claimIDs);
+        if (edited.length) {
+          rows.push({
+            tone: 'draft', severity: 'needs_you',
+            text: countLabel(edited.length, 'claim') + ' ' + (edited.length === 1 ? 'has' : 'have') +
+              ' edits that have not been approved'
+          });
+        }
+        if (!rows.length) {
+          rows.push({
+            tone: 'alarm', severity: '',
+            text: countLabel(groups.length, 'grouped issue') + ' in this facet'
+          });
+        }
+        return rows;
       }

+      // renderStatusStripCard paints the head. Tinted while every row shares
+      // a tone, neutral the moment they disagree — Paper EH1-0's own rule,
+      // and the reason it is a rule: a tinted surface IS a severity claim,
+      // so a card holding a red fact and an amber one has to stop making it
+      // and let the dots carry the colour instead.
+      // statusStripRowAction names the way in each row offers. A blocked row
+      // opens the Issues screen as it always did; an edited-since-approval
+      // row says what it is for (Paper CDS-0: "Review changes").
+      function statusStripRowAction(row) {
+        return row.tone === 'draft' ? 'Review changes' : 'Show issues';
+      }
+
+      // openIssuesForBandRow: a blocked row opens the screen UNFILTERED — its
+      // sentence names one finding but the band stands for the whole facet.
+      // An edited row names exactly what it filters to (the claims waiting on
+      // the reader), which is what earns it the filter, same as a card row.
+      function openIssuesForBandRow(tone, severity) {
+        stripSeverityFilter = tone === 'draft' ? (severity || '') : '';
+        renderStatusStrip(lastStatusData);
+        openIssuesView();
+      }
+
+      // renderStatusStripBand paints the DESKTOP form: the static first row
+      // (#statusStripToggle, so the ids the rest of the viewer and its tests
+      // address stay put) takes the first finding, and one more
+      // .status-strip-head button is appended per further finding. Each row
+      // carries data-tone, which is the only thing the stylesheet reads to
+      // pick its tint, its icon and its action colour.
+      function renderStatusStripBand(rows) {
+        if (!stripToggle || !stripTitle || !stripEl) { return; }
+        var first = rows[0];
+        stripToggle.setAttribute('data-tone', first.tone);
+        stripToggle.setAttribute('data-severity', first.severity || '');
+        stripTitle.textContent = first.text;
+        var firstAction = document.getElementById('statusStripAction');
+        if (firstAction) { firstAction.textContent = statusStripRowAction(first); }
+
+        stripEl.querySelectorAll(':scope > .status-strip-head--row').forEach(function (node) { node.remove(); });
+        rows.slice(1).forEach(function (row) {
+          var btn = el('button', 'status-strip-head status-strip-head--row');
+          btn.type = 'button';
+          btn.setAttribute('data-tone', row.tone);
+          btn.setAttribute('data-severity', row.severity || '');
+          var summary = el('span', 'status-strip-summary');
+          var icon = dxIcon('triangle-alert');
+          icon.classList.add('status-strip-icon');
+          summary.appendChild(icon);
+          summary.appendChild(el('span', 'status-strip-dot'));
+          var text = el('span', 'status-strip-summary-text');
+          text.appendChild(textEl('strong', 'status-strip-title', row.text));
+          summary.appendChild(text);
+          btn.appendChild(summary);
+          var actionRow = el('span', 'status-strip-action-row');
+          actionRow.appendChild(textEl('span', 'status-strip-action', statusStripRowAction(row)));
+          btn.appendChild(actionRow);
+          btn.addEventListener('click', function (event) {
+            event.preventDefault();
+            openIssuesForBandRow(row.tone, row.severity);
+          });
+          // After the previous row and before the phone card, so the band is
+          // one stack of rows whatever the count.
+          var before = stripCard || stripBody;
+          if (before && before.parentNode === stripEl) { stripEl.insertBefore(btn, before); }
+          else { stripEl.appendChild(btn); }
+        });
+      }
+
+      function renderStatusStripCard(rows) {
+        if (!stripCard || !stripFindings) { return; }
+        var tones = {};
+        rows.forEach(function (row) { tones[row.tone] = true; });
+        var toneKeys = Object.keys(tones);
+        var tinted = toneKeys.length === 1 ? toneKeys[0] : '';
+        stripCard.classList.toggle('status-strip-card--alarm', tinted === 'alarm');
+        stripCard.classList.toggle('status-strip-card--draft', tinted === 'draft');
+        if (stripCardCount) { stripCardCount.textContent = String(rows.length); }
+
+        stripFindings.textContent = '';
+        rows.forEach(function (row) {
+          // A button, not a div with a handler: each row is a way into the
+          // Issues screen and has to be reachable by keyboard and named to a
+          // screen reader like the one control it is.
+          var el_ = el('button', 'status-strip-finding');
+          el_.type = 'button';
+          el_.setAttribute('data-tone', row.tone);
+          var dot = el('span', 'status-strip-finding-dot');
+          dot.setAttribute('aria-hidden', 'true');
+          el_.appendChild(dot);
+          el_.appendChild(textEl('span', 'status-strip-finding-text', row.text));
+          // A text glyph, not the 14px icon: EH1-0 draws the row's way-in as
+          // a 12px "›" in the card's own hue.
+          var chev = textEl('span', 'status-strip-finding-chevron', '›');
+          chev.setAttribute('aria-hidden', 'true');
+          el_.appendChild(chev);
+          el_.addEventListener('click', function (event) {
+            event.preventDefault();
+            // Filtered to the row's own severity. On a real corpus the
+            // unfiltered screen is hundreds of dependency rows and the thing
+            // this row named is somewhere inside them; a way in that lands a
+            // reader in a list they then have to search has not answered the
+            // question it asked. Same stripSeverityFilter the chips set, so
+            // the chip is pressed on arrival and one more click clears it.
+            stripSeverityFilter = row.severity || '';
+            renderStatusStrip(lastStatusData);
+            openIssuesView();
+          });
+          stripFindings.appendChild(el_);
+        });
+      }
+
       // refreshStatus polls the endpoint. A FAILED poll deliberately leaves the
       // last verdict on screen untouched: a transient fetch failure (the server
       // restarting mid-reload) must never be able to clear an integrity warning,
@@ -14487,25 +15710,20 @@
       }

       // R09.6 (Paper ZP-0): "Issues is its own screen, not an expansion. The
-      // banner is the only way in." Paper backs that with node evidence:
-      // 56T-0, 574-0 and 6P-0 draw ONE state — the sentence plus "Show
-      // issues" — the desktop banner is a single flex ROW with no slot for a
-      // body, the string "Hide issues" does not exist anywhere in the design
-      // file, and 04 section 4 records that no board in the group draws an
-      // expanded state. The inline expand listener that lived here is gone;
-      // the toggle's only job is opening the Issues screen.
+      // banner is the only way in." No board in the group draws an expanded
+      // banner, and the string "Hide issues" does not exist anywhere in the
+      // design file, so the strip has no inline disclosure at all.
       //
-      // setStripExpanded is deliberately KEPT: renderStatusStrip still calls
-      // it every poll with a permanently-false stripExpanded, and that is
-      // what holds #statusStripBody hidden, aria-expanded="false" and the
-      // action reading "Show issues".
+      // EH1-0 then replaced the one sentence with one row per finding, and
+      // the entry point went with it: each row opens the Issues screen,
+      // filtered to that row's own severity. "The banner is the only way in"
+      // still holds — the banner simply has more than one door now, because
+      // a facet routinely has more than one kind of thing waiting.
       //
       // #statusStripBody is never emptied, removed or left unpopulated:
       // renderStatusStrip still builds into it, issuesSyncFromStrip still
       // MOVES its children into the Issues screen, and closeIssuesView still
-      // moves them back. Only its visibility to the reader is withdrawn — the
-      // children now return into a hidden body instead of an expanded one,
-      // which is the defect this removes.
+      // moves them back. It is simply never shown in place.

       // ================================================================
       // Issues view (04-issues-screen.md) — lane L8a
@@ -14514,9 +15732,8 @@
       // The screen's own chrome around lane L8's status-strip vocabulary:
       // breadcrumb, title/subtitle, scope and sort controls, the findings
       // card and the "WHERE THE BLOCKERS LIVE" rail (04 §3, §4.1-4.3, §4.5,
-      // §4.6, §4.9). R09.6: reached ONLY from "Show issues" — the listener
-      // just below runs AFTER the one directly above (source order), so by
-      // the time it fires `stripExpanded` already holds L8's own new value.
+      // §4.6, §4.9). R09.6: reached ONLY from a row of the banner card, each
+      // of which wires openIssuesView itself as it is built.
       //
       // #issuesFilters and #issuesFindingsCard never build their own copy of
       // the severity chips or the grouped findings list: issuesSyncFromStrip
@@ -14621,11 +15838,59 @@
         var m = issuesModuleWeights(issuesScope).claims;
         issuesFindingsCard.querySelectorAll('.status-group-head-note').forEach(function (note) {
           var match = /^blocks (\d+) of \d+ claims? here$/.exec(note.textContent || '');
-          if (!match) { return; }
-          note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+          if (match) {
+            note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+            return;
+          }
+          // The Needs-you phrase: the same denominator rule, over the rows
+          // the filter is showing (issuesGroupsForScope already applies it).
+          var waiting = /^(\d+) of \d+ need you here$/.exec(note.textContent || '');
+          if (waiting) { note.textContent = waiting[1] + ' of ' + m + ' need you here'; }
         });
       }

+      // issuesCauseWeights is the rail's OTHER ranking (Paper EKL-0, "WHAT IS
+      // WAITING"): under the Needs-you filter the question is what kind of
+      // work is waiting rather than where the blockers live, so the rail
+      // counts causes, by kind, and colours each bar by the kind's hue. A
+      // claim waiting for two reasons is counted under each, and the caveat
+      // says so.
+      var ISSUES_CAUSE_LABELS = {
+        unapproved_edit: 'Unapproved edits',
+        direct_dependency_change: 'Dependency changed',
+        upstream_dependency_review: 'Upstream review',
+        own_flag: 'Flagged for review',
+        own_thread: 'Open review thread',
+        approval_content_drift: 'Approved wording drifted',
+        approval_released: 'Approval released',
+        approval_missing: 'Approval record missing',
+        approval_unknown: 'Approval state unknown'
+      };
+      function issuesCauseWeights(scope) {
+        var groups = issuesGroupsForScope(scope);
+        var byKind = {};
+        var order = [];
+        groups.forEach(function (g) {
+          var kind = g.kind || 'review';
+          if (!Object.prototype.hasOwnProperty.call(byKind, kind)) { byKind[kind] = []; order.push(kind); }
+          byKind[kind].push(g);
+        });
+        var rows = order.map(function (kind) {
+          return {
+            id: kind,
+            label: ISSUES_CAUSE_LABELS[kind] || kind.replace(/_/g, ' '),
+            tone: kind === 'unapproved_edit' ? 'draft' : 'alarm',
+            weight: uniqueClaimCount(byKind[kind])
+          };
+        });
+        rows.sort(function (a, b) { return b.weight - a.weight; });
+        return {
+          rows: rows,
+          claims: uniqueClaimCount(groups),
+          causes: rows.reduce(function (sum, r) { return sum + r.weight; }, 0)
+        };
+      }
+
       // renderIssuesRail (04 §4.9): a ranking with bars, proportional to the
       // facet's blocked-claim TOTAL (§4.9's own measured widths — retry fix
       // 4, coordinator ruling: "rail bars use the spec's rounding basis" —
@@ -14645,10 +15910,15 @@
       // corpus size or scope.
       function renderIssuesRail() {
         if (!issuesRailRowsEl) { return; }
-        var data = issuesModuleWeights(issuesScope);
+        var waiting = stripSeverityFilter === 'needs_you';
+        var data = waiting ? issuesCauseWeights(issuesScope) : issuesModuleWeights(issuesScope);
+        var heading = document.getElementById('issuesRailHeading');
+        if (heading) { heading.textContent = waiting ? 'WHAT IS WAITING' : 'WHERE THE BLOCKERS LIVE'; }
         issuesRailRowsEl.textContent = '';
         var shown = data.rows.slice(0, ISSUES_RAIL_MAX_ROWS);
-        var total = data.claims;
+        // Module bars are proportional to the blocked-claim total; cause bars
+        // to the cause total (EKL-0: 2 of 4 causes draws round(50%)).
+        var total = waiting ? data.causes : data.claims;
         shown.forEach(function (row) {
           var wrap = el('div', 'issues-rail-row');
           var labelRow = el('div', 'issues-rail-row-label');
@@ -14657,6 +15927,7 @@
           wrap.appendChild(labelRow);
           var track = el('div', 'issues-rail-bar-track');
           var fill = el('div', 'issues-rail-bar-fill');
+          if (row.tone) { fill.setAttribute('data-tone', row.tone); }
           fill.style.width = (total ? Math.round((row.weight / total) * 100) : 0) + '%';
           track.appendChild(fill);
           wrap.appendChild(track);
@@ -14666,10 +15937,14 @@
           issuesRailRowsEl.appendChild(textEl('p', 'issues-rail-more',
             (data.rows.length - ISSUES_RAIL_MAX_ROWS) + ' more not shown'));
         }
-        var caveat = data.claims > 0
-          ? (countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
-             ' — a claim blocked through two modules counts under both.')
-          : '';
+        var caveat = '';
+        if (waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ', ' + countLabel(data.causes, 'cause') +
+            ' — a claim can be waiting for more than one reason and is counted under each.';
+        } else if (!waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
+            ' — a claim blocked through two modules counts under both.';
+        }
         if (issuesRailCaveatEl) { issuesRailCaveatEl.textContent = caveat; }
         if (issuesCaveatEl) { issuesCaveatEl.textContent = caveat; }
         // §8 item 5: an empty ranking is a ranking of nothing — the panel is
@@ -14695,7 +15970,13 @@
             (lastStatusData && lastStatusData.readiness) || offlineReadiness(),
             activeFacetClaimIDs(), [], [], [], []
           );
-          issuesSubtitleEl.textContent = blockerHeadline(groups) || 'Nothing in this facet is blocked.';
+          // Under the Needs-you filter the sentence is about the reader's
+          // work, not the facet's blockers (Paper ENQ-0).
+          var waiting = stripSeverityFilter === 'needs_you' ? countSeverity(groups, 'needs_you') : 0;
+          issuesSubtitleEl.textContent = waiting
+            ? countLabel(waiting, 'claim') + (waiting === 1 ? ' is' : ' are') +
+              ' waiting on you in this facet. Each one names what moved and what it was measured against.'
+            : (blockerHeadline(groups) || 'Nothing in this facet is blocked.');
         }
       }

@@ -14743,6 +16024,10 @@
         }
         issuesApplyScopeDenominator();
         renderIssuesRail();
+        // The subtitle follows the filter (a chip click repaints the strip,
+        // which lands here through the observer), so it is re-read on every
+        // sync and not only when the screen opens.
+        issuesSyncHeader();
         if (issuesStripObserver) { issuesStripObserver.observe(stripBody, { childList: true }); }
       }

@@ -14855,14 +16140,20 @@
       if (issuesSortControl) {
         issuesSortControl.addEventListener('click', function (event) { event.preventDefault(); });
       }
-      // "Show issues" (R09.6) opens this view — UNCONDITIONALLY on every
-      // click of this control, whichever label it currently reads. The
-      // strip's own inline expand/collapse (the listener directly above,
-      // L8's, toggling stripExpanded/#statusStripBody) changes the sticky
-      // disclosure state first. R09.6 names this same control as the Issues
-      // entry point, so this listener does not gate on that resulting state.
+      // R09.6's "the banner is the only way in" holds for both forms. The
+      // desktop band has one door, bound here; the phone card has one per
+      // finding, each wired as it is built (see renderStatusStripCard)
+      // because each carries its own severity filter.
+      //
+      // The band's door opens the screen UNFILTERED, and that is not an
+      // oversight: its sentence names one finding but the band stands for the
+      // whole facet, so narrowing to that one row's severity would hide the
+      // others behind a filter the reader did not choose. A card row names
+      // exactly what it filters to, which is what earns it the filter.
       if (stripToggle) {
-        stripToggle.addEventListener('click', openIssuesView);
+        stripToggle.addEventListener('click', function () {
+          openIssuesForBandRow(stripToggle.getAttribute('data-tone'), stripToggle.getAttribute('data-severity'));
+        });
       }
       // Any ordinary navigation — a sidebar tab, a subtab, a citation link —
       // leaves this screen the same way the back chevron does. Capture phase
@@ -15733,8 +17024,23 @@
     window.scrollTo({ top: Math.max(0, Math.round(window.scrollY + top - offset)), behavior: 'instant' });
   }

+  // BODY_LIKE is what the four-line disclosure clamps: a claim's own body, and
+  // the passage-by-passage diff that stands in for it while the reader is
+  // looking at what changed since approval.
+  //
+  // The diff is in this set because it IS the body, shown a passage at a time
+  // — so a claim that clamps to four lines with "... more" in one state and
+  // runs to full height in the other is the same claim behaving as two
+  // different components. Sharing the mechanism rather than copying it also
+  // means the toggle's label, its aria wiring and its scroll compensation
+  // cannot drift between the two.
+  // .claim-edit-current is the other state of the same surface: the current
+  // wording rendered from the same passages, with the changed one ruled. It
+  // stands in for the body exactly as the diff does, so it clamps like it.
+  var BODY_LIKE = '.claim-body, .claim-edit-diff, .claim-edit-current';
+
   function setClaimBodyExpanded(wrapper, expanded) {
-    var body = wrapper && wrapper.querySelector(':scope > .claim-body');
+    var body = wrapper && wrapper.querySelector(':scope > .claim-body, :scope > .claim-edit-diff, :scope > .claim-edit-current');
     var toggle = wrapper && wrapper.querySelector(':scope > .claim-body-disclosure__toggle');
     if (!body || !toggle || toggle.hidden) { return; }
     wrapper.classList.toggle('claim-body-disclosure--expanded', expanded);
@@ -15747,13 +17053,19 @@
   }

   function syncClaimBodyDisclosures() {
-    document.querySelectorAll('.claim-body').forEach(function (body) {
+    document.querySelectorAll(BODY_LIKE).forEach(function (body) {
       var wrapper = body.parentElement && body.parentElement.classList.contains('claim-body-disclosure')
         ? body.parentElement
         : null;
       if (!wrapper) {
         wrapper = document.createElement('div');
         wrapper.className = 'claim-body-disclosure';
+        // The diff's wrapper is marked, because the card hides the claim's
+        // own body while the changes are showing and must not hide the diff
+        // along with it — both are a .claim-body-disclosure by then.
+        if (body.classList.contains('claim-edit-diff') || body.classList.contains('claim-edit-current')) {
+          wrapper.classList.add('claim-body-disclosure--edit');
+        }
         body.parentNode.insertBefore(wrapper, body);
         wrapper.appendChild(body);
         if (!body.id) { body.id = 'claim-body-' + (++claimBodyDisclosureSequence); }
@@ -15809,7 +17121,6 @@

   function enhanceConformanceFooters() {
     document.querySelectorAll('details.claim-conformance').forEach(function (original) {
-      if (original.dataset.footerEnhanced === 'true') { return; }
       var claim = original.closest('.claim');
       var footer = claim && claim.querySelector('.claim-footer');
       var summary = original.querySelector(':scope > summary');
@@ -15914,13 +17225,6 @@
   }

   function renderToc() {
-    // Theme-parity's hover probe forces :hover through CDP node ids. This
-    // function replaceChildren()s the TOC list on every enhance pass (the
-    // layout MutationObserver retriggers enhance when addModuleHeaders
-    // rewrites the module head), so a node id dies between QuerySelector
-    // and ForcePseudoState. The harness sets this flag for the duration of
-    // that probe; it is otherwise unset.
-    if (window.__dxParityFreezeToc) { return; }
     var toc = document.getElementById('systemFacetToc');
     if (!toc) {
       toc = document.createElement('aside');
@@ -16614,19 +17918,6 @@
     return { from: from, to: to, type: type };
   }

-  // normalizeEdges maps a whole edge list, dropping unusable entries.
-  function normalizeEdges(edges) {
-    var list = asArray(edges);
-    var out = [];
-    for (var i = 0; i < list.length; i++) {
-      var e = normalizeEdge(list[i]);
-      if (e) {
-        out.push(e);
-      }
-    }
-    return out;
-  }
-
   // idSet builds a lookup Set from an id array. Internal only.
   function idSet(ids) {
     var s = new Set();
@@ -18367,7 +19658,6 @@
   var parseTimeHash = ''; // the graph state segment seen at parse time
   var originClaimId = ''; // OPEN_ID_ATTR off the trigger that opened the pane, '' from the nav trigger
   var collapseOverride = false; // the reader insisted on every claim
-  var notices = []; // {text, kind, action} rendered above the canvas
   var dragging = null; // {id} while a node is being dragged
   var panning = null; // {x, y} while the background is being dragged
   var movedWhileDown = false;

testdata/fixture-portability/build/viewer/index.html  (+1552 -262)
    size            21977 lines / 1180989 bytes -> 23267 lines / 1238682 bytes
@@ -5365,6 +5365,15 @@
    form; only the bottom hairline separates it from the body beneath. The
    520px inset-card form below is a DIFFERENT, deliberately distinct board
    (R-H.1) and keeps its own margin/border/radius, untouched by this. */
+/* THIS IS THE DESKTOP FORM and is unchanged: a full-bleed band carrying one
+   summary sentence with "Show issues" beside it. At 1440 a row has the space
+   to carry a sentence and an action without either crowding the other.
+
+   The PHONE form is a different component — a card with one row per finding
+   (Paper EH1-0) — and lives under "The banner card" near the end of this
+   file. Both are in the DOM; the 520px block picks one. Everything from here
+   to .status-strip-action-chevron therefore applies to the desktop form only,
+   and the mobile block switches all of it off rather than re-tuning it. */
 .status-strip {
   width: 100%;
   background: var(--card-bg);
@@ -5377,25 +5386,26 @@
   display: none;
 }

+/* The strip itself is card ground. Its TINT lives on each row (below,
+   keyed by data-tone), because a band with two findings on it carries two
+   hues and a surface cannot make two severity claims at once (Paper board
+   15, C2M-0 red over C92-0 amber, hairline-divided). */
 .status-strip--lint {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
 }

 .status-strip--integrity {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
   box-shadow: none;
 }

-/* retry fix 20: 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop
-   banner at 13px/32px, not 13px/14px — 32px is the reading card's own
-   gutter (--card-gutter, G8), reproduced here as a literal because
-   .status-strip mounts as a sibling of the record header, never as a
-   descendant of .card/.claim-tree, so the custom property does not cascade
-   to it. The 520px inset-card form re-pins this back down to 14px in this
-   lane's own appended mobile block, matching the value this rule carried
-   (and was verified against) before this fix. */
+/* 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop banner at
+   13px/32px — 32px is the reading card's own gutter (--card-gutter, G8),
+   reproduced here as a literal because .status-strip mounts as a sibling of
+   the record header, never as a descendant of .card/.claim-tree, so the
+   custom property does not cascade to it. */
 .status-strip-head {
   display: flex;
   align-items: center;
@@ -5409,11 +5419,9 @@
   cursor: pointer;
 }

-/* retry fix 14: 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning
-   triangle ahead of the banner sentence at every width, in both schemes — it
-   was absent everywhere. .status-strip-summary becomes the icon's row; the
-   title/note stack (unchanged) moves into its own -text wrapper so the two
-   IDs the runtime writes to keep working untouched. */
+/* 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning triangle
+   ahead of the banner sentence. .status-strip-summary is the icon's row; the
+   title stack sits in its own -text wrapper. */
 .status-strip-summary {
   display: flex;
   align-items: flex-start;
@@ -5430,20 +5438,70 @@
   flex-shrink: 0;
 }

+/* One row per finding, each its own tint (Paper C2M-0: blocked ground
+   #9E3B360F; CDR-0: edited ground #9A6A1612), divided by the hairline. The
+   icon slot is 15px either way: the triangle for an alarm row, a 7px dot in
+   the draft hue for an edited row, so the sentences of stacked rows share
+   one left edge. */
+.status-strip-head[data-tone="alarm"] { background: var(--status-strip-bg); }
+.status-strip-head[data-tone="draft"] { background: var(--status-draft-bg, rgba(154, 106, 22, .11)); }
+.status-strip-head + .status-strip-head { border-top: 1px solid var(--border); }
+
+.status-strip-dot {
+  display: none;
+  width: 15px;
+  height: 15px;
+  flex-shrink: 0;
+  align-items: center;
+  justify-content: center;
+}
+
+.status-strip-dot::before {
+  content: "";
+  width: 7px;
+  height: 7px;
+  border-radius: 999px;
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-head[data-tone="draft"] .status-strip-icon { display: none; }
+.status-strip-head[data-tone="draft"] .status-strip-dot { display: inline-flex; }
+.status-strip-head[data-tone="draft"] .status-strip-action { color: var(--status-draft, #976600); }
+
 .status-strip-summary-text {
   display: grid;
   gap: 3px;
   min-width: 0;
 }

-/* retry fix 21: 02 §4.10 "Body" / 03 §4.5 "Body text" measure the banner
-   sentence at Inter 14/18 weight 400, not 13px weight 680 — the sentence
-   reads as body prose, not an emphasized heading. */
+/* 02 §4.10 "Body" / 03 §4.5 "Body text": the banner sentence is Inter 14/18
+   weight 400 — it reads as body prose, not as an emphasized heading. */
 .status-strip-title {
   font: 400 14px/18px var(--font-sans);
   color: var(--ink);
 }

+/* 02 §4.10/§4.19, 03 §4.5/§7.5, R-H.1: "Show issues" is the ALARM half of the
+   banner (the body sentence stays --color-ink) and reads in the same red as
+   the icon, never the generic --accent link colour. */
+.status-strip-action {
+  color: var(--warn);
+  font: 500 13px/16px var(--font-sans);
+  letter-spacing: 0;
+  text-transform: none;
+  white-space: nowrap;
+}
+
+.status-strip-action-row {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+}
+
+/* § 4.10 draws no trailing chevron on the desktop banner. */
+.status-strip-action-chevron {
+  display: none;
+}
 /* .status-strip-note (the "Critical 0 · Needs you 3 · ..." tally line) is
    REMOVED per the coordinator's ruling on this retry: no 02/03/04 board
    draws it, and the severity chips already carry every count it repeated.
@@ -5458,30 +5516,12 @@
    500, 13/16 — R-H.1's weight 600 at 13/16 is the PHONE step (added in
    L8's own 520 block below), not the desktop default. --warn is correct at
    both widths and is unchanged. */
-.status-strip-action {
-  color: var(--warn);
-  font: 500 13px/16px var(--font-sans);
-  letter-spacing: 0;
-  text-transform: none;
-  white-space: nowrap;
-}
-
 /* retry fix 15: the action text (#statusStripAction, whose textContent the
    runtime overwrites on every toggle) and its trailing chevron are separate
    elements inside this row wrapper — the row, not the text span, is what
    the 520 block turns into R-H.1's divided action row, so a re-render can
    never wipe the chevron out from under it. The chevron itself is hidden
    above 520 (§ 4.10 draws no trailing chevron on the desktop banner). */
-.status-strip-action-row {
-  display: inline-flex;
-  align-items: center;
-  gap: 6px;
-}
-
-.status-strip-action-chevron {
-  display: none;
-}
-
 /* The detail list is capped and scrolls internally: a project with forty lint
    errors must not grow a dock that swallows the page it is describing. */
 /* retry fix 16: the promoted list is 04 §3/§9 item 4's bottom-open card —
@@ -5744,6 +5784,81 @@
   font-weight: 600;
 }

+/* Paper board 15 (BR5-0 / ETC-0 / EUD-0): a row's third line — the cause
+   kind in mono, a mono summary of what moved, and for a blocker row whose
+   owner was approved and is being rewritten, the way to its diff. */
+.status-finding-detail {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 9px;
+  padding-top: 1px;
+}
+
+.status-finding-kind {
+  padding: 2px 7px;
+  border-radius: 999px;
+  font: 500 10px/12px var(--font-mono);
+  background: var(--warn-bg);
+  color: var(--warn);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-kind,
+.status-finding-kind--edited {
+  background: var(--status-draft-bg);
+  color: var(--status-draft);
+}
+
+/* BR5-0: the chip under a blocker row is the claim chip's word, at the
+   row's scale — Inter 10/14 600, 0.06em, uppercase. */
+.status-finding-kind--edited {
+  font: 600 10px/14px var(--font-sans);
+  letter-spacing: .06em;
+  text-transform: uppercase;
+}
+
+.status-finding-meta {
+  font: 400 11px/14px var(--font-mono);
+  color: var(--muted);
+}
+
+.status-finding-link {
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  font: 600 11px/14px var(--font-sans);
+  color: var(--link);
+}
+
+.status-finding-link:hover { text-decoration: underline; }
+
+/* EUD-0: a Needs-you row's right-hand slot is its way in, in the row's hue,
+   where a blocker row keeps its count. */
+.status-finding-action {
+  flex: 0 0 auto;
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  text-align: right;
+  font: 600 14px/18px var(--font-sans);
+  color: var(--warn);
+}
+
+.status-finding-action:hover { text-decoration: underline; }
+
+/* A row caused by an edit since approval is draft work, not an alarm: its
+   dot and its action take the draft hue (EUD-0). */
+.status-finding--group[data-tone="draft"] .status-finding-dot,
+.status-finding--group[data-severity="needs_you"][data-tone="draft"] .status-finding-dot {
+  background: var(--status-draft);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-action {
+  color: var(--status-draft);
+}
+
 /* file:// has no /api/status and therefore no ledger_findings at all (04 §8
    item 11 / §9 item 5) — stated in the body rather than silently omitted. */
 .status-strip-static-note {
@@ -5870,7 +5985,7 @@
   .comment-reply-trigger,
   .comment-composer-submit,
   .comment-composer-input,
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
   }
@@ -5934,12 +6049,15 @@
     border-left: 0;
     /* R-J.2 / OD14a.7 / M4, verifier RETRY item 4(iii): a dark-only 1px top
        hairline (the light sheet relies on its 16px corners and the scrim
-       alone). `light-dark()` expresses "present only under dark" without a
-       second selector-scoped dark query or a new dark-only token. A
+       alone). --sheet-hairline is the engine-owned token declared for
+       exactly this edge (:root and the two dark blocks): `transparent` in
+       light, var(--border) in dark, on both the OS path and the explicit
+       Dark toggle. It used to be a `light-dark()` literal while the token
+       sat unread; the token is the single place the edge is decided. A
        component-scoped `@media screen and (prefers-color-scheme: dark)`
-       block is unavailable
-       here (theme_tokens_test.go's ExactlyOneScreenScopedDarkQuery). */
-    border-top: 1px solid light-dark(transparent, var(--border));
+       block is unavailable here (theme_tokens_test.go's
+       ExactlyOneScreenScopedDarkQuery). */
+    border-top: 1px solid var(--sheet-hairline);
     border-top-left-radius: 16px;
     border-top-right-radius: 16px;
     border-radius: 16px 16px 0 0;
@@ -6015,25 +6133,10 @@
     max-width: 100%;
     box-sizing: border-box;
   }
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
-  }
-  .status-strip-head {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) 10px;
-    align-items: flex-start;
-  }
-  .status-strip-summary {
-    grid-column: 1;
-    grid-row: 1;
-  }
-  .status-strip-action-row {
-    grid-column: 1;
-    grid-row: 2;
-    justify-self: start;
-  }
-}
+  }}

 /* The claim head line. v0.4.1 gives it two children instead of loose text:
    a <span class="label"> carrying the label plus its status pill, and the
@@ -6576,24 +6679,9 @@
   background: var(--accent-bg);
 }

-/* nav-group-label separates the Tracks run from the modules above it. The
-   sidebar is otherwise a flat list of .sec-tab buttons, and a track tab looks
-   exactly like a module tab because mechanically it IS one — so without a
-   caption a reader would have no way to tell that the axis changed halfway
-   down. It borrows the .eyebrow register (mono, uppercase, --faint) rather
-   than inventing one, because it is the same kind of thing: a quiet caption
-   over the content beneath it, never a control. The top border and margin are
-   what make it read as a division rather than as a first entry in the run. */
-.nav-group-label {
-  margin: 14px 0 4px;
-  padding: 10px 20px 0;
-  border-top: 1px solid var(--border);
-  font-family: var(--font-mono);
-  font-size: 9px;
-  letter-spacing: .14em;
-  text-transform: uppercase;
-  color: var(--faint);
-}
+/* .nav-group-label (the caption over the Tracks run) is gone: the sidebar
+   groups modules and tracks in .system-nav-group <details> (shell.html), and
+   nothing has emitted the class since the system-record redesign. */

 /* Secondary horizontal facet tab strip inside a module's content area,
    ported verbatim (selector + declarations) from the reference docs
@@ -7222,14 +7310,6 @@
   color: var(--link);
 }

-.nav-group-label {
-  margin: 17px 0 5px;
-  padding: 12px 10px 0;
-  color: var(--faint);
-  font-size: 9px;
-  letter-spacing: .12em;
-}
-
 .sidebar-footer {
   padding: 11px 18px 15px;
   color: var(--faint);
@@ -8015,9 +8095,23 @@
   max-width: 760px;
 }

-.claim-body-disclosure > .claim-body { max-width: none; }
+.claim-body-disclosure > .claim-body,
+.claim-body-disclosure > .claim-edit-diff,
+.claim-body-disclosure > .claim-edit-current { max-width: none; }

-.claim-body-disclosure--collapsed > .claim-body {
+/* .claim-edit-diff is clamped alongside .claim-body because it IS the body,
+   shown a passage at a time (see system-record.js's BODY_LIKE). A claim that
+   truncates to four lines in one state and runs to full height in the other
+   is the same claim behaving as two components.
+   The clamp counts LINE BOXES and is not a height: the diff's passages carry
+   their own margins, and a tinted one carries padding, so a collapsed diff is
+   four lines of prose PLUS whatever box the first passage sits in. It is
+   therefore a little taller than a collapsed body and not a fixed multiple of
+   the line height — which is the honest description, and what the test
+   asserts rather than an exact 4x that does not hold. */
+.claim-body-disclosure--collapsed > .claim-body,
+.claim-body-disclosure--collapsed > .claim-edit-diff,
+.claim-body-disclosure--collapsed > .claim-edit-current {
   display: -webkit-box;
   overflow: hidden;
   -webkit-box-orient: vertical;
@@ -9762,31 +9856,10 @@
     border-radius: 10px;
     border-color: color-mix(in srgb, var(--warn) 26%, var(--border));
   }
-  /* retry fix 29 (this lane's third attempt): the desktop banner's own
-     padding moved from a literal 14px to the reading card's 32px gutter
-     (retry fix 20 above), but the 520px form stays the inset-card treatment
-     R-H.1 always specified. 02 §4.19 "Body block" gives the mobile form's
-     own padding as 12px/14px — one px shallower than the pre-retry-3 value
-     this rule carried, which was never checked against §4.19 directly. */
-  .status-strip-head {
-    padding: 12px 14px;
-  }
-  /* retry fix 33 (this lane's third attempt, 02 §4.19 "Icon"): the mobile
-     icon's optical offset is 2px, not the desktop row's 1px. */
-  .status-strip-icon {
-    margin-top: 2px;
-  }
-  /* retry fix 34 (this lane's third attempt, 02 §4.19 "Body text"): the
-     mobile banner sentence is Inter 13/19, a full step down from the
-     desktop 14/18 fix 21 (above) set — §4.19 is a distinct node (`574-0`)
-     from §4.10's `6T-0`, not the same rule at a smaller viewport. */
-  .status-strip-title {
-    font-size: 13px;
-    line-height: 19px;
-  }
-  .status-strip--open {
-    border-radius: 10px 10px 0 0;
-  }
+  /* The banner's own head padding, icon offset and sentence size used to be
+     re-pinned here for the phone. All three described the single-sentence
+     head that Paper EH1-0 replaced with a card, and their rules went with
+     it; the card carries its own metrics at both widths. */
   .status-strip-body {
     border-top-color: color-mix(in srgb, var(--warn) 18%, var(--border));
   }
@@ -9861,32 +9934,6 @@
     height: 30px;
     padding-inline: 12px;
   }
-  /* retry fix 15 (R-H.1, 02 §4.19): the action gets its own divided row —
-     a 1px top hairline at ~18% blocked tint, a 38px row and 14px inline
-     padding, bled to the head's own inline/bottom padding edges so the tap
-     target is the card's full width. `grid-column: 1 / -1` reclaims the
-     10px caret column too, since the divider must span edge to edge. */
-  .status-strip-action-row {
-    grid-column: 1 / -1;
-    justify-self: stretch;
-    display: flex;
-    align-items: center;
-    justify-content: space-between;
-    height: 38px;
-    margin: 8px -14px -13px;
-    padding-inline: 14px;
-    border-top: 1px solid color-mix(in srgb, var(--warn) 18%, transparent);
-  }
-  .status-strip-action {
-    font-weight: 600; /* retry fix 13: the 600 step is the PHONE form only. */
-  }
-  .status-strip-action-chevron {
-    display: inline-block;
-    width: 13px;
-    height: 13px;
-    color: var(--warn);
-    flex-shrink: 0;
-  }
 }

 /* retry fix 17 (LANES.md OD3, and fix 11's technique): .status-severity-chip
@@ -10287,13 +10334,19 @@
   background: var(--paper);
 }

-/* 04 §4.9: the bar is always draft amber, ranking VOLUME, not severity —
-   never colour these bars per row severity. */
+/* 04 §4.9: the module ranking's bar is always draft amber, ranking VOLUME,
+   not severity. The cause ranking under the Needs-you filter (Paper EKL-0,
+   "WHAT IS WAITING") is the one exception: each bar takes its cause's hue,
+   because there the rows ARE kinds of work and the hue is what tells them
+   apart. */
 .issues-rail-bar-fill {
   height: 5px;
   background: var(--status-draft);
 }

+.issues-rail-bar-fill[data-tone="alarm"] { background: var(--warn); }
+.issues-rail-bar-fill[data-tone="draft"] { background: var(--status-draft); }
+
 /* 04 §8 item 8: the rail is a ranking, so it truncates rather than scrolls. */
 .issues-rail-more {
   margin: 0;
@@ -11001,9 +11054,7 @@
   font: 13px/16px var(--font-sans);
 }
 .reading-canvas .claim-footer .comment-chip .dx-icon { width: 14px; height: 14px; }
-@media screen and (min-width: 861px) {
-  .reading-canvas .status-strip-action-chevron { display: none; }
-}
+@media screen and (min-width: 861px) {}
 @media screen and (max-width: 520px) {
   /* `display: flow-root` gives the canvas a block-formatting context without
      the side effects of restoring a border or `overflow: clip`. Without one,
@@ -11048,15 +11099,6 @@
      carries a 20px margin-bottom standing in for both and .reading-canvas has
      no border or padding at this tier, so the banner's own 14px collapses
      into that 20px and the reader gets 20px. State the no-tab-row case. */
-  .reading-canvas .status-strip-head { padding: 12px 14px; }
-  /* 56U-0: the triangle hangs on the FIRST text line (align-items: start,
-     10px gap), not centred against a three-line sentence. */
-  .reading-canvas .status-strip-summary { gap: 10px; align-items: flex-start; }
-  /* 56Y-0: Inter 13/19 — a full step down from the desktop 14/18. */
-  .reading-canvas .status-strip-title { font-size: 13px; line-height: 19px; }
-  /* 56Z-0: the divided action row bleeds to the head's own 14px inline and
-     12px bottom padding edges; Paper draws no extra gap above the rule. */
-  .reading-canvas .status-strip-action-row { margin: 12px -14px -12px; }
   .reading-canvas .claim-footer {
     margin-top: 6px;
     padding-top: 13px;
@@ -11097,6 +11139,534 @@
   .reading-canvas .claim-footer-chip--blocked .claim-footer-chip-count { color: var(--warn); }
 }

+/* ---- The banner card, and unapproved edits -------------------------------
+
+   ONE CARD, A ROW PER FINDING (Paper node EH1-0).
+
+   The banner used to be a single sentence with a "Show issues" action beside
+   it. That shape holds exactly one statement, and a facet routinely has two
+   kinds of thing waiting: claims blocked by unapproved dependencies, and
+   claims rewritten since they were approved. The second fact had to go into a
+   second tinted band stacked underneath, and two tinted bands touching read as
+   one broken component rather than as two facts.
+
+   TINT IS A SEVERITY CLAIM, so the card makes at most one. While every row
+   shares a hue the whole surface takes it; the moment two rows disagree the
+   surface goes neutral and the dots carry the colour instead. That is why
+   --alarm and --draft are separate modifiers and the neutral form is the base
+   rule: which one applies is computed from the rows, never from whichever kind
+   of finding happens to sort first. */
+
+/* The card is the PHONE form. Everything below describes the card itself; the
+   one thing that describes WHEN it applies is `display`, which is none here
+   and flex in the 520px block at the end of this file. Keeping the switch to
+   a single property in a single place is what stops a later rule in this
+   block from quietly turning the card back on at every width — which is
+   exactly what a `display: flex` sitting among the card's own layout rules
+   did on the first attempt. */
+.status-strip-card {
+  display: none;
+  flex-direction: column;
+  border: 1px solid var(--border-strong);
+  border-radius: 10px;
+  overflow: clip;
+  background: var(--card-bg);
+}
+
+.status-strip-card-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+  padding: 9px 14px;
+  background: var(--code-bg);
+  border-bottom: 1px solid var(--border);
+}
+
+.status-strip-card-eyebrow {
+  font: 600 11px/14px var(--font-sans);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+  color: var(--muted);
+}
+
+/* Mono, like every other machine-produced count in this viewer: the number is
+   read off the findings, not written by anyone. */
+.status-strip-card-count {
+  font: 11px/14px var(--font-mono, var(--font-sans));
+  color: var(--faint);
+}
+
+/* A button, not a row with a click handler. Each row is one way into the
+   Issues screen and has to be reachable by keyboard and announced as the
+   control it is. */
+.status-strip-finding {
+  display: flex;
+  align-items: flex-start;
+  gap: 10px;
+  width: 100%;
+  padding: 11px 14px;
+  border: 0;
+  background: none;
+  text-align: left;
+  cursor: pointer;
+  color: var(--ink);
+}
+
+.status-strip-finding + .status-strip-finding {
+  border-top: 1px solid var(--border);
+}
+
+.status-strip-finding-dot {
+  width: 7px;
+  height: 7px;
+  flex-shrink: 0;
+  margin-top: 6px;
+  border-radius: 999px;
+  background: var(--warn);
+}
+
+.status-strip-finding[data-tone="draft"] .status-strip-finding-dot {
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-finding-text {
+  flex: 1;
+  min-width: 0;
+  font: 13px/19px var(--font-sans);
+  color: var(--ink);
+}
+
+/* EH1-0: a 12px "\203a" glyph, in the card's hue (the tinted forms below
+   recolour it), optically dropped 2px to sit on the sentence's first line. */
+.status-strip-finding-chevron {
+  flex-shrink: 0;
+  margin-top: 2px;
+  color: var(--faint);
+  font: 400 12px/16px var(--font-sans);
+}
+
+/* The two tinted forms. The alphas are EH1-0's own (#9A6A1612 / #9A6A1645 /
+   #9A6A163D over the draft token), written as color-mix so the dark scheme
+   redefines them from its own hue instead of carrying a second literal. */
+.status-strip-card--alarm {
+  background: color-mix(in srgb, var(--warn) 7%, transparent);
+  border-color: color-mix(in srgb, var(--warn) 27%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-eyebrow,
+.status-strip-card--alarm .status-strip-card-count,
+.status-strip-card--alarm .status-strip-finding-chevron {
+  color: var(--warn);
+}
+
+.status-strip-card--alarm .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--draft {
+  background: color-mix(in srgb, var(--status-draft, #976600) 7%, transparent);
+  border-color: color-mix(in srgb, var(--status-draft, #976600) 27%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-eyebrow,
+.status-strip-card--draft .status-strip-card-count,
+.status-strip-card--draft .status-strip-finding-chevron {
+  color: var(--status-draft, #976600);
+}
+
+.status-strip-card--draft .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+/* The per-claim edit surface (Paper DON-0 / C9H-0).
+
+   It is a BAR ABOVE THE BODY with a two-segment switch, not a disclosure, and
+   the difference is which state is the default. A disclosure says the diff is
+   an aside a reader opens if they want it; the board says the opposite — a
+   claim that was approved and then rewritten opens SHOWING what moved,
+   because that is the thing about it a reader has to know before they read a
+   word of it. "Current" is the escape hatch, not the resting state. */
+/* While the changes are showing, the claim's own body is hidden by this class
+   on the CARD. See paintApprovedEdit for why the state does not live on the
+   body: the long-body disclosure re-wraps it after first paint, so the body
+   is not the same node before and after. */
+.claim--edit-view > .claim-body,
+.claim--edit-view > .claim-body-disclosure:not(.claim-body-disclosure--edit),
+.claim--edit-view > .claim-empty,
+.claim--edit-view > .claim-mockup-body {
+  display: none;
+}
+
+/* THE BAR WRAPS BY CONTENT, NOT BY VIEWPORT.
+   Its width is the reading column's, not the window's — about 690px at 1440
+   — so a media query cannot see the moment it runs out of room. The Current
+   state's line ("1 passage differs from the approval of 20 Sep") is the long
+   one, and at that column it overran by a few pixels and broke mid-date:
+   "...the approval of 20 / Sep".
+   Wrapping instead of shrinking is deliberate. The three ways to make it fit
+   were truncating the sentence, stepping the type down, or letting the row
+   become two rows; the first hides a fact, the second makes a 12px mono line
+   smaller than anything else on the card, and the third costs 8px of height
+   in the rare case. The switch is what moves, because it is the only part
+   with no sentence in it. */
+/* Paper CKF-0: the bar is the draft tint (--color-draft-bg), radius 6,
+   9px/13px, and it sits in the card's own 16px rhythm — no margin of its
+   own. Title and meta share a baseline with 10px between them. */
+.claim-edit-bar {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 8px 10px;
+  margin: 0;
+  padding: 9px 13px;
+  border-radius: 6px;
+  background: var(--status-draft-bg, rgba(154, 106, 22, .11));
+  font-family: var(--font-sans);
+}
+
+.claim-edit-bar-title {
+  flex: 0 0 auto;
+  font: 600 13px/18px var(--font-sans);
+  color: var(--status-draft, #976600);
+}
+
+/* Mono, like every other machine-read fact in this viewer: the date, the
+   actor and the passage count are read off the ledger, not written by
+   anyone. */
+/* The meta's own width is its CONTENT's, which is what makes the switch the
+   first thing to wrap: flex lays the row out from the items' base sizes, so
+   title + this sentence + switch overflowing moves the switch — the only item
+   with no sentence in it — down to its own row, and the sentence gets the
+   whole first row instead of being squeezed into what was left over.
+   It stays shrinkable on purpose. Refusing to shrink made it overrun the bar
+   entirely at 900px, where even a full row is not enough; wrapping inside
+   itself is a worse line but a contained one, and it is the last resort
+   rather than the first thing that happens. */
+.claim-edit-bar-meta {
+  flex: 1 1 auto;
+  min-width: 0;
+  font: 11px/16px var(--font-mono, var(--font-sans));
+  color: var(--muted);
+}
+
+/* The switch. The selected segment is a filled dark pill, which is the one
+   place in this panel that spends a strong value — everything else is a tint,
+   and the reader has to be able to see which of two states they are in from
+   across the page. */
+/* Paper CKJ-0: a card-ground pill with a 1px hairline, 2px inset, segments
+   3px/11px at 11/14. */
+.claim-edit-switch {
+  display: inline-flex;
+  align-items: center;
+  flex: 0 0 auto;
+  /* Pinned right on the first row, and still right when it wraps to its own. */
+  margin-left: auto;
+  gap: 0;
+  padding: 2px;
+  border: 1px solid var(--border);
+  border-radius: 999px;
+  background: var(--card-bg);
+}
+
+.claim-edit-switch-seg {
+  border: 0;
+  border-radius: 999px;
+  padding: 3px 11px;
+  background: none;
+  cursor: pointer;
+  font: 500 11px/14px var(--font-sans);
+  color: var(--muted);
+}
+
+.claim-edit-switch-seg[aria-pressed="true"] {
+  background: var(--ink);
+  color: var(--card-bg);
+  font-weight: 600;
+}
+
+/* The passages. Typography is the claim body's own — this IS the claim body,
+   shown a passage at a time — so a reader moving between Changes and Current
+   is reading the same prose in the same face, not a second document. */
+.claim-edit-diff,
+.claim-edit-current {
+  font-family: var(--font-serif);
+  font-size: 17px;
+  line-height: 28px;
+  max-width: 760px;
+}
+
+/* Passages keep the body's 16px rhythm between them (the card's own gap).
+   Margins rather than flex gap, because the four-line clamp turns this box
+   into a -webkit-box and a flex gap would vanish with it. */
+.claim-edit-passage {
+  margin: 0;
+}
+
+.claim-edit-passage + .claim-edit-passage {
+  margin-top: 16px;
+}
+
+.claim-edit-passage > :first-child {
+  margin-top: 0;
+}
+
+.claim-edit-passage > :last-child {
+  margin-bottom: 0;
+}
+
+/* Paper CK7-0 / CKP-0: a changed passage is a tinted row, radius 4, with a
+   3px rule INSIDE it (12px in from the edge, 12px before the text) that runs
+   the height of the text, old above new with 2px between them and 4px above
+   the pair. The prose itself takes the hue: blocked for the approved
+   passage, locked for the current one. Colour is not the only carrier — the
+   removed passage is struck through and the added one is not, so the two
+   stay apart in a monochrome print, under a colour-vision deficiency, and
+   in the dark scheme where the two tints are close in luminance. */
+.claim-edit-passage--removed,
+.claim-edit-passage--added,
+.claim-edit-passage--current {
+  position: relative;
+  border-radius: 4px;
+}
+
+.claim-edit-passage--removed,
+.claim-edit-passage--added {
+  padding: 7px 12px 7px 27px;
+}
+
+.claim-edit-passage--removed::before,
+.claim-edit-passage--added::before,
+.claim-edit-passage--current::before {
+  content: "";
+  position: absolute;
+  left: 12px;
+  top: 7px;
+  bottom: 7px;
+  width: 3px;
+  border-radius: 2px;
+  background: currentColor;
+}
+
+.claim-edit-passage--removed {
+  color: var(--warn);
+  background: var(--warn-bg, rgba(158, 59, 54, .10));
+  text-decoration: line-through 1px;
+  text-underline-position: from-font;
+}
+
+.claim-edit-passage--added {
+  color: var(--accent);
+  background: var(--accent-bg, rgba(44, 107, 82, .10));
+}
+
+.claim-edit-passage + .claim-edit-passage--removed {
+  margin-top: 20px;
+}
+
+.claim-edit-passage--removed + .claim-edit-passage--added {
+  margin-top: 2px;
+}
+
+/* The Current state (Paper DPZ-0): the passage that differs carries only a
+   draft-coloured rule at the left edge — no tint, prose in ink — so the
+   claim reads as ordinary prose with the seam marked. */
+.claim-edit-passage--current {
+  padding: 0 0 0 15px;
+}
+
+.claim-edit-passage--current::before {
+  left: 0;
+  top: 0;
+  bottom: 0;
+  background: var(--status-draft, #976600);
+}
+
+/* The words that moved, inside the passage they moved in. They stay in the
+   DOM for the passage's aria-label and for a reader who selects the text,
+   but Paper draws the passage as one tinted, coloured, struck (or not)
+   block and paints nothing on the individual words — the struck passage and
+   the coloured one are the comparison. */
+.claim-edit-word--removed,
+.claim-edit-word--added {
+  background: none;
+  color: inherit;
+  padding: 0;
+}
+
+.claim-edit-note {
+  margin: 8px 0 0;
+}
+
+.claim-edit-note-text,
+.claim-edit-note-hint,
+.claim-edit-field-note {
+  margin: 0;
+  font: 400 13px/19px var(--font-sans);
+  color: var(--muted);
+}
+
+/* The hint names the command that ENDS this state. A record written before
+   the approved wording was kept says "there is nothing to compare against",
+   and until this line existed that was the end of the sentence — true, and
+   with no way out of it. The wording is usually still in the project's own
+   git history, so the panel that reports the gap also names the verb that
+   closes it. */
+.claim-edit-note-hint {
+  margin-top: 4px;
+  color: var(--faint);
+}
+
+/* The fields that moved. This block is TEXT, not prose: it shows the YAML the
+   claim file holds, so it is monospace and pre-wrapped, and the tints are the
+   same two the passage diff uses so a reader does not learn a second colour
+   language halfway down one panel. */
+.claim-edit-fields {
+  margin: 8px 0 0;
+}
+
+.claim-edit-fields-toggle {
+  appearance: none;
+  -webkit-appearance: none;
+  background: none;
+  border: 0;
+  padding: 0;
+  cursor: pointer;
+  font: 500 13px/19px var(--font-sans);
+  color: var(--accent);
+  text-align: left;
+}
+
+.claim-edit-fields-toggle:hover { text-decoration: underline; }
+
+.claim-edit-fields-list {
+  margin-top: 6px;
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+}
+
+.claim-edit-field-name {
+  display: block;
+  font: 600 12px/18px var(--font-mono);
+  color: var(--muted);
+}
+
+.claim-edit-field-hunk {
+  margin: 3px 0 0;
+  padding: 6px 8px;
+  border-radius: 6px;
+  border-left: 3px solid transparent;
+  background: var(--code-bg);
+  font: 400 12px/18px var(--font-mono);
+  /* pre-wrap and not pre: a claims file's embodiment block is indented deep
+     enough to overflow a narrow column, and a horizontal scrollbar inside a
+     panel inside a claim is three nested scroll regions. Wrapping keeps the
+     indentation and loses only the guarantee that a line is one line. */
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+}
+
+.claim-edit-field-hunk--removed {
+  border-left-color: var(--warn);
+  background: color-mix(in srgb, var(--warn) 7%, var(--code-bg));
+}
+
+.claim-edit-field-hunk--added {
+  border-left-color: var(--border-strong);
+}
+
+@media (max-width: 520px) {
+  /* The phone swaps the whole banner: the desktop band's sentence-and-action
+     row goes away and the card takes its place, so .status-strip stops being
+     a painted surface and becomes the gutter that positions the card.
+     .reading-canvas > .status-strip is named alongside it because that rule
+     is two-class and unqueried, and would otherwise outrank this one and
+     restore the band's border underneath the card. */
+  .status-strip-head {
+    display: none;
+  }
+
+  .status-strip-card {
+    display: flex;
+  }
+
+  .reading-canvas > .status-strip,
+  .status-strip,
+  .status-strip--lint,
+  .status-strip--integrity {
+    background: none;
+    border: 0;
+    border-radius: 0;
+    margin: 0;
+    /* The gutter is the reading card's own 16px, not the desktop 32px —
+       EH1-0's mobile frame draws the card at mx-4 inside a 390px canvas. */
+    padding: 14px 16px 0;
+  }
+
+  /* The edit bar stacks into three rows (Paper CHK-0): the sentence, the
+     machine line under it, and the switch full width with two equal halves.
+     Side by side at this width the sentence wrapped to two lines, the mono
+     meta broke mid-word, and the switch was squeezed against them — three
+     things competing for a 343px row that only ever held one comfortably. */
+  .claim-edit-bar {
+    flex-direction: column;
+    align-items: stretch;
+    gap: 8px;
+    padding: 10px 12px;
+  }
+
+  .claim-edit-bar-title {
+    /* CKY-0: the title and its machine line are 2px apart on the phone. */
+    margin-bottom: -6px;
+  }
+
+  .claim-edit-bar-meta {
+    /* The phone stack gives every part its own row already. */
+    flex: none;
+    line-height: 15px;
+  }
+
+  .claim-edit-switch {
+    display: flex;
+    margin-left: 0;
+  }
+
+  .claim-edit-switch-seg {
+    flex: 1;
+    padding: 5px 10px;
+    font-size: 12px;
+    line-height: 16px;
+    text-align: center;
+  }
+
+  /* CKP-0: the rule and the text sit 10px in on a phone, not 12. */
+  .claim-edit-passage--removed,
+  .claim-edit-passage--added {
+    padding: 7px 10px 7px 23px;
+  }
+
+  .claim-edit-passage--removed::before,
+  .claim-edit-passage--added::before {
+    left: 10px;
+  }
+
+  .claim-edit-passage + .claim-edit-passage--removed {
+    margin-top: 18px;
+  }
+}
+
+
 @media print {
   :root { color-scheme: light; }

@@ -11115,10 +11685,6 @@
     display: block;
   }

-  .claim-links-summary {
-    cursor: auto;
-  }
-
   /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
@@ -11338,7 +11904,7 @@
     <div class="issues-body">
       <div id="issuesFindingsCard" class="issues-findings-card"></div>
       <aside id="issuesRailPanel" class="issues-rail-panel">
-        <p class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
+        <p id="issuesRailHeading" class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
         <p id="issuesRailCaveat" class="issues-rail-caveat"></p>
         <div id="issuesRailRows" class="issues-rail-rows"></div>
       </aside>
@@ -11365,9 +11931,10 @@


   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
-    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
+    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody" data-tone="alarm">
       <span id="statusStripSummary" class="status-strip-summary">
         <svg class="dx-icon status-strip-icon" aria-hidden="true"><use href="#dx-icon-triangle-alert"/></svg>
+        <span class="status-strip-dot" aria-hidden="true"></span>
         <span class="status-strip-summary-text">
           <strong id="statusStripTitle" class="status-strip-title"></strong>
         </span>
@@ -11377,6 +11944,13 @@
         <svg class="dx-icon status-strip-action-chevron" aria-hidden="true"><use href="#dx-icon-chevron-right"/></svg>
       </span>
     </button>
+    <div id="statusStripCard" class="status-strip-card">
+      <div class="status-strip-card-head">
+        <span id="statusStripCardEyebrow" class="status-strip-card-eyebrow">Needs you here</span>
+        <span id="statusStripCardCount" class="status-strip-card-count"></span>
+      </div>
+      <div id="statusStripFindings" class="status-strip-card-findings"></div>
+    </div>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>

@@ -11425,8 +11999,6 @@
       // each SSE re-render. It attaches NO listeners — those are delegated once
       // on surviving nodes below, so re-running this never double-binds.
       function initViewer() {
-        // Fragment swaps replace <main>; drop the stale mount pointer.
-        mountedSurfaceID = null;
         // Two-level show/hide: one .module-section per sidebar .sec-tab, and
         // (when a module has more than one facet) one .claim-group per .subtab
         // inside that module's .sub-nav. A single-facet module still has exactly
@@ -11553,7 +12125,6 @@
       // surface on first paint; smaller fixtures mount every surface so print,
       // theme probes, and live-reload witnesses that call getElementById keep
       // working without visiting every facet first.
-      var mountedSurfaceID = null;
       var SOFT_MOUNT_MIN_CLAIMS = 80;

       function claimCorpusSize() {
@@ -11581,7 +12152,6 @@
         var host = group.querySelector(':scope > [data-dossierx-surface-host]');
         var tmpl = group.querySelector(':scope > template.dossierx-surface-template');
         if (!host || !tmpl) {
-          mountedSurfaceID = surfaceID;
           return;
         }
         if (!host.querySelector('.claim')) {
@@ -11589,13 +12159,31 @@
           if (typeof window.dossierxEnhanceSystemRecord === 'function') {
             window.dossierxEnhanceSystemRecord();
           }
+          // The edited-since-approval chips and panels are attached HERE, at
+          // mount, and BEFORE the clamps below — not left to the next
+          // renderStatusStrip pass.
+          //
+          // They insert a node above each edited claim's body, so attaching
+          // them later moves everything under them. Two things read that
+          // height and cannot be allowed to read it early: a deep link has
+          // already scrolled to its claim, so the panels appearing afterwards
+          // slid the page out from under the sticky header; and the
+          // source-note clamps measure scrollHeight against clientHeight, so
+          // a mid-measurement insertion decides "show more" from a layout
+          // that is about to change. Decorating first means the surface is at
+          // its final height before anything measures or scrolls it.
+          renderApprovedEdits();
           // Source-note clamps observe live nodes only; re-arm after every
           // first-time mount so a freshly revealed surface is measured.
           if (typeof mountSourceNoteClamps === 'function') {
             mountSourceNoteClamps();
           }
         }
-        mountedSurfaceID = surfaceID;
+        // The no-template path (an eagerly rendered surface) never enters the
+        // block above, and still has claims to decorate. renderApprovedEdits
+        // is idempotent — every node it creates is either replaced wholesale
+        // or guarded — so the second call costs nothing on the path that did.
+        renderApprovedEdits();
       }

       function mountAllSurfaces() {
@@ -11857,10 +12445,8 @@
         if (!toastEl) { return; }
         toastEl.textContent = msg; // textContent — error text is never trusted markup
         toastEl.hidden = false;
-        toastEl.classList.add('comments-toast--show');
         if (toastTimer) { window.clearTimeout(toastTimer); }
         toastTimer = window.setTimeout(function () {
-          toastEl.classList.remove('comments-toast--show');
           toastEl.hidden = true;
         }, 4000);
       }
@@ -12701,21 +13287,30 @@
       // claim has no edges".

       var stripEl = document.getElementById('statusStrip');
-      var stripToggle = document.getElementById('statusStripToggle');
-      var stripSummary = document.getElementById('statusStripSummary');
-      var stripTitle = document.getElementById('statusStripTitle');
       // retry fix 21 (this lane's third attempt): #statusStripNote (the
       // "Critical 0 · Needs you 3 · ..." tally) is REMOVED per the
       // coordinator's ruling — no 02/03/04 board draws it, the chips
       // already carry every count it repeated — and dropped from shell.html
       // along with its element lookup here.
-      var stripAction = document.getElementById('statusStripAction');
+      //
+      // THE HEAD HAS TWO FORMS, ONE PER WIDTH, and both are written on every
+      // paint. #statusStripToggle is the desktop band — one summary sentence
+      // with "Show issues" beside it — and #statusStripCard is the phone form
+      // (Paper EH1-0), a card with one ROW PER FINDING. CSS shows one; see
+      // shell.html for why the choice is a media query and not a width read
+      // in here.
+      //
+      // Both are filled unconditionally rather than behind a matchMedia
+      // check. The cost is a few DOM nodes nobody sees; the alternative is a
+      // resize listener and a breakpoint duplicated out of the stylesheet,
+      // which is one more thing to keep in step for no gain.
+      var stripToggle = document.getElementById('statusStripToggle');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripCard = document.getElementById('statusStripCard');
+      var stripCardCount = document.getElementById('statusStripCardCount');
+      var stripFindings = document.getElementById('statusStripFindings');
       var stripBody = document.getElementById('statusStripBody');
       var lastStatusData = null;
-      // Paper starts every status strip collapsed. stripExpanded then stays
-      // sticky across refreshes, so a poll (or an SSE tick) never takes the
-      // disclosure state away from the reader.
-      var stripExpanded = false;

       function countLabel(n, word) {
         return n + ' ' + word + (n === 1 ? '' : 's');
@@ -12922,12 +13517,51 @@
       // demoted slug, and the fixed-width count slot — rather than three
       // loose wrapping siblings. See style.css's .status-finding-dot /
       // .status-finding-text for the layout this DOM shape enables.
+      // editChangeSummary is the mono line under an Issues row for a claim
+      // rewritten since approval (Paper BR5-0 / EUD-0): what moved, and when
+      // the approval it moved from was given.
+      function editChangeSummary(change) {
+        var parts = [];
+        if (!change.content_retained) {
+          parts.push('approved wording not retained');
+        } else if (change.changed_passages) {
+          parts.push(countLabel(change.changed_passages, 'passage') + ' changed');
+        } else if ((change.field_changes || []).length) {
+          parts.push(countLabel(change.field_changes.length, 'field') + ' moved');
+        }
+        var approved = editDateLabel(change.approved_at);
+        if (approved) { parts.push('approved ' + approved); }
+        return parts.join(' · ');
+      }
+
+      // goToClaimChanges leaves the Issues screen for the claim itself, with
+      // the Changes state showing — the row said "See changes", so landing
+      // on the claim with the diff put away would not be what it offered.
+      function goToClaimChanges(id, change) {
+        closeIssuesView();
+        if (window.location.hash !== '#' + id) { window.location.hash = '#' + id; }
+        var card = document.getElementById(id);
+        if (card && change) { paintApprovedEdit(card, change, true); }
+        if (card && typeof card.scrollIntoView === 'function') { card.scrollIntoView({ block: 'start' }); }
+      }
+
       function renderStatusGroup(group) {
         var row = el('li', 'status-finding status-finding--group');
         row.setAttribute('data-severity', group.severity);
+        // A row whose cause is an edit since approval is draft work, not an
+        // alarm (Paper EUD-0 draws its dot and action in the draft hue).
+        var edits = offlineApprovedEdits();
+        var editedOwner = group.ownerModuleClaimID && edits[group.ownerModuleClaimID] ? group.ownerModuleClaimID : '';
+        var isEditCause = group.severity === 'needs_you' && group.kind === 'unapproved_edit' && !!editedOwner;
+        row.setAttribute('data-tone', isEditCause ? 'draft' : 'alarm');
         row.appendChild(el('span', 'status-finding-dot'));
         var text = el('span', 'status-finding-text');
-        text.appendChild(textEl('span', 'status-finding-rule', group.title || 'Issue'));
+        // Paper EUD-0 names the claim and what happened to it in one line:
+        // "<Title> was approved and is being rewritten".
+        var title = isEditCause
+          ? readinessClaimLabel(editedOwner) + ' was approved and is being rewritten'
+          : (group.title || 'Issue');
+        text.appendChild(textEl('span', 'status-finding-rule', title));
         var ids = Object.keys(group.claimIDs).sort();
         // Paper 1KX-0: every row carries a second, demoted line under its
         // title holding the dotted claim id of the claim the reader must go
@@ -12945,11 +13579,52 @@
         if (ownerID) {
           text.appendChild(textEl('span', 'status-finding-claim', ownerID));
         }
+
+        // The third line (Paper board 15, F and I). A Needs-you row names its
+        // cause KIND in mono, because the sentence alone cannot distinguish
+        // "someone never wrote this" from "someone approved it and then
+        // moved it". A blocker row whose owner was approved and is being
+        // rewritten says so under the row it already had (BR5-0): no new
+        // group and no new severity, one line added to the existing row.
+        var detail = null;
+        if (group.severity === 'needs_you') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind', group.kind || 'review'));
+          if (isEditCause) {
+            detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          }
+        } else if (editedOwner && group.severity === 'blocker') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind status-finding-kind--edited', 'Edited · was approved'));
+          detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          var link = textEl('button', 'status-finding-link', 'See changes');
+          link.type = 'button';
+          link.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(editedOwner, edits[editedOwner]);
+          });
+          detail.appendChild(link);
+        }
+        if (detail) { text.appendChild(detail); }
         row.appendChild(text);
-        // retry fix 7: 04 §4.8 "Count label" / §6 "Row count" promotes the
-        // phrase INTO the count slot as a bare `N claims`, never `blocks N
-        // claim(s)` — that longer phrase was written for a full-width third
-        // line, which the fixed 104px slot (style.css) no longer is.
+
+        // The right-hand slot. A blocker row keeps its count (04 §4.8: the
+        // phrase promoted INTO the 104px slot as a bare `N claims`). A
+        // Needs-you row carries its way in instead (Paper ETC-0 / EUD-0):
+        // the thing waiting is one claim, and the count would always be 1.
+        if (group.severity === 'needs_you' && group.ownerModuleClaimID) {
+          var owner = group.ownerModuleClaimID;
+          var action = textEl('button', 'status-finding-action', isEditCause ? 'See changes' : 'Open claim');
+          action.type = 'button';
+          action.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(owner, isEditCause ? edits[owner] : null);
+          });
+          row.appendChild(action);
+          return row;
+        }
         var n = ids.length || group.count;
         row.appendChild(textEl('span', 'status-finding-msg', n + ' claim' + (n === 1 ? '' : 's')));
         return row;
@@ -13048,7 +13723,7 @@
         var label = card && card.querySelector('.k .label');
         if (label) {
           var copy = label.cloneNode(true);
-          copy.querySelectorAll('.pill, .k-id, .claim-collapse-chevron').forEach(function (node) { node.remove(); });
+          copy.querySelectorAll('.pill, .k-id').forEach(function (node) { node.remove(); });
           var rendered = (copy.textContent || '').replace(/\s+/g, ' ').trim();
           if (rendered) { return rendered; }
         }
@@ -13075,7 +13750,12 @@
           approval_content_drift: 'This claim changed after approval',
           approval_missing: 'This claim has no approval record',
           approval_released: 'This claim\'s approval was released',
-          approval_unknown: 'This claim\'s approval state is unknown'
+          approval_unknown: 'This claim\'s approval state is unknown',
+          // Deliberately not "changed after approval" (approval_content_drift's
+          // wording): that names a LOCKED claim whose bytes no longer match a
+          // STANDING approval, which is the tamper finding. This names the
+          // honest, intended act — unlock, rewrite, re-lock — caught mid-way.
+          unapproved_edit: 'This claim was approved, then rewritten'
         };
         return labels[record.kind] || String(record.kind || 'Readiness obstacle').replace(/_/g, ' ');
       }
@@ -13583,12 +14263,397 @@
         } catch (_) { return {}; }
       }

+      // ---- Unapproved edits -------------------------------------------------
+      //
+      // The state these three functions draw is the ordinary one, and it was
+      // the one the viewer could not show. A claim is locked; someone unlocks
+      // it, rewrites it, and has not locked it again yet. The new wording
+      // replaced the old in the file, so the reading view showed text with
+      // nothing to compare it against and a chip reading DRAFT — the same chip
+      // a claim nobody ever approved carries. "Never written" and "approved,
+      // then moved" are different jobs for a reviewer, and one word was being
+      // used for both.
+      //
+      // The engine now answers both questions: readiness emits an
+      // `unapproved_edit` cause (so the claim reaches the Issues screen on its
+      // own, without needing a dependent to notice for it), and the graph
+      // payload carries the line diff from the approved body to the current
+      // one. Everything below is presentation of those two facts. It computes
+      // nothing about whether an edit is allowed, and never writes.
+
+      // offlineApprovedEdits reads the per-claim approvaledit.Change records
+      // off the SAME rendered graph payload offlineReadiness reads. There is
+      // deliberately no /api/status path for this: a checked-in file:// export
+      // has no endpoint to ask, and a panel that appeared only under
+      // "dossierx serve" would be missing exactly where a reviewer reads a
+      // repository they did not build.
+      function offlineApprovedEdits() {
+        var node = document.getElementById('dossierx-graph');
+        if (!node) { return {}; }
+        try {
+          var payload = JSON.parse(node.textContent || '{}');
+          var out = {};
+          (payload.nodes || []).forEach(function (graphNode) {
+            if (graphNode.id && graphNode.approved_edit) { out[graphNode.id] = graphNode.approved_edit; }
+          });
+          return out;
+        } catch (_) { return {}; }
+      }
+
+      // editDateLabel turns an RFC3339 instant into the board's own short
+      // form ("12 Sep"). It is deliberately not a full date: the panel's job
+      // is to place the approval in the reader's recent memory, and a
+      // four-part date would be the widest thing on a row that is otherwise
+      // a sentence.
+      function editDateLabel(iso) {
+        if (!iso) { return ''; }
+        var d = new Date(iso);
+        if (isNaN(d.getTime())) { return String(iso).slice(0, 10); }
+        return d.getDate() + ' ' + ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun',
+          'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][d.getMonth()];
+      }
+
+      // approvedEditHasDiff reports whether there is a second wording to show
+      // at all.
+      //
+      // Two states look like an edit and have nothing to put behind a
+      // "Changes" tab. A record written before the approved wording was kept
+      // proves the text moved and does not carry what it moved from. A claim
+      // whose hash moved because its rests_on or build_role moved has an
+      // approved wording identical to its current one. In both, the panel has
+      // one sentence to say and no second version of the claim — so there is
+      // no switch, and the claim's own body stays on screen.
+      //
+      // Offering the switch anyway is what this replaces, and it was worse
+      // than a useless control: choosing "Changes" hid the body and put the
+      // sentence in its place, so the claim rendered with no content at all.
+      function approvedEditHasDiff(change) {
+        return !!(change.content_retained &&
+          (change.hunks || []).length &&
+          (change.changed_passages || 0) > 0);
+      }
+
+      // approvedEditDiff builds the passage-by-passage view: unchanged
+      // passages as ordinary prose, and each changed one as the approved
+      // passage above the current one, tinted and ruled.
+      //
+      // The HTML is produced by the ENGINE (internal/approvaledit), not here,
+      // because internal/render/markdown is this project's only markdown
+      // renderer and the viewer has none. Shipping the source and parsing it
+      // in the browser would mean growing a second renderer that could
+      // disagree with the first about the same claim, on the same page.
+      function approvedEditDiff(change) {
+        var body = el('div', 'claim-edit-diff');
+        (change.hunks || []).forEach(function (hunk) {
+          var cls = hunk.op === 'remove' ? 'claim-edit-passage claim-edit-passage--removed'
+            : hunk.op === 'add' ? 'claim-edit-passage claim-edit-passage--added'
+            : 'claim-edit-passage';
+          var block = el('div', cls);
+          // innerHTML, and only here. The value came from
+          // internal/render/markdown, which is the escaping boundary every
+          // other claim body in this viewer crosses — the same function, over
+          // the same author bytes, reached through the same payload as the
+          // readiness projection beside it. It is exactly as trusted as the
+          // body it sits above, and no more.
+          block.innerHTML = hunk.html || '';
+          if ((hunk.changed || []).length) {
+            block.setAttribute('aria-label',
+              (hunk.op === 'remove' ? 'Removed since approval: ' : 'Added since approval: ') +
+              hunk.changed.join(', '));
+          }
+          body.appendChild(block);
+        });
+        var fields = approvedEditFields(change);
+        if (fields) { body.appendChild(fields); }
+        return body;
+      }
+
+      // approvedEditCurrent builds the OTHER state of the same surface: the
+      // current wording, passage by passage, with the passage that differs
+      // from the approval carrying a draft-coloured rule and no tint (Paper
+      // DPZ-0: "the same claim with the diff put away, reading as ordinary
+      // prose, with an amber rule marking where the difference sits").
+      //
+      // It is rendered from the same hunks as the diff rather than by
+      // un-hiding the claim's own body, because the body cannot say WHERE the
+      // difference sits: the rule is the one thing this state adds, and the
+      // body has no passage boundaries to hang it on.
+      function approvedEditCurrent(change) {
+        var body = el('div', 'claim-edit-current');
+        (change.hunks || []).forEach(function (hunk) {
+          if (hunk.op === 'remove') { return; }
+          var block = el('div', hunk.op === 'add'
+            ? 'claim-edit-passage claim-edit-passage--current'
+            : 'claim-edit-passage');
+          // Same escaping boundary as approvedEditDiff: engine-rendered
+          // markdown, the same bytes the diff shows.
+          block.innerHTML = hunk.html || '';
+          if (hunk.op === 'add') {
+            block.setAttribute('aria-label', 'This passage differs from the approved wording');
+          }
+          body.appendChild(block);
+        });
+        return body;
+      }
+
+      // approvedEditFields draws the non-body fields that moved, each as its
+      // approved YAML above its current YAML with the words that moved marked.
+      //
+      // It is here because "Also changed: embodiment, audit_notes." was not an
+      // answer. For a claim whose prose never moved that sentence WAS the whole
+      // panel, and it named the change without showing it — the reader was told
+      // a field they cannot see had moved in a way they cannot see, and had to
+      // go and read the file to find out what they were being asked about. Six
+      // of the fourteen claims this feature was built for are in exactly that
+      // state.
+      //
+      // The blocks are drawn as text and not as prose. A field is YAML, so it
+      // is escaped by the engine (internal/approvaledit/fields.go) and shown in
+      // the monospace the file itself would show; running it through the
+      // markdown renderer would turn a list into a list and a `*` into
+      // emphasis, and stop it being the thing on disk.
+      function approvedEditFields(change) {
+        var changes = change.field_changes || [];
+        if (!changes.length) { return null; }
+        var wrap = el('div', 'claim-edit-fields');
+
+        var toggle = el('button', 'claim-edit-fields-toggle');
+        toggle.type = 'button';
+        var list = el('div', 'claim-edit-fields-list');
+        var open = true;
+        function paint() {
+          toggle.setAttribute('aria-expanded', String(open));
+          toggle.textContent = (open ? 'Hide' : 'Show') + ' the ' +
+            (changes.length === 1 ? 'field' : changes.length + ' fields') + ' that moved';
+          list.hidden = !open;
+        }
+        toggle.addEventListener('click', function (event) {
+          event.preventDefault();
+          open = !open;
+          paint();
+        });
+
+        changes.forEach(function (field) {
+          var row = el('div', 'claim-edit-field');
+          row.appendChild(textEl('span', 'claim-edit-field-name', field.field));
+          if (field.truncated) {
+            row.appendChild(textEl('p', 'claim-edit-field-note',
+              'This field moved. It is too large to show here; read it in the claim file.'));
+            list.appendChild(row);
+            return;
+          }
+          (field.hunks || []).forEach(function (hunk) {
+            var cls = hunk.op === 'remove' ? 'claim-edit-field-hunk claim-edit-field-hunk--removed'
+              : hunk.op === 'add' ? 'claim-edit-field-hunk claim-edit-field-hunk--added'
+              : 'claim-edit-field-hunk';
+            var block = el('pre', cls);
+            // innerHTML over engine-escaped text. internal/approvaledit's
+            // field renderer HTML-escapes the YAML before it marks anything,
+            // so the only tags that can be in here are the mark spans it
+            // wrote itself — a field holding <script> arrives as text.
+            block.innerHTML = hunk.html || '';
+            if ((hunk.changed || []).length) {
+              block.setAttribute('aria-label',
+                (hunk.op === 'remove' ? 'Removed from ' : 'Added to ') + field.field + ': ' +
+                hunk.changed.join(', '));
+            }
+            row.appendChild(block);
+          });
+          list.appendChild(row);
+        });
+
+        paint();
+        wrap.appendChild(toggle);
+        wrap.appendChild(list);
+        return wrap;
+      }
+
+      // approvedEditNote is what a claim with no second wording says instead of
+      // offering a switch. It sits under the bar, beside the claim's own body
+      // rather than in place of it.
+      function approvedEditNote(change) {
+        var wrap = el('div', 'claim-edit-note');
+        if (!change.content_retained) {
+          wrap.appendChild(textEl('p', 'claim-edit-note-text',
+            'This claim was approved before the approved wording was kept on the record, so there is nothing to compare against. The approval is released and the text has since changed.'));
+          wrap.appendChild(textEl('p', 'claim-edit-note-hint',
+            'Run dossierx claim recover-approved-content to look for the approved revision in this project\u2019s git history.'));
+          return wrap;
+        }
+        var fields = change.other_fields || [];
+        wrap.appendChild(textEl('p', 'claim-edit-note-text', fields.length
+          ? 'The wording is unchanged since approval. ' +
+            (fields.length === 1 ? '1 field moved.' : fields.length + ' fields moved.')
+          : 'The wording is unchanged since approval.'));
+        var diff = approvedEditFields(change);
+        if (diff) { wrap.appendChild(diff); }
+        return wrap;
+      }
+
+      // approvedEditBar is the control the board draws in place of the old
+      // disclosure: a tinted row above the body that says what the reader is
+      // looking at, and a two-segment switch between the two states.
+      //
+      // It is a BAR AND NOT A DISCLOSURE, and the difference is which state
+      // is the default. A disclosure says the diff is an aside a reader opens
+      // if they want it; the board says the opposite — a claim that was
+      // approved and then rewritten opens SHOWING what moved, because that is
+      // the thing about it a reader has to know before they read a word of
+      // it. "Current" is the escape hatch, not the resting state.
+      function approvedEditBar(card, change, showingChanges) {
+        var bar = el('div', 'claim-edit-bar');
+        var approved = editDateLabel(change.approved_at);
+        var hasDiff = approvedEditHasDiff(change);
+        bar.appendChild(textEl('span', 'claim-edit-bar-title',
+          !hasDiff || showingChanges ? 'Edited since it was approved' : 'Showing the current wording'));
+
+        var meta;
+        if (!hasDiff || showingChanges) {
+          meta = 'approved ' + approved + (change.approved_by ? ' \u00b7 ' + change.approved_by : '');
+        } else {
+          var passages = change.changed_passages || 0;
+          meta = passages === 1
+            ? '1 passage differs from the approval of ' + approved
+            : passages + ' passages differ from the approval of ' + approved;
+        }
+        bar.appendChild(textEl('span', 'claim-edit-bar-meta', meta));
+
+        // No second wording, no switch. A control whose two states show the
+        // same thing is not a control, and the one that used to be here did
+        // worse than nothing: picking "Changes" hid the claim's body to make
+        // room for a diff that did not exist.
+        if (!hasDiff) { return bar; }
+
+        var group = el('div', 'claim-edit-switch');
+        group.setAttribute('role', 'group');
+        group.setAttribute('aria-label', 'Which wording to show');
+        [['Changes', true], ['Current', false]].forEach(function (pair) {
+          var seg = el('button', 'claim-edit-switch-seg');
+          seg.type = 'button';
+          seg.textContent = pair[0];
+          seg.setAttribute('aria-pressed', String(showingChanges === pair[1]));
+          seg.addEventListener('click', function (event) {
+            event.preventDefault();
+            paintApprovedEdit(card, change, pair[1]);
+          });
+          group.appendChild(seg);
+        });
+        bar.appendChild(group);
+        return bar;
+      }
+
+      // paintApprovedEdit puts one claim card into one of the two states, and
+      // is the only thing that writes either of them, so the bar and the body
+      // can never disagree about which one is showing.
+      function paintApprovedEdit(card, change, showingChanges) {
+        var hasDiff = approvedEditHasDiff(change);
+        showingChanges = hasDiff && showingChanges;
+        var existingBar = card.querySelector(':scope > .claim-edit-bar');
+        // The diff may already be wrapped: the four-line disclosure wraps it
+        // exactly as it wraps a claim body (system-record.js's BODY_LIKE), so
+        // what has to be taken out is the wrapper when there is one.
+        var existingDiff = card.querySelector(
+          ':scope > .claim-edit-diff, :scope > .claim-edit-current, :scope > .claim-edit-note, :scope > .claim-body-disclosure--edit');
+
+        var bar = approvedEditBar(card, change, showingChanges);
+        if (existingBar) { existingBar.replaceWith(bar); }
+        else {
+          var head = card.querySelector(':scope > .k');
+          if (head && head.parentNode) { head.parentNode.insertBefore(bar, head.nextSibling); }
+          else { card.insertBefore(bar, card.firstChild); }
+        }
+        if (existingDiff) { existingDiff.remove(); }
+        if (!hasDiff) {
+          bar.parentNode.insertBefore(approvedEditNote(change), bar.nextSibling);
+        } else {
+          bar.parentNode.insertBefore(
+            showingChanges ? approvedEditDiff(change) : approvedEditCurrent(change), bar.nextSibling);
+          // Hand the new surface to the disclosure so it is measured and
+          // clamped like any other body. Without this the diff renders at
+          // full height while the claim beside it truncates at four lines,
+          // which is the same claim behaving as two components.
+          if (typeof window.dossierxEnhanceSystemRecord === 'function') {
+            window.dossierxEnhanceSystemRecord();
+          }
+        }
+        // While there is a second wording the claim's own body is off screen
+        // in BOTH states: Changes shows the diff and Current shows the same
+        // passages with the changed one ruled. The body comes back only for
+        // a claim with nothing to compare against (no switch).
+        //
+        // The body is hidden by a class ON THE CARD, and not by setting
+        // `hidden` on the body itself. Which element the body IS moves: the
+        // long-body disclosure wraps
+        // .claim-body inside .claim-body-disclosure after first paint, so a
+        // handle taken before the wrap and a handle taken after it are
+        // different nodes. Hiding whichever one a selector matched at the
+        // time left the first one hidden and the second one shown, and the
+        // switch stopped working on exactly the claims long enough to be
+        // wrapped. A class on the card is one state, in one place, whatever
+        // the body is wearing.
+        //
+        // It is also why the body is hidden rather than removed: it carries
+        // the source-note clamps and that disclosure, and tearing it out
+        // would take their state with it every time the reader flipped.
+        card.classList.toggle('claim--edit-view', hasDiff);
+      }
+
+      // renderApprovedEdits upgrades each edited claim's chip and puts its
+      // card into the Changes state. It is driven from mountSurface (so the
+      // surface reaches its final height before anything measures or scrolls
+      // it) and again from renderStatusStrip, so everything it creates is
+      // either guarded or left alone on a repaint.
+      function renderApprovedEdits() {
+        var edits = offlineApprovedEdits();
+        Object.keys(edits).forEach(function (id) {
+          var card = document.getElementById(id);
+          if (!card) { return; }
+          var change = edits[id];
+
+          // The chip. It stays .pill.pv — the claim IS a draft, and inventing
+          // a fourth colour for it would say the state is unrelated to the
+          // one the reader already knows. Only the word changes, because only
+          // the word was wrong: DRAFT is true and incomplete, and a reviewer
+          // deciding what to open next needs the part it leaves out.
+          var pill = card.querySelector('.k .label .pill');
+          if (pill && pill.getAttribute('data-dx-edited') !== '1') {
+            pill.setAttribute('data-dx-edited', '1');
+            pill.setAttribute('title', 'This claim held an approval, which was released when it was unlocked. What is written now differs from what was approved.');
+            // No padlock. Paper (CDJ-0, CIY-0) draws the chip as the word
+            // alone: the open padlock is DRAFT's glyph, and this chip exists
+            // to say the state is NOT plain draft.
+            pill.textContent = '';
+            pill.appendChild(textEl('span', '', 'Edited \u00b7 was approved'));
+          }
+
+          // The bar and the body state, painted only when the bar is not
+          // already there: a repaint must not take the reader's own "Current"
+          // choice away from them on the next poll.
+          if (!card.querySelector(':scope > .claim-edit-bar')) {
+            paintApprovedEdit(card, change, true);
+          }
+        });
+      }
+
+      // approvedEditIDsIn returns the claims in the id set that have been
+      // rewritten since the approval their unlock released.
+      //
+      // It is scoped to the facet on screen, by the SAME id set the rest of
+      // the strip scopes itself with — a count of claims on a page the reader
+      // is not looking at is a count they cannot act on. The set is a
+      // null-prototype object keyed by id (activeFacetClaimIDs), so this
+      // indexes it rather than calling Set or Array methods it does not have.
+      function approvedEditIDsIn(claimIDs) {
+        var ids = claimIDs || {};
+        return Object.keys(offlineApprovedEdits()).filter(function (id) { return ids[id]; });
+      }
+
       // renderStatusStrip paints one /api/status payload. An empty verdict hides
       // the strip entirely rather than showing a green badge: the viewer already
       // reads as "fine" by default, and a persistent all-clear chip would be one
       // more thing to stop noticing.
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary || !stripTitle) { return; }
+        if (!stripEl || !stripBody || !stripCard || !stripFindings || !stripTitle) { return; }
         lastStatusData = data || {};
         var claimIDs = activeFacetClaimIDs();
         var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
@@ -13611,6 +14676,7 @@
         // PREVIOUS pass's state, since this function only writes it below.
         var bannerShowing = groups.length > 0 && actionable;
         renderClaimReadiness(lastStatusData.readiness || offlineReadiness(), bannerShowing);
+        renderApprovedEdits();
         if (!groups.length || !actionable) {
           stripEl.hidden = true;
           stripEl.classList.remove('status-strip--integrity', 'status-strip--lint');
@@ -13704,12 +14770,18 @@
           recordEl.classList.add('status-group--approval-record');
           stripBody.appendChild(recordEl);
         }
+        // Under the Needs-you filter the group's weight phrase counts what is
+        // waiting on the reader, not what is blocked (Paper EMP-0: "3 of 4
+        // need you here").
+        var needsTotal = countSeverity(groups, 'needs_you');
         moduleOrder.forEach(function (moduleID) {
           var rows = byModule[moduleID].slice().sort(function (a, b) {
             return statusGroupClaimCount(b) - statusGroupClaimCount(a);
           });
           var weight = uniqueClaimCount(rows);
-          var note = 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
+          var note = stripSeverityFilter === 'needs_you'
+            ? weight + ' of ' + needsTotal + ' need you here'
+            : 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
           stripBody.appendChild(findingGroup(moduleLabel(moduleID), rows.map(renderStatusGroup), note));
         });

@@ -13719,38 +14791,189 @@
         // used to repeat as a second string. statusChipLine, the function
         // that built that string, is now dead (no other call site) and is
         // deleted.
-        var headline = blockerHeadline(groups);
-        var needsYou = countSeverity(groups, 'needs_you') > 0;
-        if (ledger.length) {
-          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
-        } else if (lintErrors.length && !headline && !needsYou) {
-          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
-        } else if (headline) {
-          stripTitle.textContent = headline;
-        } else {
-          stripTitle.textContent = countLabel(groups.length, 'grouped issue') + ' in this facet';
-        }
+        //
+        // Both forms of the head, every paint. The desktop band carries the
+        // single headline sentence it always did; the phone card carries one
+        // row per finding. They are derived from the SAME groups, ledger and
+        // lint lists a line apart, so the two can differ in shape and never
+        // in what they say.
+        var rows = statusStripRows(groups, ledger, lintErrors, claimIDs);
+        renderStatusStripCard(rows);
+        // The band gets the same rows, one full-bleed row each (Paper board
+        // 15, C2M-0 / C92-0), in the same order — most serious first — so the
+        // two forms differ in shape and never in what they say.
+        renderStatusStripBand(rows);

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        // Paper's default reading view always starts with the findings body
-        // collapsed. Critical and needs-you counts still determine the
-        // truthful summary/chips above; they do not take control of the
-        // reader's disclosure state. Once the reader opens the strip, the
-        // existing stripExpanded state remains sticky across later polls.
-        setStripExpanded(stripExpanded);
+        // #statusStripBody is populated above and stays hidden: the Issues
+        // screen MOVES its children into itself (issuesSyncFromStrip), so it
+        // must exist and must not be shown here.
+        if (stripBody) { stripBody.hidden = true; }
         positionStatusStrip();
         stripEl.hidden = false;
       }

-      function setStripExpanded(expanded) {
-        stripExpanded = !!expanded;
-        if (stripBody) { stripBody.hidden = !stripExpanded; }
-        if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
-        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
-        if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
+      // statusStripRows turns the facet's findings into the card's rows —
+      // one row per KIND of thing waiting, never one per claim.
+      //
+      // The rows are the summary sentences the strip already computed, each
+      // now carrying its own hue instead of four of them competing to be the
+      // one headline. `tone` is the row's colour family, and it is the only
+      // thing the card's tint rule reads: 'alarm' for anything the approval
+      // record or the dependency chain is refusing, 'draft' for work in
+      // progress that nobody has approved yet.
+      function statusStripRows(groups, ledger, lintErrors, claimIDs) {
+        var rows = [];
+        if (ledger.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(ledger.length, 'approval record issue') + ' in this facet need' +
+              (ledger.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        var headline = blockerHeadline(groups);
+        if (headline) {
+          rows.push({ tone: 'alarm', severity: 'blocker', text: headline });
+        }
+        if (lintErrors.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(lintErrors.length, 'issue') + ' in this facet need' +
+              (lintErrors.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        // Claims rewritten since approval. This is the row the old
+        // single-sentence head had no space for, and the reason it became a
+        // card: it is amber where everything above it is red, and one tinted
+        // surface cannot make two severity claims at once.
+        var edited = approvedEditIDsIn(claimIDs);
+        if (edited.length) {
+          rows.push({
+            tone: 'draft', severity: 'needs_you',
+            text: countLabel(edited.length, 'claim') + ' ' + (edited.length === 1 ? 'has' : 'have') +
+              ' edits that have not been approved'
+          });
+        }
+        if (!rows.length) {
+          rows.push({
+            tone: 'alarm', severity: '',
+            text: countLabel(groups.length, 'grouped issue') + ' in this facet'
+          });
+        }
+        return rows;
       }

+      // renderStatusStripCard paints the head. Tinted while every row shares
+      // a tone, neutral the moment they disagree — Paper EH1-0's own rule,
+      // and the reason it is a rule: a tinted surface IS a severity claim,
+      // so a card holding a red fact and an amber one has to stop making it
+      // and let the dots carry the colour instead.
+      // statusStripRowAction names the way in each row offers. A blocked row
+      // opens the Issues screen as it always did; an edited-since-approval
+      // row says what it is for (Paper CDS-0: "Review changes").
+      function statusStripRowAction(row) {
+        return row.tone === 'draft' ? 'Review changes' : 'Show issues';
+      }
+
+      // openIssuesForBandRow: a blocked row opens the screen UNFILTERED — its
+      // sentence names one finding but the band stands for the whole facet.
+      // An edited row names exactly what it filters to (the claims waiting on
+      // the reader), which is what earns it the filter, same as a card row.
+      function openIssuesForBandRow(tone, severity) {
+        stripSeverityFilter = tone === 'draft' ? (severity || '') : '';
+        renderStatusStrip(lastStatusData);
+        openIssuesView();
+      }
+
+      // renderStatusStripBand paints the DESKTOP form: the static first row
+      // (#statusStripToggle, so the ids the rest of the viewer and its tests
+      // address stay put) takes the first finding, and one more
+      // .status-strip-head button is appended per further finding. Each row
+      // carries data-tone, which is the only thing the stylesheet reads to
+      // pick its tint, its icon and its action colour.
+      function renderStatusStripBand(rows) {
+        if (!stripToggle || !stripTitle || !stripEl) { return; }
+        var first = rows[0];
+        stripToggle.setAttribute('data-tone', first.tone);
+        stripToggle.setAttribute('data-severity', first.severity || '');
+        stripTitle.textContent = first.text;
+        var firstAction = document.getElementById('statusStripAction');
+        if (firstAction) { firstAction.textContent = statusStripRowAction(first); }
+
+        stripEl.querySelectorAll(':scope > .status-strip-head--row').forEach(function (node) { node.remove(); });
+        rows.slice(1).forEach(function (row) {
+          var btn = el('button', 'status-strip-head status-strip-head--row');
+          btn.type = 'button';
+          btn.setAttribute('data-tone', row.tone);
+          btn.setAttribute('data-severity', row.severity || '');
+          var summary = el('span', 'status-strip-summary');
+          var icon = dxIcon('triangle-alert');
+          icon.classList.add('status-strip-icon');
+          summary.appendChild(icon);
+          summary.appendChild(el('span', 'status-strip-dot'));
+          var text = el('span', 'status-strip-summary-text');
+          text.appendChild(textEl('strong', 'status-strip-title', row.text));
+          summary.appendChild(text);
+          btn.appendChild(summary);
+          var actionRow = el('span', 'status-strip-action-row');
+          actionRow.appendChild(textEl('span', 'status-strip-action', statusStripRowAction(row)));
+          btn.appendChild(actionRow);
+          btn.addEventListener('click', function (event) {
+            event.preventDefault();
+            openIssuesForBandRow(row.tone, row.severity);
+          });
+          // After the previous row and before the phone card, so the band is
+          // one stack of rows whatever the count.
+          var before = stripCard || stripBody;
+          if (before && before.parentNode === stripEl) { stripEl.insertBefore(btn, before); }
+          else { stripEl.appendChild(btn); }
+        });
+      }
+
+      function renderStatusStripCard(rows) {
+        if (!stripCard || !stripFindings) { return; }
+        var tones = {};
+        rows.forEach(function (row) { tones[row.tone] = true; });
+        var toneKeys = Object.keys(tones);
+        var tinted = toneKeys.length === 1 ? toneKeys[0] : '';
+        stripCard.classList.toggle('status-strip-card--alarm', tinted === 'alarm');
+        stripCard.classList.toggle('status-strip-card--draft', tinted === 'draft');
+        if (stripCardCount) { stripCardCount.textContent = String(rows.length); }
+
+        stripFindings.textContent = '';
+        rows.forEach(function (row) {
+          // A button, not a div with a handler: each row is a way into the
+          // Issues screen and has to be reachable by keyboard and named to a
+          // screen reader like the one control it is.
+          var el_ = el('button', 'status-strip-finding');
+          el_.type = 'button';
+          el_.setAttribute('data-tone', row.tone);
+          var dot = el('span', 'status-strip-finding-dot');
+          dot.setAttribute('aria-hidden', 'true');
+          el_.appendChild(dot);
+          el_.appendChild(textEl('span', 'status-strip-finding-text', row.text));
+          // A text glyph, not the 14px icon: EH1-0 draws the row's way-in as
+          // a 12px "›" in the card's own hue.
+          var chev = textEl('span', 'status-strip-finding-chevron', '›');
+          chev.setAttribute('aria-hidden', 'true');
+          el_.appendChild(chev);
+          el_.addEventListener('click', function (event) {
+            event.preventDefault();
+            // Filtered to the row's own severity. On a real corpus the
+            // unfiltered screen is hundreds of dependency rows and the thing
+            // this row named is somewhere inside them; a way in that lands a
+            // reader in a list they then have to search has not answered the
+            // question it asked. Same stripSeverityFilter the chips set, so
+            // the chip is pressed on arrival and one more click clears it.
+            stripSeverityFilter = row.severity || '';
+            renderStatusStrip(lastStatusData);
+            openIssuesView();
+          });
+          stripFindings.appendChild(el_);
+        });
+      }
+
       // refreshStatus polls the endpoint. A FAILED poll deliberately leaves the
       // last verdict on screen untouched: a transient fetch failure (the server
       // restarting mid-reload) must never be able to clear an integrity warning,
@@ -13764,25 +14987,20 @@
       }

       // R09.6 (Paper ZP-0): "Issues is its own screen, not an expansion. The
-      // banner is the only way in." Paper backs that with node evidence:
-      // 56T-0, 574-0 and 6P-0 draw ONE state — the sentence plus "Show
-      // issues" — the desktop banner is a single flex ROW with no slot for a
-      // body, the string "Hide issues" does not exist anywhere in the design
-      // file, and 04 section 4 records that no board in the group draws an
-      // expanded state. The inline expand listener that lived here is gone;
-      // the toggle's only job is opening the Issues screen.
+      // banner is the only way in." No board in the group draws an expanded
+      // banner, and the string "Hide issues" does not exist anywhere in the
+      // design file, so the strip has no inline disclosure at all.
       //
-      // setStripExpanded is deliberately KEPT: renderStatusStrip still calls
-      // it every poll with a permanently-false stripExpanded, and that is
-      // what holds #statusStripBody hidden, aria-expanded="false" and the
-      // action reading "Show issues".
+      // EH1-0 then replaced the one sentence with one row per finding, and
+      // the entry point went with it: each row opens the Issues screen,
+      // filtered to that row's own severity. "The banner is the only way in"
+      // still holds — the banner simply has more than one door now, because
+      // a facet routinely has more than one kind of thing waiting.
       //
       // #statusStripBody is never emptied, removed or left unpopulated:
       // renderStatusStrip still builds into it, issuesSyncFromStrip still
       // MOVES its children into the Issues screen, and closeIssuesView still
-      // moves them back. Only its visibility to the reader is withdrawn — the
-      // children now return into a hidden body instead of an expanded one,
-      // which is the defect this removes.
+      // moves them back. It is simply never shown in place.

       // ================================================================
       // Issues view (04-issues-screen.md) — lane L8a
@@ -13791,9 +15009,8 @@
       // The screen's own chrome around lane L8's status-strip vocabulary:
       // breadcrumb, title/subtitle, scope and sort controls, the findings
       // card and the "WHERE THE BLOCKERS LIVE" rail (04 §3, §4.1-4.3, §4.5,
-      // §4.6, §4.9). R09.6: reached ONLY from "Show issues" — the listener
-      // just below runs AFTER the one directly above (source order), so by
-      // the time it fires `stripExpanded` already holds L8's own new value.
+      // §4.6, §4.9). R09.6: reached ONLY from a row of the banner card, each
+      // of which wires openIssuesView itself as it is built.
       //
       // #issuesFilters and #issuesFindingsCard never build their own copy of
       // the severity chips or the grouped findings list: issuesSyncFromStrip
@@ -13898,11 +15115,59 @@
         var m = issuesModuleWeights(issuesScope).claims;
         issuesFindingsCard.querySelectorAll('.status-group-head-note').forEach(function (note) {
           var match = /^blocks (\d+) of \d+ claims? here$/.exec(note.textContent || '');
-          if (!match) { return; }
-          note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+          if (match) {
+            note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+            return;
+          }
+          // The Needs-you phrase: the same denominator rule, over the rows
+          // the filter is showing (issuesGroupsForScope already applies it).
+          var waiting = /^(\d+) of \d+ need you here$/.exec(note.textContent || '');
+          if (waiting) { note.textContent = waiting[1] + ' of ' + m + ' need you here'; }
         });
       }

+      // issuesCauseWeights is the rail's OTHER ranking (Paper EKL-0, "WHAT IS
+      // WAITING"): under the Needs-you filter the question is what kind of
+      // work is waiting rather than where the blockers live, so the rail
+      // counts causes, by kind, and colours each bar by the kind's hue. A
+      // claim waiting for two reasons is counted under each, and the caveat
+      // says so.
+      var ISSUES_CAUSE_LABELS = {
+        unapproved_edit: 'Unapproved edits',
+        direct_dependency_change: 'Dependency changed',
+        upstream_dependency_review: 'Upstream review',
+        own_flag: 'Flagged for review',
+        own_thread: 'Open review thread',
+        approval_content_drift: 'Approved wording drifted',
+        approval_released: 'Approval released',
+        approval_missing: 'Approval record missing',
+        approval_unknown: 'Approval state unknown'
+      };
+      function issuesCauseWeights(scope) {
+        var groups = issuesGroupsForScope(scope);
+        var byKind = {};
+        var order = [];
+        groups.forEach(function (g) {
+          var kind = g.kind || 'review';
+          if (!Object.prototype.hasOwnProperty.call(byKind, kind)) { byKind[kind] = []; order.push(kind); }
+          byKind[kind].push(g);
+        });
+        var rows = order.map(function (kind) {
+          return {
+            id: kind,
+            label: ISSUES_CAUSE_LABELS[kind] || kind.replace(/_/g, ' '),
+            tone: kind === 'unapproved_edit' ? 'draft' : 'alarm',
+            weight: uniqueClaimCount(byKind[kind])
+          };
+        });
+        rows.sort(function (a, b) { return b.weight - a.weight; });
+        return {
+          rows: rows,
+          claims: uniqueClaimCount(groups),
+          causes: rows.reduce(function (sum, r) { return sum + r.weight; }, 0)
+        };
+      }
+
       // renderIssuesRail (04 §4.9): a ranking with bars, proportional to the
       // facet's blocked-claim TOTAL (§4.9's own measured widths — retry fix
       // 4, coordinator ruling: "rail bars use the spec's rounding basis" —
@@ -13922,10 +15187,15 @@
       // corpus size or scope.
       function renderIssuesRail() {
         if (!issuesRailRowsEl) { return; }
-        var data = issuesModuleWeights(issuesScope);
+        var waiting = stripSeverityFilter === 'needs_you';
+        var data = waiting ? issuesCauseWeights(issuesScope) : issuesModuleWeights(issuesScope);
+        var heading = document.getElementById('issuesRailHeading');
+        if (heading) { heading.textContent = waiting ? 'WHAT IS WAITING' : 'WHERE THE BLOCKERS LIVE'; }
         issuesRailRowsEl.textContent = '';
         var shown = data.rows.slice(0, ISSUES_RAIL_MAX_ROWS);
-        var total = data.claims;
+        // Module bars are proportional to the blocked-claim total; cause bars
+        // to the cause total (EKL-0: 2 of 4 causes draws round(50%)).
+        var total = waiting ? data.causes : data.claims;
         shown.forEach(function (row) {
           var wrap = el('div', 'issues-rail-row');
           var labelRow = el('div', 'issues-rail-row-label');
@@ -13934,6 +15204,7 @@
           wrap.appendChild(labelRow);
           var track = el('div', 'issues-rail-bar-track');
           var fill = el('div', 'issues-rail-bar-fill');
+          if (row.tone) { fill.setAttribute('data-tone', row.tone); }
           fill.style.width = (total ? Math.round((row.weight / total) * 100) : 0) + '%';
           track.appendChild(fill);
           wrap.appendChild(track);
@@ -13943,10 +15214,14 @@
           issuesRailRowsEl.appendChild(textEl('p', 'issues-rail-more',
             (data.rows.length - ISSUES_RAIL_MAX_ROWS) + ' more not shown'));
         }
-        var caveat = data.claims > 0
-          ? (countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
-             ' — a claim blocked through two modules counts under both.')
-          : '';
+        var caveat = '';
+        if (waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ', ' + countLabel(data.causes, 'cause') +
+            ' — a claim can be waiting for more than one reason and is counted under each.';
+        } else if (!waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
+            ' — a claim blocked through two modules counts under both.';
+        }
         if (issuesRailCaveatEl) { issuesRailCaveatEl.textContent = caveat; }
         if (issuesCaveatEl) { issuesCaveatEl.textContent = caveat; }
         // §8 item 5: an empty ranking is a ranking of nothing — the panel is
@@ -13972,7 +15247,13 @@
             (lastStatusData && lastStatusData.readiness) || offlineReadiness(),
             activeFacetClaimIDs(), [], [], [], []
           );
-          issuesSubtitleEl.textContent = blockerHeadline(groups) || 'Nothing in this facet is blocked.';
+          // Under the Needs-you filter the sentence is about the reader's
+          // work, not the facet's blockers (Paper ENQ-0).
+          var waiting = stripSeverityFilter === 'needs_you' ? countSeverity(groups, 'needs_you') : 0;
+          issuesSubtitleEl.textContent = waiting
+            ? countLabel(waiting, 'claim') + (waiting === 1 ? ' is' : ' are') +
+              ' waiting on you in this facet. Each one names what moved and what it was measured against.'
+            : (blockerHeadline(groups) || 'Nothing in this facet is blocked.');
         }
       }

@@ -14020,6 +15301,10 @@
         }
         issuesApplyScopeDenominator();
         renderIssuesRail();
+        // The subtitle follows the filter (a chip click repaints the strip,
+        // which lands here through the observer), so it is re-read on every
+        // sync and not only when the screen opens.
+        issuesSyncHeader();
         if (issuesStripObserver) { issuesStripObserver.observe(stripBody, { childList: true }); }
       }

@@ -14132,14 +15417,20 @@
       if (issuesSortControl) {
         issuesSortControl.addEventListener('click', function (event) { event.preventDefault(); });
       }
-      // "Show issues" (R09.6) opens this view — UNCONDITIONALLY on every
-      // click of this control, whichever label it currently reads. The
-      // strip's own inline expand/collapse (the listener directly above,
-      // L8's, toggling stripExpanded/#statusStripBody) changes the sticky
-      // disclosure state first. R09.6 names this same control as the Issues
-      // entry point, so this listener does not gate on that resulting state.
+      // R09.6's "the banner is the only way in" holds for both forms. The
+      // desktop band has one door, bound here; the phone card has one per
+      // finding, each wired as it is built (see renderStatusStripCard)
+      // because each carries its own severity filter.
+      //
+      // The band's door opens the screen UNFILTERED, and that is not an
+      // oversight: its sentence names one finding but the band stands for the
+      // whole facet, so narrowing to that one row's severity would hide the
+      // others behind a filter the reader did not choose. A card row names
+      // exactly what it filters to, which is what earns it the filter.
       if (stripToggle) {
-        stripToggle.addEventListener('click', openIssuesView);
+        stripToggle.addEventListener('click', function () {
+          openIssuesForBandRow(stripToggle.getAttribute('data-tone'), stripToggle.getAttribute('data-severity'));
+        });
       }
       // Any ordinary navigation — a sidebar tab, a subtab, a citation link —
       // leaves this screen the same way the back chevron does. Capture phase
@@ -15010,8 +16301,23 @@
     window.scrollTo({ top: Math.max(0, Math.round(window.scrollY + top - offset)), behavior: 'instant' });
   }

+  // BODY_LIKE is what the four-line disclosure clamps: a claim's own body, and
+  // the passage-by-passage diff that stands in for it while the reader is
+  // looking at what changed since approval.
+  //
+  // The diff is in this set because it IS the body, shown a passage at a time
+  // — so a claim that clamps to four lines with "... more" in one state and
+  // runs to full height in the other is the same claim behaving as two
+  // different components. Sharing the mechanism rather than copying it also
+  // means the toggle's label, its aria wiring and its scroll compensation
+  // cannot drift between the two.
+  // .claim-edit-current is the other state of the same surface: the current
+  // wording rendered from the same passages, with the changed one ruled. It
+  // stands in for the body exactly as the diff does, so it clamps like it.
+  var BODY_LIKE = '.claim-body, .claim-edit-diff, .claim-edit-current';
+
   function setClaimBodyExpanded(wrapper, expanded) {
-    var body = wrapper && wrapper.querySelector(':scope > .claim-body');
+    var body = wrapper && wrapper.querySelector(':scope > .claim-body, :scope > .claim-edit-diff, :scope > .claim-edit-current');
     var toggle = wrapper && wrapper.querySelector(':scope > .claim-body-disclosure__toggle');
     if (!body || !toggle || toggle.hidden) { return; }
     wrapper.classList.toggle('claim-body-disclosure--expanded', expanded);
@@ -15024,13 +16330,19 @@
   }

   function syncClaimBodyDisclosures() {
-    document.querySelectorAll('.claim-body').forEach(function (body) {
+    document.querySelectorAll(BODY_LIKE).forEach(function (body) {
       var wrapper = body.parentElement && body.parentElement.classList.contains('claim-body-disclosure')
         ? body.parentElement
         : null;
       if (!wrapper) {
         wrapper = document.createElement('div');
         wrapper.className = 'claim-body-disclosure';
+        // The diff's wrapper is marked, because the card hides the claim's
+        // own body while the changes are showing and must not hide the diff
+        // along with it — both are a .claim-body-disclosure by then.
+        if (body.classList.contains('claim-edit-diff') || body.classList.contains('claim-edit-current')) {
+          wrapper.classList.add('claim-body-disclosure--edit');
+        }
         body.parentNode.insertBefore(wrapper, body);
         wrapper.appendChild(body);
         if (!body.id) { body.id = 'claim-body-' + (++claimBodyDisclosureSequence); }
@@ -15086,7 +16398,6 @@

   function enhanceConformanceFooters() {
     document.querySelectorAll('details.claim-conformance').forEach(function (original) {
-      if (original.dataset.footerEnhanced === 'true') { return; }
       var claim = original.closest('.claim');
       var footer = claim && claim.querySelector('.claim-footer');
       var summary = original.querySelector(':scope > summary');
@@ -15191,13 +16502,6 @@
   }

   function renderToc() {
-    // Theme-parity's hover probe forces :hover through CDP node ids. This
-    // function replaceChildren()s the TOC list on every enhance pass (the
-    // layout MutationObserver retriggers enhance when addModuleHeaders
-    // rewrites the module head), so a node id dies between QuerySelector
-    // and ForcePseudoState. The harness sets this flag for the duration of
-    // that probe; it is otherwise unset.
-    if (window.__dxParityFreezeToc) { return; }
     var toc = document.getElementById('systemFacetToc');
     if (!toc) {
       toc = document.createElement('aside');
@@ -15891,19 +17195,6 @@
     return { from: from, to: to, type: type };
   }

-  // normalizeEdges maps a whole edge list, dropping unusable entries.
-  function normalizeEdges(edges) {
-    var list = asArray(edges);
-    var out = [];
-    for (var i = 0; i < list.length; i++) {
-      var e = normalizeEdge(list[i]);
-      if (e) {
-        out.push(e);
-      }
-    }
-    return out;
-  }
-
   // idSet builds a lookup Set from an id array. Internal only.
   function idSet(ids) {
     var s = new Set();
@@ -17644,7 +18935,6 @@
   var parseTimeHash = ''; // the graph state segment seen at parse time
   var originClaimId = ''; // OPEN_ID_ATTR off the trigger that opened the pane, '' from the nav trigger
   var collapseOverride = false; // the reader insisted on every claim
-  var notices = []; // {text, kind, action} rendered above the canvas
   var dragging = null; // {id} while a node is being dragged
   var panning = null; // {x, y} while the background is being dragged
   var movedWhileDown = false;

testdata/fixture-theme-flat/build/viewer/index.html  (+1552 -262)
    size            26113 lines / 4819893 bytes -> 27403 lines / 4877586 bytes
@@ -5365,6 +5365,15 @@
    form; only the bottom hairline separates it from the body beneath. The
    520px inset-card form below is a DIFFERENT, deliberately distinct board
    (R-H.1) and keeps its own margin/border/radius, untouched by this. */
+/* THIS IS THE DESKTOP FORM and is unchanged: a full-bleed band carrying one
+   summary sentence with "Show issues" beside it. At 1440 a row has the space
+   to carry a sentence and an action without either crowding the other.
+
+   The PHONE form is a different component — a card with one row per finding
+   (Paper EH1-0) — and lives under "The banner card" near the end of this
+   file. Both are in the DOM; the 520px block picks one. Everything from here
+   to .status-strip-action-chevron therefore applies to the desktop form only,
+   and the mobile block switches all of it off rather than re-tuning it. */
 .status-strip {
   width: 100%;
   background: var(--card-bg);
@@ -5377,25 +5386,26 @@
   display: none;
 }

+/* The strip itself is card ground. Its TINT lives on each row (below,
+   keyed by data-tone), because a band with two findings on it carries two
+   hues and a surface cannot make two severity claims at once (Paper board
+   15, C2M-0 red over C92-0 amber, hairline-divided). */
 .status-strip--lint {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
 }

 .status-strip--integrity {
   border-color: color-mix(in srgb, var(--warn) 36%, var(--border));
-  background: var(--status-strip-bg);
+  background: var(--card-bg);
   box-shadow: none;
 }

-/* retry fix 20: 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop
-   banner at 13px/32px, not 13px/14px — 32px is the reading card's own
-   gutter (--card-gutter, G8), reproduced here as a literal because
-   .status-strip mounts as a sibling of the record header, never as a
-   descendant of .card/.claim-tree, so the custom property does not cascade
-   to it. The 520px inset-card form re-pins this back down to 14px in this
-   lane's own appended mobile block, matching the value this rule carried
-   (and was verified against) before this fix. */
+/* 02 §4.10 "Padding / gap" and 03 §4.5 measure the desktop banner at
+   13px/32px — 32px is the reading card's own gutter (--card-gutter, G8),
+   reproduced here as a literal because .status-strip mounts as a sibling of
+   the record header, never as a descendant of .card/.claim-tree, so the
+   custom property does not cascade to it. */
 .status-strip-head {
   display: flex;
   align-items: center;
@@ -5409,11 +5419,9 @@
   cursor: pointer;
 }

-/* retry fix 14: 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning
-   triangle ahead of the banner sentence at every width, in both schemes — it
-   was absent everywhere. .status-strip-summary becomes the icon's row; the
-   title/note stack (unchanged) moves into its own -text wrapper so the two
-   IDs the runtime writes to keep working untouched. */
+/* 02 §4.10 "Icon" / §4.19 "Icon" / R-H.1 "Content" put a warning triangle
+   ahead of the banner sentence. .status-strip-summary is the icon's row; the
+   title stack sits in its own -text wrapper. */
 .status-strip-summary {
   display: flex;
   align-items: flex-start;
@@ -5430,20 +5438,70 @@
   flex-shrink: 0;
 }

+/* One row per finding, each its own tint (Paper C2M-0: blocked ground
+   #9E3B360F; CDR-0: edited ground #9A6A1612), divided by the hairline. The
+   icon slot is 15px either way: the triangle for an alarm row, a 7px dot in
+   the draft hue for an edited row, so the sentences of stacked rows share
+   one left edge. */
+.status-strip-head[data-tone="alarm"] { background: var(--status-strip-bg); }
+.status-strip-head[data-tone="draft"] { background: var(--status-draft-bg, rgba(154, 106, 22, .11)); }
+.status-strip-head + .status-strip-head { border-top: 1px solid var(--border); }
+
+.status-strip-dot {
+  display: none;
+  width: 15px;
+  height: 15px;
+  flex-shrink: 0;
+  align-items: center;
+  justify-content: center;
+}
+
+.status-strip-dot::before {
+  content: "";
+  width: 7px;
+  height: 7px;
+  border-radius: 999px;
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-head[data-tone="draft"] .status-strip-icon { display: none; }
+.status-strip-head[data-tone="draft"] .status-strip-dot { display: inline-flex; }
+.status-strip-head[data-tone="draft"] .status-strip-action { color: var(--status-draft, #976600); }
+
 .status-strip-summary-text {
   display: grid;
   gap: 3px;
   min-width: 0;
 }

-/* retry fix 21: 02 §4.10 "Body" / 03 §4.5 "Body text" measure the banner
-   sentence at Inter 14/18 weight 400, not 13px weight 680 — the sentence
-   reads as body prose, not an emphasized heading. */
+/* 02 §4.10 "Body" / 03 §4.5 "Body text": the banner sentence is Inter 14/18
+   weight 400 — it reads as body prose, not as an emphasized heading. */
 .status-strip-title {
   font: 400 14px/18px var(--font-sans);
   color: var(--ink);
 }

+/* 02 §4.10/§4.19, 03 §4.5/§7.5, R-H.1: "Show issues" is the ALARM half of the
+   banner (the body sentence stays --color-ink) and reads in the same red as
+   the icon, never the generic --accent link colour. */
+.status-strip-action {
+  color: var(--warn);
+  font: 500 13px/16px var(--font-sans);
+  letter-spacing: 0;
+  text-transform: none;
+  white-space: nowrap;
+}
+
+.status-strip-action-row {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+}
+
+/* § 4.10 draws no trailing chevron on the desktop banner. */
+.status-strip-action-chevron {
+  display: none;
+}
 /* .status-strip-note (the "Critical 0 · Needs you 3 · ..." tally line) is
    REMOVED per the coordinator's ruling on this retry: no 02/03/04 board
    draws it, and the severity chips already carry every count it repeated.
@@ -5458,30 +5516,12 @@
    500, 13/16 — R-H.1's weight 600 at 13/16 is the PHONE step (added in
    L8's own 520 block below), not the desktop default. --warn is correct at
    both widths and is unchanged. */
-.status-strip-action {
-  color: var(--warn);
-  font: 500 13px/16px var(--font-sans);
-  letter-spacing: 0;
-  text-transform: none;
-  white-space: nowrap;
-}
-
 /* retry fix 15: the action text (#statusStripAction, whose textContent the
    runtime overwrites on every toggle) and its trailing chevron are separate
    elements inside this row wrapper — the row, not the text span, is what
    the 520 block turns into R-H.1's divided action row, so a re-render can
    never wipe the chevron out from under it. The chevron itself is hidden
    above 520 (§ 4.10 draws no trailing chevron on the desktop banner). */
-.status-strip-action-row {
-  display: inline-flex;
-  align-items: center;
-  gap: 6px;
-}
-
-.status-strip-action-chevron {
-  display: none;
-}
-
 /* The detail list is capped and scrolls internally: a project with forty lint
    errors must not grow a dock that swallows the page it is describing. */
 /* retry fix 16: the promoted list is 04 §3/§9 item 4's bottom-open card —
@@ -5744,6 +5784,81 @@
   font-weight: 600;
 }

+/* Paper board 15 (BR5-0 / ETC-0 / EUD-0): a row's third line — the cause
+   kind in mono, a mono summary of what moved, and for a blocker row whose
+   owner was approved and is being rewritten, the way to its diff. */
+.status-finding-detail {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 9px;
+  padding-top: 1px;
+}
+
+.status-finding-kind {
+  padding: 2px 7px;
+  border-radius: 999px;
+  font: 500 10px/12px var(--font-mono);
+  background: var(--warn-bg);
+  color: var(--warn);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-kind,
+.status-finding-kind--edited {
+  background: var(--status-draft-bg);
+  color: var(--status-draft);
+}
+
+/* BR5-0: the chip under a blocker row is the claim chip's word, at the
+   row's scale — Inter 10/14 600, 0.06em, uppercase. */
+.status-finding-kind--edited {
+  font: 600 10px/14px var(--font-sans);
+  letter-spacing: .06em;
+  text-transform: uppercase;
+}
+
+.status-finding-meta {
+  font: 400 11px/14px var(--font-mono);
+  color: var(--muted);
+}
+
+.status-finding-link {
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  font: 600 11px/14px var(--font-sans);
+  color: var(--link);
+}
+
+.status-finding-link:hover { text-decoration: underline; }
+
+/* EUD-0: a Needs-you row's right-hand slot is its way in, in the row's hue,
+   where a blocker row keeps its count. */
+.status-finding-action {
+  flex: 0 0 auto;
+  border: 0;
+  padding: 0;
+  background: none;
+  cursor: pointer;
+  text-align: right;
+  font: 600 14px/18px var(--font-sans);
+  color: var(--warn);
+}
+
+.status-finding-action:hover { text-decoration: underline; }
+
+/* A row caused by an edit since approval is draft work, not an alarm: its
+   dot and its action take the draft hue (EUD-0). */
+.status-finding--group[data-tone="draft"] .status-finding-dot,
+.status-finding--group[data-severity="needs_you"][data-tone="draft"] .status-finding-dot {
+  background: var(--status-draft);
+}
+
+.status-finding--group[data-tone="draft"] .status-finding-action {
+  color: var(--status-draft);
+}
+
 /* file:// has no /api/status and therefore no ledger_findings at all (04 §8
    item 11 / §9 item 5) — stated in the body rather than silently omitted. */
 .status-strip-static-note {
@@ -5870,7 +5985,7 @@
   .comment-reply-trigger,
   .comment-composer-submit,
   .comment-composer-input,
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
   }
@@ -5934,12 +6049,15 @@
     border-left: 0;
     /* R-J.2 / OD14a.7 / M4, verifier RETRY item 4(iii): a dark-only 1px top
        hairline (the light sheet relies on its 16px corners and the scrim
-       alone). `light-dark()` expresses "present only under dark" without a
-       second selector-scoped dark query or a new dark-only token. A
+       alone). --sheet-hairline is the engine-owned token declared for
+       exactly this edge (:root and the two dark blocks): `transparent` in
+       light, var(--border) in dark, on both the OS path and the explicit
+       Dark toggle. It used to be a `light-dark()` literal while the token
+       sat unread; the token is the single place the edge is decided. A
        component-scoped `@media screen and (prefers-color-scheme: dark)`
-       block is unavailable
-       here (theme_tokens_test.go's ExactlyOneScreenScopedDarkQuery). */
-    border-top: 1px solid light-dark(transparent, var(--border));
+       block is unavailable here (theme_tokens_test.go's
+       ExactlyOneScreenScopedDarkQuery). */
+    border-top: 1px solid var(--sheet-hairline);
     border-top-left-radius: 16px;
     border-top-right-radius: 16px;
     border-radius: 16px 16px 0 0;
@@ -6015,25 +6133,10 @@
     max-width: 100%;
     box-sizing: border-box;
   }
-  .status-strip-head,
+  .status-strip-finding,
   .claim-body-disclosure__toggle {
     min-height: 44px;
-  }
-  .status-strip-head {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) 10px;
-    align-items: flex-start;
-  }
-  .status-strip-summary {
-    grid-column: 1;
-    grid-row: 1;
-  }
-  .status-strip-action-row {
-    grid-column: 1;
-    grid-row: 2;
-    justify-self: start;
-  }
-}
+  }}

 /* The claim head line. v0.4.1 gives it two children instead of loose text:
    a <span class="label"> carrying the label plus its status pill, and the
@@ -6576,24 +6679,9 @@
   background: var(--accent-bg);
 }

-/* nav-group-label separates the Tracks run from the modules above it. The
-   sidebar is otherwise a flat list of .sec-tab buttons, and a track tab looks
-   exactly like a module tab because mechanically it IS one — so without a
-   caption a reader would have no way to tell that the axis changed halfway
-   down. It borrows the .eyebrow register (mono, uppercase, --faint) rather
-   than inventing one, because it is the same kind of thing: a quiet caption
-   over the content beneath it, never a control. The top border and margin are
-   what make it read as a division rather than as a first entry in the run. */
-.nav-group-label {
-  margin: 14px 0 4px;
-  padding: 10px 20px 0;
-  border-top: 1px solid var(--border);
-  font-family: var(--font-mono);
-  font-size: 9px;
-  letter-spacing: .14em;
-  text-transform: uppercase;
-  color: var(--faint);
-}
+/* .nav-group-label (the caption over the Tracks run) is gone: the sidebar
+   groups modules and tracks in .system-nav-group <details> (shell.html), and
+   nothing has emitted the class since the system-record redesign. */

 /* Secondary horizontal facet tab strip inside a module's content area,
    ported verbatim (selector + declarations) from the reference docs
@@ -7222,14 +7310,6 @@
   color: var(--link);
 }

-.nav-group-label {
-  margin: 17px 0 5px;
-  padding: 12px 10px 0;
-  color: var(--faint);
-  font-size: 9px;
-  letter-spacing: .12em;
-}
-
 .sidebar-footer {
   padding: 11px 18px 15px;
   color: var(--faint);
@@ -8015,9 +8095,23 @@
   max-width: 760px;
 }

-.claim-body-disclosure > .claim-body { max-width: none; }
+.claim-body-disclosure > .claim-body,
+.claim-body-disclosure > .claim-edit-diff,
+.claim-body-disclosure > .claim-edit-current { max-width: none; }

-.claim-body-disclosure--collapsed > .claim-body {
+/* .claim-edit-diff is clamped alongside .claim-body because it IS the body,
+   shown a passage at a time (see system-record.js's BODY_LIKE). A claim that
+   truncates to four lines in one state and runs to full height in the other
+   is the same claim behaving as two components.
+   The clamp counts LINE BOXES and is not a height: the diff's passages carry
+   their own margins, and a tinted one carries padding, so a collapsed diff is
+   four lines of prose PLUS whatever box the first passage sits in. It is
+   therefore a little taller than a collapsed body and not a fixed multiple of
+   the line height — which is the honest description, and what the test
+   asserts rather than an exact 4x that does not hold. */
+.claim-body-disclosure--collapsed > .claim-body,
+.claim-body-disclosure--collapsed > .claim-edit-diff,
+.claim-body-disclosure--collapsed > .claim-edit-current {
   display: -webkit-box;
   overflow: hidden;
   -webkit-box-orient: vertical;
@@ -9762,31 +9856,10 @@
     border-radius: 10px;
     border-color: color-mix(in srgb, var(--warn) 26%, var(--border));
   }
-  /* retry fix 29 (this lane's third attempt): the desktop banner's own
-     padding moved from a literal 14px to the reading card's 32px gutter
-     (retry fix 20 above), but the 520px form stays the inset-card treatment
-     R-H.1 always specified. 02 §4.19 "Body block" gives the mobile form's
-     own padding as 12px/14px — one px shallower than the pre-retry-3 value
-     this rule carried, which was never checked against §4.19 directly. */
-  .status-strip-head {
-    padding: 12px 14px;
-  }
-  /* retry fix 33 (this lane's third attempt, 02 §4.19 "Icon"): the mobile
-     icon's optical offset is 2px, not the desktop row's 1px. */
-  .status-strip-icon {
-    margin-top: 2px;
-  }
-  /* retry fix 34 (this lane's third attempt, 02 §4.19 "Body text"): the
-     mobile banner sentence is Inter 13/19, a full step down from the
-     desktop 14/18 fix 21 (above) set — §4.19 is a distinct node (`574-0`)
-     from §4.10's `6T-0`, not the same rule at a smaller viewport. */
-  .status-strip-title {
-    font-size: 13px;
-    line-height: 19px;
-  }
-  .status-strip--open {
-    border-radius: 10px 10px 0 0;
-  }
+  /* The banner's own head padding, icon offset and sentence size used to be
+     re-pinned here for the phone. All three described the single-sentence
+     head that Paper EH1-0 replaced with a card, and their rules went with
+     it; the card carries its own metrics at both widths. */
   .status-strip-body {
     border-top-color: color-mix(in srgb, var(--warn) 18%, var(--border));
   }
@@ -9861,32 +9934,6 @@
     height: 30px;
     padding-inline: 12px;
   }
-  /* retry fix 15 (R-H.1, 02 §4.19): the action gets its own divided row —
-     a 1px top hairline at ~18% blocked tint, a 38px row and 14px inline
-     padding, bled to the head's own inline/bottom padding edges so the tap
-     target is the card's full width. `grid-column: 1 / -1` reclaims the
-     10px caret column too, since the divider must span edge to edge. */
-  .status-strip-action-row {
-    grid-column: 1 / -1;
-    justify-self: stretch;
-    display: flex;
-    align-items: center;
-    justify-content: space-between;
-    height: 38px;
-    margin: 8px -14px -13px;
-    padding-inline: 14px;
-    border-top: 1px solid color-mix(in srgb, var(--warn) 18%, transparent);
-  }
-  .status-strip-action {
-    font-weight: 600; /* retry fix 13: the 600 step is the PHONE form only. */
-  }
-  .status-strip-action-chevron {
-    display: inline-block;
-    width: 13px;
-    height: 13px;
-    color: var(--warn);
-    flex-shrink: 0;
-  }
 }

 /* retry fix 17 (LANES.md OD3, and fix 11's technique): .status-severity-chip
@@ -10287,13 +10334,19 @@
   background: var(--paper);
 }

-/* 04 §4.9: the bar is always draft amber, ranking VOLUME, not severity —
-   never colour these bars per row severity. */
+/* 04 §4.9: the module ranking's bar is always draft amber, ranking VOLUME,
+   not severity. The cause ranking under the Needs-you filter (Paper EKL-0,
+   "WHAT IS WAITING") is the one exception: each bar takes its cause's hue,
+   because there the rows ARE kinds of work and the hue is what tells them
+   apart. */
 .issues-rail-bar-fill {
   height: 5px;
   background: var(--status-draft);
 }

+.issues-rail-bar-fill[data-tone="alarm"] { background: var(--warn); }
+.issues-rail-bar-fill[data-tone="draft"] { background: var(--status-draft); }
+
 /* 04 §8 item 8: the rail is a ranking, so it truncates rather than scrolls. */
 .issues-rail-more {
   margin: 0;
@@ -11001,9 +11054,7 @@
   font: 13px/16px var(--font-sans);
 }
 .reading-canvas .claim-footer .comment-chip .dx-icon { width: 14px; height: 14px; }
-@media screen and (min-width: 861px) {
-  .reading-canvas .status-strip-action-chevron { display: none; }
-}
+@media screen and (min-width: 861px) {}
 @media screen and (max-width: 520px) {
   /* `display: flow-root` gives the canvas a block-formatting context without
      the side effects of restoring a border or `overflow: clip`. Without one,
@@ -11048,15 +11099,6 @@
      carries a 20px margin-bottom standing in for both and .reading-canvas has
      no border or padding at this tier, so the banner's own 14px collapses
      into that 20px and the reader gets 20px. State the no-tab-row case. */
-  .reading-canvas .status-strip-head { padding: 12px 14px; }
-  /* 56U-0: the triangle hangs on the FIRST text line (align-items: start,
-     10px gap), not centred against a three-line sentence. */
-  .reading-canvas .status-strip-summary { gap: 10px; align-items: flex-start; }
-  /* 56Y-0: Inter 13/19 — a full step down from the desktop 14/18. */
-  .reading-canvas .status-strip-title { font-size: 13px; line-height: 19px; }
-  /* 56Z-0: the divided action row bleeds to the head's own 14px inline and
-     12px bottom padding edges; Paper draws no extra gap above the rule. */
-  .reading-canvas .status-strip-action-row { margin: 12px -14px -12px; }
   .reading-canvas .claim-footer {
     margin-top: 6px;
     padding-top: 13px;
@@ -11097,6 +11139,534 @@
   .reading-canvas .claim-footer-chip--blocked .claim-footer-chip-count { color: var(--warn); }
 }

+/* ---- The banner card, and unapproved edits -------------------------------
+
+   ONE CARD, A ROW PER FINDING (Paper node EH1-0).
+
+   The banner used to be a single sentence with a "Show issues" action beside
+   it. That shape holds exactly one statement, and a facet routinely has two
+   kinds of thing waiting: claims blocked by unapproved dependencies, and
+   claims rewritten since they were approved. The second fact had to go into a
+   second tinted band stacked underneath, and two tinted bands touching read as
+   one broken component rather than as two facts.
+
+   TINT IS A SEVERITY CLAIM, so the card makes at most one. While every row
+   shares a hue the whole surface takes it; the moment two rows disagree the
+   surface goes neutral and the dots carry the colour instead. That is why
+   --alarm and --draft are separate modifiers and the neutral form is the base
+   rule: which one applies is computed from the rows, never from whichever kind
+   of finding happens to sort first. */
+
+/* The card is the PHONE form. Everything below describes the card itself; the
+   one thing that describes WHEN it applies is `display`, which is none here
+   and flex in the 520px block at the end of this file. Keeping the switch to
+   a single property in a single place is what stops a later rule in this
+   block from quietly turning the card back on at every width — which is
+   exactly what a `display: flex` sitting among the card's own layout rules
+   did on the first attempt. */
+.status-strip-card {
+  display: none;
+  flex-direction: column;
+  border: 1px solid var(--border-strong);
+  border-radius: 10px;
+  overflow: clip;
+  background: var(--card-bg);
+}
+
+.status-strip-card-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+  padding: 9px 14px;
+  background: var(--code-bg);
+  border-bottom: 1px solid var(--border);
+}
+
+.status-strip-card-eyebrow {
+  font: 600 11px/14px var(--font-sans);
+  letter-spacing: .08em;
+  text-transform: uppercase;
+  color: var(--muted);
+}
+
+/* Mono, like every other machine-produced count in this viewer: the number is
+   read off the findings, not written by anyone. */
+.status-strip-card-count {
+  font: 11px/14px var(--font-mono, var(--font-sans));
+  color: var(--faint);
+}
+
+/* A button, not a row with a click handler. Each row is one way into the
+   Issues screen and has to be reachable by keyboard and announced as the
+   control it is. */
+.status-strip-finding {
+  display: flex;
+  align-items: flex-start;
+  gap: 10px;
+  width: 100%;
+  padding: 11px 14px;
+  border: 0;
+  background: none;
+  text-align: left;
+  cursor: pointer;
+  color: var(--ink);
+}
+
+.status-strip-finding + .status-strip-finding {
+  border-top: 1px solid var(--border);
+}
+
+.status-strip-finding-dot {
+  width: 7px;
+  height: 7px;
+  flex-shrink: 0;
+  margin-top: 6px;
+  border-radius: 999px;
+  background: var(--warn);
+}
+
+.status-strip-finding[data-tone="draft"] .status-strip-finding-dot {
+  background: var(--status-draft, #976600);
+}
+
+.status-strip-finding-text {
+  flex: 1;
+  min-width: 0;
+  font: 13px/19px var(--font-sans);
+  color: var(--ink);
+}
+
+/* EH1-0: a 12px "\203a" glyph, in the card's hue (the tinted forms below
+   recolour it), optically dropped 2px to sit on the sentence's first line. */
+.status-strip-finding-chevron {
+  flex-shrink: 0;
+  margin-top: 2px;
+  color: var(--faint);
+  font: 400 12px/16px var(--font-sans);
+}
+
+/* The two tinted forms. The alphas are EH1-0's own (#9A6A1612 / #9A6A1645 /
+   #9A6A163D over the draft token), written as color-mix so the dark scheme
+   redefines them from its own hue instead of carrying a second literal. */
+.status-strip-card--alarm {
+  background: color-mix(in srgb, var(--warn) 7%, transparent);
+  border-color: color-mix(in srgb, var(--warn) 27%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--alarm .status-strip-card-eyebrow,
+.status-strip-card--alarm .status-strip-card-count,
+.status-strip-card--alarm .status-strip-finding-chevron {
+  color: var(--warn);
+}
+
+.status-strip-card--alarm .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--warn) 24%, transparent);
+}
+
+.status-strip-card--draft {
+  background: color-mix(in srgb, var(--status-draft, #976600) 7%, transparent);
+  border-color: color-mix(in srgb, var(--status-draft, #976600) 27%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-head {
+  background: none;
+  border-bottom-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+.status-strip-card--draft .status-strip-card-eyebrow,
+.status-strip-card--draft .status-strip-card-count,
+.status-strip-card--draft .status-strip-finding-chevron {
+  color: var(--status-draft, #976600);
+}
+
+.status-strip-card--draft .status-strip-finding + .status-strip-finding {
+  border-top-color: color-mix(in srgb, var(--status-draft, #976600) 24%, transparent);
+}
+
+/* The per-claim edit surface (Paper DON-0 / C9H-0).
+
+   It is a BAR ABOVE THE BODY with a two-segment switch, not a disclosure, and
+   the difference is which state is the default. A disclosure says the diff is
+   an aside a reader opens if they want it; the board says the opposite — a
+   claim that was approved and then rewritten opens SHOWING what moved,
+   because that is the thing about it a reader has to know before they read a
+   word of it. "Current" is the escape hatch, not the resting state. */
+/* While the changes are showing, the claim's own body is hidden by this class
+   on the CARD. See paintApprovedEdit for why the state does not live on the
+   body: the long-body disclosure re-wraps it after first paint, so the body
+   is not the same node before and after. */
+.claim--edit-view > .claim-body,
+.claim--edit-view > .claim-body-disclosure:not(.claim-body-disclosure--edit),
+.claim--edit-view > .claim-empty,
+.claim--edit-view > .claim-mockup-body {
+  display: none;
+}
+
+/* THE BAR WRAPS BY CONTENT, NOT BY VIEWPORT.
+   Its width is the reading column's, not the window's — about 690px at 1440
+   — so a media query cannot see the moment it runs out of room. The Current
+   state's line ("1 passage differs from the approval of 20 Sep") is the long
+   one, and at that column it overran by a few pixels and broke mid-date:
+   "...the approval of 20 / Sep".
+   Wrapping instead of shrinking is deliberate. The three ways to make it fit
+   were truncating the sentence, stepping the type down, or letting the row
+   become two rows; the first hides a fact, the second makes a 12px mono line
+   smaller than anything else on the card, and the third costs 8px of height
+   in the rare case. The switch is what moves, because it is the only part
+   with no sentence in it. */
+/* Paper CKF-0: the bar is the draft tint (--color-draft-bg), radius 6,
+   9px/13px, and it sits in the card's own 16px rhythm — no margin of its
+   own. Title and meta share a baseline with 10px between them. */
+.claim-edit-bar {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 8px 10px;
+  margin: 0;
+  padding: 9px 13px;
+  border-radius: 6px;
+  background: var(--status-draft-bg, rgba(154, 106, 22, .11));
+  font-family: var(--font-sans);
+}
+
+.claim-edit-bar-title {
+  flex: 0 0 auto;
+  font: 600 13px/18px var(--font-sans);
+  color: var(--status-draft, #976600);
+}
+
+/* Mono, like every other machine-read fact in this viewer: the date, the
+   actor and the passage count are read off the ledger, not written by
+   anyone. */
+/* The meta's own width is its CONTENT's, which is what makes the switch the
+   first thing to wrap: flex lays the row out from the items' base sizes, so
+   title + this sentence + switch overflowing moves the switch — the only item
+   with no sentence in it — down to its own row, and the sentence gets the
+   whole first row instead of being squeezed into what was left over.
+   It stays shrinkable on purpose. Refusing to shrink made it overrun the bar
+   entirely at 900px, where even a full row is not enough; wrapping inside
+   itself is a worse line but a contained one, and it is the last resort
+   rather than the first thing that happens. */
+.claim-edit-bar-meta {
+  flex: 1 1 auto;
+  min-width: 0;
+  font: 11px/16px var(--font-mono, var(--font-sans));
+  color: var(--muted);
+}
+
+/* The switch. The selected segment is a filled dark pill, which is the one
+   place in this panel that spends a strong value — everything else is a tint,
+   and the reader has to be able to see which of two states they are in from
+   across the page. */
+/* Paper CKJ-0: a card-ground pill with a 1px hairline, 2px inset, segments
+   3px/11px at 11/14. */
+.claim-edit-switch {
+  display: inline-flex;
+  align-items: center;
+  flex: 0 0 auto;
+  /* Pinned right on the first row, and still right when it wraps to its own. */
+  margin-left: auto;
+  gap: 0;
+  padding: 2px;
+  border: 1px solid var(--border);
+  border-radius: 999px;
+  background: var(--card-bg);
+}
+
+.claim-edit-switch-seg {
+  border: 0;
+  border-radius: 999px;
+  padding: 3px 11px;
+  background: none;
+  cursor: pointer;
+  font: 500 11px/14px var(--font-sans);
+  color: var(--muted);
+}
+
+.claim-edit-switch-seg[aria-pressed="true"] {
+  background: var(--ink);
+  color: var(--card-bg);
+  font-weight: 600;
+}
+
+/* The passages. Typography is the claim body's own — this IS the claim body,
+   shown a passage at a time — so a reader moving between Changes and Current
+   is reading the same prose in the same face, not a second document. */
+.claim-edit-diff,
+.claim-edit-current {
+  font-family: var(--font-serif);
+  font-size: 17px;
+  line-height: 28px;
+  max-width: 760px;
+}
+
+/* Passages keep the body's 16px rhythm between them (the card's own gap).
+   Margins rather than flex gap, because the four-line clamp turns this box
+   into a -webkit-box and a flex gap would vanish with it. */
+.claim-edit-passage {
+  margin: 0;
+}
+
+.claim-edit-passage + .claim-edit-passage {
+  margin-top: 16px;
+}
+
+.claim-edit-passage > :first-child {
+  margin-top: 0;
+}
+
+.claim-edit-passage > :last-child {
+  margin-bottom: 0;
+}
+
+/* Paper CK7-0 / CKP-0: a changed passage is a tinted row, radius 4, with a
+   3px rule INSIDE it (12px in from the edge, 12px before the text) that runs
+   the height of the text, old above new with 2px between them and 4px above
+   the pair. The prose itself takes the hue: blocked for the approved
+   passage, locked for the current one. Colour is not the only carrier — the
+   removed passage is struck through and the added one is not, so the two
+   stay apart in a monochrome print, under a colour-vision deficiency, and
+   in the dark scheme where the two tints are close in luminance. */
+.claim-edit-passage--removed,
+.claim-edit-passage--added,
+.claim-edit-passage--current {
+  position: relative;
+  border-radius: 4px;
+}
+
+.claim-edit-passage--removed,
+.claim-edit-passage--added {
+  padding: 7px 12px 7px 27px;
+}
+
+.claim-edit-passage--removed::before,
+.claim-edit-passage--added::before,
+.claim-edit-passage--current::before {
+  content: "";
+  position: absolute;
+  left: 12px;
+  top: 7px;
+  bottom: 7px;
+  width: 3px;
+  border-radius: 2px;
+  background: currentColor;
+}
+
+.claim-edit-passage--removed {
+  color: var(--warn);
+  background: var(--warn-bg, rgba(158, 59, 54, .10));
+  text-decoration: line-through 1px;
+  text-underline-position: from-font;
+}
+
+.claim-edit-passage--added {
+  color: var(--accent);
+  background: var(--accent-bg, rgba(44, 107, 82, .10));
+}
+
+.claim-edit-passage + .claim-edit-passage--removed {
+  margin-top: 20px;
+}
+
+.claim-edit-passage--removed + .claim-edit-passage--added {
+  margin-top: 2px;
+}
+
+/* The Current state (Paper DPZ-0): the passage that differs carries only a
+   draft-coloured rule at the left edge — no tint, prose in ink — so the
+   claim reads as ordinary prose with the seam marked. */
+.claim-edit-passage--current {
+  padding: 0 0 0 15px;
+}
+
+.claim-edit-passage--current::before {
+  left: 0;
+  top: 0;
+  bottom: 0;
+  background: var(--status-draft, #976600);
+}
+
+/* The words that moved, inside the passage they moved in. They stay in the
+   DOM for the passage's aria-label and for a reader who selects the text,
+   but Paper draws the passage as one tinted, coloured, struck (or not)
+   block and paints nothing on the individual words — the struck passage and
+   the coloured one are the comparison. */
+.claim-edit-word--removed,
+.claim-edit-word--added {
+  background: none;
+  color: inherit;
+  padding: 0;
+}
+
+.claim-edit-note {
+  margin: 8px 0 0;
+}
+
+.claim-edit-note-text,
+.claim-edit-note-hint,
+.claim-edit-field-note {
+  margin: 0;
+  font: 400 13px/19px var(--font-sans);
+  color: var(--muted);
+}
+
+/* The hint names the command that ENDS this state. A record written before
+   the approved wording was kept says "there is nothing to compare against",
+   and until this line existed that was the end of the sentence — true, and
+   with no way out of it. The wording is usually still in the project's own
+   git history, so the panel that reports the gap also names the verb that
+   closes it. */
+.claim-edit-note-hint {
+  margin-top: 4px;
+  color: var(--faint);
+}
+
+/* The fields that moved. This block is TEXT, not prose: it shows the YAML the
+   claim file holds, so it is monospace and pre-wrapped, and the tints are the
+   same two the passage diff uses so a reader does not learn a second colour
+   language halfway down one panel. */
+.claim-edit-fields {
+  margin: 8px 0 0;
+}
+
+.claim-edit-fields-toggle {
+  appearance: none;
+  -webkit-appearance: none;
+  background: none;
+  border: 0;
+  padding: 0;
+  cursor: pointer;
+  font: 500 13px/19px var(--font-sans);
+  color: var(--accent);
+  text-align: left;
+}
+
+.claim-edit-fields-toggle:hover { text-decoration: underline; }
+
+.claim-edit-fields-list {
+  margin-top: 6px;
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+}
+
+.claim-edit-field-name {
+  display: block;
+  font: 600 12px/18px var(--font-mono);
+  color: var(--muted);
+}
+
+.claim-edit-field-hunk {
+  margin: 3px 0 0;
+  padding: 6px 8px;
+  border-radius: 6px;
+  border-left: 3px solid transparent;
+  background: var(--code-bg);
+  font: 400 12px/18px var(--font-mono);
+  /* pre-wrap and not pre: a claims file's embodiment block is indented deep
+     enough to overflow a narrow column, and a horizontal scrollbar inside a
+     panel inside a claim is three nested scroll regions. Wrapping keeps the
+     indentation and loses only the guarantee that a line is one line. */
+  white-space: pre-wrap;
+  overflow-wrap: anywhere;
+}
+
+.claim-edit-field-hunk--removed {
+  border-left-color: var(--warn);
+  background: color-mix(in srgb, var(--warn) 7%, var(--code-bg));
+}
+
+.claim-edit-field-hunk--added {
+  border-left-color: var(--border-strong);
+}
+
+@media (max-width: 520px) {
+  /* The phone swaps the whole banner: the desktop band's sentence-and-action
+     row goes away and the card takes its place, so .status-strip stops being
+     a painted surface and becomes the gutter that positions the card.
+     .reading-canvas > .status-strip is named alongside it because that rule
+     is two-class and unqueried, and would otherwise outrank this one and
+     restore the band's border underneath the card. */
+  .status-strip-head {
+    display: none;
+  }
+
+  .status-strip-card {
+    display: flex;
+  }
+
+  .reading-canvas > .status-strip,
+  .status-strip,
+  .status-strip--lint,
+  .status-strip--integrity {
+    background: none;
+    border: 0;
+    border-radius: 0;
+    margin: 0;
+    /* The gutter is the reading card's own 16px, not the desktop 32px —
+       EH1-0's mobile frame draws the card at mx-4 inside a 390px canvas. */
+    padding: 14px 16px 0;
+  }
+
+  /* The edit bar stacks into three rows (Paper CHK-0): the sentence, the
+     machine line under it, and the switch full width with two equal halves.
+     Side by side at this width the sentence wrapped to two lines, the mono
+     meta broke mid-word, and the switch was squeezed against them — three
+     things competing for a 343px row that only ever held one comfortably. */
+  .claim-edit-bar {
+    flex-direction: column;
+    align-items: stretch;
+    gap: 8px;
+    padding: 10px 12px;
+  }
+
+  .claim-edit-bar-title {
+    /* CKY-0: the title and its machine line are 2px apart on the phone. */
+    margin-bottom: -6px;
+  }
+
+  .claim-edit-bar-meta {
+    /* The phone stack gives every part its own row already. */
+    flex: none;
+    line-height: 15px;
+  }
+
+  .claim-edit-switch {
+    display: flex;
+    margin-left: 0;
+  }
+
+  .claim-edit-switch-seg {
+    flex: 1;
+    padding: 5px 10px;
+    font-size: 12px;
+    line-height: 16px;
+    text-align: center;
+  }
+
+  /* CKP-0: the rule and the text sit 10px in on a phone, not 12. */
+  .claim-edit-passage--removed,
+  .claim-edit-passage--added {
+    padding: 7px 10px 7px 23px;
+  }
+
+  .claim-edit-passage--removed::before,
+  .claim-edit-passage--added::before {
+    left: 10px;
+  }
+
+  .claim-edit-passage + .claim-edit-passage--removed {
+    margin-top: 18px;
+  }
+}
+
+
 @media print {
   :root { color-scheme: light; }

@@ -11115,10 +11685,6 @@
     display: block;
   }

-  .claim-links-summary {
-    cursor: auto;
-  }
-
   /* layout: chrome off the page, gutters closed */
   .system-record-head { padding-top: 0; }
   .sidebar,
@@ -11611,7 +12177,7 @@
     <div class="issues-body">
       <div id="issuesFindingsCard" class="issues-findings-card"></div>
       <aside id="issuesRailPanel" class="issues-rail-panel">
-        <p class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
+        <p id="issuesRailHeading" class="issues-rail-heading">WHERE THE BLOCKERS LIVE</p>
         <p id="issuesRailCaveat" class="issues-rail-caveat"></p>
         <div id="issuesRailRows" class="issues-rail-rows"></div>
       </aside>
@@ -11638,9 +12204,10 @@


   <section id="statusStrip" class="status-strip" role="status" aria-live="polite" hidden>
-    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody">
+    <button id="statusStripToggle" class="status-strip-head" type="button" aria-expanded="false" aria-controls="statusStripBody" data-tone="alarm">
       <span id="statusStripSummary" class="status-strip-summary">
         <svg class="dx-icon status-strip-icon" aria-hidden="true"><use href="#dx-icon-triangle-alert"/></svg>
+        <span class="status-strip-dot" aria-hidden="true"></span>
         <span class="status-strip-summary-text">
           <strong id="statusStripTitle" class="status-strip-title"></strong>
         </span>
@@ -11650,6 +12217,13 @@
         <svg class="dx-icon status-strip-action-chevron" aria-hidden="true"><use href="#dx-icon-chevron-right"/></svg>
       </span>
     </button>
+    <div id="statusStripCard" class="status-strip-card">
+      <div class="status-strip-card-head">
+        <span id="statusStripCardEyebrow" class="status-strip-card-eyebrow">Needs you here</span>
+        <span id="statusStripCardCount" class="status-strip-card-count"></span>
+      </div>
+      <div id="statusStripFindings" class="status-strip-card-findings"></div>
+    </div>
     <div id="statusStripBody" class="status-strip-body" hidden></div>
   </section>

@@ -11698,8 +12272,6 @@
       // each SSE re-render. It attaches NO listeners — those are delegated once
       // on surviving nodes below, so re-running this never double-binds.
       function initViewer() {
-        // Fragment swaps replace <main>; drop the stale mount pointer.
-        mountedSurfaceID = null;
         // Two-level show/hide: one .module-section per sidebar .sec-tab, and
         // (when a module has more than one facet) one .claim-group per .subtab
         // inside that module's .sub-nav. A single-facet module still has exactly
@@ -11826,7 +12398,6 @@
       // surface on first paint; smaller fixtures mount every surface so print,
       // theme probes, and live-reload witnesses that call getElementById keep
       // working without visiting every facet first.
-      var mountedSurfaceID = null;
       var SOFT_MOUNT_MIN_CLAIMS = 80;

       function claimCorpusSize() {
@@ -11854,7 +12425,6 @@
         var host = group.querySelector(':scope > [data-dossierx-surface-host]');
         var tmpl = group.querySelector(':scope > template.dossierx-surface-template');
         if (!host || !tmpl) {
-          mountedSurfaceID = surfaceID;
           return;
         }
         if (!host.querySelector('.claim')) {
@@ -11862,13 +12432,31 @@
           if (typeof window.dossierxEnhanceSystemRecord === 'function') {
             window.dossierxEnhanceSystemRecord();
           }
+          // The edited-since-approval chips and panels are attached HERE, at
+          // mount, and BEFORE the clamps below — not left to the next
+          // renderStatusStrip pass.
+          //
+          // They insert a node above each edited claim's body, so attaching
+          // them later moves everything under them. Two things read that
+          // height and cannot be allowed to read it early: a deep link has
+          // already scrolled to its claim, so the panels appearing afterwards
+          // slid the page out from under the sticky header; and the
+          // source-note clamps measure scrollHeight against clientHeight, so
+          // a mid-measurement insertion decides "show more" from a layout
+          // that is about to change. Decorating first means the surface is at
+          // its final height before anything measures or scrolls it.
+          renderApprovedEdits();
           // Source-note clamps observe live nodes only; re-arm after every
           // first-time mount so a freshly revealed surface is measured.
           if (typeof mountSourceNoteClamps === 'function') {
             mountSourceNoteClamps();
           }
         }
-        mountedSurfaceID = surfaceID;
+        // The no-template path (an eagerly rendered surface) never enters the
+        // block above, and still has claims to decorate. renderApprovedEdits
+        // is idempotent — every node it creates is either replaced wholesale
+        // or guarded — so the second call costs nothing on the path that did.
+        renderApprovedEdits();
       }

       function mountAllSurfaces() {
@@ -12130,10 +12718,8 @@
         if (!toastEl) { return; }
         toastEl.textContent = msg; // textContent — error text is never trusted markup
         toastEl.hidden = false;
-        toastEl.classList.add('comments-toast--show');
         if (toastTimer) { window.clearTimeout(toastTimer); }
         toastTimer = window.setTimeout(function () {
-          toastEl.classList.remove('comments-toast--show');
           toastEl.hidden = true;
         }, 4000);
       }
@@ -12974,21 +13560,30 @@
       // claim has no edges".

       var stripEl = document.getElementById('statusStrip');
-      var stripToggle = document.getElementById('statusStripToggle');
-      var stripSummary = document.getElementById('statusStripSummary');
-      var stripTitle = document.getElementById('statusStripTitle');
       // retry fix 21 (this lane's third attempt): #statusStripNote (the
       // "Critical 0 · Needs you 3 · ..." tally) is REMOVED per the
       // coordinator's ruling — no 02/03/04 board draws it, the chips
       // already carry every count it repeated — and dropped from shell.html
       // along with its element lookup here.
-      var stripAction = document.getElementById('statusStripAction');
+      //
+      // THE HEAD HAS TWO FORMS, ONE PER WIDTH, and both are written on every
+      // paint. #statusStripToggle is the desktop band — one summary sentence
+      // with "Show issues" beside it — and #statusStripCard is the phone form
+      // (Paper EH1-0), a card with one ROW PER FINDING. CSS shows one; see
+      // shell.html for why the choice is a media query and not a width read
+      // in here.
+      //
+      // Both are filled unconditionally rather than behind a matchMedia
+      // check. The cost is a few DOM nodes nobody sees; the alternative is a
+      // resize listener and a breakpoint duplicated out of the stylesheet,
+      // which is one more thing to keep in step for no gain.
+      var stripToggle = document.getElementById('statusStripToggle');
+      var stripTitle = document.getElementById('statusStripTitle');
+      var stripCard = document.getElementById('statusStripCard');
+      var stripCardCount = document.getElementById('statusStripCardCount');
+      var stripFindings = document.getElementById('statusStripFindings');
       var stripBody = document.getElementById('statusStripBody');
       var lastStatusData = null;
-      // Paper starts every status strip collapsed. stripExpanded then stays
-      // sticky across refreshes, so a poll (or an SSE tick) never takes the
-      // disclosure state away from the reader.
-      var stripExpanded = false;

       function countLabel(n, word) {
         return n + ' ' + word + (n === 1 ? '' : 's');
@@ -13195,12 +13790,51 @@
       // demoted slug, and the fixed-width count slot — rather than three
       // loose wrapping siblings. See style.css's .status-finding-dot /
       // .status-finding-text for the layout this DOM shape enables.
+      // editChangeSummary is the mono line under an Issues row for a claim
+      // rewritten since approval (Paper BR5-0 / EUD-0): what moved, and when
+      // the approval it moved from was given.
+      function editChangeSummary(change) {
+        var parts = [];
+        if (!change.content_retained) {
+          parts.push('approved wording not retained');
+        } else if (change.changed_passages) {
+          parts.push(countLabel(change.changed_passages, 'passage') + ' changed');
+        } else if ((change.field_changes || []).length) {
+          parts.push(countLabel(change.field_changes.length, 'field') + ' moved');
+        }
+        var approved = editDateLabel(change.approved_at);
+        if (approved) { parts.push('approved ' + approved); }
+        return parts.join(' · ');
+      }
+
+      // goToClaimChanges leaves the Issues screen for the claim itself, with
+      // the Changes state showing — the row said "See changes", so landing
+      // on the claim with the diff put away would not be what it offered.
+      function goToClaimChanges(id, change) {
+        closeIssuesView();
+        if (window.location.hash !== '#' + id) { window.location.hash = '#' + id; }
+        var card = document.getElementById(id);
+        if (card && change) { paintApprovedEdit(card, change, true); }
+        if (card && typeof card.scrollIntoView === 'function') { card.scrollIntoView({ block: 'start' }); }
+      }
+
       function renderStatusGroup(group) {
         var row = el('li', 'status-finding status-finding--group');
         row.setAttribute('data-severity', group.severity);
+        // A row whose cause is an edit since approval is draft work, not an
+        // alarm (Paper EUD-0 draws its dot and action in the draft hue).
+        var edits = offlineApprovedEdits();
+        var editedOwner = group.ownerModuleClaimID && edits[group.ownerModuleClaimID] ? group.ownerModuleClaimID : '';
+        var isEditCause = group.severity === 'needs_you' && group.kind === 'unapproved_edit' && !!editedOwner;
+        row.setAttribute('data-tone', isEditCause ? 'draft' : 'alarm');
         row.appendChild(el('span', 'status-finding-dot'));
         var text = el('span', 'status-finding-text');
-        text.appendChild(textEl('span', 'status-finding-rule', group.title || 'Issue'));
+        // Paper EUD-0 names the claim and what happened to it in one line:
+        // "<Title> was approved and is being rewritten".
+        var title = isEditCause
+          ? readinessClaimLabel(editedOwner) + ' was approved and is being rewritten'
+          : (group.title || 'Issue');
+        text.appendChild(textEl('span', 'status-finding-rule', title));
         var ids = Object.keys(group.claimIDs).sort();
         // Paper 1KX-0: every row carries a second, demoted line under its
         // title holding the dotted claim id of the claim the reader must go
@@ -13218,11 +13852,52 @@
         if (ownerID) {
           text.appendChild(textEl('span', 'status-finding-claim', ownerID));
         }
+
+        // The third line (Paper board 15, F and I). A Needs-you row names its
+        // cause KIND in mono, because the sentence alone cannot distinguish
+        // "someone never wrote this" from "someone approved it and then
+        // moved it". A blocker row whose owner was approved and is being
+        // rewritten says so under the row it already had (BR5-0): no new
+        // group and no new severity, one line added to the existing row.
+        var detail = null;
+        if (group.severity === 'needs_you') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind', group.kind || 'review'));
+          if (isEditCause) {
+            detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          }
+        } else if (editedOwner && group.severity === 'blocker') {
+          detail = el('span', 'status-finding-detail');
+          detail.appendChild(textEl('span', 'status-finding-kind status-finding-kind--edited', 'Edited · was approved'));
+          detail.appendChild(textEl('span', 'status-finding-meta', editChangeSummary(edits[editedOwner])));
+          var link = textEl('button', 'status-finding-link', 'See changes');
+          link.type = 'button';
+          link.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(editedOwner, edits[editedOwner]);
+          });
+          detail.appendChild(link);
+        }
+        if (detail) { text.appendChild(detail); }
         row.appendChild(text);
-        // retry fix 7: 04 §4.8 "Count label" / §6 "Row count" promotes the
-        // phrase INTO the count slot as a bare `N claims`, never `blocks N
-        // claim(s)` — that longer phrase was written for a full-width third
-        // line, which the fixed 104px slot (style.css) no longer is.
+
+        // The right-hand slot. A blocker row keeps its count (04 §4.8: the
+        // phrase promoted INTO the 104px slot as a bare `N claims`). A
+        // Needs-you row carries its way in instead (Paper ETC-0 / EUD-0):
+        // the thing waiting is one claim, and the count would always be 1.
+        if (group.severity === 'needs_you' && group.ownerModuleClaimID) {
+          var owner = group.ownerModuleClaimID;
+          var action = textEl('button', 'status-finding-action', isEditCause ? 'See changes' : 'Open claim');
+          action.type = 'button';
+          action.addEventListener('click', function (event) {
+            event.preventDefault();
+            event.stopPropagation();
+            goToClaimChanges(owner, isEditCause ? edits[owner] : null);
+          });
+          row.appendChild(action);
+          return row;
+        }
         var n = ids.length || group.count;
         row.appendChild(textEl('span', 'status-finding-msg', n + ' claim' + (n === 1 ? '' : 's')));
         return row;
@@ -13321,7 +13996,7 @@
         var label = card && card.querySelector('.k .label');
         if (label) {
           var copy = label.cloneNode(true);
-          copy.querySelectorAll('.pill, .k-id, .claim-collapse-chevron').forEach(function (node) { node.remove(); });
+          copy.querySelectorAll('.pill, .k-id').forEach(function (node) { node.remove(); });
           var rendered = (copy.textContent || '').replace(/\s+/g, ' ').trim();
           if (rendered) { return rendered; }
         }
@@ -13348,7 +14023,12 @@
           approval_content_drift: 'This claim changed after approval',
           approval_missing: 'This claim has no approval record',
           approval_released: 'This claim\'s approval was released',
-          approval_unknown: 'This claim\'s approval state is unknown'
+          approval_unknown: 'This claim\'s approval state is unknown',
+          // Deliberately not "changed after approval" (approval_content_drift's
+          // wording): that names a LOCKED claim whose bytes no longer match a
+          // STANDING approval, which is the tamper finding. This names the
+          // honest, intended act — unlock, rewrite, re-lock — caught mid-way.
+          unapproved_edit: 'This claim was approved, then rewritten'
         };
         return labels[record.kind] || String(record.kind || 'Readiness obstacle').replace(/_/g, ' ');
       }
@@ -13856,12 +14536,397 @@
         } catch (_) { return {}; }
       }

+      // ---- Unapproved edits -------------------------------------------------
+      //
+      // The state these three functions draw is the ordinary one, and it was
+      // the one the viewer could not show. A claim is locked; someone unlocks
+      // it, rewrites it, and has not locked it again yet. The new wording
+      // replaced the old in the file, so the reading view showed text with
+      // nothing to compare it against and a chip reading DRAFT — the same chip
+      // a claim nobody ever approved carries. "Never written" and "approved,
+      // then moved" are different jobs for a reviewer, and one word was being
+      // used for both.
+      //
+      // The engine now answers both questions: readiness emits an
+      // `unapproved_edit` cause (so the claim reaches the Issues screen on its
+      // own, without needing a dependent to notice for it), and the graph
+      // payload carries the line diff from the approved body to the current
+      // one. Everything below is presentation of those two facts. It computes
+      // nothing about whether an edit is allowed, and never writes.
+
+      // offlineApprovedEdits reads the per-claim approvaledit.Change records
+      // off the SAME rendered graph payload offlineReadiness reads. There is
+      // deliberately no /api/status path for this: a checked-in file:// export
+      // has no endpoint to ask, and a panel that appeared only under
+      // "dossierx serve" would be missing exactly where a reviewer reads a
+      // repository they did not build.
+      function offlineApprovedEdits() {
+        var node = document.getElementById('dossierx-graph');
+        if (!node) { return {}; }
+        try {
+          var payload = JSON.parse(node.textContent || '{}');
+          var out = {};
+          (payload.nodes || []).forEach(function (graphNode) {
+            if (graphNode.id && graphNode.approved_edit) { out[graphNode.id] = graphNode.approved_edit; }
+          });
+          return out;
+        } catch (_) { return {}; }
+      }
+
+      // editDateLabel turns an RFC3339 instant into the board's own short
+      // form ("12 Sep"). It is deliberately not a full date: the panel's job
+      // is to place the approval in the reader's recent memory, and a
+      // four-part date would be the widest thing on a row that is otherwise
+      // a sentence.
+      function editDateLabel(iso) {
+        if (!iso) { return ''; }
+        var d = new Date(iso);
+        if (isNaN(d.getTime())) { return String(iso).slice(0, 10); }
+        return d.getDate() + ' ' + ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun',
+          'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][d.getMonth()];
+      }
+
+      // approvedEditHasDiff reports whether there is a second wording to show
+      // at all.
+      //
+      // Two states look like an edit and have nothing to put behind a
+      // "Changes" tab. A record written before the approved wording was kept
+      // proves the text moved and does not carry what it moved from. A claim
+      // whose hash moved because its rests_on or build_role moved has an
+      // approved wording identical to its current one. In both, the panel has
+      // one sentence to say and no second version of the claim — so there is
+      // no switch, and the claim's own body stays on screen.
+      //
+      // Offering the switch anyway is what this replaces, and it was worse
+      // than a useless control: choosing "Changes" hid the body and put the
+      // sentence in its place, so the claim rendered with no content at all.
+      function approvedEditHasDiff(change) {
+        return !!(change.content_retained &&
+          (change.hunks || []).length &&
+          (change.changed_passages || 0) > 0);
+      }
+
+      // approvedEditDiff builds the passage-by-passage view: unchanged
+      // passages as ordinary prose, and each changed one as the approved
+      // passage above the current one, tinted and ruled.
+      //
+      // The HTML is produced by the ENGINE (internal/approvaledit), not here,
+      // because internal/render/markdown is this project's only markdown
+      // renderer and the viewer has none. Shipping the source and parsing it
+      // in the browser would mean growing a second renderer that could
+      // disagree with the first about the same claim, on the same page.
+      function approvedEditDiff(change) {
+        var body = el('div', 'claim-edit-diff');
+        (change.hunks || []).forEach(function (hunk) {
+          var cls = hunk.op === 'remove' ? 'claim-edit-passage claim-edit-passage--removed'
+            : hunk.op === 'add' ? 'claim-edit-passage claim-edit-passage--added'
+            : 'claim-edit-passage';
+          var block = el('div', cls);
+          // innerHTML, and only here. The value came from
+          // internal/render/markdown, which is the escaping boundary every
+          // other claim body in this viewer crosses — the same function, over
+          // the same author bytes, reached through the same payload as the
+          // readiness projection beside it. It is exactly as trusted as the
+          // body it sits above, and no more.
+          block.innerHTML = hunk.html || '';
+          if ((hunk.changed || []).length) {
+            block.setAttribute('aria-label',
+              (hunk.op === 'remove' ? 'Removed since approval: ' : 'Added since approval: ') +
+              hunk.changed.join(', '));
+          }
+          body.appendChild(block);
+        });
+        var fields = approvedEditFields(change);
+        if (fields) { body.appendChild(fields); }
+        return body;
+      }
+
+      // approvedEditCurrent builds the OTHER state of the same surface: the
+      // current wording, passage by passage, with the passage that differs
+      // from the approval carrying a draft-coloured rule and no tint (Paper
+      // DPZ-0: "the same claim with the diff put away, reading as ordinary
+      // prose, with an amber rule marking where the difference sits").
+      //
+      // It is rendered from the same hunks as the diff rather than by
+      // un-hiding the claim's own body, because the body cannot say WHERE the
+      // difference sits: the rule is the one thing this state adds, and the
+      // body has no passage boundaries to hang it on.
+      function approvedEditCurrent(change) {
+        var body = el('div', 'claim-edit-current');
+        (change.hunks || []).forEach(function (hunk) {
+          if (hunk.op === 'remove') { return; }
+          var block = el('div', hunk.op === 'add'
+            ? 'claim-edit-passage claim-edit-passage--current'
+            : 'claim-edit-passage');
+          // Same escaping boundary as approvedEditDiff: engine-rendered
+          // markdown, the same bytes the diff shows.
+          block.innerHTML = hunk.html || '';
+          if (hunk.op === 'add') {
+            block.setAttribute('aria-label', 'This passage differs from the approved wording');
+          }
+          body.appendChild(block);
+        });
+        return body;
+      }
+
+      // approvedEditFields draws the non-body fields that moved, each as its
+      // approved YAML above its current YAML with the words that moved marked.
+      //
+      // It is here because "Also changed: embodiment, audit_notes." was not an
+      // answer. For a claim whose prose never moved that sentence WAS the whole
+      // panel, and it named the change without showing it — the reader was told
+      // a field they cannot see had moved in a way they cannot see, and had to
+      // go and read the file to find out what they were being asked about. Six
+      // of the fourteen claims this feature was built for are in exactly that
+      // state.
+      //
+      // The blocks are drawn as text and not as prose. A field is YAML, so it
+      // is escaped by the engine (internal/approvaledit/fields.go) and shown in
+      // the monospace the file itself would show; running it through the
+      // markdown renderer would turn a list into a list and a `*` into
+      // emphasis, and stop it being the thing on disk.
+      function approvedEditFields(change) {
+        var changes = change.field_changes || [];
+        if (!changes.length) { return null; }
+        var wrap = el('div', 'claim-edit-fields');
+
+        var toggle = el('button', 'claim-edit-fields-toggle');
+        toggle.type = 'button';
+        var list = el('div', 'claim-edit-fields-list');
+        var open = true;
+        function paint() {
+          toggle.setAttribute('aria-expanded', String(open));
+          toggle.textContent = (open ? 'Hide' : 'Show') + ' the ' +
+            (changes.length === 1 ? 'field' : changes.length + ' fields') + ' that moved';
+          list.hidden = !open;
+        }
+        toggle.addEventListener('click', function (event) {
+          event.preventDefault();
+          open = !open;
+          paint();
+        });
+
+        changes.forEach(function (field) {
+          var row = el('div', 'claim-edit-field');
+          row.appendChild(textEl('span', 'claim-edit-field-name', field.field));
+          if (field.truncated) {
+            row.appendChild(textEl('p', 'claim-edit-field-note',
+              'This field moved. It is too large to show here; read it in the claim file.'));
+            list.appendChild(row);
+            return;
+          }
+          (field.hunks || []).forEach(function (hunk) {
+            var cls = hunk.op === 'remove' ? 'claim-edit-field-hunk claim-edit-field-hunk--removed'
+              : hunk.op === 'add' ? 'claim-edit-field-hunk claim-edit-field-hunk--added'
+              : 'claim-edit-field-hunk';
+            var block = el('pre', cls);
+            // innerHTML over engine-escaped text. internal/approvaledit's
+            // field renderer HTML-escapes the YAML before it marks anything,
+            // so the only tags that can be in here are the mark spans it
+            // wrote itself — a field holding <script> arrives as text.
+            block.innerHTML = hunk.html || '';
+            if ((hunk.changed || []).length) {
+              block.setAttribute('aria-label',
+                (hunk.op === 'remove' ? 'Removed from ' : 'Added to ') + field.field + ': ' +
+                hunk.changed.join(', '));
+            }
+            row.appendChild(block);
+          });
+          list.appendChild(row);
+        });
+
+        paint();
+        wrap.appendChild(toggle);
+        wrap.appendChild(list);
+        return wrap;
+      }
+
+      // approvedEditNote is what a claim with no second wording says instead of
+      // offering a switch. It sits under the bar, beside the claim's own body
+      // rather than in place of it.
+      function approvedEditNote(change) {
+        var wrap = el('div', 'claim-edit-note');
+        if (!change.content_retained) {
+          wrap.appendChild(textEl('p', 'claim-edit-note-text',
+            'This claim was approved before the approved wording was kept on the record, so there is nothing to compare against. The approval is released and the text has since changed.'));
+          wrap.appendChild(textEl('p', 'claim-edit-note-hint',
+            'Run dossierx claim recover-approved-content to look for the approved revision in this project\u2019s git history.'));
+          return wrap;
+        }
+        var fields = change.other_fields || [];
+        wrap.appendChild(textEl('p', 'claim-edit-note-text', fields.length
+          ? 'The wording is unchanged since approval. ' +
+            (fields.length === 1 ? '1 field moved.' : fields.length + ' fields moved.')
+          : 'The wording is unchanged since approval.'));
+        var diff = approvedEditFields(change);
+        if (diff) { wrap.appendChild(diff); }
+        return wrap;
+      }
+
+      // approvedEditBar is the control the board draws in place of the old
+      // disclosure: a tinted row above the body that says what the reader is
+      // looking at, and a two-segment switch between the two states.
+      //
+      // It is a BAR AND NOT A DISCLOSURE, and the difference is which state
+      // is the default. A disclosure says the diff is an aside a reader opens
+      // if they want it; the board says the opposite — a claim that was
+      // approved and then rewritten opens SHOWING what moved, because that is
+      // the thing about it a reader has to know before they read a word of
+      // it. "Current" is the escape hatch, not the resting state.
+      function approvedEditBar(card, change, showingChanges) {
+        var bar = el('div', 'claim-edit-bar');
+        var approved = editDateLabel(change.approved_at);
+        var hasDiff = approvedEditHasDiff(change);
+        bar.appendChild(textEl('span', 'claim-edit-bar-title',
+          !hasDiff || showingChanges ? 'Edited since it was approved' : 'Showing the current wording'));
+
+        var meta;
+        if (!hasDiff || showingChanges) {
+          meta = 'approved ' + approved + (change.approved_by ? ' \u00b7 ' + change.approved_by : '');
+        } else {
+          var passages = change.changed_passages || 0;
+          meta = passages === 1
+            ? '1 passage differs from the approval of ' + approved
+            : passages + ' passages differ from the approval of ' + approved;
+        }
+        bar.appendChild(textEl('span', 'claim-edit-bar-meta', meta));
+
+        // No second wording, no switch. A control whose two states show the
+        // same thing is not a control, and the one that used to be here did
+        // worse than nothing: picking "Changes" hid the claim's body to make
+        // room for a diff that did not exist.
+        if (!hasDiff) { return bar; }
+
+        var group = el('div', 'claim-edit-switch');
+        group.setAttribute('role', 'group');
+        group.setAttribute('aria-label', 'Which wording to show');
+        [['Changes', true], ['Current', false]].forEach(function (pair) {
+          var seg = el('button', 'claim-edit-switch-seg');
+          seg.type = 'button';
+          seg.textContent = pair[0];
+          seg.setAttribute('aria-pressed', String(showingChanges === pair[1]));
+          seg.addEventListener('click', function (event) {
+            event.preventDefault();
+            paintApprovedEdit(card, change, pair[1]);
+          });
+          group.appendChild(seg);
+        });
+        bar.appendChild(group);
+        return bar;
+      }
+
+      // paintApprovedEdit puts one claim card into one of the two states, and
+      // is the only thing that writes either of them, so the bar and the body
+      // can never disagree about which one is showing.
+      function paintApprovedEdit(card, change, showingChanges) {
+        var hasDiff = approvedEditHasDiff(change);
+        showingChanges = hasDiff && showingChanges;
+        var existingBar = card.querySelector(':scope > .claim-edit-bar');
+        // The diff may already be wrapped: the four-line disclosure wraps it
+        // exactly as it wraps a claim body (system-record.js's BODY_LIKE), so
+        // what has to be taken out is the wrapper when there is one.
+        var existingDiff = card.querySelector(
+          ':scope > .claim-edit-diff, :scope > .claim-edit-current, :scope > .claim-edit-note, :scope > .claim-body-disclosure--edit');
+
+        var bar = approvedEditBar(card, change, showingChanges);
+        if (existingBar) { existingBar.replaceWith(bar); }
+        else {
+          var head = card.querySelector(':scope > .k');
+          if (head && head.parentNode) { head.parentNode.insertBefore(bar, head.nextSibling); }
+          else { card.insertBefore(bar, card.firstChild); }
+        }
+        if (existingDiff) { existingDiff.remove(); }
+        if (!hasDiff) {
+          bar.parentNode.insertBefore(approvedEditNote(change), bar.nextSibling);
+        } else {
+          bar.parentNode.insertBefore(
+            showingChanges ? approvedEditDiff(change) : approvedEditCurrent(change), bar.nextSibling);
+          // Hand the new surface to the disclosure so it is measured and
+          // clamped like any other body. Without this the diff renders at
+          // full height while the claim beside it truncates at four lines,
+          // which is the same claim behaving as two components.
+          if (typeof window.dossierxEnhanceSystemRecord === 'function') {
+            window.dossierxEnhanceSystemRecord();
+          }
+        }
+        // While there is a second wording the claim's own body is off screen
+        // in BOTH states: Changes shows the diff and Current shows the same
+        // passages with the changed one ruled. The body comes back only for
+        // a claim with nothing to compare against (no switch).
+        //
+        // The body is hidden by a class ON THE CARD, and not by setting
+        // `hidden` on the body itself. Which element the body IS moves: the
+        // long-body disclosure wraps
+        // .claim-body inside .claim-body-disclosure after first paint, so a
+        // handle taken before the wrap and a handle taken after it are
+        // different nodes. Hiding whichever one a selector matched at the
+        // time left the first one hidden and the second one shown, and the
+        // switch stopped working on exactly the claims long enough to be
+        // wrapped. A class on the card is one state, in one place, whatever
+        // the body is wearing.
+        //
+        // It is also why the body is hidden rather than removed: it carries
+        // the source-note clamps and that disclosure, and tearing it out
+        // would take their state with it every time the reader flipped.
+        card.classList.toggle('claim--edit-view', hasDiff);
+      }
+
+      // renderApprovedEdits upgrades each edited claim's chip and puts its
+      // card into the Changes state. It is driven from mountSurface (so the
+      // surface reaches its final height before anything measures or scrolls
+      // it) and again from renderStatusStrip, so everything it creates is
+      // either guarded or left alone on a repaint.
+      function renderApprovedEdits() {
+        var edits = offlineApprovedEdits();
+        Object.keys(edits).forEach(function (id) {
+          var card = document.getElementById(id);
+          if (!card) { return; }
+          var change = edits[id];
+
+          // The chip. It stays .pill.pv — the claim IS a draft, and inventing
+          // a fourth colour for it would say the state is unrelated to the
+          // one the reader already knows. Only the word changes, because only
+          // the word was wrong: DRAFT is true and incomplete, and a reviewer
+          // deciding what to open next needs the part it leaves out.
+          var pill = card.querySelector('.k .label .pill');
+          if (pill && pill.getAttribute('data-dx-edited') !== '1') {
+            pill.setAttribute('data-dx-edited', '1');
+            pill.setAttribute('title', 'This claim held an approval, which was released when it was unlocked. What is written now differs from what was approved.');
+            // No padlock. Paper (CDJ-0, CIY-0) draws the chip as the word
+            // alone: the open padlock is DRAFT's glyph, and this chip exists
+            // to say the state is NOT plain draft.
+            pill.textContent = '';
+            pill.appendChild(textEl('span', '', 'Edited \u00b7 was approved'));
+          }
+
+          // The bar and the body state, painted only when the bar is not
+          // already there: a repaint must not take the reader's own "Current"
+          // choice away from them on the next poll.
+          if (!card.querySelector(':scope > .claim-edit-bar')) {
+            paintApprovedEdit(card, change, true);
+          }
+        });
+      }
+
+      // approvedEditIDsIn returns the claims in the id set that have been
+      // rewritten since the approval their unlock released.
+      //
+      // It is scoped to the facet on screen, by the SAME id set the rest of
+      // the strip scopes itself with — a count of claims on a page the reader
+      // is not looking at is a count they cannot act on. The set is a
+      // null-prototype object keyed by id (activeFacetClaimIDs), so this
+      // indexes it rather than calling Set or Array methods it does not have.
+      function approvedEditIDsIn(claimIDs) {
+        var ids = claimIDs || {};
+        return Object.keys(offlineApprovedEdits()).filter(function (id) { return ids[id]; });
+      }
+
       // renderStatusStrip paints one /api/status payload. An empty verdict hides
       // the strip entirely rather than showing a green badge: the viewer already
       // reads as "fine" by default, and a persistent all-clear chip would be one
       // more thing to stop noticing.
       function renderStatusStrip(data) {
-        if (!stripEl || !stripBody || !stripSummary || !stripTitle) { return; }
+        if (!stripEl || !stripBody || !stripCard || !stripFindings || !stripTitle) { return; }
         lastStatusData = data || {};
         var claimIDs = activeFacetClaimIDs();
         var ledger = findingsForActiveFacet(lastStatusData.ledger_findings || [], claimIDs);
@@ -13884,6 +14949,7 @@
         // PREVIOUS pass's state, since this function only writes it below.
         var bannerShowing = groups.length > 0 && actionable;
         renderClaimReadiness(lastStatusData.readiness || offlineReadiness(), bannerShowing);
+        renderApprovedEdits();
         if (!groups.length || !actionable) {
           stripEl.hidden = true;
           stripEl.classList.remove('status-strip--integrity', 'status-strip--lint');
@@ -13977,12 +15043,18 @@
           recordEl.classList.add('status-group--approval-record');
           stripBody.appendChild(recordEl);
         }
+        // Under the Needs-you filter the group's weight phrase counts what is
+        // waiting on the reader, not what is blocked (Paper EMP-0: "3 of 4
+        // need you here").
+        var needsTotal = countSeverity(groups, 'needs_you');
         moduleOrder.forEach(function (moduleID) {
           var rows = byModule[moduleID].slice().sort(function (a, b) {
             return statusGroupClaimCount(b) - statusGroupClaimCount(a);
           });
           var weight = uniqueClaimCount(rows);
-          var note = 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
+          var note = stripSeverityFilter === 'needs_you'
+            ? weight + ' of ' + needsTotal + ' need you here'
+            : 'blocks ' + weight + ' of ' + facetTotal + ' claim' + (facetTotal === 1 ? '' : 's') + ' here';
           stripBody.appendChild(findingGroup(moduleLabel(moduleID), rows.map(renderStatusGroup), note));
         });

@@ -13992,38 +15064,189 @@
         // used to repeat as a second string. statusChipLine, the function
         // that built that string, is now dead (no other call site) and is
         // deleted.
-        var headline = blockerHeadline(groups);
-        var needsYou = countSeverity(groups, 'needs_you') > 0;
-        if (ledger.length) {
-          stripTitle.textContent = countLabel(ledger.length, 'approval record issue') + ' in this facet need' + (ledger.length === 1 ? 's' : '') + ' attention';
-        } else if (lintErrors.length && !headline && !needsYou) {
-          stripTitle.textContent = countLabel(lintErrors.length, 'issue') + ' in this facet need' + (lintErrors.length === 1 ? 's' : '') + ' attention';
-        } else if (headline) {
-          stripTitle.textContent = headline;
-        } else {
-          stripTitle.textContent = countLabel(groups.length, 'grouped issue') + ' in this facet';
-        }
+        //
+        // Both forms of the head, every paint. The desktop band carries the
+        // single headline sentence it always did; the phone card carries one
+        // row per finding. They are derived from the SAME groups, ledger and
+        // lint lists a line apart, so the two can differ in shape and never
+        // in what they say.
+        var rows = statusStripRows(groups, ledger, lintErrors, claimIDs);
+        renderStatusStripCard(rows);
+        // The band gets the same rows, one full-bleed row each (Paper board
+        // 15, C2M-0 / C92-0), in the same order — most serious first — so the
+        // two forms differ in shape and never in what they say.
+        renderStatusStripBand(rows);

         stripEl.classList.toggle('status-strip--integrity', ledger.length > 0);
         stripEl.classList.toggle('status-strip--lint', ledger.length === 0);
-        // Paper's default reading view always starts with the findings body
-        // collapsed. Critical and needs-you counts still determine the
-        // truthful summary/chips above; they do not take control of the
-        // reader's disclosure state. Once the reader opens the strip, the
-        // existing stripExpanded state remains sticky across later polls.
-        setStripExpanded(stripExpanded);
+        // #statusStripBody is populated above and stays hidden: the Issues
+        // screen MOVES its children into itself (issuesSyncFromStrip), so it
+        // must exist and must not be shown here.
+        if (stripBody) { stripBody.hidden = true; }
         positionStatusStrip();
         stripEl.hidden = false;
       }

-      function setStripExpanded(expanded) {
-        stripExpanded = !!expanded;
-        if (stripBody) { stripBody.hidden = !stripExpanded; }
-        if (stripToggle) { stripToggle.setAttribute('aria-expanded', String(stripExpanded)); }
-        if (stripAction) { stripAction.textContent = stripExpanded ? 'Hide issues' : 'Show issues'; }
-        if (stripEl) { stripEl.classList.toggle('status-strip--open', stripExpanded); }
+      // statusStripRows turns the facet's findings into the card's rows —
+      // one row per KIND of thing waiting, never one per claim.
+      //
+      // The rows are the summary sentences the strip already computed, each
+      // now carrying its own hue instead of four of them competing to be the
+      // one headline. `tone` is the row's colour family, and it is the only
+      // thing the card's tint rule reads: 'alarm' for anything the approval
+      // record or the dependency chain is refusing, 'draft' for work in
+      // progress that nobody has approved yet.
+      function statusStripRows(groups, ledger, lintErrors, claimIDs) {
+        var rows = [];
+        if (ledger.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(ledger.length, 'approval record issue') + ' in this facet need' +
+              (ledger.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        var headline = blockerHeadline(groups);
+        if (headline) {
+          rows.push({ tone: 'alarm', severity: 'blocker', text: headline });
+        }
+        if (lintErrors.length) {
+          rows.push({
+            tone: 'alarm', severity: 'critical',
+            text: countLabel(lintErrors.length, 'issue') + ' in this facet need' +
+              (lintErrors.length === 1 ? 's' : '') + ' attention'
+          });
+        }
+        // Claims rewritten since approval. This is the row the old
+        // single-sentence head had no space for, and the reason it became a
+        // card: it is amber where everything above it is red, and one tinted
+        // surface cannot make two severity claims at once.
+        var edited = approvedEditIDsIn(claimIDs);
+        if (edited.length) {
+          rows.push({
+            tone: 'draft', severity: 'needs_you',
+            text: countLabel(edited.length, 'claim') + ' ' + (edited.length === 1 ? 'has' : 'have') +
+              ' edits that have not been approved'
+          });
+        }
+        if (!rows.length) {
+          rows.push({
+            tone: 'alarm', severity: '',
+            text: countLabel(groups.length, 'grouped issue') + ' in this facet'
+          });
+        }
+        return rows;
       }

+      // renderStatusStripCard paints the head. Tinted while every row shares
+      // a tone, neutral the moment they disagree — Paper EH1-0's own rule,
+      // and the reason it is a rule: a tinted surface IS a severity claim,
+      // so a card holding a red fact and an amber one has to stop making it
+      // and let the dots carry the colour instead.
+      // statusStripRowAction names the way in each row offers. A blocked row
+      // opens the Issues screen as it always did; an edited-since-approval
+      // row says what it is for (Paper CDS-0: "Review changes").
+      function statusStripRowAction(row) {
+        return row.tone === 'draft' ? 'Review changes' : 'Show issues';
+      }
+
+      // openIssuesForBandRow: a blocked row opens the screen UNFILTERED — its
+      // sentence names one finding but the band stands for the whole facet.
+      // An edited row names exactly what it filters to (the claims waiting on
+      // the reader), which is what earns it the filter, same as a card row.
+      function openIssuesForBandRow(tone, severity) {
+        stripSeverityFilter = tone === 'draft' ? (severity || '') : '';
+        renderStatusStrip(lastStatusData);
+        openIssuesView();
+      }
+
+      // renderStatusStripBand paints the DESKTOP form: the static first row
+      // (#statusStripToggle, so the ids the rest of the viewer and its tests
+      // address stay put) takes the first finding, and one more
+      // .status-strip-head button is appended per further finding. Each row
+      // carries data-tone, which is the only thing the stylesheet reads to
+      // pick its tint, its icon and its action colour.
+      function renderStatusStripBand(rows) {
+        if (!stripToggle || !stripTitle || !stripEl) { return; }
+        var first = rows[0];
+        stripToggle.setAttribute('data-tone', first.tone);
+        stripToggle.setAttribute('data-severity', first.severity || '');
+        stripTitle.textContent = first.text;
+        var firstAction = document.getElementById('statusStripAction');
+        if (firstAction) { firstAction.textContent = statusStripRowAction(first); }
+
+        stripEl.querySelectorAll(':scope > .status-strip-head--row').forEach(function (node) { node.remove(); });
+        rows.slice(1).forEach(function (row) {
+          var btn = el('button', 'status-strip-head status-strip-head--row');
+          btn.type = 'button';
+          btn.setAttribute('data-tone', row.tone);
+          btn.setAttribute('data-severity', row.severity || '');
+          var summary = el('span', 'status-strip-summary');
+          var icon = dxIcon('triangle-alert');
+          icon.classList.add('status-strip-icon');
+          summary.appendChild(icon);
+          summary.appendChild(el('span', 'status-strip-dot'));
+          var text = el('span', 'status-strip-summary-text');
+          text.appendChild(textEl('strong', 'status-strip-title', row.text));
+          summary.appendChild(text);
+          btn.appendChild(summary);
+          var actionRow = el('span', 'status-strip-action-row');
+          actionRow.appendChild(textEl('span', 'status-strip-action', statusStripRowAction(row)));
+          btn.appendChild(actionRow);
+          btn.addEventListener('click', function (event) {
+            event.preventDefault();
+            openIssuesForBandRow(row.tone, row.severity);
+          });
+          // After the previous row and before the phone card, so the band is
+          // one stack of rows whatever the count.
+          var before = stripCard || stripBody;
+          if (before && before.parentNode === stripEl) { stripEl.insertBefore(btn, before); }
+          else { stripEl.appendChild(btn); }
+        });
+      }
+
+      function renderStatusStripCard(rows) {
+        if (!stripCard || !stripFindings) { return; }
+        var tones = {};
+        rows.forEach(function (row) { tones[row.tone] = true; });
+        var toneKeys = Object.keys(tones);
+        var tinted = toneKeys.length === 1 ? toneKeys[0] : '';
+        stripCard.classList.toggle('status-strip-card--alarm', tinted === 'alarm');
+        stripCard.classList.toggle('status-strip-card--draft', tinted === 'draft');
+        if (stripCardCount) { stripCardCount.textContent = String(rows.length); }
+
+        stripFindings.textContent = '';
+        rows.forEach(function (row) {
+          // A button, not a div with a handler: each row is a way into the
+          // Issues screen and has to be reachable by keyboard and named to a
+          // screen reader like the one control it is.
+          var el_ = el('button', 'status-strip-finding');
+          el_.type = 'button';
+          el_.setAttribute('data-tone', row.tone);
+          var dot = el('span', 'status-strip-finding-dot');
+          dot.setAttribute('aria-hidden', 'true');
+          el_.appendChild(dot);
+          el_.appendChild(textEl('span', 'status-strip-finding-text', row.text));
+          // A text glyph, not the 14px icon: EH1-0 draws the row's way-in as
+          // a 12px "›" in the card's own hue.
+          var chev = textEl('span', 'status-strip-finding-chevron', '›');
+          chev.setAttribute('aria-hidden', 'true');
+          el_.appendChild(chev);
+          el_.addEventListener('click', function (event) {
+            event.preventDefault();
+            // Filtered to the row's own severity. On a real corpus the
+            // unfiltered screen is hundreds of dependency rows and the thing
+            // this row named is somewhere inside them; a way in that lands a
+            // reader in a list they then have to search has not answered the
+            // question it asked. Same stripSeverityFilter the chips set, so
+            // the chip is pressed on arrival and one more click clears it.
+            stripSeverityFilter = row.severity || '';
+            renderStatusStrip(lastStatusData);
+            openIssuesView();
+          });
+          stripFindings.appendChild(el_);
+        });
+      }
+
       // refreshStatus polls the endpoint. A FAILED poll deliberately leaves the
       // last verdict on screen untouched: a transient fetch failure (the server
       // restarting mid-reload) must never be able to clear an integrity warning,
@@ -14037,25 +15260,20 @@
       }

       // R09.6 (Paper ZP-0): "Issues is its own screen, not an expansion. The
-      // banner is the only way in." Paper backs that with node evidence:
-      // 56T-0, 574-0 and 6P-0 draw ONE state — the sentence plus "Show
-      // issues" — the desktop banner is a single flex ROW with no slot for a
-      // body, the string "Hide issues" does not exist anywhere in the design
-      // file, and 04 section 4 records that no board in the group draws an
-      // expanded state. The inline expand listener that lived here is gone;
-      // the toggle's only job is opening the Issues screen.
+      // banner is the only way in." No board in the group draws an expanded
+      // banner, and the string "Hide issues" does not exist anywhere in the
+      // design file, so the strip has no inline disclosure at all.
       //
-      // setStripExpanded is deliberately KEPT: renderStatusStrip still calls
-      // it every poll with a permanently-false stripExpanded, and that is
-      // what holds #statusStripBody hidden, aria-expanded="false" and the
-      // action reading "Show issues".
+      // EH1-0 then replaced the one sentence with one row per finding, and
+      // the entry point went with it: each row opens the Issues screen,
+      // filtered to that row's own severity. "The banner is the only way in"
+      // still holds — the banner simply has more than one door now, because
+      // a facet routinely has more than one kind of thing waiting.
       //
       // #statusStripBody is never emptied, removed or left unpopulated:
       // renderStatusStrip still builds into it, issuesSyncFromStrip still
       // MOVES its children into the Issues screen, and closeIssuesView still
-      // moves them back. Only its visibility to the reader is withdrawn — the
-      // children now return into a hidden body instead of an expanded one,
-      // which is the defect this removes.
+      // moves them back. It is simply never shown in place.

       // ================================================================
       // Issues view (04-issues-screen.md) — lane L8a
@@ -14064,9 +15282,8 @@
       // The screen's own chrome around lane L8's status-strip vocabulary:
       // breadcrumb, title/subtitle, scope and sort controls, the findings
       // card and the "WHERE THE BLOCKERS LIVE" rail (04 §3, §4.1-4.3, §4.5,
-      // §4.6, §4.9). R09.6: reached ONLY from "Show issues" — the listener
-      // just below runs AFTER the one directly above (source order), so by
-      // the time it fires `stripExpanded` already holds L8's own new value.
+      // §4.6, §4.9). R09.6: reached ONLY from a row of the banner card, each
+      // of which wires openIssuesView itself as it is built.
       //
       // #issuesFilters and #issuesFindingsCard never build their own copy of
       // the severity chips or the grouped findings list: issuesSyncFromStrip
@@ -14171,11 +15388,59 @@
         var m = issuesModuleWeights(issuesScope).claims;
         issuesFindingsCard.querySelectorAll('.status-group-head-note').forEach(function (note) {
           var match = /^blocks (\d+) of \d+ claims? here$/.exec(note.textContent || '');
-          if (!match) { return; }
-          note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+          if (match) {
+            note.textContent = 'blocks ' + match[1] + ' of ' + m + ' claim' + (m === 1 ? '' : 's') + ' here';
+            return;
+          }
+          // The Needs-you phrase: the same denominator rule, over the rows
+          // the filter is showing (issuesGroupsForScope already applies it).
+          var waiting = /^(\d+) of \d+ need you here$/.exec(note.textContent || '');
+          if (waiting) { note.textContent = waiting[1] + ' of ' + m + ' need you here'; }
         });
       }

+      // issuesCauseWeights is the rail's OTHER ranking (Paper EKL-0, "WHAT IS
+      // WAITING"): under the Needs-you filter the question is what kind of
+      // work is waiting rather than where the blockers live, so the rail
+      // counts causes, by kind, and colours each bar by the kind's hue. A
+      // claim waiting for two reasons is counted under each, and the caveat
+      // says so.
+      var ISSUES_CAUSE_LABELS = {
+        unapproved_edit: 'Unapproved edits',
+        direct_dependency_change: 'Dependency changed',
+        upstream_dependency_review: 'Upstream review',
+        own_flag: 'Flagged for review',
+        own_thread: 'Open review thread',
+        approval_content_drift: 'Approved wording drifted',
+        approval_released: 'Approval released',
+        approval_missing: 'Approval record missing',
+        approval_unknown: 'Approval state unknown'
+      };
+      function issuesCauseWeights(scope) {
+        var groups = issuesGroupsForScope(scope);
+        var byKind = {};
+        var order = [];
+        groups.forEach(function (g) {
+          var kind = g.kind || 'review';
+          if (!Object.prototype.hasOwnProperty.call(byKind, kind)) { byKind[kind] = []; order.push(kind); }
+          byKind[kind].push(g);
+        });
+        var rows = order.map(function (kind) {
+          return {
+            id: kind,
+            label: ISSUES_CAUSE_LABELS[kind] || kind.replace(/_/g, ' '),
+            tone: kind === 'unapproved_edit' ? 'draft' : 'alarm',
+            weight: uniqueClaimCount(byKind[kind])
+          };
+        });
+        rows.sort(function (a, b) { return b.weight - a.weight; });
+        return {
+          rows: rows,
+          claims: uniqueClaimCount(groups),
+          causes: rows.reduce(function (sum, r) { return sum + r.weight; }, 0)
+        };
+      }
+
       // renderIssuesRail (04 §4.9): a ranking with bars, proportional to the
       // facet's blocked-claim TOTAL (§4.9's own measured widths — retry fix
       // 4, coordinator ruling: "rail bars use the spec's rounding basis" —
@@ -14195,10 +15460,15 @@
       // corpus size or scope.
       function renderIssuesRail() {
         if (!issuesRailRowsEl) { return; }
-        var data = issuesModuleWeights(issuesScope);
+        var waiting = stripSeverityFilter === 'needs_you';
+        var data = waiting ? issuesCauseWeights(issuesScope) : issuesModuleWeights(issuesScope);
+        var heading = document.getElementById('issuesRailHeading');
+        if (heading) { heading.textContent = waiting ? 'WHAT IS WAITING' : 'WHERE THE BLOCKERS LIVE'; }
         issuesRailRowsEl.textContent = '';
         var shown = data.rows.slice(0, ISSUES_RAIL_MAX_ROWS);
-        var total = data.claims;
+        // Module bars are proportional to the blocked-claim total; cause bars
+        // to the cause total (EKL-0: 2 of 4 causes draws round(50%)).
+        var total = waiting ? data.causes : data.claims;
         shown.forEach(function (row) {
           var wrap = el('div', 'issues-rail-row');
           var labelRow = el('div', 'issues-rail-row-label');
@@ -14207,6 +15477,7 @@
           wrap.appendChild(labelRow);
           var track = el('div', 'issues-rail-bar-track');
           var fill = el('div', 'issues-rail-bar-fill');
+          if (row.tone) { fill.setAttribute('data-tone', row.tone); }
           fill.style.width = (total ? Math.round((row.weight / total) * 100) : 0) + '%';
           track.appendChild(fill);
           wrap.appendChild(track);
@@ -14216,10 +15487,14 @@
           issuesRailRowsEl.appendChild(textEl('p', 'issues-rail-more',
             (data.rows.length - ISSUES_RAIL_MAX_ROWS) + ' more not shown'));
         }
-        var caveat = data.claims > 0
-          ? (countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
-             ' — a claim blocked through two modules counts under both.')
-          : '';
+        var caveat = '';
+        if (waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ', ' + countLabel(data.causes, 'cause') +
+            ' — a claim can be waiting for more than one reason and is counted under each.';
+        } else if (!waiting && data.claims > 0) {
+          caveat = countLabel(data.claims, 'claim') + ' blocked, ' + countLabel(data.paths, 'path') +
+            ' — a claim blocked through two modules counts under both.';
+        }
         if (issuesRailCaveatEl) { issuesRailCaveatEl.textContent = caveat; }
         if (issuesCaveatEl) { issuesCaveatEl.textContent = caveat; }
         // §8 item 5: an empty ranking is a ranking of nothing — the panel is
@@ -14245,7 +15520,13 @@
             (lastStatusData && lastStatusData.readiness) || offlineReadiness(),
             activeFacetClaimIDs(), [], [], [], []
           );
-          issuesSubtitleEl.textContent = blockerHeadline(groups) || 'Nothing in this facet is blocked.';
+          // Under the Needs-you filter the sentence is about the reader's
+          // work, not the facet's blockers (Paper ENQ-0).
+          var waiting = stripSeverityFilter === 'needs_you' ? countSeverity(groups, 'needs_you') : 0;
+          issuesSubtitleEl.textContent = waiting
+            ? countLabel(waiting, 'claim') + (waiting === 1 ? ' is' : ' are') +
+              ' waiting on you in this facet. Each one names what moved and what it was measured against.'
+            : (blockerHeadline(groups) || 'Nothing in this facet is blocked.');
         }
       }

@@ -14293,6 +15574,10 @@
         }
         issuesApplyScopeDenominator();
         renderIssuesRail();
+        // The subtitle follows the filter (a chip click repaints the strip,
+        // which lands here through the observer), so it is re-read on every
+        // sync and not only when the screen opens.
+        issuesSyncHeader();
         if (issuesStripObserver) { issuesStripObserver.observe(stripBody, { childList: true }); }
       }

@@ -14405,14 +15690,20 @@
       if (issuesSortControl) {
         issuesSortControl.addEventListener('click', function (event) { event.preventDefault(); });
       }
-      // "Show issues" (R09.6) opens this view — UNCONDITIONALLY on every
-      // click of this control, whichever label it currently reads. The
-      // strip's own inline expand/collapse (the listener directly above,
-      // L8's, toggling stripExpanded/#statusStripBody) changes the sticky
-      // disclosure state first. R09.6 names this same control as the Issues
-      // entry point, so this listener does not gate on that resulting state.
+      // R09.6's "the banner is the only way in" holds for both forms. The
+      // desktop band has one door, bound here; the phone card has one per
+      // finding, each wired as it is built (see renderStatusStripCard)
+      // because each carries its own severity filter.
+      //
+      // The band's door opens the screen UNFILTERED, and that is not an
+      // oversight: its sentence names one finding but the band stands for the
+      // whole facet, so narrowing to that one row's severity would hide the
+      // others behind a filter the reader did not choose. A card row names
+      // exactly what it filters to, which is what earns it the filter.
       if (stripToggle) {
-        stripToggle.addEventListener('click', openIssuesView);
+        stripToggle.addEventListener('click', function () {
+          openIssuesForBandRow(stripToggle.getAttribute('data-tone'), stripToggle.getAttribute('data-severity'));
+        });
       }
       // Any ordinary navigation — a sidebar tab, a subtab, a citation link —
       // leaves this screen the same way the back chevron does. Capture phase
@@ -15283,8 +16574,23 @@
     window.scrollTo({ top: Math.max(0, Math.round(window.scrollY + top - offset)), behavior: 'instant' });
   }

+  // BODY_LIKE is what the four-line disclosure clamps: a claim's own body, and
+  // the passage-by-passage diff that stands in for it while the reader is
+  // looking at what changed since approval.
+  //
+  // The diff is in this set because it IS the body, shown a passage at a time
+  // — so a claim that clamps to four lines with "... more" in one state and
+  // runs to full height in the other is the same claim behaving as two
+  // different components. Sharing the mechanism rather than copying it also
+  // means the toggle's label, its aria wiring and its scroll compensation
+  // cannot drift between the two.
+  // .claim-edit-current is the other state of the same surface: the current
+  // wording rendered from the same passages, with the changed one ruled. It
+  // stands in for the body exactly as the diff does, so it clamps like it.
+  var BODY_LIKE = '.claim-body, .claim-edit-diff, .claim-edit-current';
+
   function setClaimBodyExpanded(wrapper, expanded) {
-    var body = wrapper && wrapper.querySelector(':scope > .claim-body');
+    var body = wrapper && wrapper.querySelector(':scope > .claim-body, :scope > .claim-edit-diff, :scope > .claim-edit-current');
     var toggle = wrapper && wrapper.querySelector(':scope > .claim-body-disclosure__toggle');
     if (!body || !toggle || toggle.hidden) { return; }
     wrapper.classList.toggle('claim-body-disclosure--expanded', expanded);
@@ -15297,13 +16603,19 @@
   }

   function syncClaimBodyDisclosures() {
-    document.querySelectorAll('.claim-body').forEach(function (body) {
+    document.querySelectorAll(BODY_LIKE).forEach(function (body) {
       var wrapper = body.parentElement && body.parentElement.classList.contains('claim-body-disclosure')
         ? body.parentElement
         : null;
       if (!wrapper) {
         wrapper = document.createElement('div');
         wrapper.className = 'claim-body-disclosure';
+        // The diff's wrapper is marked, because the card hides the claim's
+        // own body while the changes are showing and must not hide the diff
+        // along with it — both are a .claim-body-disclosure by then.
+        if (body.classList.contains('claim-edit-diff') || body.classList.contains('claim-edit-current')) {
+          wrapper.classList.add('claim-body-disclosure--edit');
+        }
         body.parentNode.insertBefore(wrapper, body);
         wrapper.appendChild(body);
         if (!body.id) { body.id = 'claim-body-' + (++claimBodyDisclosureSequence); }
@@ -15359,7 +16671,6 @@

   function enhanceConformanceFooters() {
     document.querySelectorAll('details.claim-conformance').forEach(function (original) {
-      if (original.dataset.footerEnhanced === 'true') { return; }
       var claim = original.closest('.claim');
       var footer = claim && claim.querySelector('.claim-footer');
       var summary = original.querySelector(':scope > summary');
@@ -15464,13 +16775,6 @@
   }

   function renderToc() {
-    // Theme-parity's hover probe forces :hover through CDP node ids. This
-    // function replaceChildren()s the TOC list on every enhance pass (the
-    // layout MutationObserver retriggers enhance when addModuleHeaders
-    // rewrites the module head), so a node id dies between QuerySelector
-    // and ForcePseudoState. The harness sets this flag for the duration of
-    // that probe; it is otherwise unset.
-    if (window.__dxParityFreezeToc) { return; }
     var toc = document.getElementById('systemFacetToc');
     if (!toc) {
       toc = document.createElement('aside');
@@ -16164,19 +17468,6 @@
     return { from: from, to: to, type: type };
   }

-  // normalizeEdges maps a whole edge list, dropping unusable entries.
-  function normalizeEdges(edges) {
-    var list = asArray(edges);
-    var out = [];
-    for (var i = 0; i < list.length; i++) {
-      var e = normalizeEdge(list[i]);
-      if (e) {
-        out.push(e);
-      }
-    }
-    return out;
-  }
-
   // idSet builds a lookup Set from an id array. Internal only.
   function idSet(ids) {
     var s = new Set();
@@ -17917,7 +19208,6 @@
   var parseTimeHash = ''; // the graph state segment seen at parse time
   var originClaimId = ''; // OPEN_ID_ATTR off the trigger that opened the pane, '' from the nav trigger
   var collapseOverride = false; // the reader insisted on every claim
-  var notices = []; // {text, kind, action} rendered above the canvas
   var dragging = null; // {id} while a node is being dragged
   var panning = null; // {x, y} while the background is being dragged
   var movedWhileDown = false;
