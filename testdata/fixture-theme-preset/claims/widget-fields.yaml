id: widget.internals.fields
facet: internals
module: widget
status: draft
layout: table
section: Field meta
rows:
  - field: family
    type: string
    enum: any
    example: DossierX Probe
  - field: src
    type: path
    enum: relative
    example: fonts/probe.ttf
governed_by:
  type: none
  reason: fixture claim, not backed by any real doctrine
rests_on:
  - widget.contract.overview
