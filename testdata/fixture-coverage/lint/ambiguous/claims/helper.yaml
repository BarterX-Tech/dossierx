id: ambmod.contract.helper
facet: contract
module: ambmod
status: draft
layout: card
body: |
  Helper claim that rests_on the duplicated id, purely so neither copy of
  the duplicated claim is also flagged by the (separate, warning-severity)
  orphan lint -- keeps this fixture isolated to exactly one rule: ambiguous.
governed_by:
  type: none
  reason: fixture claim, not backed by any real doctrine
rests_on:
  - ambmod.contract.dup
