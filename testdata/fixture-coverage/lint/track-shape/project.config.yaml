schema_version: 1
facets:
  - contract
  - internals
modules:
  - trackmod
claims_dir: claims
tracks:
  - id: checkout
    title: Checkout
    summary: Declared so this fixture's malformed memberships are malformed and nothing else.
