id: idshapemod.contract.helper
summary: Fixture claim used by the engine test corpus.
facet: contract
module: idshapemod
status: draft
layout: card
body: |
  Helper claim that rests_on the malformed-id claim below, so that claim
  has an incoming edge and isn't also flagged by the separate orphan lint.
rests_on:
  - idshapemod.badid
