id: bodyhintmod.contract.source
summary: Fixture claim used by the engine test corpus.
facet: contract
module: bodyhintmod
status: draft
layout: card
body: |
  This claim's prose mentions bodyhintmod.contract.target in passing, but
  never declares it under rests_on -- exactly the
  forgotten-edge shape the body-edge-hint lint exists to catch.
rests_on:
  - bodyhintmod.contract.hub
