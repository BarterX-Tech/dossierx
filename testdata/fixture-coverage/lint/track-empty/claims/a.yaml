id: trackmod.contract.a
facet: contract
module: trackmod
status: draft
layout: card
body: |
  A perfectly ordinary claim that declares no track membership at all. The
  project declares one track and nothing joins it, which is the track-empty
  WARNING: "dossierx check --validate" still exits 0 for this fixture, because
  declaring a track before writing its claims is a legitimate way to work.
rests_on:
  - trackmod.contract.b
