id: restreq.contract.helper
summary: A helper claim so the bare claim below is not also an orphan.
facet: contract
module: restreq
status: draft
layout: card
body: A helper that rests on the bare claim, so neither is an orphan.
rests_on:
  - restreq.contract.missing
