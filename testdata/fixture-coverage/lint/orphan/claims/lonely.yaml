id: orphanmod.contract.lonely
facet: contract
module: orphanmod
status: draft
layout: card
body: |
  A standalone claim with zero mirrors/rests_on edges in either direction --
  the orphan lint's WARNING-severity finding. "dossierx lint" must still exit 0
  for this fixture: a warning is reported, not a failure.
governed_by:
  type: none
  reason: fixture claim, not backed by any real doctrine
