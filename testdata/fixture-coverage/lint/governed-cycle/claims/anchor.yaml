id: govcyc.contract.anchor
facet: contract
module: govcyc
status: draft
layout: card
body: |
  A grounded claim the cycle pair rests on. It exists only so the two
  cycle claims carry a rests_on edge and the orphan warning (which counts
  mirrors and rests_on, deliberately not governed_by) stays out of this
  fixture.
governed_by:
  type: none
  reason: fixture claim, not backed by any real doctrine
