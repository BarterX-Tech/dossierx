# Lucide icons

Viewer chrome uses a small Lucide subset (lock, menu, theme, comments,
chevrons, comment actions). Paths are inlined as an SVG sprite in
`internal/render/viewer/template/shell.html`. Lucide is ISC-licensed;
see `LICENSE`.
