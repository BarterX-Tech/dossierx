# Working in this repository

Read and follow [AGENTS.md](AGENTS.md). It owns the repository rules and the
required project skills for every agent. If automatic skill discovery is
unavailable, open the canonical skill paths named there explicitly.

This file is a Claude entry point, not a second copy of the repository rules.
